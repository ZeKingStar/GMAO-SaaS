---
phase: 04-api-publique
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/openapi.ts
  - src/lib/openapi.test.ts
  - src/app/api/openapi.json/route.ts
  - src/app/api/docs/route.ts
  - src/app/(app)/parametres/organisation/page.tsx
  - src/components/settings/api-keys-section.tsx
  - src/components/settings/api-keys-section.test.tsx
autonomous: false
requirements:
  - API-03
  - API-04
user_setup:
  - service: npm
    why: "Install @asteasolutions/zod-to-openapi (OpenAPI spec generator) and @scalar/nextjs-api-reference (docs UI). Both confirmed compatible with Next 16 / React 19 / Zod 4 in 04-RESEARCH.md."
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - "Un visiteur non authentifié peut ouvrir /api/docs dans son navigateur et voir une interface Scalar listant les endpoints /api/v1/work-orders (GET + POST)"
    - "GET /api/openapi.json retourne une spec OpenAPI 3.1 valide en JSON, sans authentification"
    - "Un admin/manager voit une section 'Clés API' dans /parametres/organisation avec un bouton 'Générer une clé', une liste des clés existantes (nom, créée le, dernière utilisation, statut), et un bouton 'Révoquer' par clé"
    - "À la génération, la clé brute s'affiche UNE seule fois avec un avertissement et un bouton copier"
    - "Une fois fermé le dialog de génération, la clé brute ne réapparaît jamais ailleurs dans l'UI (seule la liste résumée s'affiche)"
    - "Un human-verify checkpoint confirme visuellement Swagger UI + flux de génération/révocation"
  artifacts:
    - path: "src/lib/openapi.ts"
      provides: "Builder OpenAPI 3.1 depuis schémas Zod (réutilise api-validation.ts)"
      exports: ["buildOpenApiSpec"]
    - path: "src/app/api/openapi.json/route.ts"
      provides: "Route Handler GET — sert la spec OpenAPI JSON (force-static)"
      exports: ["GET"]
    - path: "src/app/api/docs/route.ts"
      provides: "Route Handler GET — sert l'UI Scalar"
      exports: ["GET"]
    - path: "src/components/settings/api-keys-section.tsx"
      provides: "Composant UI gestion des clés API (génération, liste, révocation)"
      contains: "API"
    - path: "src/app/(app)/parametres/organisation/page.tsx"
      provides: "Intègre ApiKeysSection sur la page des paramètres organisation"
      contains: "ApiKeysSection"
  key_links:
    - from: "src/components/settings/api-keys-section.tsx"
      to: "src/actions/api-keys.ts"
      via: "createApiKey/listApiKeys/revokeApiKey Server Actions"
      pattern: "createApiKey|listApiKeys|revokeApiKey"
    - from: "src/app/api/docs/route.ts"
      to: "/api/openapi.json"
      via: "Scalar ApiReference spec.url config"
      pattern: "/api/openapi.json"
    - from: "src/app/api/openapi.json/route.ts"
      to: "src/lib/openapi.ts"
      via: "buildOpenApiSpec()"
      pattern: "buildOpenApiSpec"
---

<objective>
Phase 4 Wave 2 — Documentation publique + gestion des clés API. Installer les dépendances Scalar + zod-to-openapi, générer la spec OpenAPI 3.1 depuis les schémas Zod existants, exposer la doc Swagger à `/api/docs` (public), et créer la section "Clés API" dans `/parametres/organisation` (création + liste + révocation) avec un checkpoint visuel humain.

Purpose: Concrétise API-03 (doc publique Swagger) et API-04 (gestion des clés depuis paramètres orga). Sans ce plan, les développeurs externes ne peuvent pas découvrir l'API et les admins n'ont aucun moyen de générer/révoquer des clés via l'interface.

Output:
- Dépendances NPM : `@asteasolutions/zod-to-openapi@8.5.0`, `@scalar/nextjs-api-reference@0.10.18`
- `src/lib/openapi.ts` : `buildOpenApiSpec()` qui assemble la spec depuis `workOrderCreateSchema`
- `src/app/api/openapi.json/route.ts` + `src/app/api/docs/route.ts` : 2 Route Handlers publics
- `src/components/settings/api-keys-section.tsx` : UI complète (modal génération, table, bouton révoquer)
- `src/app/(app)/parametres/organisation/page.tsx` : intégration de la section
- Tests unitaires sur le builder OpenAPI + le composant UI
- Checkpoint humain final pour validation visuelle
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-api-publique/04-RESEARCH.md
@.planning/phases/04-api-publique/04-VALIDATION.md
@.planning/phases/04-api-publique/04-01-PLAN.md
@.planning/phases/04-api-publique/04-02-PLAN.md
@src/lib/api-auth.ts
@src/actions/api-keys.ts
@src/lib/api-validation.ts
@src/app/(app)/parametres/organisation/page.tsx
@src/components/ui/button.tsx
@src/components/ui/dialog.tsx
@src/components/ui/input.tsx
@src/components/ui/card.tsx

<interfaces>
<!-- Contracts from Plan 01 + Plan 02 — use as-is. -->

From src/actions/api-keys.ts (Plan 01):
```typescript
export type CreatedApiKey = { id: string; name: string; key: string; createdAt: Date }
export type ApiKeyListItem = { id: string; name: string; createdAt: Date; lastUsedAt: Date | null; isActive: boolean; expiresAt: Date | null }
export function createApiKey(name: string): Promise<CreatedApiKey>
export function listApiKeys(): Promise<ApiKeyListItem[]>
export function revokeApiKey(id: string): Promise<{ id: string; isActive: false }>
```

From src/lib/api-validation.ts (Plan 02):
```typescript
export const workOrderCreateSchema: z.ZodObject<...>
export const paginationSchema: z.ZodObject<...>
```

From src/proxy.ts (Plan 02 — already whitelists `/api/docs(.*)` and `/api/openapi.json`).

@asteasolutions/zod-to-openapi 8.5.0 API surface (verified in RESEARCH.md):
```typescript
import { extendZodWithOpenApi, OpenAPIRegistry, OpenApiGeneratorV31 } from '@asteasolutions/zod-to-openapi'
extendZodWithOpenApi(z)
const registry = new OpenAPIRegistry()
registry.register('WorkOrderCreate', workOrderCreateSchema)
registry.registerPath({ method: 'get', path: '/api/v1/work-orders', ... })
const generator = new OpenApiGeneratorV31(registry.definitions)
const document = generator.generateDocument({ openapi: '3.1.0', info: {...} })
```

@scalar/nextjs-api-reference 0.10.18 API surface (verified in RESEARCH.md):
```typescript
import { ApiReference } from '@scalar/nextjs-api-reference'
export const GET = ApiReference({ spec: { url: '/api/openapi.json' }, theme: 'default' })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies + implement OpenAPI spec builder + JSON route</name>
  <files>package.json, package-lock.json, src/lib/openapi.ts, src/lib/openapi.test.ts, src/app/api/openapi.json/route.ts</files>
  <read_first>
    - package.json (current zod version, confirm 4.4.3)
    - src/lib/api-validation.ts (Plan 02 schemas — workOrderCreateSchema, paginationSchema)
    - .planning/phases/04-api-publique/04-RESEARCH.md (Pattern 3 + Code Examples: OpenAPI Spec Route, Public Route Handler for Swagger)
    - node_modules/@asteasolutions/zod-to-openapi/dist/index.d.ts if available after install — verify exact API
  </read_first>
  <behavior>
    - After `npm install`, both packages appear in `package.json` dependencies at the verified versions
    - `buildOpenApiSpec()` returns an object conforming to OpenAPI 3.1 schema with `openapi: '3.1.0'`, `info.title: 'Korvia API'`, `info.version: '1.0.0'`, `info.description` mentioning Bearer auth, `servers: [{ url: '/api/v1' }]` (or full URL placeholder), `paths['/work-orders']` with `get` and `post` methods, `components.securitySchemes.bearerAuth: { type: 'http', scheme: 'bearer' }`
    - GET endpoint documents 200, 401, 403 responses; POST documents 201, 400, 401, 403
    - All paths declare `security: [{ bearerAuth: [] }]`
    - GET /api/openapi.json returns the spec as JSON with Content-Type application/json and status 200, with no auth required
    - Test: `buildOpenApiSpec().paths['/work-orders']` has both `get` and `post` keys; `components.securitySchemes.bearerAuth.scheme` equals `'bearer'`
  </behavior>
  <action>
1. Install dependencies (exact versions from RESEARCH.md):

```bash
npm install @asteasolutions/zod-to-openapi@8.5.0 @scalar/nextjs-api-reference@0.10.18
```

Confirm `package.json` shows both at those versions. If npm chooses a higher compatible patch (e.g., 8.5.x), accept it.

2. Create `src/lib/openapi.ts`:

```typescript
import {
  OpenAPIRegistry,
  OpenApiGeneratorV31,
  extendZodWithOpenApi,
} from '@asteasolutions/zod-to-openapi'
import { z } from 'zod'
import { workOrderCreateSchema, paginationSchema } from '@/lib/api-validation'

extendZodWithOpenApi(z)

export function buildOpenApiSpec() {
  const registry = new OpenAPIRegistry()

  registry.registerComponent('securitySchemes', 'bearerAuth', {
    type: 'http',
    scheme: 'bearer',
    bearerFormat: 'krv_<hex>',
    description: 'Clé API Korvia générée depuis /parametres/organisation',
  })

  const WorkOrderCreate = registry.register('WorkOrderCreate', workOrderCreateSchema)

  const WorkOrder = registry.register(
    'WorkOrder',
    z.object({
      id: z.string(),
      number: z.number().int(),
      title: z.string(),
      description: z.string().nullable(),
      status: z.string(),
      priority: z.string(),
      type: z.string(),
      siteId: z.string().nullable(),
      assetId: z.string().nullable(),
      dueDate: z.string().nullable(),
      estimatedHours: z.number().nullable(),
      createdAt: z.string(),
      updatedAt: z.string(),
    })
  )

  const ErrorBody = registry.register(
    'Error',
    z.object({ error: z.string(), issues: z.array(z.any()).optional() })
  )

  registry.registerPath({
    method: 'get',
    path: '/work-orders',
    summary: 'Lister les bons de travail',
    description: 'Retourne la liste paginée des bons de travail de l\'organisation associée à la clé API.',
    tags: ['Work Orders'],
    security: [{ bearerAuth: [] }],
    request: { query: paginationSchema },
    responses: {
      200: {
        description: 'Liste paginée',
        content: {
          'application/json': {
            schema: z.object({
              data: z.array(WorkOrder),
              total: z.number().int(),
              page: z.number().int(),
              limit: z.number().int(),
            }),
          },
        },
      },
      401: { description: 'Clé API manquante ou invalide', content: { 'application/json': { schema: ErrorBody } } },
      403: { description: 'Forfait insuffisant', content: { 'application/json': { schema: ErrorBody } } },
    },
  })

  registry.registerPath({
    method: 'post',
    path: '/work-orders',
    summary: 'Créer un bon de travail',
    description: 'Crée un nouveau bon de travail dans l\'organisation associée à la clé API.',
    tags: ['Work Orders'],
    security: [{ bearerAuth: [] }],
    request: { body: { content: { 'application/json': { schema: WorkOrderCreate } } } },
    responses: {
      201: { description: 'Bon de travail créé', content: { 'application/json': { schema: WorkOrder } } },
      400: { description: 'Corps invalide', content: { 'application/json': { schema: ErrorBody } } },
      401: { description: 'Clé API manquante ou invalide', content: { 'application/json': { schema: ErrorBody } } },
      403: { description: 'Forfait insuffisant', content: { 'application/json': { schema: ErrorBody } } },
    },
  })

  const generator = new OpenApiGeneratorV31(registry.definitions)
  return generator.generateDocument({
    openapi: '3.1.0',
    info: {
      title: 'Korvia API',
      version: '1.0.0',
      description: 'API publique Korvia GMAO. Authentification par clé API : header `Authorization: Bearer krv_…`. Forfait Croissance ou Entreprise requis.',
    },
    servers: [{ url: '/api/v1', description: 'Production' }],
  })
}
```

3. Create `src/lib/openapi.test.ts` with these tests:
   - `buildOpenApiSpec()` returns an object with `openapi === '3.1.0'`
   - `info.title === 'Korvia API'`
   - `paths['/work-orders']` has both `get` and `post` keys
   - `paths['/work-orders'].get.responses` has keys `'200'`, `'401'`, `'403'`
   - `paths['/work-orders'].post.responses` has keys `'201'`, `'400'`, `'401'`, `'403'`
   - `components.securitySchemes.bearerAuth.scheme === 'bearer'`
   - Both paths have `security: [{ bearerAuth: [] }]`

4. Create `src/app/api/openapi.json/route.ts`:

```typescript
import { buildOpenApiSpec } from '@/lib/openapi'

export const dynamic = 'force-static'

export async function GET() {
  return Response.json(buildOpenApiSpec())
}
```

5. Run `npx vitest run src/lib/openapi.test.ts` — confirm GREEN.

6. Smoke test the route locally (optional): `npm run build && npm start` then `curl http://localhost:3000/api/openapi.json | head -c 500`. If TTY-bound, skip and rely on tests.

Constraints:
- Use `OpenApiGeneratorV31` (not V3) — OpenAPI 3.1.0 is the current standard for Scalar.
- `extendZodWithOpenApi(z)` must be called BEFORE registering schemas.
- DO NOT hardcode the spec as a string — must be code-generated from Zod schemas (RESEARCH.md "Don't Hand-Roll").
- `force-static` on the JSON route — spec doesn't change per request, cache at build time.
  </action>
  <verify>
    <automated>npx vitest run src/lib/openapi.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').dependencies['@asteasolutions/zod-to-openapi'])"` prints a version starting with `8.5`
    - `node -e "console.log(require('./package.json').dependencies['@scalar/nextjs-api-reference'])"` prints a version starting with `0.10`
    - `test -f src/lib/openapi.ts` returns 0
    - `test -f src/lib/openapi.test.ts` returns 0
    - `test -f src/app/api/openapi.json/route.ts` returns 0
    - `grep -c "export function buildOpenApiSpec" src/lib/openapi.ts` returns 1
    - `grep -c "OpenApiGeneratorV31" src/lib/openapi.ts` returns at least 1
    - `grep -c "bearerAuth" src/lib/openapi.ts` returns at least 2
    - `grep -c "force-static" src/app/api/openapi.json/route.ts` returns 1
    - `npx vitest run src/lib/openapi.test.ts --reporter=verbose` exits 0 with at least 7 passing tests
  </acceptance_criteria>
  <done>OpenAPI spec generated from Zod, served at /api/openapi.json without auth, tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Scalar docs route + API Keys settings UI section</name>
  <files>src/app/api/docs/route.ts, src/components/settings/api-keys-section.tsx, src/components/settings/api-keys-section.test.tsx, src/app/(app)/parametres/organisation/page.tsx</files>
  <read_first>
    - src/app/(app)/parametres/organisation/page.tsx (current page — to extend)
    - src/components/ui/button.tsx, src/components/ui/dialog.tsx, src/components/ui/input.tsx, src/components/ui/card.tsx (shadcn primitives in use)
    - src/actions/api-keys.ts (Plan 01 — Server Actions to call)
    - src/lib/auth.ts (requireRole pattern for gating the section server-side)
    - .planning/phases/04-api-publique/04-RESEARCH.md (Pattern 3 Scalar code + Pitfall 2 "raw key shown once")
  </read_first>
  <behavior>
    - Visiting `/api/docs` in a browser (anonymous, no Clerk session) returns the Scalar HTML UI rendering the spec at `/api/openapi.json`
    - The page `/parametres/organisation` shows a new Card titled "Clés API" with: subtitle "Gérez les clés permettant aux systèmes externes d'accéder à l'API Korvia.", a primary button "Générer une clé API", and a table/list of existing keys with columns: Nom, Créée le, Dernière utilisation, Statut (Actif/Révoquée), Actions (bouton Révoquer pour clés actives)
    - Clicking "Générer une clé API" opens a dialog with an input field "Nom de la clé" (placeholder "ex: Intégration ERP"), a Submit button "Générer"
    - On submit: the Server Action `createApiKey(name)` runs, the returned raw `key` is displayed in the dialog inside a read-only input with a "Copier" button and a red warning banner "Cette clé ne sera plus jamais affichée. Copiez-la maintenant."
    - On closing the dialog, the raw key is wiped from React state (not stored)
    - Clicking "Révoquer" calls `revokeApiKey(id)` and refreshes the list — the row now shows "Révoquée" status
    - The section is only rendered if the current user has role admin OR manager (server-side check via `requireRole(['admin','manager'])` — if user lacks role, section is hidden, not just disabled)
    - Test: rendering `<ApiKeysSection initialKeys={[{id:'k1', name:'Test', createdAt: new Date(), lastUsedAt: null, isActive: true, expiresAt: null}]} />` shows "Test" and the Révoquer button; on click of Générer + submit, the createApiKey mock is called with the typed name
  </behavior>
  <action>
1. **Create `src/app/api/docs/route.ts`**:

```typescript
import { ApiReference } from '@scalar/nextjs-api-reference'

export const dynamic = 'force-static'

export const GET = ApiReference({
  spec: { url: '/api/openapi.json' },
  theme: 'default',
  metaData: {
    title: 'Korvia API — Documentation',
    description: 'Documentation interactive de l\'API publique Korvia.',
  },
})
```

If `@scalar/nextjs-api-reference` exports a different name (e.g., `apiReference` lowercase) in 0.10.18, adapt — verify by reading `node_modules/@scalar/nextjs-api-reference/dist/index.d.ts` after install. The README in the package says `ApiReference`.

2. **Create `src/components/settings/api-keys-section.tsx`** — client component with state for dialog, generated-key display, revoke-in-progress. Skeleton (adapt to project's existing dialog/button/table primitives — read them first):

```typescript
'use client'

import { useState, useTransition } from 'react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger, DialogFooter } from '@/components/ui/dialog'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { createApiKey, revokeApiKey, listApiKeys, type ApiKeyListItem, type CreatedApiKey } from '@/actions/api-keys'
import { Copy, Check, AlertTriangle, KeyRound, Trash2 } from 'lucide-react'

export function ApiKeysSection({ initialKeys }: { initialKeys: ApiKeyListItem[] }) {
  const [keys, setKeys] = useState<ApiKeyListItem[]>(initialKeys)
  const [open, setOpen] = useState(false)
  const [name, setName] = useState('')
  const [created, setCreated] = useState<CreatedApiKey | null>(null)
  const [copied, setCopied] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [isPending, startTransition] = useTransition()

  async function handleCreate(e: React.FormEvent) {
    e.preventDefault()
    setError(null)
    startTransition(async () => {
      try {
        const result = await createApiKey(name)
        setCreated(result)
        setKeys(await listApiKeys())
        setName('')
      } catch (err) {
        setError(err instanceof Error ? err.message : 'Erreur inconnue')
      }
    })
  }

  async function handleRevoke(id: string) {
    if (!confirm('Révoquer cette clé ? Les systèmes utilisant cette clé perdront immédiatement l\'accès.')) return
    startTransition(async () => {
      await revokeApiKey(id)
      setKeys(await listApiKeys())
    })
  }

  function handleClose() {
    setOpen(false)
    setCreated(null)   // ← raw key wiped on close
    setCopied(false)
    setError(null)
    setName('')
  }

  async function handleCopy() {
    if (!created) return
    await navigator.clipboard.writeText(created.key)
    setCopied(true)
    setTimeout(() => setCopied(false), 2000)
  }

  return (
    <Card>
      <CardHeader>
        <CardTitle className="flex items-center gap-2"><KeyRound className="h-5 w-5" /> Clés API</CardTitle>
        <CardDescription>Gérez les clés permettant aux systèmes externes d'accéder à l'API Korvia. <a href="/api/docs" className="underline">Voir la documentation</a></CardDescription>
      </CardHeader>
      <CardContent>
        <Dialog open={open} onOpenChange={(v) => { if (!v) handleClose(); else setOpen(true) }}>
          <DialogTrigger asChild>
            <Button>Générer une clé API</Button>
          </DialogTrigger>
          <DialogContent>
            {!created ? (
              <form onSubmit={handleCreate}>
                <DialogHeader><DialogTitle>Générer une nouvelle clé API</DialogTitle></DialogHeader>
                <div className="grid gap-2 py-4">
                  <Label htmlFor="key-name">Nom de la clé</Label>
                  <Input id="key-name" value={name} onChange={(e) => setName(e.target.value)} placeholder="ex: Intégration ERP" required minLength={1} maxLength={100} />
                  {error && <p className="text-sm text-destructive">{error}</p>}
                </div>
                <DialogFooter>
                  <Button type="button" variant="outline" onClick={handleClose}>Annuler</Button>
                  <Button type="submit" disabled={isPending || !name.trim()}>Générer</Button>
                </DialogFooter>
              </form>
            ) : (
              <>
                <DialogHeader><DialogTitle>Clé générée : {created.name}</DialogTitle></DialogHeader>
                <div className="rounded-md border border-destructive/50 bg-destructive/10 p-3 text-sm text-destructive flex gap-2">
                  <AlertTriangle className="h-4 w-4 flex-shrink-0 mt-0.5" />
                  <span>Cette clé ne sera plus jamais affichée. Copiez-la maintenant et stockez-la en lieu sûr.</span>
                </div>
                <div className="flex gap-2 mt-3">
                  <Input readOnly value={created.key} className="font-mono text-xs" />
                  <Button type="button" onClick={handleCopy} variant="outline" size="icon" aria-label="Copier">
                    {copied ? <Check className="h-4 w-4" /> : <Copy className="h-4 w-4" />}
                  </Button>
                </div>
                <DialogFooter><Button onClick={handleClose}>J'ai copié la clé</Button></DialogFooter>
              </>
            )}
          </DialogContent>
        </Dialog>

        <div className="mt-6">
          {keys.length === 0 ? (
            <p className="text-sm text-muted-foreground">Aucune clé API générée.</p>
          ) : (
            <table className="w-full text-sm">
              <thead>
                <tr className="border-b text-left text-muted-foreground">
                  <th className="py-2 font-medium">Nom</th>
                  <th className="py-2 font-medium">Créée le</th>
                  <th className="py-2 font-medium">Dernière utilisation</th>
                  <th className="py-2 font-medium">Statut</th>
                  <th className="py-2 font-medium text-right">Actions</th>
                </tr>
              </thead>
              <tbody>
                {keys.map((k) => (
                  <tr key={k.id} className="border-b">
                    <td className="py-3">{k.name}</td>
                    <td className="py-3">{new Date(k.createdAt).toLocaleDateString('fr-CA')}</td>
                    <td className="py-3">{k.lastUsedAt ? new Date(k.lastUsedAt).toLocaleDateString('fr-CA') : '—'}</td>
                    <td className="py-3">{k.isActive ? <span className="text-green-700">Actif</span> : <span className="text-muted-foreground">Révoquée</span>}</td>
                    <td className="py-3 text-right">
                      {k.isActive && (
                        <Button variant="ghost" size="sm" onClick={() => handleRevoke(k.id)} disabled={isPending}>
                          <Trash2 className="h-4 w-4 mr-1" /> Révoquer
                        </Button>
                      )}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          )}
        </div>
      </CardContent>
    </Card>
  )
}
```

3. **Create `src/components/settings/api-keys-section.test.tsx`** with Vitest + React Testing Library:
   - Mock `@/actions/api-keys` (createApiKey, listApiKeys, revokeApiKey)
   - Test: renders "Aucune clé API générée." when `initialKeys=[]`
   - Test: renders a row with key name and "Révoquer" button when `initialKeys=[{id:'k1', name:'Production', createdAt, lastUsedAt:null, isActive:true, expiresAt:null}]`
   - Test: clicking "Générer une clé API" opens the dialog
   - Test: submitting the form calls `createApiKey('Test Key')` when user types "Test Key" then clicks Générer
   - Test: after successful creation, the generated key is shown in a read-only input and the warning banner is visible
   - Test: closing the dialog wipes the displayed raw key (re-opening shows the empty form, not the previous key)
   - Test: clicking "Révoquer" (after mocking window.confirm to return true) calls `revokeApiKey('k1')`

4. **Update `src/app/(app)/parametres/organisation/page.tsx`**:
   - Server component (no 'use client')
   - Add: `import { ApiKeysSection } from '@/components/settings/api-keys-section'`
   - Add: `import { listApiKeys } from '@/actions/api-keys'`
   - Add: `import { getOrganizationMembership } from '@/lib/auth'`
   - In the page function: `const membership = await getOrganizationMembership()`; if `membership && ['admin','manager'].includes(membership.role)`, fetch `const apiKeys = await listApiKeys()` and render `<ApiKeysSection initialKeys={apiKeys} />` below the existing org info section. If the role check fails, do not render the section at all.

5. Run tests: `npx vitest run src/components/settings/api-keys-section.test.tsx src/lib/openapi.test.ts`.

Constraints:
- The raw API key MUST be wiped from React state when the dialog closes (security requirement — Pitfall 2 in RESEARCH.md).
- The "Révoquer" action MUST trigger a confirm dialog before calling the server action (avoid accidental revocations).
- DO NOT use `useEffect` to fetch keys — pass them as `initialKeys` prop from the server component, then refresh via Server Action.
- DO NOT add `keyPreview` display — show full key only during creation, never in list.
- Role gating happens SERVER-SIDE in `page.tsx` (the section is not rendered for non-admins). Defense-in-depth: the Server Actions themselves also call `requireRole` (Plan 01).
- If the existing `parametres/organisation/page.tsx` doesn't have a clear "above/below" pattern, place `<ApiKeysSection>` at the end of the existing content (last Card).
  </action>
  <verify>
    <automated>npx vitest run src/components/settings/api-keys-section.test.tsx src/lib/openapi.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/app/api/docs/route.ts` returns 0
    - `test -f src/components/settings/api-keys-section.tsx` returns 0
    - `test -f src/components/settings/api-keys-section.test.tsx` returns 0
    - `grep -c "ApiReference" src/app/api/docs/route.ts` returns at least 1
    - `grep -c "/api/openapi.json" src/app/api/docs/route.ts` returns 1
    - `grep -c "'use client'" src/components/settings/api-keys-section.tsx` returns 1
    - `grep -c "createApiKey" src/components/settings/api-keys-section.tsx` returns at least 1
    - `grep -c "revokeApiKey" src/components/settings/api-keys-section.tsx` returns at least 1
    - `grep -c "Cette clé ne sera plus jamais affichée" src/components/settings/api-keys-section.tsx` returns 1
    - `grep -c "setCreated(null)" src/components/settings/api-keys-section.tsx` returns at least 1 (raw key wipe on close)
    - `grep -c "ApiKeysSection" src/app/\(app\)/parametres/organisation/page.tsx` returns at least 1
    - `grep -c "admin.*manager\|manager.*admin" src/app/\(app\)/parametres/organisation/page.tsx` returns at least 1 (role gate)
    - `npx vitest run src/components/settings/api-keys-section.test.tsx --reporter=verbose` exits 0 with at least 7 passing tests
    - `npx tsc --noEmit` returns 0
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Scalar docs accessible at /api/docs, ApiKeysSection rendered in /parametres/organisation for admin/manager, raw key shown once and wiped, tests GREEN, build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Swagger UI + API key flow + curl integration</name>
  <what-built>
- Public Swagger UI at `/api/docs` rendering the OpenAPI spec from `/api/openapi.json`
- API Keys section in `/parametres/organisation` (admin/manager only) with generate, list, revoke flows
- Full REST API: `GET /api/v1/work-orders` (list, paginated) and `POST /api/v1/work-orders` (create)
- Bearer token auth + plan gating (growth/enterprise only)
  </what-built>
  <how-to-verify>
**Préparation — démarrer le serveur :**
```bash
npm run dev
# Wait for "Ready on http://localhost:3000"
```

**Vérif 1 — Documentation Swagger publique (API-03):**
1. Ouvrir un onglet PRIVÉ/INCOGNITO (pour garantir pas de session Clerk) à `http://localhost:3000/api/docs`.
2. Vérifier qu'une interface Scalar s'affiche avec :
   - Titre "Korvia API"
   - Deux endpoints listés : `GET /work-orders`, `POST /work-orders`
   - Section "Authentication" mentionnant `bearerAuth`
3. Cliquer sur `GET /work-orders` — vérifier que les réponses 200, 401, 403 sont documentées
4. Cliquer sur `POST /work-orders` — vérifier que le schéma de body est affiché avec les champs (title, type, priority, etc.)
5. Vérifier que la page se charge SANS redirection vers /sign-in

**Vérif 2 — Spec JSON brute :**
```bash
curl -s http://localhost:3000/api/openapi.json | jq '.openapi, .info.title, .paths | keys'
# Expected output:
# "3.1.0"
# "Korvia API"
# ["/work-orders"]
```

**Vérif 3 — Gestion des clés API (API-04):**
1. Se connecter avec un compte admin OU manager d'une organisation au forfait Croissance.
2. Naviguer vers `/parametres/organisation`.
3. Vérifier qu'une carte "Clés API" est présente, sous les infos de l'organisation.
4. Cliquer "Générer une clé API" → dialog s'ouvre.
5. Saisir "Test integration" → cliquer "Générer".
6. Vérifier que :
   - Une clé `krv_…` s'affiche en monospace
   - Un encadré rouge avertit "Cette clé ne sera plus jamais affichée"
   - Un bouton "Copier" copie la clé dans le presse-papier (vérifier en collant dans un éditeur)
7. Cliquer "J'ai copié la clé" → dialog se ferme.
8. Vérifier que dans le tableau, une ligne "Test integration" apparaît avec statut "Actif" et "Créée le" = aujourd'hui.
9. Rouvrir le dialog en cliquant "Générer une clé API" — vérifier qu'il s'ouvre VIDE (pas de clé précédente affichée).
10. Cliquer "Annuler" pour fermer.

**Vérif 4 — Appel REST end-to-end (API-01, API-02) :**

Avec la clé `krv_…` copiée à l'étape 6 :

```bash
export KORVIA_KEY="krv_<your-key-here>"

# A) Liste — doit retourner 200 et { data: [...], total, page, limit }
curl -s -w "\nHTTP %{http_code}\n" \
  -H "Authorization: Bearer $KORVIA_KEY" \
  http://localhost:3000/api/v1/work-orders | jq .

# B) Sans clé — doit retourner 401
curl -s -w "\nHTTP %{http_code}\n" http://localhost:3000/api/v1/work-orders

# C) Création — doit retourner 201 + objet WO
curl -s -w "\nHTTP %{http_code}\n" \
  -X POST -H "Authorization: Bearer $KORVIA_KEY" -H "Content-Type: application/json" \
  -d '{"title":"Test API integration","type":"corrective","priority":"medium"}' \
  http://localhost:3000/api/v1/work-orders | jq .

# D) Validation — doit retourner 400 (title trop court)
curl -s -w "\nHTTP %{http_code}\n" \
  -X POST -H "Authorization: Bearer $KORVIA_KEY" -H "Content-Type: application/json" \
  -d '{"title":"ab","type":"corrective","priority":"medium"}' \
  http://localhost:3000/api/v1/work-orders
```

5. Naviguer vers `/bons-de-travail` dans le navigateur — vérifier que le BT "Test API integration" créé via curl apparaît dans la liste.

**Vérif 5 — Révocation :**
1. Retourner à `/parametres/organisation`.
2. Cliquer "Révoquer" sur la ligne "Test integration" → confirmer le prompt.
3. Vérifier que le statut passe à "Révoquée" (gris) et que le bouton "Révoquer" disparaît.
4. Refaire la requête A) ci-dessus avec la même clé — doit retourner 401 (clé révoquée).

**Vérif 6 — Forfait Démarrage bloqué (GATE-01 integration):**
1. Se connecter sur une organisation au forfait Démarrage (starter).
2. Vérifier que la section "Clés API" n'apparaît PAS si l'utilisateur n'est pas admin/manager (et même pour un admin starter, le requireApiPlan bloque les appels REST avec 403).
3. (Optionnel) Générer une clé sur un compte growth, puis downgrader (si testable) et vérifier que les appels REST retournent 403.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 verifications pass. If any fails, describe the failure with: which step, expected vs actual, screenshot path (if applicable). The planner will route to /gsd-plan-phase --gaps.</resume-signal>  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/04-api-publique/04-RESEARCH.md (verify expected behavior matches research)
    - .planning/phases/04-api-publique/04-01-SUMMARY.md and 04-02-SUMMARY.md (confirm Plan 01 + 02 outputs)
  </read_first>
  <action>Run the manual verification steps documented in &lt;how-to-verify&gt; below. This task is operator-driven; the executor agent should print the steps clearly, start the dev server with `npm run dev`, then PAUSE for human input. Upon resume, record results in the SUMMARY (which vérif passed/failed, defect details if any).</action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All 6 verifications pass (operator types &quot;approved&quot;). Any failures route to /gsd-plan-phase --gaps with the failing requirement IDs.</done>

  <acceptance_criteria>
    - Vérif 1: Scalar UI loads at /api/docs without Clerk redirect
    - Vérif 2: /api/openapi.json returns valid JSON with openapi=3.1.0
    - Vérif 3: API key generation flow works end-to-end with raw key shown once
    - Vérif 4: All 4 curl scenarios return expected HTTP codes (200, 401, 201, 400)
    - Vérif 5: Revoked key returns 401 on subsequent calls
    - Vérif 6: Starter tier blocked from API access (no section OR 403 on REST calls)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous browser → /api/docs | Public, no auth — intentional |
| anonymous browser → /api/openapi.json | Public, no auth — intentional |
| admin/manager session → Server Actions | Clerk-authed; requireRole enforces admin/manager |
| React state holding raw key → DOM | Sensitive — must be wiped on dialog close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-18 | Information Disclosure | Raw key persisted in React state | mitigate | `setCreated(null)` in `handleClose()` wipes the state. Acceptance criterion `grep -c "setCreated(null)"` ≥ 1 enforces this. |
| T-4-19 | Information Disclosure | OpenAPI spec exposes internal models | mitigate | Spec is intentionally public (API-03). Schema only includes externally-relevant fields (id, number, title, type, priority, etc.) — NO internal fields like `clerkUserId`, `hashedKey`, or membership relations. |
| T-4-20 | Elevation of Privilege | Non-admin renders ApiKeysSection | mitigate | Role check in `page.tsx` (server) BEFORE rendering. Server Actions also re-check via `requireRole(['admin','manager'])` (Plan 01) — defense-in-depth. |
| T-4-21 | Tampering | Forged role in client state | mitigate | Server component controls rendering; client cannot trigger Server Actions if not admin/manager (Action throws). |
| T-4-22 | Spoofing | CSRF on Server Actions | accept | Next.js Server Actions include built-in CSRF protection via the framework's encrypted action ID. Inherits same protection as existing actions in the project. |
| T-4-23 | Information Disclosure | Accidental key leak in logs | mitigate | No `console.log(rawKey)` in code. Server Action returns key only in response; client never logs it. |
| T-4-24 | Information Disclosure | Swagger UI accidentally requires auth | accept | Intentionally public — verified in human checkpoint (incognito tab loads). |
| T-4-25 | Repudiation | Untracked key revocation | accept | Revoke sets `isActive: false`; the row remains for audit. Future enhancement: add an audit log table. |
</threat_model>

<verification>
After all 3 tasks:

```bash
# All Phase 4 tests green
npx vitest run \
  src/lib/api-auth.test.ts \
  src/actions/api-keys.test.ts \
  src/app/api/v1/work-orders/route.test.ts \
  src/lib/openapi.test.ts \
  src/components/settings/api-keys-section.test.tsx \
  --reporter=verbose

# Full project test suite
npx vitest run

# Type check + build
npx tsc --noEmit
npm run build

# Dependencies installed
node -e "console.log(require('./package.json').dependencies['@asteasolutions/zod-to-openapi'])"
node -e "console.log(require('./package.json').dependencies['@scalar/nextjs-api-reference'])"

# Human checkpoint approved
```
</verification>

<success_criteria>
- OpenAPI 3.1 spec generated from Zod schemas, served at /api/openapi.json (public, force-static)
- Scalar docs UI accessible at /api/docs without auth (validated in incognito)
- ApiKeysSection rendered on /parametres/organisation for admin/manager
- Raw API key shown once at creation with warning + copy button, wiped from state on close
- Generated key works against /api/v1/work-orders (curl verifies 200 + 201)
- Revoked key returns 401 on subsequent calls
- All Phase 4 tests GREEN (Plan 01 + 02 + 03 combined): 30+ passing tests
- `npm run build` succeeds with zero errors
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-publique/04-03-SUMMARY.md` documenting:
- Dependencies added with exact versions
- Routes exposed (/api/docs, /api/openapi.json) and their public/cached behavior
- Settings UI section behavior + security properties (raw key wipe, role gate)
- Curl examples for partner onboarding documentation
- Human verification results (which scenarios passed; any defects noted)
</output>
