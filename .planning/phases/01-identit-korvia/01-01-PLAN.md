---
phase: 01-identit-korvia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/brand/korvia-logo.tsx
  - src/app/icon.svg
autonomous: true
requirements:
  - BRAND-02
  - BRAND-03
must_haves:
  truths:
    - "Les tokens CSS de globals.css contiennent la palette Korvia (navy + ambre en oklch)"
    - "Le composant KorviaLogo existe et expose 3 variantes (color, white, dark)"
    - "Un favicon SVG Korvia est servi par Next.js depuis src/app/icon.svg"
    - "La police Inter est déclarée comme --font-heading dans @theme inline"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Tokens CSS Korvia (oklch) + mapping --font-heading vers --font-inter"
      contains: "oklch(0.17 0.04 240)"
    - path: "src/components/brand/korvia-logo.tsx"
      provides: "Composant React KorviaLogo avec variantes color/white/dark"
      exports: ["KorviaLogo"]
    - path: "src/app/icon.svg"
      provides: "Favicon SVG Korvia (symbole K ambre sur navy)"
      contains: "<svg"
  key_links:
    - from: "src/app/globals.css"
      to: "@theme inline"
      via: "mapping --font-heading: var(--font-inter)"
      pattern: "--font-heading:\\s*var\\(--font-inter\\)"
    - from: "src/components/brand/korvia-logo.tsx"
      to: "consommateurs (layout/landing)"
      via: "export named KorviaLogo"
      pattern: "export function KorviaLogo"
---

<objective>
Créer les fondations visuelles de l'identité Korvia : tokens CSS de couleur (palette "Acier québécois"), composant logo SVG réutilisable avec 3 variantes, et favicon SVG Next.js App Router.

Purpose: Ces 3 artefacts sont les contrats consommés par les Plans 02 et 03. Sans tokens corrects, l'app n'a pas la bonne palette. Sans composant logo, on dupliquerait le SVG dans 4 fichiers. Sans icon.svg, le favicon reste celui de shadcn.

Output:
- src/app/globals.css mis à jour (palette Korvia + --font-heading correctement mappé)
- src/components/brand/korvia-logo.tsx créé (composant avec variantes)
- src/app/icon.svg créé (favicon SVG conforme convention App Router)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-identit-korvia/01-RESEARCH.md
@.planning/phases/01-identit-korvia/01-UI-SPEC.md
@src/app/globals.css
@AGENTS.md

<interfaces>
<!-- Interface du composant à créer — consommée par Plans 02 et 03 -->

```typescript
// src/components/brand/korvia-logo.tsx
interface KorviaLogoProps {
  variant?: "color" | "white" | "dark"  // défaut: "color"
  size?: number                          // défaut: 32 (px)
  showWordmark?: boolean                 // défaut: true
  className?: string
}

export function KorviaLogo(props: KorviaLogoProps): JSX.Element
```

Variantes:
- "color" : symbole ambre #E8830C + wordmark "Korvia" en couleur dépendant du fond
- "white" : symbole blanc #FFFFFF + wordmark blanc (sur fond sombre)
- "dark"  : symbole navy #0F1C2E + wordmark navy (sur fond clair)
</interfaces>

<docs_to_consult>
Avant de toucher au code Next.js, consulter:
- node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/app-icons.md (convention icon.svg)
- node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md (variable fonts + CSS variables)
</docs_to_consult>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1: Mettre à jour globals.css avec les tokens Korvia</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (lecture complète avant modification)
    - .planning/phases/01-identit-korvia/01-UI-SPEC.md (section "Tokens CSS à remplacer dans globals.css")
    - .planning/phases/01-identit-korvia/01-RESEARCH.md (section "Code Examples / globals.css")
  </read_first>
  <action>
Modifier src/app/globals.css avec exactement les changements suivants :

1. Dans le bloc `@theme inline` (lignes 7-49) :
   - REMPLACER la ligne `--font-sans: var(--font-sans);` par `--font-sans: var(--font-geist-sans);`
   - REMPLACER la ligne `--font-heading: var(--font-sans);` par `--font-heading: var(--font-inter);`
   (Le reste du bloc @theme inline reste identique.)

2. REMPLACER intégralement le bloc `:root { ... }` (lignes 51-84) par les tokens du thème CLAIR Korvia (app authentifiée — surface claire #F5F6F8 avec sidebar navy) :

```css
:root {
  /* Thème clair — pages app authentifiée (surface claire, sidebar navy) */
  --background: oklch(0.97 0.005 240);       /* #F5F6F8 surface claire */
  --foreground: oklch(0.17 0.04 240);        /* navy sur fond clair */
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.17 0.04 240);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.17 0.04 240);
  --primary: oklch(0.64 0.15 55);            /* #E8830C ambre */
  --primary-foreground: oklch(0.98 0 0);     /* blanc sur ambre */
  --secondary: oklch(0.95 0.01 240);
  --secondary-foreground: oklch(0.17 0.04 240);
  --muted: oklch(0.95 0.01 240);
  --muted-foreground: oklch(0.45 0.03 240);
  --accent: oklch(0.64 0.15 55);             /* ambre = accent */
  --accent-foreground: oklch(0.98 0 0);
  --destructive: oklch(0.58 0.22 25);        /* #DC2626 */
  --border: oklch(0.90 0.01 240);
  --input: oklch(0.90 0.01 240);
  --ring: oklch(0.64 0.15 55);               /* ring = ambre */
  --chart-1: oklch(0.64 0.15 55);
  --chart-2: oklch(0.20 0.04 240);
  --chart-3: oklch(0.45 0.03 240);
  --chart-4: oklch(0.65 0.04 240);
  --chart-5: oklch(0.17 0.04 240);
  --radius: 0.5rem;                          /* 8px radius par UI-SPEC */
  /* Sidebar reste sombre même en mode :root clair */
  --sidebar: oklch(0.20 0.04 240);           /* #162136 */
  --sidebar-foreground: oklch(0.95 0.01 240);
  --sidebar-primary: oklch(0.64 0.15 55);    /* ambre */
  --sidebar-primary-foreground: oklch(0.98 0 0);
  --sidebar-accent: oklch(0.25 0.04 240);
  --sidebar-accent-foreground: oklch(0.95 0.01 240);
  --sidebar-border: oklch(1 0 0 / 8%);
  --sidebar-ring: oklch(0.64 0.15 55);
}
```

3. REMPLACER intégralement le bloc `.dark { ... }` (lignes 86-118) par les tokens du thème SOMBRE Korvia (landing page) :

```css
.dark {
  /* Thème sombre — landing page (fond navy plein) */
  --background: oklch(0.17 0.04 240);        /* #0F1C2E navy */
  --foreground: oklch(0.95 0.01 240);        /* #F0F4F8 */
  --card: oklch(0.20 0.04 240);              /* #162136 */
  --card-foreground: oklch(0.95 0.01 240);
  --popover: oklch(0.20 0.04 240);
  --popover-foreground: oklch(0.95 0.01 240);
  --primary: oklch(0.64 0.15 55);            /* #E8830C ambre */
  --primary-foreground: oklch(0.98 0 0);
  --secondary: oklch(0.20 0.04 240);
  --secondary-foreground: oklch(0.95 0.01 240);
  --muted: oklch(0.22 0.04 240);
  --muted-foreground: oklch(0.65 0.04 240);  /* #8BA3C0 */
  --accent: oklch(0.64 0.15 55);
  --accent-foreground: oklch(0.98 0 0);
  --destructive: oklch(0.58 0.22 25);
  --border: oklch(1 0 0 / 10%);
  --input: oklch(1 0 0 / 15%);
  --ring: oklch(0.64 0.15 55);
  --chart-1: oklch(0.64 0.15 55);
  --chart-2: oklch(0.20 0.04 240);
  --chart-3: oklch(0.45 0.03 240);
  --chart-4: oklch(0.65 0.04 240);
  --chart-5: oklch(0.95 0.01 240);
  --sidebar: oklch(0.20 0.04 240);
  --sidebar-foreground: oklch(0.95 0.01 240);
  --sidebar-primary: oklch(0.64 0.15 55);
  --sidebar-primary-foreground: oklch(0.98 0 0);
  --sidebar-accent: oklch(0.25 0.04 240);
  --sidebar-accent-foreground: oklch(0.95 0.01 240);
  --sidebar-border: oklch(1 0 0 / 10%);
  --sidebar-ring: oklch(0.64 0.15 55);
}
```

4. NE PAS modifier le bloc `@layer base { ... }` (lignes 120-130) — il reste tel quel.
5. NE PAS modifier les imports en tête de fichier (lignes 1-3) ni `@custom-variant dark` (ligne 5).

IMPORTANT — ne pas inventer d'autres valeurs : utiliser exactement les valeurs oklch ci-dessus, qui viennent de la UI-SPEC.
  </action>
  <verify>
    <automated>grep -q "oklch(0.17 0.04 240)" src/app/globals.css && grep -q "oklch(0.64 0.15 55)" src/app/globals.css && grep -q "var(--font-inter)" src/app/globals.css && grep -q "oklch(0.20 0.04 240)" src/app/globals.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "oklch(0.17 0.04 240)" src/app/globals.css` retourne au moins 1 ligne (navy primaire présent)
    - `grep "oklch(0.64 0.15 55)" src/app/globals.css` retourne au moins 4 lignes (--primary, --accent, --ring, --sidebar-primary dans :root et .dark)
    - `grep "var(--font-inter)" src/app/globals.css` retourne exactement 1 ligne
    - `grep "var(--font-geist-sans)" src/app/globals.css` retourne exactement 1 ligne
    - `grep "oklch(0.20 0.04 240)" src/app/globals.css` retourne au moins 2 lignes (--sidebar dans :root et :dark)
    - Aucune occurrence de `oklch(0.205 0 0)` ou `oklch(0.145 0 0)` (anciens tokens neutral) ne subsiste
  </acceptance_criteria>
  <done>
Tokens CSS de globals.css remplacés par la palette Korvia oklch. Mapping `--font-heading: var(--font-inter)` et `--font-sans: var(--font-geist-sans)` en place. Anciens tokens neutral shadcn supprimés.
  </done>
</task>

<task type="auto">
  <name>Tâche 2: Créer le composant KorviaLogo</name>
  <files>src/components/brand/korvia-logo.tsx</files>
  <read_first>
    - .planning/phases/01-identit-korvia/01-UI-SPEC.md (sections "Logo SVG — Spécification" et "Composants de phase / Logo Component")
    - .planning/phases/01-identit-korvia/01-RESEARCH.md (section "Pattern 5: Composant Logo SVG avec variantes")
    - src/lib/utils.ts (vérifier signature `cn` — déjà utilisé par les autres composants)
  </read_first>
  <action>
Créer le fichier `src/components/brand/korvia-logo.tsx` avec exactement le contenu suivant :

```tsx
import { cn } from "@/lib/utils"

interface KorviaLogoProps {
  variant?: "color" | "white" | "dark"
  size?: number
  showWordmark?: boolean
  className?: string
}

/**
 * Logo Korvia — symbole K géométrique + wordmark "Korvia".
 *
 * Variantes :
 * - "color" : symbole ambre (#E8830C), wordmark adapté au fond
 * - "white" : tout blanc (sur fond sombre)
 * - "dark"  : tout navy (sur fond clair / documents)
 *
 * Conforme UI-SPEC Phase 1 : viewBox 0 0 32 32, fill uniquement,
 * pas de stroke, accessible (role=img + title).
 */
export function KorviaLogo({
  variant = "color",
  size = 32,
  showWordmark = true,
  className,
}: KorviaLogoProps) {
  // Couleur du symbole K selon variante
  const symbolColor =
    variant === "color" ? "#E8830C" :
    variant === "white" ? "#FFFFFF" :
    "#0F1C2E"

  // Couleur du wordmark "Korvia"
  // - color : navy par défaut (lisible sur fond clair) ; sur fond sombre, le parent applique text-foreground
  // - white : blanc
  // - dark  : navy
  const wordmarkColor =
    variant === "white" ? "#FFFFFF" :
    variant === "dark" ? "#0F1C2E" :
    "currentColor"

  return (
    <span
      className={cn("inline-flex items-center gap-2", className)}
      aria-label="Korvia"
    >
      <svg
        width={size}
        height={size}
        viewBox="0 0 32 32"
        fill="none"
        xmlns="http://www.w3.org/2000/svg"
        role="img"
        aria-hidden={showWordmark ? "true" : undefined}
      >
        <title>Logo Korvia</title>
        {/* Barre verticale du K — rect plein 30% largeur (6px sur 20px utile) */}
        <rect x="6" y="6" width="6" height="20" fill={symbolColor} />
        {/* Diagonale supérieure du K — pointe effilée haut-droit, 35° */}
        <polygon
          points="12,15 22,6 26,6 14,18"
          fill={symbolColor}
        />
        {/* Diagonale inférieure du K — légèrement plus épaisse pour ancrage visuel, -35° */}
        <polygon
          points="12,15 14,14 26,26 22,26"
          fill={symbolColor}
        />
      </svg>
      {showWordmark && (
        <span
          style={{ color: wordmarkColor }}
          className="font-bold text-lg tracking-[-0.02em] leading-none"
        >
          Korvia
        </span>
      )}
    </span>
  )
}
```

Notes :
- Pas de "use client" — composant pur sans hooks, peut être rendu côté serveur.
- L'import `cn` depuis `@/lib/utils` doit déjà exister (vérifié dans d'autres composants).
- Géométrie du K : barre verticale rect + 2 polygones diagonaux. Fill uniquement, aucun stroke.
- Wordmark utilise `currentColor` pour la variante "color" : le parent applique `text-foreground` ou autre selon contexte (sidebar navy : on passera `style` ou wrapper text-white via variant="white").
- Le style inline `color: wordmarkColor` n'est utilisé que pour "white" et "dark" — pour "color", on hérite via currentColor.

Créer le répertoire `src/components/brand/` s'il n'existe pas (mkdir avant Write si besoin).
  </action>
  <verify>
    <automated>test -f src/components/brand/korvia-logo.tsx && grep -q "export function KorviaLogo" src/components/brand/korvia-logo.tsx && grep -q "variant.*color.*white.*dark" src/components/brand/korvia-logo.tsx && grep -q "#E8830C" src/components/brand/korvia-logo.tsx && grep -q "viewBox=\"0 0 32 32\"" src/components/brand/korvia-logo.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "korvia-logo" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `src/components/brand/korvia-logo.tsx` existe
    - Export named `KorviaLogo` présent (`grep "export function KorviaLogo"`)
    - Les 3 variantes color/white/dark sont gérées (`grep -E 'variant === "(color|white|dark)"'` retourne 3+ lignes)
    - Les 3 couleurs clés sont littéralement présentes : `#E8830C`, `#FFFFFF`, `#0F1C2E`
    - Le SVG a `viewBox="0 0 32 32"` et `role="img"` et `<title>Logo Korvia</title>`
    - Aucune erreur TypeScript : `npx tsc --noEmit` ne signale pas d'erreur dans korvia-logo.tsx
    - Pas de directive `"use client"` au sommet (composant serveur-compatible)
  </acceptance_criteria>
  <done>
Composant KorviaLogo créé avec interface `{ variant, size, showWordmark, className }`, 3 variantes color/white/dark, SVG inline 32×32, accessible (role=img + title), aucune dépendance externe.
  </done>
</task>

<task type="auto">
  <name>Tâche 3: Créer le favicon SVG Korvia (convention Next.js App Router)</name>
  <files>src/app/icon.svg</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/01-metadata/app-icons.md (convention `icon.(svg|png|...)` reconnue par Next.js App Router)
    - .planning/phases/01-identit-korvia/01-UI-SPEC.md (section "Favicon" — symbole K blanc sur fond ambre)
    - .planning/phases/01-identit-korvia/01-RESEARCH.md (Pitfall 1 : nom obligatoire `icon.svg`, jamais `favicon.svg`)
  </read_first>
  <action>
Créer le fichier `src/app/icon.svg` avec exactement le contenu suivant :

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <title>Korvia</title>
  <rect width="32" height="32" rx="6" fill="#E8830C"/>
  <rect x="9" y="7" width="5" height="18" fill="#FFFFFF"/>
  <polygon points="14,15 22,7 26,7 16,17" fill="#FFFFFF"/>
  <polygon points="14,15 16,14 26,25 22,25" fill="#FFFFFF"/>
</svg>
```

Caractéristiques :
- Fond : rect 32×32 arrondi rx=6 en ambre #E8830C
- Lettre K blanche : barre verticale + 2 diagonales (même structure géométrique que le composant React, légèrement décalée pour centrer visuellement dans le carré ambre)
- Pas de stroke, fill uniquement
- viewBox + width + height explicites pour rendu favicon net à toutes tailles
- Contient `<title>Korvia</title>` pour lecteurs d'écran

IMPORTANT : Le fichier DOIT s'appeler exactement `icon.svg` (pas `favicon.svg`). C'est la convention Next.js App Router qui injecte automatiquement `<link rel="icon" href="/icon?<hash>" type="image/svg+xml" sizes="any" />` dans le `<head>`.

Le fichier `src/app/favicon.ico` existant reste en place — il sert de fallback pour les navigateurs très anciens, `icon.svg` prend priorité dans les navigateurs modernes.
  </action>
  <verify>
    <automated>test -f src/app/icon.svg && grep -q "#E8830C" src/app/icon.svg && grep -q "viewBox=\"0 0 32 32\"" src/app/icon.svg && grep -q "<title>Korvia</title>" src/app/icon.svg && test ! -f src/app/favicon.svg && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Fichier `src/app/icon.svg` existe
    - Contient `#E8830C` (fond ambre)
    - Contient `#FFFFFF` (lettre K blanche)
    - Contient `viewBox="0 0 32 32"`
    - Contient `<title>Korvia</title>`
    - Le fichier `src/app/favicon.svg` n'existe PAS (mauvaise convention Next.js)
    - Le fichier `src/app/favicon.ico` existe toujours (fallback)
  </acceptance_criteria>
  <done>
Favicon SVG Korvia créé selon la convention Next.js App Router (`src/app/icon.svg`). Next.js injectera automatiquement le `<link rel="icon">` dans `<head>` au prochain build/dev.
  </done>
</task>

</tasks>

<verification>
Vérifications globales du Plan 01 :

```bash
# 1. Les 3 artefacts existent
test -f src/app/globals.css && test -f src/components/brand/korvia-logo.tsx && test -f src/app/icon.svg

# 2. Tokens Korvia présents dans globals.css
grep -q "oklch(0.17 0.04 240)" src/app/globals.css   # navy
grep -q "oklch(0.64 0.15 55)" src/app/globals.css    # ambre
grep -q "oklch(0.20 0.04 240)" src/app/globals.css   # sidebar navy

# 3. Mapping fonts correct
grep -q "var(--font-inter)" src/app/globals.css
grep -q "var(--font-geist-sans)" src/app/globals.css

# 4. Composant logo exporté et typé
grep -q "export function KorviaLogo" src/components/brand/korvia-logo.tsx

# 5. Pas d'erreur TypeScript
npx tsc --noEmit

# 6. Build Next.js réussit
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
Plan 01 réussi si :
- [ ] globals.css contient la palette Korvia complète (navy + ambre en oklch)
- [ ] globals.css mappe `--font-heading: var(--font-inter)`
- [ ] src/components/brand/korvia-logo.tsx exporte `KorviaLogo` avec 3 variantes
- [ ] src/app/icon.svg existe et est un SVG ambre/blanc valide
- [ ] `npx tsc --noEmit` ne signale aucune erreur introduite par ce plan
- [ ] `npm run build` réussit (Next.js détecte icon.svg automatiquement)
- [ ] Aucun reste des tokens shadcn neutral (oklch(0.205 0 0), oklch(0.145 0 0)) dans globals.css
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-identit-korvia/01-01-SUMMARY.md` documentant :
- Tokens finaux retenus (navy + ambre)
- Interface publique de KorviaLogo (à référencer dans Plans 02 et 03)
- Confirmation que favicon.ico reste en fallback
- Tout écart par rapport au plan (ex: ajustement géométrique du K)
</output>