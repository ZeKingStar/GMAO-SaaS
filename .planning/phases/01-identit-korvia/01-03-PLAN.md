---
phase: 01-identit-korvia
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/page.tsx
autonomous: false
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
must_haves:
  truths:
    - "La landing page (/) est encapsulée dans un wrapper className='dark' (active les tokens .dark de globals.css)"
    - "La navbar landing est fond navy plein (#0F1C2E) — pas de backdrop-blur, pas de bg-background/95"
    - "Le logo affiché dans la navbar landing est KorviaLogo variant='white'"
    - "Le H1 utilise font-heading (Inter) et le poids 700"
    - "Le CTA primaire 'Commencer gratuitement' est fond ambre #E8830C, texte blanc"
    - "Le footer affiche '© 2026 Korvia — Fait au Québec 🍁'"
    - "Aucune occurrence de 'GMAO SaaS' ou 'GMAO' (sauf le terme catégorie dans le H1) n'apparaît"
    - "Un checkpoint humain valide visuellement la landing après les changements"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page Korvia dark navy + accents ambre + KorviaLogo + copy validée"
      contains: "KorviaLogo"
  key_links:
    - from: "src/app/page.tsx"
      to: "@/components/brand/korvia-logo"
      via: "import { KorviaLogo }"
      pattern: "from ['\"]@/components/brand/korvia-logo['\"]"
    - from: "src/app/page.tsx racine div"
      to: "tokens .dark de globals.css"
      via: "className=\"dark\""
      pattern: "className=\"[^\"]*\\bdark\\b"
---

<objective>
Refondre intégralement la landing page (/) pour incarner l'identité visuelle Korvia : fond navy plein, accents ambre exclusivement aux endroits autorisés par la UI-SPEC, typographie Inter pour les headings, copy "Korvia" partout, et logo KorviaLogo dans la navbar.

Purpose: Couvre BRAND-02 ("La landing page reflète l'identité visuelle Korvia — couleurs, typographie distinctives") + BRAND-03 (variante "white" du logo sur fond sombre) + finalise BRAND-01 (élimination des dernières occurrences "GMAO SaaS").

Output:
- src/app/page.tsx : landing complète refondue, encapsulée dans `<div className="dark">` pour forcer les tokens sombres uniquement sur cette page (l'app authentifiée reste en :root clair).
- Checkpoint visuel humain pour valider la cohérence avec la UI-SPEC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-identit-korvia/01-RESEARCH.md
@.planning/phases/01-identit-korvia/01-UI-SPEC.md
@.planning/phases/01-identit-korvia/01-01-SUMMARY.md
@src/app/page.tsx
@AGENTS.md

<interfaces>
<!-- Composant à consommer depuis Plan 01 -->

```typescript
import { KorviaLogo } from "@/components/brand/korvia-logo"

// Pour la navbar landing (fond navy plein) : variant white
<KorviaLogo variant="white" size={32} showWordmark />
```

<!-- Tokens .dark disponibles via globals.css (Plan 01) -->
Quand <div className="dark"> wrap toute la page :
- `bg-background` → navy #0F1C2E
- `text-foreground` → #F0F4F8
- `text-muted-foreground` → #8BA3C0
- `bg-card` → #162136
- `bg-primary` / `text-primary` → ambre #E8830C
- `bg-secondary` → #162136
- `border-border` → rgba(255,255,255,0.10)
</interfaces>

<copywriting_contract>
Source : 01-UI-SPEC.md section "Copywriting Contract" — valeurs CONTRACTUELLES, à respecter au caractère près :

| Élément | Copie exacte |
|---------|--------------|
| CTA primaire (landing) | "Commencer gratuitement" |
| CTA secondaire (landing) | "Voir une démo" |
| CTA nav | "Démarrer" |
| Lien connexion | "Se connecter" |
| Badge hero | "Nouveau · Gestion de maintenance moderne" |
| H1 hero | "La GMAO moderne pour les PME québécoises" |
| Sous-titre hero | "Gérez vos actifs, planifiez la maintenance et coordonnez vos équipes — en français, conçu pour le marché québécois." |
| Social proof | "Essai gratuit 14 jours" / "Sans carte de crédit" / "Support en français" |
| H2 features | "Tout ce dont vous avez besoin" |
| H2 pricing | "Tarification simple et transparente" |
| Sous-titre pricing | "Facturation par organisation, pas par utilisateur. Changez de plan à tout moment." |
| CTA section finale (H2) | "Prêt à moderniser votre maintenance?" |
| Sous-titre section finale | "Rejoignez les entreprises québécoises qui ont choisi Korvia." |
| Footer | "© 2026 Korvia — Fait au Québec 🍁" |

Note : Le H1 conserve "GMAO" en tant que terme générique de catégorie produit ("La GMAO moderne pour..." = la solution de Gestion de Maintenance Assistée par Ordinateur). Ce n'est PAS le nom de marque — c'est le mot-clé SEO/positionnement. La marque reste exclusivement "Korvia" (badge, CTA, footer, etc.).
</copywriting_contract>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1: Refondre src/app/page.tsx en landing Korvia dark navy</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (état actuel — landing shadcn générique avec "GMAO" multiple)
    - .planning/phases/01-identit-korvia/01-UI-SPEC.md (sections "Color", "Typography", "Navbar Landing", "Hero Section", "Copywriting Contract")
    - .planning/phases/01-identit-korvia/01-01-SUMMARY.md (interface KorviaLogo)
  </read_first>
  <action>
Remplacer intégralement le contenu de `src/app/page.tsx` par le code ci-dessous.

Conservation : logique d'auth `auth()` + redirect (lignes 16-24 de l'original) reste identique.
Transformation : tout le JSX visuel est refondu pour la palette Korvia dark.

```tsx
import { auth } from "@clerk/nextjs/server"
import { redirect } from "next/navigation"
import Link from "next/link"
import {
  Cpu,
  ClipboardList,
  Calendar,
  Package,
  QrCode,
  Smartphone,
  CheckCircle2,
} from "lucide-react"
import { KorviaLogo } from "@/components/brand/korvia-logo"

export default async function Home() {
  const { userId, orgId } = await auth()

  if (userId && orgId) {
    redirect("/dashboard")
  }

  if (userId && !orgId) {
    redirect("/onboarding")
  }

  // !userId → afficher la landing page (forcer thème sombre Korvia local à cette page)
  return (
    <div className="dark min-h-screen bg-background text-foreground font-sans">
      {/* ── Navbar ── fond navy plein, pas de backdrop-blur */}
      <header className="sticky top-0 z-50 border-b border-border bg-background">
        <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-3">
          <Link href="/" className="flex items-center transition-opacity hover:opacity-85" aria-label="Korvia — Accueil">
            <KorviaLogo variant="white" size={32} showWordmark />
          </Link>

          <nav className="flex items-center gap-4">
            <Link
              href="/sign-in"
              className="text-sm font-normal text-muted-foreground transition-colors hover:text-foreground"
            >
              Se connecter
            </Link>
            <Link
              href="/sign-up"
              className="inline-flex h-11 items-center justify-center rounded-lg bg-primary px-4 text-sm font-bold text-primary-foreground transition-colors hover:bg-[#C97009] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background"
            >
              Démarrer
            </Link>
          </nav>
        </div>
      </header>

      {/* ── Hero ── */}
      <section className="bg-gradient-to-b from-[#0F1C2E] to-[#0D1824] py-24 text-center">
        <div className="mx-auto max-w-3xl px-6">
          <span className="mb-6 inline-block rounded-full border border-[rgba(232,131,12,0.3)] bg-[rgba(232,131,12,0.15)] px-3 py-1 text-sm font-normal text-primary">
            Nouveau · Gestion de maintenance moderne
          </span>

          <h1 className="mb-6 font-heading text-4xl font-bold leading-[1.15] tracking-tight text-foreground sm:text-[48px]">
            La GMAO moderne pour les{" "}
            <span className="text-primary">PME québécoises</span>
          </h1>

          <p className="mb-10 text-base font-normal leading-[1.5] text-muted-foreground sm:text-lg">
            Gérez vos actifs, planifiez la maintenance et coordonnez vos équipes
            — en français, conçu pour le marché québécois.
          </p>

          <div className="mb-8 flex flex-col items-center justify-center gap-3 sm:flex-row">
            <Link
              href="/sign-up"
              className="inline-flex h-11 items-center justify-center rounded-lg bg-primary px-6 text-base font-bold text-primary-foreground transition-colors hover:bg-[#C97009] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background"
            >
              Commencer gratuitement
            </Link>
            <Link
              href="#demo"
              className="inline-flex h-11 items-center justify-center rounded-lg border border-[rgba(255,255,255,0.2)] bg-transparent px-6 text-base font-normal text-foreground transition-colors hover:bg-[rgba(255,255,255,0.08)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-foreground focus-visible:ring-offset-2 focus-visible:ring-offset-background"
            >
              Voir une démo
            </Link>
          </div>

          <div className="flex flex-col items-center justify-center gap-2 text-sm text-muted-foreground sm:flex-row sm:gap-6">
            <span className="flex items-center gap-1.5">
              <CheckCircle2 className="size-4 text-primary" />
              Essai gratuit 14 jours
            </span>
            <span className="flex items-center gap-1.5">
              <CheckCircle2 className="size-4 text-primary" />
              Sans carte de crédit
            </span>
            <span className="flex items-center gap-1.5">
              <CheckCircle2 className="size-4 text-primary" />
              Support en français
            </span>
          </div>
        </div>
      </section>

      {/* ── Features ── */}
      <section className="bg-background py-20">
        <div className="mx-auto max-w-6xl px-6">
          <h2 className="mb-12 text-center font-heading text-[28px] font-bold leading-[1.2] text-foreground">
            Tout ce dont vous avez besoin
          </h2>

          <div className="grid gap-6 sm:grid-cols-2 md:grid-cols-3">
            {[
              {
                icon: Cpu,
                title: "Gestion des actifs",
                desc: "Centralisez tous vos équipements avec suivi QR et historique complet.",
              },
              {
                icon: ClipboardList,
                title: "Bons de travail",
                desc: "Créez, assignez et suivez les interventions en temps réel.",
              },
              {
                icon: Calendar,
                title: "Maintenance préventive",
                desc: "Planifiez automatiquement les maintenances récurrentes.",
              },
              {
                icon: Package,
                title: "Inventaire",
                desc: "Gérez vos pièces de rechange et recevez des alertes de stock faible.",
              },
              {
                icon: QrCode,
                title: "Codes QR",
                desc: "Scannez un actif pour accéder instantanément à son historique.",
              },
              {
                icon: Smartphone,
                title: "Application mobile",
                desc: "Interface PWA installable sur iOS et Android, fonctionne hors-ligne.",
              },
            ].map(({ icon: Icon, title, desc }) => (
              <div
                key={title}
                className="rounded-xl border border-border bg-card p-6"
              >
                <div className="mb-4 flex size-10 items-center justify-center rounded-lg bg-[rgba(232,131,12,0.12)]">
                  <Icon className="size-5 text-primary" />
                </div>
                <h3 className="mb-2 font-heading text-base font-bold text-card-foreground">{title}</h3>
                <p className="text-sm font-normal leading-[1.5] text-muted-foreground">{desc}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* ── Pricing ── */}
      <section className="bg-background py-20">
        <div className="mx-auto max-w-6xl px-6">
          <div className="mb-12 text-center">
            <h2 className="mb-3 font-heading text-[28px] font-bold leading-[1.2] text-foreground">
              Tarification simple et transparente
            </h2>
            <p className="text-base font-normal text-muted-foreground">
              Facturation par organisation, pas par utilisateur. Changez de plan
              à tout moment.
            </p>
          </div>

          <div className="grid gap-6 md:grid-cols-3">
            {/* Démarrage */}
            <div className="rounded-xl border border-border bg-card p-8">
              <h3 className="mb-1 font-heading text-lg font-bold text-card-foreground">Démarrage</h3>
              <p className="mb-4 text-sm text-muted-foreground">
                Jusqu&apos;à 5 utilisateurs
              </p>
              <div className="mb-6">
                <span className="font-heading text-4xl font-bold text-card-foreground">59 $</span>
                <span className="text-muted-foreground"> CAD/mois</span>
              </div>
              <ul className="mb-8 space-y-2 text-sm text-muted-foreground">
                {[
                  "Actifs illimités",
                  "BTs illimités",
                  "Maintenance préventive",
                  "QR codes",
                  "Support courriel",
                ].map((f) => (
                  <li key={f} className="flex items-center gap-2">
                    <CheckCircle2 className="size-4 shrink-0 text-primary" />
                    {f}
                  </li>
                ))}
              </ul>
              <Link
                href="/sign-up"
                className="block w-full rounded-lg border border-[rgba(255,255,255,0.2)] py-2.5 text-center text-sm font-bold text-foreground transition-colors hover:bg-[rgba(255,255,255,0.08)]"
              >
                Commencer
              </Link>
            </div>

            {/* Croissance — mis en avant avec accent ambre */}
            <div className="relative rounded-xl border border-primary bg-card p-8 ring-2 ring-primary">
              <span className="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full bg-primary px-3 py-1 text-xs font-bold text-primary-foreground">
                Populaire
              </span>
              <h3 className="mb-1 font-heading text-lg font-bold text-card-foreground">Croissance</h3>
              <p className="mb-4 text-sm text-muted-foreground">
                Jusqu&apos;à 15 utilisateurs
              </p>
              <div className="mb-6">
                <span className="font-heading text-4xl font-bold text-card-foreground">149 $</span>
                <span className="text-muted-foreground"> CAD/mois</span>
              </div>
              <ul className="mb-8 space-y-2 text-sm text-muted-foreground">
                {[
                  "Tout Démarrage",
                  "Rapports avancés",
                  "Inventaire pièces",
                  "API accès",
                  "Support prioritaire",
                ].map((f) => (
                  <li key={f} className="flex items-center gap-2">
                    <CheckCircle2 className="size-4 shrink-0 text-primary" />
                    {f}
                  </li>
                ))}
              </ul>
              <Link
                href="/sign-up"
                className="block w-full rounded-lg bg-primary py-2.5 text-center text-sm font-bold text-primary-foreground transition-colors hover:bg-[#C97009]"
              >
                Commencer
              </Link>
            </div>

            {/* Entreprise */}
            <div className="rounded-xl border border-border bg-card p-8">
              <h3 className="mb-1 font-heading text-lg font-bold text-card-foreground">Entreprise</h3>
              <p className="mb-4 text-sm text-muted-foreground">
                Utilisateurs illimités
              </p>
              <div className="mb-6">
                <span className="font-heading text-4xl font-bold text-card-foreground">349 $</span>
                <span className="text-muted-foreground"> CAD/mois</span>
              </div>
              <ul className="mb-8 space-y-2 text-sm text-muted-foreground">
                {[
                  "Tout Croissance",
                  "SSO",
                  "Audit log",
                  "SLA garanti",
                  "Support dédié",
                ].map((f) => (
                  <li key={f} className="flex items-center gap-2">
                    <CheckCircle2 className="size-4 shrink-0 text-primary" />
                    {f}
                  </li>
                ))}
              </ul>
              <Link
                href="/sign-up"
                className="block w-full rounded-lg border border-[rgba(255,255,255,0.2)] py-2.5 text-center text-sm font-bold text-foreground transition-colors hover:bg-[rgba(255,255,255,0.08)]"
              >
                Commencer
              </Link>
            </div>
          </div>
        </div>
      </section>

      {/* ── CTA final ── fond secondary (#162136), pas full ambre */}
      <section className="bg-secondary py-16 text-center">
        <div className="mx-auto max-w-2xl px-6">
          <h2 className="mb-4 font-heading text-[28px] font-bold leading-[1.2] text-foreground">
            Prêt à moderniser votre maintenance?
          </h2>
          <p className="mb-8 text-base font-normal text-muted-foreground">
            Rejoignez les entreprises québécoises qui ont choisi Korvia.
          </p>
          <Link
            href="/sign-up"
            className="inline-flex h-11 items-center justify-center rounded-lg bg-primary px-6 text-base font-bold text-primary-foreground transition-colors hover:bg-[#C97009] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 focus-visible:ring-offset-background"
          >
            Commencer gratuitement
          </Link>
        </div>
      </section>

      {/* ── Footer ── */}
      <footer className="border-t border-border bg-background py-8 text-center text-sm font-normal text-muted-foreground">
        <div className="mx-auto max-w-6xl px-6">
          <p className="mb-3">© 2026 Korvia — Fait au Québec 🍁</p>
          <nav className="flex items-center justify-center gap-6">
            <a href="#" className="transition-colors hover:text-foreground">
              Confidentialité
            </a>
            <a href="#" className="transition-colors hover:text-foreground">
              Conditions
            </a>
            <a href="#" className="transition-colors hover:text-foreground">
              Support
            </a>
          </nav>
        </div>
      </footer>
    </div>
  )
}
```

Choix de design conformes UI-SPEC :
- Wrapper racine `<div className="dark ...">` : isole le thème sombre à la landing (l'app authentifiée reste claire).
- Navbar : `bg-background` (= navy via .dark), pas de backdrop-blur (UI-SPEC : "fond plein navy").
- CTA primaire ambre + hover #C97009 (assombri 15%, UI-SPEC).
- H1 : `text-[48px]` au breakpoint sm, font-heading (Inter), poids 700, leading 1.15.
- Headings de section : `text-[28px]` font-heading, poids 700, leading 1.2.
- Body : `text-base` (16px), font-sans (Geist via base), poids 400.
- Labels : `text-sm` (14px).
- L'accent ambre n'apparaît QUE dans : badge hero (1), CTA primaires (2,3 occurrences), badge "Populaire" (3), CheckCircle2 (4), icônes feature cards (5), span H1 "PME québécoises" (6), card pricing Croissance ring (7), focus-visible ring. Conforme à la liste réservée UI-SPEC.
- CTA final section : `bg-secondary` (#162136) au lieu de full ambre, le bouton ambre attire dans un fond plus sombre. UI-SPEC interdit l'ambre comme grand fond plein.
- Footer : "© 2026" (mise à jour de "© 2025") + "Korvia" (au lieu de "GMAO SaaS").
- Anneau focus visible sur tous les CTA (accessibilité WCAG).
- Hover du logo : `hover:opacity-85` (UI-SPEC : opacité 0.85 sur hover, transition 150ms).
  </action>
  <verify>
    <automated>grep -q 'import { KorviaLogo } from "@/components/brand/korvia-logo"' src/app/page.tsx && grep -q 'className="dark min-h-screen' src/app/page.tsx && grep -q 'variant="white"' src/app/page.tsx && grep -q "Commencer gratuitement" src/app/page.tsx && grep -q "© 2026 Korvia — Fait au Québec" src/app/page.tsx && grep -q "Rejoignez les entreprises québécoises qui ont choisi Korvia" src/app/page.tsx && ! grep -q "GMAO SaaS" src/app/page.tsx && ! grep -q "© 2025" src/app/page.tsx && ! grep -q "ont déjà fait confiance à" src/app/page.tsx && grep -q "font-heading" src/app/page.tsx && grep -q "#C97009" src/app/page.tsx && grep -c "GMAO" src/app/page.tsx | awk '$1==1{print "OK"} $1!=1{print "FAIL count="$1}'</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `src/app/page.tsx` importe `KorviaLogo` depuis `@/components/brand/korvia-logo`
    - La div racine retournée a `className="dark min-h-screen ..."` (commence par "dark")
    - `<KorviaLogo variant="white" size={32} showWordmark />` est utilisé exactement une fois (navbar)
    - Le H1 contient exactement "La GMAO moderne pour les PME québécoises" (le mot "GMAO" ici est volontaire — terme catégorie)
    - Le footer contient exactement "© 2026 Korvia — Fait au Québec 🍁"
    - Le sous-titre CTA final contient "Rejoignez les entreprises québécoises qui ont choisi Korvia"
    - Le badge hero contient "Nouveau · Gestion de maintenance moderne"
    - Le CTA navbar dit "Démarrer" (pas "Commencer gratuitement")
    - Le CTA hero primaire dit "Commencer gratuitement"
    - `grep "GMAO SaaS" src/app/page.tsx` retourne 0 ligne
    - `grep "© 2025" src/app/page.tsx` retourne 0 ligne
    - `grep "ont déjà fait confiance à" src/app/page.tsx` retourne 0 ligne (ancienne copy)
    - `grep -c "GMAO" src/app/page.tsx` retourne exactement 1 (uniquement dans le H1)
    - Au moins une classe `font-heading` est appliquée (H1, H2)
    - La couleur de hover #C97009 est utilisée pour les CTA primaires
    - Aucune occurrence de `bg-primary py-16 text-center text-primary-foreground` (ancienne CTA section ambre pleine) — UI-SPEC interdit ambre comme grand fond
    - `npx tsc --noEmit` ne signale aucune erreur
  </acceptance_criteria>
  <done>
La landing page est intégralement refondue en thème dark Korvia (navy + accents ambre), copy mise à jour ("Korvia" partout sauf catégorie SEO "GMAO" dans le H1), KorviaLogo intégré, conforme UI-SPEC.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tâche 2: Validation visuelle humaine de la landing</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - .planning/phases/01-identit-korvia/01-UI-SPEC.md (Checker Sign-Off — 6 dimensions)
  </read_first>
  <action>
PAUSE OBLIGATOIRE — L'agent exécutant doit STOPPER après la Tâche 1 et présenter à l'utilisateur le bloc `<what-built>` + `<how-to-verify>` ci-dessous, puis attendre le signal de reprise (`<resume-signal>`).

Aucune modification de code n'est attendue dans cette tâche : c'est une porte humaine bloquante. L'agent NE doit PAS continuer à d'autres tâches ou plans tant que l'utilisateur n'a pas répondu.
  </action>
  <what-built>
Landing page Korvia complète refondue :
- Wrapper `<div className="dark">` pour isoler le thème sombre
- Navbar fond navy plein avec KorviaLogo (variant white) + CTA "Démarrer" ambre
- Hero gradient navy avec badge ambre pill, H1 Inter 700 avec span "PME québécoises" en ambre, CTAs primaire ambre / secondaire outline blanc
- Section Features : 6 cards `bg-card` (#162136), icônes ambre dans squircles ambre 12%
- Section Pricing : 3 plans, card Croissance avec ring ambre + badge "Populaire" ambre
- Section CTA final : `bg-secondary` (#162136) avec CTA primaire ambre
- Footer "© 2026 Korvia — Fait au Québec 🍁"
- Favicon SVG ambre/blanc dans l'onglet du navigateur
- Inter chargé pour les headings, Geist pour le body
  </what-built>
  <how-to-verify>
1. Démarrer le serveur de dev :
   ```bash
   npm run dev
   ```

2. Ouvrir http://localhost:3000 (en navigation privée ou déconnecté de Clerk pour voir la landing — sinon vous serez redirigé).

3. Vérifications visuelles obligatoires :
   - [ ] **Onglet navigateur** : le favicon affiche un carré ambre avec un K blanc (PAS l'icône shadcn par défaut)
   - [ ] **Titre onglet** : "Korvia — GMAO pour PME québécoises"
   - [ ] **Navbar** : fond navy plein (#0F1C2E), pas de transparence/blur. Logo KorviaLogo (K blanc + "Korvia" blanc) à gauche. À droite : "Se connecter" en gris clair + "Démarrer" en bouton ambre
   - [ ] **Hero** : fond gradient subtil navy. Badge "Nouveau · Gestion de maintenance moderne" en pill ambre semi-transparent. H1 en Inter 700 grand. "PME québécoises" en ambre. Sous-titre en gris bleu (#8BA3C0). CTA "Commencer gratuitement" en ambre, CTA "Voir une démo" en outline blanc transparent
   - [ ] **Social proof** : 3 lignes avec CheckCircle2 ambre + texte gris
   - [ ] **Features** : 6 cards #162136 avec bordure subtile, icône ambre dans carré ambre semi-transparent, titre blanc, description grise
   - [ ] **Pricing** : 3 cards. La carte "Croissance" du milieu a un anneau ambre épais + badge "Populaire" ambre au sommet. Tous les prix en gros. Bouton de la carte Croissance en ambre, les deux autres en outline
   - [ ] **CTA final** : fond #162136 (PAS ambre plein), titre blanc "Prêt à moderniser votre maintenance?", sous-titre "Rejoignez les entreprises québécoises qui ont choisi Korvia.", bouton ambre
   - [ ] **Footer** : "© 2026 Korvia — Fait au Québec 🍁" + 3 liens (Confidentialité / Conditions / Support)
   - [ ] **Aucune occurrence visible** de "GMAO SaaS" ou "© 2025" nulle part sur la page

4. Vérifications interactives :
   - [ ] Hover sur le logo : opacité ~0.85
   - [ ] Hover sur le bouton "Démarrer" / "Commencer gratuitement" : la couleur passe de #E8830C à #C97009 (légèrement plus foncé)
   - [ ] Tab clavier sur les CTA : un anneau ambre épais s'affiche autour de l'élément focusé
   - [ ] Le lien "Se connecter" passe de gris à blanc au hover

5. Vérifications typographiques :
   - [ ] Les H1 et H2 ont une graisse forte (700) avec une lettre légèrement plus serrée — c'est Inter
   - [ ] Le body texte est dans Geist (différent visuellement d'Inter — Geist a des proportions plus "modernes/geometriques")

6. Vérifications navigation app (très important pour ne pas casser l'authentifié) :
   - Se connecter avec Clerk dans un autre onglet
   - [ ] Le dashboard `/dashboard` reste en mode CLAIR (surface #F5F6F8) avec sidebar navy
   - [ ] La sidebar montre le logo Korvia (symbole ambre + wordmark blanc)
   - [ ] L'onboarding `/onboarding` (si vous créez un nouveau compte) affiche "Bienvenue dans Korvia"

7. Vérifications mobile (responsive) :
   - Réduire la fenêtre à largeur < 1024px (lg breakpoint)
   - [ ] Sur landing : layout 1 colonne, navbar reste navy
   - [ ] Sur app authentifiée : la sidebar disparaît, un bouton menu (Sheet) apparaît
   - [ ] Cliquer le menu : le drawer mobile montre aussi le KorviaLogo

8. Vérifications grep finales :
   ```bash
   grep -rn "GMAO" src/ --include="*.tsx" --include="*.ts"
   ```
   Doit retourner uniquement :
   - `src/app/layout.tsx` : `"Korvia — GMAO pour PME québécoises"` (catégorie SEO)
   - `src/app/manifest.ts` : `"Korvia — GMAO pour PME québécoises"` (idem)
   - `src/app/page.tsx` : `"La GMAO moderne pour les PME québécoises"` (H1 catégorie)
   Aucune autre occurrence ne doit subsister.
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint humain — vérification visuelle, pas d'automation possible"</automated>
  </verify>
  <acceptance_criteria>
    - L'utilisateur a tapé "approuvé" (ou équivalent positif) en réponse au resume-signal
    - OU l'utilisateur a fourni des correctifs et la Tâche 1 a été reprise pour les appliquer
  </acceptance_criteria>
  <done>
L'utilisateur a validé visuellement la landing page Korvia. Les 6 dimensions du Checker Sign-Off de la UI-SPEC sont confirmées comme PASS (Copywriting, Visuals, Color, Typography, Spacing, Registry Safety).
  </done>
  <resume-signal>Tapez "approuvé" pour confirmer, ou décrivez précisément les écarts observés (ex: "le badge Populaire dépasse le card", "le hover du logo ne fonctionne pas", "le footer affiche 2025 au lieu de 2026").</resume-signal>
</task>

</tasks>

<verification>
Vérifications globales du Plan 03 :

```bash
# 1. KorviaLogo importé une seule fois
grep -c "import { KorviaLogo }" src/app/page.tsx
# → 1

# 2. Wrapper dark présent
grep -q 'className="dark' src/app/page.tsx

# 3. Copy contractuelle conforme
grep -q "© 2026 Korvia — Fait au Québec 🍁" src/app/page.tsx
grep -q "Rejoignez les entreprises québécoises qui ont choisi Korvia" src/app/page.tsx

# 4. Anciennes copies éliminées
! grep -q "GMAO SaaS" src/app/page.tsx
! grep -q "© 2025" src/app/page.tsx
! grep -q "ont déjà fait confiance à" src/app/page.tsx

# 5. Une seule occurrence "GMAO" autorisée (H1 catégorie)
[ "$(grep -c "GMAO" src/app/page.tsx)" = "1" ]

# 6. Inventaire global "GMAO" dans src/ — uniquement les 3 emplacements catégorie SEO
grep -rn "GMAO" src/ --include="*.tsx" --include="*.ts" | wc -l
# → 3 (layout.tsx, manifest.ts, page.tsx — un chacun)

# 7. Build Next.js réussit
npm run build 2>&1 | tail -5

# 8. TypeScript propre
npx tsc --noEmit
```
</verification>

<success_criteria>
Plan 03 réussi si :
- [ ] src/app/page.tsx est encapsulé dans `<div className="dark">`
- [ ] Toutes les copies contractuelles de la UI-SPEC sont présentes au caractère près
- [ ] Aucune trace de "GMAO SaaS" ou "© 2025" dans page.tsx
- [ ] Une seule occurrence "GMAO" dans page.tsx (le H1 catégorie SEO)
- [ ] KorviaLogo variant="white" est utilisé dans la navbar
- [ ] L'ambre n'apparaît qu'aux emplacements autorisés par UI-SPEC (badge, CTA, ring Croissance, CheckCircle2, icons features, span H1)
- [ ] Le checkpoint visuel humain est approuvé
- [ ] `npm run build` réussit
- [ ] `npx tsc --noEmit` passe sans erreur
- [ ] `grep -rn "GMAO" src/ --include="*.tsx" --include="*.ts" | wc -l` retourne 3 (les 3 emplacements catégorie SEO autorisés)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-identit-korvia/01-03-SUMMARY.md` documentant :
- Toutes les sections de la landing refondues et leur correspondance UI-SPEC
- Captures (ou descriptions) des avant/après pour : navbar, hero, features, pricing, CTA final, footer
- Résultat du checkpoint visuel et toute correction apportée
- Inventaire final des occurrences "GMAO" résiduelles (les 3 SEO autorisées)
- Confirmation que l'app authentifiée n'est pas affectée par le `.dark` local
</output>
