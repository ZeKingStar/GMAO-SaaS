---
phase: 01-identit-korvia
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/layout.tsx
  - src/components/layout/sidebar.tsx
  - src/components/layout/sidebar-sheet.tsx
  - src/components/layout/header.tsx
  - src/app/(auth)/onboarding/page.tsx
  - src/app/manifest.ts
autonomous: true
requirements:
  - BRAND-01
must_haves:
  truths:
    - "Le metadata title HTML du site est 'Korvia — GMAO pour PME québécoises'"
    - "La sidebar app authentifiée affiche le logo Korvia (KorviaLogo, variant='color')"
    - "La sidebar mobile (sheet) affiche le logo Korvia"
    - "Le fallback de titre du Header est 'Korvia' (plus 'GMAO')"
    - "La page onboarding ne contient plus 'votre GMAO' générique"
    - "Le manifest PWA déclare name='Korvia' et themeColor='#0F1C2E'"
    - "La police Inter est chargée par next/font/google avec variable='--font-inter'"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Inter + Geist chargés, metadata Korvia, themeColor navy"
      contains: "Inter("
    - path: "src/components/layout/sidebar.tsx"
      provides: "Sidebar avec KorviaLogo (color) au lieu de l'icône Wrench + 'GMAO'"
      contains: "KorviaLogo"
    - path: "src/components/layout/sidebar-sheet.tsx"
      provides: "Sheet mobile avec KorviaLogo"
      contains: "KorviaLogo"
    - path: "src/app/manifest.ts"
      provides: "Manifest PWA Korvia"
      contains: "Korvia"
  key_links:
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "import { Inter, Geist }"
      pattern: "from ['\"]next/font/google['\"]"
    - from: "src/app/layout.tsx"
      to: "html className"
      via: "inter.variable + geistSans.variable"
      pattern: "inter\\.variable.*geistSans\\.variable|geistSans\\.variable.*inter\\.variable"
    - from: "src/components/layout/sidebar.tsx"
      to: "@/components/brand/korvia-logo"
      via: "import { KorviaLogo }"
      pattern: "from ['\"]@/components/brand/korvia-logo['\"]"
---

<objective>
Rebrander toute l'interface authentifiée (layout racine, sidebar desktop, sidebar mobile, header, onboarding, manifest PWA) avec l'identité Korvia : remplacer chaque occurrence textuelle "GMAO", brancher la police Inter, intégrer le composant KorviaLogo, mettre à jour les metadata.

Purpose: Couvre BRAND-01 ("L'interface affiche le nom Korvia et le logo SVG partout"). Toutes les vues de l'app authentifiée et la PWA doivent montrer Korvia, pas "GMAO".

Output:
- layout.tsx : Inter chargé, metadata title/description Korvia, themeColor navy
- sidebar.tsx + sidebar-sheet.tsx : logo KorviaLogo au lieu de l'icône Wrench
- header.tsx : fallback "Korvia" au lieu de "GMAO"
- onboarding/page.tsx : titre "Bienvenue dans Korvia" au lieu de "votre GMAO"
- manifest.ts : name "Korvia" + themeColor "#0F1C2E"
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-identit-korvia/01-RESEARCH.md
@.planning/phases/01-identit-korvia/01-UI-SPEC.md
@.planning/phases/01-identit-korvia/01-01-SUMMARY.md
@src/app/layout.tsx
@src/components/layout/sidebar.tsx
@src/components/layout/sidebar-sheet.tsx
@src/components/layout/header.tsx
@src/app/(auth)/onboarding/page.tsx
@src/app/manifest.ts
@AGENTS.md

<interfaces>
<!-- Interface du composant KorviaLogo créé au Plan 01 — à consommer ici -->

```typescript
// src/components/brand/korvia-logo.tsx (créé par Plan 01-01)
import { KorviaLogo } from "@/components/brand/korvia-logo"

<KorviaLogo
  variant="color" | "white" | "dark"   // défaut: "color"
  size={32}                             // défaut: 32 (px)
  showWordmark={true}                   // défaut: true
  className="..."                       // optionnel
/>
```

Pour la sidebar (fond navy --sidebar #162136) :
- Utiliser `variant="white"` car le fond sidebar est sombre dans tous les thèmes
- Le wordmark sera blanc, le symbole K sera blanc
- Alternative : `variant="color"` avec wrapper `text-sidebar-foreground` pour wordmark blanc + symbole ambre (préféré pour la touche d'accent)

Recommandation UI-SPEC : sidebar app utilise variant "color" (symbole ambre + wordmark blanc via text-sidebar-foreground sur le span parent).
</interfaces>

<docs_to_consult>
- node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md (variable fonts : ne pas spécifier weight pour Inter — c'est une variable font)
- node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-metadata.md (Metadata title/template/appleWebApp)
- node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-viewport.md (themeColor — export séparé)
</docs_to_consult>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1: Mettre à jour layout.tsx (Inter + Geist + metadata Korvia + themeColor)</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (état actuel — seul Geist chargé, metadata "GMAO")
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/font.md (sections "Variable fonts" et "Using multiple fonts")
  </read_first>
  <action>
Remplacer intégralement le contenu de `src/app/layout.tsx` par :

```tsx
import type { Metadata, Viewport } from "next"
import { Geist, Inter } from "next/font/google"
import { ClerkProvider } from "@clerk/nextjs"
import { frFR } from "@clerk/localizations"
import { Toaster } from "sonner"
import { ServiceWorkerRegister } from "@/components/pwa/service-worker-register"
import "./globals.css"

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
})

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
})

export const metadata: Metadata = {
  title: { default: "Korvia — GMAO pour PME québécoises", template: "%s | Korvia" },
  description: "Gérez vos actifs, bons de travail et maintenance préventive en français.",
  appleWebApp: {
    capable: true,
    statusBarStyle: "default",
    title: "Korvia",
  },
}

export const viewport: Viewport = {
  themeColor: "#0F1C2E",
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  viewportFit: "cover",
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <ClerkProvider localization={frFR} afterSignOutUrl="/sign-in">
      <html lang="fr" className={`${geistSans.variable} ${inter.variable} h-full antialiased`}>
        <body className="min-h-full bg-background text-foreground">
          {children}
          <Toaster richColors position="top-right" />
          <ServiceWorkerRegister />
        </body>
      </html>
    </ClerkProvider>
  )
}
```

Changements précis par rapport à l'existant :
- Import : ajouter `Inter` à la liste depuis `next/font/google`
- Déclarer la constante `inter` (PAS de `weight` spécifié — Inter est une variable font, inclut tous les poids ; doc Next.js confirme : "If loading a variable font, you don't need to specify the font weight")
- Ajouter `display: "swap"` sur les deux fonts (best practice anti FOUT)
- `metadata.title.default` : `"Korvia — GMAO pour PME québécoises"` (la mention "GMAO" ici est le terme générique de catégorie produit, intentionnel pour SEO — pas la marque)
- `metadata.title.template` : `"%s | Korvia"`
- `metadata.description` : `"Gérez vos actifs, bons de travail et maintenance préventive en français."`
- `metadata.appleWebApp.title` : `"Korvia"`
- `viewport.themeColor` : `"#0F1C2E"` (était `"#09090b"`)
- `<html className>` : ajouter `${inter.variable}` à côté de `${geistSans.variable}`

Note : "GMAO" dans le `title.default` est volontaire — c'est la catégorie produit (Gestion de Maintenance Assistée par Ordinateur) utilisée comme mot-clé SEO. Le nom de marque reste exclusivement "Korvia" partout ailleurs (template, appleWebApp.title, description).
  </action>
  <verify>
    <automated>grep -q "import { Geist, Inter }" src/app/layout.tsx && grep -q "variable: \"--font-inter\"" src/app/layout.tsx && grep -q "Korvia — GMAO pour PME québécoises" src/app/layout.tsx && grep -q "\"%s | Korvia\"" src/app/layout.tsx && grep -q "themeColor: \"#0F1C2E\"" src/app/layout.tsx && grep -q 'title: "Korvia"' src/app/layout.tsx && grep -q "inter.variable" src/app/layout.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { Geist, Inter }" src/app/layout.tsx` matche
    - `grep "variable: \"--font-inter\"" src/app/layout.tsx` matche
    - Pas de `weight:` spécifié pour Inter (variable font)
    - `grep "Korvia — GMAO pour PME québécoises" src/app/layout.tsx` matche
    - `grep "%s | Korvia" src/app/layout.tsx` matche
    - `grep "themeColor: \"#0F1C2E\"" src/app/layout.tsx` matche (et pas `#09090b`)
    - `grep "appleWebApp" -A 4 src/app/layout.tsx` montre `title: "Korvia"`
    - `<html className>` contient `${inter.variable}` ET `${geistSans.variable}`
    - `npx tsc --noEmit` ne signale aucune erreur sur layout.tsx
  </acceptance_criteria>
  <done>
layout.tsx charge Inter (variable font) + Geist, expose les deux CSS variables, déclare metadata Korvia, themeColor navy. Le titre HTML du site sera "Korvia — GMAO pour PME québécoises" au prochain build.
  </done>
</task>

<task type="auto">
  <name>Tâche 2: Mettre à jour la sidebar desktop et le sheet mobile avec KorviaLogo</name>
  <files>src/components/layout/sidebar.tsx, src/components/layout/sidebar-sheet.tsx, src/components/layout/header.tsx</files>
  <read_first>
    - src/components/layout/sidebar.tsx (état actuel — div bg-primary + Wrench + "GMAO" lignes 35-40)
    - src/components/layout/sidebar-sheet.tsx (état actuel — même bloc à la ligne 32-37)
    - src/components/layout/header.tsx (état actuel — fallback "GMAO" ligne 23)
    - .planning/phases/01-identit-korvia/01-01-SUMMARY.md (interface KorviaLogo finalisée)
  </read_first>
  <action>
**Fichier A : `src/components/layout/sidebar.tsx`**

1. Dans les imports en tête (ligne 4-14) :
   - SUPPRIMER `Wrench,` de la liste d'icônes importée depuis `lucide-react` (ligne 13)
   - AJOUTER après l'import lucide-react : `import { KorviaLogo } from "@/components/brand/korvia-logo"`

2. REMPLACER le bloc logo (lignes 35-40) :
   ```tsx
   <div className="flex items-center gap-2 px-6 py-5 border-b">
     <div className="flex items-center justify-center w-8 h-8 rounded-md bg-primary text-primary-foreground">
       <Wrench className="h-4 w-4" />
     </div>
     <span className="font-bold text-lg tracking-tight">GMAO</span>
   </div>
   ```
   PAR :
   ```tsx
   <div className="flex items-center px-6 py-5 border-b text-sidebar-foreground">
     <KorviaLogo variant="color" size={32} showWordmark />
   </div>
   ```

   Justification du choix variant="color" : le fond sidebar est navy (--sidebar #162136) — le symbole K en ambre apporte la touche d'accent ; le wordmark hérite via `currentColor` du `text-sidebar-foreground` (blanc cassé #F0F4F8) du parent.

**Fichier B : `src/components/layout/sidebar-sheet.tsx`**

1. Dans les imports (ligne 6) :
   - SUPPRIMER `Wrench,` de l'import lucide-react
   - AJOUTER : `import { KorviaLogo } from "@/components/brand/korvia-logo"`

2. REMPLACER le bloc logo (lignes 32-37) :
   ```tsx
   <div className="flex items-center gap-2 px-6 py-5 border-b">
     <div className="flex items-center justify-center w-8 h-8 rounded-md bg-primary text-primary-foreground">
       <Wrench className="h-4 w-4" />
     </div>
     <span className="font-bold text-lg tracking-tight">GMAO</span>
   </div>
   ```
   PAR :
   ```tsx
   <div className="flex items-center px-6 py-5 border-b text-sidebar-foreground">
     <KorviaLogo variant="color" size={32} showWordmark />
   </div>
   ```

**Fichier C : `src/components/layout/header.tsx`**

REMPLACER la ligne 23 :
```ts
  return "GMAO"
```
PAR :
```ts
  return "Korvia"
```

Aucun autre changement dans header.tsx.
  </action>
  <verify>
    <automated>grep -q 'import { KorviaLogo } from "@/components/brand/korvia-logo"' src/components/layout/sidebar.tsx && grep -q 'import { KorviaLogo } from "@/components/brand/korvia-logo"' src/components/layout/sidebar-sheet.tsx && ! grep -q "Wrench" src/components/layout/sidebar.tsx && ! grep -q "Wrench" src/components/layout/sidebar-sheet.tsx && ! grep -q '"GMAO"' src/components/layout/sidebar.tsx && ! grep -q '"GMAO"' src/components/layout/sidebar-sheet.tsx && grep -q 'return "Korvia"' src/components/layout/header.tsx && ! grep -q 'return "GMAO"' src/components/layout/header.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `sidebar.tsx` importe `KorviaLogo` depuis `@/components/brand/korvia-logo`
    - `sidebar.tsx` n'importe plus `Wrench` de lucide-react
    - `sidebar.tsx` ne contient plus la chaîne `"GMAO"` (test : `! grep '"GMAO"' src/components/layout/sidebar.tsx`)
    - `sidebar.tsx` contient `<KorviaLogo variant="color" size={32} showWordmark />`
    - `sidebar-sheet.tsx` même validations : import KorviaLogo, pas de Wrench, pas de "GMAO", `<KorviaLogo ... />` présent
    - `header.tsx` : `grep 'return "Korvia"' src/components/layout/header.tsx` matche
    - `header.tsx` : `grep 'return "GMAO"' src/components/layout/header.tsx` ne matche pas
    - `npx tsc --noEmit` ne signale pas d'erreur dans ces 3 fichiers
  </acceptance_criteria>
  <done>
Sidebar desktop, sidebar mobile (sheet) et header arborent l'identité Korvia. L'icône Wrench shadcn est éliminée. Le composant KorviaLogo est consommé via son import canonique.
  </done>
</task>

<task type="auto">
  <name>Tâche 3: Rebrand onboarding + manifest PWA</name>
  <files>src/app/(auth)/onboarding/page.tsx, src/app/manifest.ts</files>
  <read_first>
    - src/app/(auth)/onboarding/page.tsx (ligne 76 — "votre GMAO")
    - src/app/manifest.ts (état actuel — vérifier name, short_name, theme_color)
    - .planning/phases/01-identit-korvia/01-UI-SPEC.md (copywriting "Korvia" exclusif)
  </read_first>
  <action>
**Fichier A : `src/app/(auth)/onboarding/page.tsx`**

REMPLACER la ligne 76 :
```tsx
          <CardTitle className="text-2xl">Bienvenue dans votre GMAO</CardTitle>
```
PAR :
```tsx
          <CardTitle className="text-2xl">Bienvenue dans Korvia</CardTitle>
```

Aucun autre changement dans ce fichier (l'emoji 🔧 ligne 75 est neutre, on le laisse).

**Fichier B : `src/app/manifest.ts`**

D'abord lire le fichier en entier (taille inconnue, le faire avant de modifier).

Puis appliquer les changements suivants au champ par champ :
- `name` : valeur cible exacte → `"Korvia — GMAO pour PME québécoises"`
- `short_name` : valeur cible exacte → `"Korvia"`
- `description` : valeur cible exacte → `"Gérez vos actifs, bons de travail et maintenance préventive en français."`
- `theme_color` : valeur cible exacte → `"#0F1C2E"`
- `background_color` : valeur cible exacte → `"#0F1C2E"` (si présent ; sinon ne pas ajouter)
- Les références à `/icon.svg`, `/icon-192.png`, `/icon-512.png` dans `icons[]` : NE PAS MODIFIER les chemins (ces fichiers existent déjà dans `public/`). Si `purpose` ou `sizes` sont définis, les laisser tels quels.

Si un champ existant (ex : `start_url`, `display`, `orientation`) n'est pas listé ci-dessus : NE PAS LE MODIFIER.

Si le fichier ne contient pas l'un des champs `name`/`short_name`/`description`/`theme_color`, ajouter le champ manquant avec la valeur cible (en respectant la syntaxe TypeScript de `MetadataRoute.Manifest`).

Exemple de cible attendue (à adapter selon ce qui existe déjà) :
```ts
import type { MetadataRoute } from "next"

export default function manifest(): MetadataRoute.Manifest {
  return {
    name: "Korvia — GMAO pour PME québécoises",
    short_name: "Korvia",
    description: "Gérez vos actifs, bons de travail et maintenance préventive en français.",
    start_url: "/",
    display: "standalone",
    background_color: "#0F1C2E",
    theme_color: "#0F1C2E",
    icons: [
      { src: "/icon.svg", sizes: "any", type: "image/svg+xml" },
      { src: "/icon-192.png", sizes: "192x192", type: "image/png" },
      { src: "/icon-512.png", sizes: "512x512", type: "image/png" },
    ],
  }
}
```
(Les valeurs hors brand — start_url, display, structure icons[] — sont préservées telles qu'elles existent ; seules les chaînes brand changent.)
  </action>
  <verify>
    <automated>grep -q "Bienvenue dans Korvia" "src/app/(auth)/onboarding/page.tsx" && ! grep -q "votre GMAO" "src/app/(auth)/onboarding/page.tsx" && grep -q "Korvia — GMAO pour PME québécoises" src/app/manifest.ts && grep -q '"Korvia"' src/app/manifest.ts && grep -q '"#0F1C2E"' src/app/manifest.ts && ! grep -q "name: \"GMAO" src/app/manifest.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(auth)/onboarding/page.tsx` contient "Bienvenue dans Korvia"
    - `src/app/(auth)/onboarding/page.tsx` ne contient plus "votre GMAO"
    - `src/app/manifest.ts` contient le name "Korvia — GMAO pour PME québécoises"
    - `src/app/manifest.ts` contient short_name "Korvia"
    - `src/app/manifest.ts` contient theme_color "#0F1C2E"
    - `src/app/manifest.ts` ne contient plus `name: "GMAO` (chaîne ancien nom de marque)
    - `npx tsc --noEmit` ne signale pas d'erreur sur ces fichiers
  </acceptance_criteria>
  <done>
La page onboarding accueille l'utilisateur avec "Bienvenue dans Korvia". Le manifest PWA déclare la marque Korvia et le themeColor navy.
  </done>
</task>

</tasks>

<verification>
Vérifications globales du Plan 02 :

```bash
# 1. Inventaire textuel "GMAO" restant dans ces fichiers (doit être 0 sauf le title catégorie SEO dans layout.tsx)
grep -n "GMAO" src/components/layout/sidebar.tsx src/components/layout/sidebar-sheet.tsx src/components/layout/header.tsx "src/app/(auth)/onboarding/page.tsx"
# → doit retourner aucune ligne

# 2. layout.tsx peut conserver "GMAO" UNIQUEMENT dans le title catégorie SEO
grep "GMAO" src/app/layout.tsx
# → doit retourner uniquement la ligne "Korvia — GMAO pour PME québécoises"

# 3. manifest.ts ne contient que le terme catégorie dans name (pas le nom de marque)
grep "GMAO" src/app/manifest.ts
# → doit retourner uniquement la ligne "Korvia — GMAO pour PME québécoises"

# 4. KorviaLogo consommé dans les 2 emplacements layout
grep -l "KorviaLogo" src/components/layout/
# → sidebar.tsx et sidebar-sheet.tsx

# 5. Inter chargé
grep "Inter(" src/app/layout.tsx

# 6. Build Next.js réussit
npm run build 2>&1 | tail -5

# 7. Pas d'erreur TypeScript
npx tsc --noEmit
```
</verification>

<success_criteria>
Plan 02 réussi si :
- [ ] `grep "GMAO" src/components/layout/ src/app/(auth)/onboarding/page.tsx` ne retourne aucune ligne
- [ ] `src/app/layout.tsx` charge Inter via next/font/google avec variable="--font-inter"
- [ ] `<html>` applique `${inter.variable}` ET `${geistSans.variable}`
- [ ] metadata.title.default = "Korvia — GMAO pour PME québécoises", template = "%s | Korvia"
- [ ] viewport.themeColor = "#0F1C2E"
- [ ] sidebar.tsx et sidebar-sheet.tsx affichent `<KorviaLogo variant="color" size={32} showWordmark />`
- [ ] header.tsx fallback = "Korvia"
- [ ] Onboarding affiche "Bienvenue dans Korvia"
- [ ] manifest.ts : name/short_name/theme_color brandés Korvia
- [ ] `npx tsc --noEmit` passe sans erreur
- [ ] `npm run build` réussit
</success_criteria>

<output>
Après complétion, créer `.planning/phases/01-identit-korvia/01-02-SUMMARY.md` documentant :
- Récap des fichiers modifiés et nature des changements
- Confirmation des occurrences "GMAO" éliminées (avec liste résiduelle SEO dans title)
- Toute divergence (ex: manifest avait des champs supplémentaires non spécifiés ici — comment ils ont été traités)
- Tests visuels suggérés (sidebar desktop, sheet mobile, onboarding, favicon dans l'onglet)
</output>