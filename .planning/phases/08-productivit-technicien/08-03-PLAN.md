---
phase: 08-productivit-technicien
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/emails/urgent-escalation.tsx
  - src/lib/email.ts
  - src/app/api/cron/urgent-escalation/route.ts
  - src/components/settings/escalation-config-section.tsx
  - src/app/(app)/parametres/organisation/page.tsx
  - vercel.json
autonomous: false
requirements: [PROD-03]
must_haves:
  truths:
    - "Vercel Cron exécute /api/cron/urgent-escalation toutes les heures"
    - "Le cron filtre les BTs où priority='urgent', status in [open, in_progress, on_hold], createdAt <= now - delayHours, escalationSentAt IS NULL"
    - "Pour chaque BT identifié, un email est envoyé aux admins+managers de l'org propriétaire"
    - "Après envoi réussi, escalationSentAt est mis à jour pour rendre le cron idempotent"
    - "Si escalationConfig.enabled = false sur une org, AUCUN email n'est envoyé pour ses BTs"
    - "Dans /parametres/organisation, l'admin voit une section 'Escalade des bons urgents' avec toggle enabled + champ delayHours"
    - "L'admin peut sauvegarder une config (ex: enabled=true, delayHours=4) et la valeur persiste"
    - "Un humain peut valider end-to-end via le checkpoint final"
  artifacts:
    - path: "src/emails/urgent-escalation.tsx"
      provides: "Template email d'escalade urgente"
      exports: ["UrgentEscalationEmail"]
    - path: "src/lib/email.ts"
      provides: "sendUrgentEscalationEmail helper"
      contains: "sendUrgentEscalationEmail"
    - path: "src/app/api/cron/urgent-escalation/route.ts"
      provides: "Route Handler GET du cron Vercel"
      contains: "escalationSentAt"
    - path: "src/components/settings/escalation-config-section.tsx"
      provides: "UI config escalade (admin/manager)"
      contains: "updateEscalationConfig"
    - path: "vercel.json"
      provides: "Schedule cron horaire"
      contains: "urgent-escalation"
  key_links:
    - from: "vercel.json"
      to: "/api/cron/urgent-escalation"
      via: "schedule '0 * * * *'"
      pattern: "urgent-escalation"
    - from: "cron route handler"
      to: "sendUrgentEscalationEmail"
      via: "appel direct"
      pattern: "sendUrgentEscalationEmail"
    - from: "cron route handler"
      to: "WorkOrder.escalationSentAt"
      via: "db.workOrder.update"
      pattern: "escalationSentAt:\\s*new Date"
    - from: "EscalationConfigSection"
      to: "updateEscalationConfig"
      via: "Server Action"
      pattern: "updateEscalationConfig"
---

<objective>
Livrer PROD-03 : escalade automatique des BTs urgents non résolus après un délai configurable. Inclut le template d'email, le helper d'envoi, le Route Handler du cron horaire (avec idempotence via `escalationSentAt`), l'UI de configuration dans les paramètres organisation, et l'enregistrement du cron dans `vercel.json`. Termine par un checkpoint humain end-to-end couvrant les trois requirements de la phase (PROD-01 + PROD-02 + PROD-03).

Purpose: PROD-03 ferme la phase en livrant le mécanisme automatique de notification superviseur — c'est la valeur perçue immédiate côté manager (« plus de BTs urgents oubliés »).
Output: Cron actif sur Vercel, configurable par admin, idempotent, et une validation humaine sur l'ensemble de la phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-productivit-technicien/08-RESEARCH.md
@.planning/phases/08-productivit-technicien/08-01-PLAN.md
@.planning/phases/08-productivit-technicien/08-02-PLAN.md

<interfaces>
<!-- Contrats issus de Plan 08-01 ; pas d'exploration nécessaire -->

From src/lib/escalation-config.ts (créé en 08-01):
```typescript
export type EscalationConfig = { enabled: boolean; delayHours: number }
export const DEFAULT_ESCALATION_CONFIG: EscalationConfig
export function parseEscalationConfig(raw: unknown): EscalationConfig
```

From src/actions/settings.ts (créé en 08-01):
```typescript
export async function updateEscalationConfig(cfg: EscalationConfig): Promise<void>
```

From prisma/schema.prisma (étendu en 08-01):
```prisma
// Organization.escalationConfig Json?
// WorkOrder.escalationSentAt DateTime?
// WorkOrder déjà : priority, status, createdAt, organizationId
```

From src/lib/email.ts (patron Resend existant):
```typescript
import { render } from '@react-email/render'
import { resend } from './resend-client'
// Le helper existant pour maintenance-reminder retourne void et utilise resend.emails.send({...})
export async function sendMaintenanceReminderEmail(params: { to: string[]; planName: string; assetName: string | null; nextDueAt: Date; organizationName: string }): Promise<void>
```

From src/emails/maintenance-reminder.tsx (patron template à dupliquer pour le style HTML email):
```typescript
// Inline-style React Email avec couleur header #E8830C (orange Korvia)
// Footer standard "Korvia — Gestion de maintenance assistée par ordinateur"
```

From src/app/api/cron/maintenance-reminder/route.ts (patron exact à dupliquer):
```typescript
// 1. Vérifier Bearer CRON_SECRET ou 401
// 2. Charger entités cibles + members admins
// 3. Boucler, envoyer emails, try/catch par cible
// 4. Retourner JSON { ok, found, sent, errors }
```

From src/components/settings/closure-requirements-section.tsx (patron UI exact à dupliquer):
```typescript
'use client'
// useState + useTransition + sonner toast
// Form avec checkbox + input + bouton Sauvegarder (disabled si !dirty || pending)
```

From src/app/(app)/parametres/organisation/page.tsx (où intégrer la nouvelle section):
```typescript
// Charge org avec select { ..., escalationConfig: true } — à étendre
// Calcule canManagePortals = role admin|manager — réutiliser la même garde pour escalation section
// Rend <ClosureRequirementsSection initial={closureReq} /> — modèle exact
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template email UrgentEscalationEmail + helper sendUrgentEscalationEmail</name>
  <files>src/emails/urgent-escalation.tsx, src/lib/email.ts</files>
  <read_first>
    - src/emails/maintenance-reminder.tsx (patron exact pour le style HTML inline, header orange #E8830C, layout 600px, footer)
    - src/lib/email.ts (lire complet — pour ajouter `sendUrgentEscalationEmail` dans le même style que les autres helpers)
  </read_first>
  <action>
    **A. Créer `src/emails/urgent-escalation.tsx`** en dupliquant la structure exacte de `src/emails/maintenance-reminder.tsx`.

    Props attendus :
    ```typescript
    interface Props {
      workOrderNumber: number
      workOrderTitle: string
      workOrderId: string
      priority: string  // 'urgent' attendu, on l'affiche pour clarté
      createdAt: Date
      ageHours: number      // calculé côté appelant
      organizationName: string
      appUrl: string         // URL d'origine de l'app (ex: https://app.korvia.ca)
    }
    export function UrgentEscalationEmail({ ... }: Props)
    ```

    Contenu HTML (en français, style inline identique au template existant) :
    - Header orange `#E8830C` : "Korvia"
    - Corps : titre rouge `<h2 style="color: #DC2626;">Bon de travail urgent non résolu</h2>`
    - Phrase principale : `Un bon de travail prioritaire <strong>urgent</strong> dans <strong>{organizationName}</strong> n'a pas été résolu depuis <strong>{ageHours} heures</strong>.`
    - Carte gris clair `#f3f4f6` listant :
      - "Numéro : #{workOrderNumber}"
      - "Titre : {workOrderTitle}"
      - "Créé : {createdAt.toLocaleString('fr-CA', { dateStyle: 'long', timeStyle: 'short' })}"
      - "Âge : {ageHours} h"
    - Bouton CTA orange `<a href="{appUrl}/bons-de-travail/{workOrderId}" style="background:#E8830C; color:#fff; padding:12px 24px; border-radius:6px; text-decoration:none; display:inline-block; font-weight:bold;">Ouvrir le bon de travail</a>`
    - Footer standard identique aux autres templates : "Korvia — Gestion de maintenance assistée par ordinateur / Cet email a été envoyé automatiquement. Ne pas répondre à ce message."

    **B. Étendre `src/lib/email.ts`** : ajouter le helper après les autres `send*Email` (suivre l'ordre alphabétique ou append à la fin).

    ```typescript
    import { UrgentEscalationEmail } from '@/emails/urgent-escalation'

    export async function sendUrgentEscalationEmail(params: {
      to: string[]
      workOrderNumber: number
      workOrderTitle: string
      workOrderId: string
      createdAt: Date
      ageHours: number
      organizationName: string
    }) {
      const appUrl = process.env.NEXT_PUBLIC_APP_URL ?? 'https://app.korvia.ca'
      const html = await render(UrgentEscalationEmail({
        ...params,
        priority: 'urgent',
        appUrl,
      }))
      await resend.emails.send({
        from: process.env.RESEND_FROM_EMAIL ?? 'Korvia <noreply@korvia.ca>',
        to: params.to,
        subject: `[Korvia] BT urgent #${params.workOrderNumber} non résolu — escalade`,
        html,
      })
    }
    ```

    Note : si le fichier `src/lib/email.ts` utilise une constante locale `const from = ...` ou un autre import `resend` (ex. `import { resend } from './resend-client'`), suivre EXACTEMENT le style utilisé par les autres `send*Email` du même fichier — ne pas inventer de nouvelles conventions. Lire les fonctions voisines pour aligner.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/emails/urgent-escalation.tsx` existe et exporte `UrgentEscalationEmail`
    - `grep -q "export function UrgentEscalationEmail" src/emails/urgent-escalation.tsx` retourne 0
    - `grep -q "Bon de travail urgent non résolu" src/emails/urgent-escalation.tsx` retourne 0
    - `grep -q "export async function sendUrgentEscalationEmail" src/lib/email.ts` retourne 0
    - `grep -q "UrgentEscalationEmail" src/lib/email.ts` retourne 0
    - `grep -q "subject: .*urgent" src/lib/email.ts` retourne 0
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>Template + helper prêts. Appel `sendUrgentEscalationEmail` utilisable depuis le Route Handler.</done>
</task>

<task type="auto">
  <name>Task 2: Route Handler /api/cron/urgent-escalation (idempotent, org-scoped)</name>
  <files>src/app/api/cron/urgent-escalation/route.ts, vercel.json</files>
  <read_first>
    - src/app/api/cron/maintenance-reminder/route.ts (patron exact à dupliquer — vérification CRON_SECRET, fetch, boucle, retour JSON)
    - vercel.json (état actuel — un seul cron pour le moment)
    - src/lib/escalation-config.ts (créé en 08-01 — pour parseEscalationConfig)
    - src/lib/email.ts (créé en Task 1 — pour sendUrgentEscalationEmail)
  </read_first>
  <action>
    **A. Créer `src/app/api/cron/urgent-escalation/route.ts`** :

    ```typescript
    import { NextRequest, NextResponse } from 'next/server'
    import { db } from '@/lib/db'
    import { parseEscalationConfig } from '@/lib/escalation-config'
    import { sendUrgentEscalationEmail } from '@/lib/email'

    export async function GET(request: NextRequest) {
      // Auth — pattern identique à maintenance-reminder
      const authHeader = request.headers.get('authorization')
      const expectedToken = process.env.CRON_SECRET
      if (!expectedToken || authHeader !== `Bearer ${expectedToken}`) {
        return NextResponse.json({ error: 'Non autorisé' }, { status: 401 })
      }

      const now = new Date()

      // 1. Charger toutes les organisations qui ont une config escalade non-null
      const orgs = await db.organization.findMany({
        where: { escalationConfig: { not: null as never } },
        select: {
          id: true,
          name: true,
          escalationConfig: true,
          members: {
            where: { role: { in: ['admin', 'manager'] } },
            select: { email: true },
          },
        },
      })

      let totalFound = 0
      let totalSent = 0
      const errors: string[] = []

      for (const org of orgs) {
        const cfg = parseEscalationConfig(org.escalationConfig)
        if (!cfg.enabled) continue

        const recipients = org.members.map(m => m.email).filter((e): e is string => !!e)
        if (recipients.length === 0) continue

        const threshold = new Date(now.getTime() - cfg.delayHours * 60 * 60 * 1000)

        const overdueWOs = await db.workOrder.findMany({
          where: {
            organizationId: org.id,
            priority: 'urgent',
            status: { in: ['open', 'in_progress', 'on_hold'] },
            createdAt: { lte: threshold },
            escalationSentAt: null,
          },
          select: { id: true, number: true, title: true, createdAt: true },
        })

        totalFound += overdueWOs.length

        for (const wo of overdueWOs) {
          const ageMs = now.getTime() - wo.createdAt.getTime()
          const ageHours = Math.floor(ageMs / (60 * 60 * 1000))
          try {
            await sendUrgentEscalationEmail({
              to: recipients,
              workOrderNumber: wo.number,
              workOrderTitle: wo.title,
              workOrderId: wo.id,
              createdAt: wo.createdAt,
              ageHours,
              organizationName: org.name,
            })
            await db.workOrder.update({
              where: { id: wo.id },
              data: { escalationSentAt: new Date() },
            })
            totalSent++
          } catch (err) {
            const msg = err instanceof Error ? err.message : String(err)
            errors.push(`WO ${wo.id} (org ${org.id}): ${msg}`)
            console.error('[cron/urgent-escalation] failed for WO', wo.id, err)
          }
        }
      }

      console.log(`[cron/urgent-escalation] orgs=${orgs.length} found=${totalFound} sent=${totalSent}`)

      return NextResponse.json({
        ok: true,
        orgsProcessed: orgs.length,
        workOrdersFound: totalFound,
        emailsSent: totalSent,
        errors: errors.length > 0 ? errors : undefined,
      })
    }
    ```

    Note technique : `escalationConfig: { not: null as never }` est le patron Prisma 7 pour filtrer un Json nullable. Si le typage Prisma rejette `null as never`, utiliser à la place :
    ```typescript
    import { Prisma } from '@/generated/prisma'
    where: { escalationConfig: { not: Prisma.JsonNull } }
    ```

    Pour les valeurs de `priority: 'urgent'` et `status: { in: [...] }`, ce sont des enums Prisma string — passer les chaînes directement fonctionne (cohérent avec le reste du codebase qui utilise des string literals pour ces enums). Si le typage rejette, importer `WorkOrderPriority` et `WorkOrderStatus` depuis `@/generated/prisma/enums`.

    **B. Étendre `vercel.json`** :

    Modifier le fichier en ajoutant la nouvelle entrée dans le tableau `crons` :
    ```json
    {
      "crons": [
        { "path": "/api/cron/maintenance-reminder", "schedule": "0 * * * *" },
        { "path": "/api/cron/urgent-escalation",   "schedule": "0 * * * *" }
      ]
    }
    ```

    Conserver la syntaxe JSON valide (pas de trailing comma).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const j = require('./vercel.json'); if (!j.crons.some(c => c.path === '/api/cron/urgent-escalation')) process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/cron/urgent-escalation/route.ts` existe
    - `grep -q "export async function GET" src/app/api/cron/urgent-escalation/route.ts` retourne 0
    - `grep -q "CRON_SECRET" src/app/api/cron/urgent-escalation/route.ts` retourne 0
    - `grep -q "priority: 'urgent'" src/app/api/cron/urgent-escalation/route.ts` retourne 0
    - `grep -q "escalationSentAt: null" src/app/api/cron/urgent-escalation/route.ts` retourne 0
    - `grep -q "escalationSentAt: new Date()" src/app/api/cron/urgent-escalation/route.ts` retourne 0
    - `grep -q "sendUrgentEscalationEmail" src/app/api/cron/urgent-escalation/route.ts` retourne 0
    - `grep -q "urgent-escalation" vercel.json` retourne 0
    - La commande automated retourne `OK` et exit 0
  </acceptance_criteria>
  <done>Cron déployable, idempotent par `escalationSentAt`, scoped par config par org.</done>
</task>

<task type="auto">
  <name>Task 3: UI EscalationConfigSection + intégration paramètres</name>
  <files>src/components/settings/escalation-config-section.tsx, src/app/(app)/parametres/organisation/page.tsx</files>
  <read_first>
    - src/components/settings/closure-requirements-section.tsx (patron exact à dupliquer — useState + useTransition + dirty check + toast + form submit)
    - src/lib/escalation-config.ts (créé en 08-01 — type EscalationConfig + labels + DEFAULT)
    - src/actions/settings.ts (créé en 08-01 — updateEscalationConfig)
    - src/app/(app)/parametres/organisation/page.tsx (où intégrer ; étudier surtout l'usage de ClosureRequirementsSection et les gardes role admin|manager)
  </read_first>
  <action>
    **A. Créer `src/components/settings/escalation-config-section.tsx`** en dupliquant la structure de `closure-requirements-section.tsx` :

    ```typescript
    'use client'

    import { useState, useTransition } from 'react'
    import { Button } from '@/components/ui/button'
    import { Input } from '@/components/ui/input'
    import { toast } from 'sonner'
    import { updateEscalationConfig } from '@/actions/settings'
    import {
      type EscalationConfig,
      ESCALATION_FIELD_LABELS,
    } from '@/lib/escalation-config'

    type Props = { initial: EscalationConfig }

    export function EscalationConfigSection({ initial }: Props) {
      const [pending, startTransition] = useTransition()
      const [cfg, setCfg] = useState<EscalationConfig>(initial)
      const dirty = cfg.enabled !== initial.enabled || cfg.delayHours !== initial.delayHours

      function handleSubmit(e: React.FormEvent) {
        e.preventDefault()
        if (!Number.isFinite(cfg.delayHours) || cfg.delayHours <= 0 || cfg.delayHours > 168) {
          toast.error('Le délai doit être entre 1 et 168 heures')
          return
        }
        startTransition(async () => {
          try {
            await updateEscalationConfig(cfg)
            toast.success('Configuration d\'escalade enregistrée')
          } catch (e) {
            toast.error(e instanceof Error ? e.message : 'Erreur')
          }
        })
      }

      return (
        <form onSubmit={handleSubmit} className="space-y-4">
          <p className="text-sm text-muted-foreground">
            Envoie automatiquement un email aux administrateurs et gestionnaires lorsqu'un bon de
            travail de priorité <strong>urgente</strong> n'a pas été résolu après le délai configuré.
            Le cron tourne toutes les heures.
          </p>

          <label className="flex items-start gap-3 p-3 rounded-lg border bg-card cursor-pointer">
            <input
              type="checkbox"
              checked={cfg.enabled}
              onChange={() => setCfg(c => ({ ...c, enabled: !c.enabled }))}
              className="mt-0.5 h-4 w-4 rounded border-input"
            />
            <span className="text-sm">{ESCALATION_FIELD_LABELS.enabled}</span>
          </label>

          <div className="space-y-2">
            <label className="text-sm font-medium" htmlFor="delayHours">
              {ESCALATION_FIELD_LABELS.delayHours}
            </label>
            <Input
              id="delayHours"
              type="number"
              min={1}
              max={168}
              step={1}
              value={cfg.delayHours}
              onChange={(e) => setCfg(c => ({ ...c, delayHours: Number(e.target.value) }))}
              disabled={!cfg.enabled}
              className="max-w-xs"
            />
            <p className="text-xs text-muted-foreground">
              Entre 1 et 168 heures (1 semaine). Par défaut : 4 heures.
            </p>
          </div>

          <div className="flex justify-end">
            <Button type="submit" disabled={!dirty || pending}>
              {pending ? 'Enregistrement…' : 'Sauvegarder'}
            </Button>
          </div>
        </form>
      )
    }
    ```

    **B. Étendre `src/app/(app)/parametres/organisation/page.tsx`** :

    1. Étendre le `db.organization.findUnique({ ..., select: { ... } })` pour inclure `escalationConfig: true` :
    ```typescript
    select: { id: true, name: true, industry: true, size: true, closureRequirements: true, escalationConfig: true },
    ```

    2. Importer les nouveaux modules :
    ```typescript
    import { EscalationConfigSection } from '@/components/settings/escalation-config-section'
    import { parseEscalationConfig } from '@/lib/escalation-config'
    import { Bell } from 'lucide-react'  // icône de la section
    ```

    3. Parser la config après le `parseClosureRequirements` existant :
    ```typescript
    const escalationCfg = parseEscalationConfig(org.escalationConfig)
    ```

    4. Reuse de la garde existante `canManagePortals` (role admin|manager) — créer une variable `canManageEscalation = canManagePortals` (même gating). Si elle n'existe pas avec ce nom, déclarer :
    ```typescript
    const canManageEscalation = currentMembership && ['admin', 'manager'].includes(currentMembership.role)
    ```

    5. Dans le JSX, ajouter une nouvelle section juste après la section ClosureRequirements (qui utilise déjà l'icône ClipboardCheck). Suivre le pattern visuel des autres sections :
    ```tsx
    {canManageEscalation && (
      <section className="space-y-4">
        <div className="flex items-center gap-2">
          <Bell className="h-5 w-5 text-muted-foreground" />
          <h2 className="text-lg font-semibold">Escalade des bons urgents</h2>
        </div>
        <EscalationConfigSection initial={escalationCfg} />
      </section>
    )}
    ```

    Si la page utilise un layout Card/séparateurs entre sections, suivre EXACTEMENT le wrapper utilisé par ClosureRequirementsSection (lire les ~20 lignes qui l'entourent pour reproduire le même style).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/settings/escalation-config-section.tsx` existe et démarre par `'use client'`
    - `grep -q "export function EscalationConfigSection" src/components/settings/escalation-config-section.tsx` retourne 0
    - `grep -q "updateEscalationConfig" src/components/settings/escalation-config-section.tsx` retourne 0
    - `grep -q "cfg.delayHours > 168" src/components/settings/escalation-config-section.tsx` retourne 0
    - `grep -q "EscalationConfigSection" src/app/\\(app\\)/parametres/organisation/page.tsx` retourne 0
    - `grep -q "escalationConfig: true" src/app/\\(app\\)/parametres/organisation/page.tsx` retourne 0
    - `grep -q "parseEscalationConfig" src/app/\\(app\\)/parametres/organisation/page.tsx` retourne 0
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>UI de config escalade accessible à admin/manager dans les paramètres, dirty-check, validation client, toast feedback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint humain — validation end-to-end Phase 8</name>
  <files></files>
  <read_first>
    - .planning/phases/08-productivit-technicien/08-01-PLAN.md (résultats attendus de la base de données et des Server Actions)
    - .planning/phases/08-productivit-technicien/08-02-PLAN.md (résultats attendus côté UI job plans et checklist)
    - .planning/REQUIREMENTS.md (libellés exacts de PROD-01, PROD-02, PROD-03 pour confronter à l'attendu)
  </read_first>
  <action>
    Cette tâche est un checkpoint humain bloquant — voir <how-to-verify> ci-dessous pour le protocole en 25 étapes. Avant de présenter le checkpoint à l'utilisateur, l'exécuteur DOIT :
    1. Démarrer l'app : `npm run dev` en arrière-plan
    2. Vérifier que les variables d'env nécessaires sont présentes : `CRON_SECRET` et `RESEND_API_KEY` (sinon afficher au user les valeurs manquantes avant de lui demander d'avancer)
    3. S'assurer qu'au moins un MaintenancePlan existe en seed/db ; sinon en créer un de démo via Prisma Studio
    4. Présenter le protocole de vérification (les 25 étapes) à l'utilisateur dans l'ordre, avec les commandes curl prêtes à copier-coller
    5. Attendre la réponse "approved" OU une liste d'anomalies à corriger (auquel cas créer une todo-list dans .planning/todos/ pour le suivi)
  </action>
  <verify>
    <automated>MISSING — vérification humaine bloquante ; aucune commande automatisable</automated>
  </verify>
  <acceptance_criteria>
    L'humain a confirmé les 25 étapes du protocole et tapé "approved" OU a documenté les anomalies avec capture/log dans le SUMMARY de fin de plan.
  </acceptance_criteria>
  <done>L'utilisateur a écrit "approved" (ou "approuvé") confirmant que les 25 étapes du protocole ont produit les résultats attendus, OU les anomalies relevées ont été listées et traitées avant validation finale.</done>
  <what-built>
    Phase 8 complète :
    - PROD-01 : Job plans avec pièces requises et bouton "Générer un BT" qui crée un BT pré-rempli
    - PROD-02 : Checklist interactive (cases à cocher + champs de mesure) sur les BT issus d'un plan
    - PROD-03 : Cron horaire `/api/cron/urgent-escalation` envoyant un email aux superviseurs après le délai configuré ; UI de config dans /parametres/organisation
  </what-built>
  <how-to-verify>
    Démarrer l'app : `npm run dev`. Se connecter en admin/manager d'une org de test.

    **PROD-01 — Job plans + génération BT**
    1. Aller sur `/maintenance`
    2. Ouvrir un plan existant (ou en créer un avec au moins 2 tâches)
    3. Dans le dialog d'édition, scroller jusqu'à la section "Pièces requises"
    4. Ajouter une pièce via SparePart existante (sélectionner dans le combobox) → toast "Pièce ajoutée"
    5. Ajouter une pièce "hors inventaire" (nom libre "Filtre 12V") avec quantité 2 → toast "Pièce ajoutée"
    6. Supprimer une pièce → toast confirmation
    7. Fermer le dialog. Vérifier que la liste du plan affiche maintenant les pièces requises (sous les tasks)
    8. Cliquer sur "Générer un BT" sur ce plan → confirmation native → puis redirection vers `/bons-de-travail/{id}` avec toast "Bon de travail #N créé"
    9. Vérifier que le BT a : titre = nom du plan, type = preventive, priorité = celle du plan, asset = celui du plan
    10. Vérifier que la section "Pièces" du BT contient les pièces du plan (sans avoir décrémenté le stock — vérifier `quantityOnHand` de la SparePart liée inchangé)

    **PROD-02 — Checklist BT**
    11. Toujours sur le BT généré à l'étape 8, scroller jusqu'à la section "Checklist"
    12. Vérifier que les items correspondent aux tâches du plan, dans l'ordre
    13. Cocher un item → la coche persiste, le texte se barre, le compteur "X/Y complétés" augmente
    14. Saisir une valeur dans le champ "Mesure / valeur" d'un item → tabber ou cliquer ailleurs → toast "Mesure enregistrée"
    15. Refresh la page → coches et mesures sont préservées
    16. Ouvrir un BT correctif créé manuellement (sans plan de maintenance) → AUCUNE section Checklist ne doit s'afficher

    **PROD-03 — Escalade urgence**
    17. Aller sur `/parametres/organisation` → section "Escalade des bons urgents"
    18. Cocher "Activer l'escalade des bons urgents", entrer delayHours = 1, cliquer Sauvegarder → toast "Configuration d'escalade enregistrée"
    19. Créer un BT de priorité Urgente avec statut Ouvert
    20. Pour tester sans attendre 1 heure, modifier manuellement `createdAt` en base à `now() - 2 hours` (via Prisma Studio : `npx prisma studio`)
    21. Déclencher le cron manuellement :
        ```bash
        curl -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/urgent-escalation
        ```
    22. Vérifier la réponse JSON : `{ ok: true, orgsProcessed: ≥1, workOrdersFound: ≥1, emailsSent: ≥1 }`
    23. Vérifier que les admins/managers de l'org ont reçu un email "Bon de travail urgent non résolu" via la dashboard Resend
    24. Re-déclencher le cron immédiatement → `emailsSent: 0` (idempotence via `escalationSentAt`)
    25. Désactiver le toggle dans les paramètres → Sauvegarder → re-créer un BT urgent ancien → relancer le cron → `emailsSent: 0`

    Confirmer que :
    - Les 25 étapes ci-dessus produisent les résultats attendus
    - Aucune erreur en console (browser + serveur Next)
    - L'UX mobile est utilisable (tester en émulation iPhone DevTools sur la page d'un BT avec checklist)
  </how-to-verify>
  <acceptance_criteria>
    L'humain a confirmé les 25 étapes du protocole et tapé "approved" OU a documenté les anomalies avec capture/log.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → /api/cron/urgent-escalation | Endpoint exposé publiquement (sans Clerk) ; auth via header Bearer |
| client → updateEscalationConfig | Saisie utilisateur (delayHours, enabled) traverse vers DB |
| cron Vercel → Resend | Sortie email avec données BT (titre, numéro) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Elevation of Privilege | /api/cron/urgent-escalation appelé sans auth | mitigate | Header `Authorization: Bearer ${CRON_SECRET}` vérifié en début de handler, 401 sinon (patron identique maintenance-reminder) |
| T-08-15 | Denial of Service | Cron rejoue → spam emails superviseurs | mitigate | Idempotence via `WorkOrder.escalationSentAt` — le `findMany` filtre `escalationSentAt: null` ; update avant nouveau cycle |
| T-08-16 | Information Disclosure | Email envoyé à mauvaise org | mitigate | Recipients calculés `org.members where role IN admin|manager` — toujours scopés par `org.id` du WO |
| T-08-17 | Tampering | escalationConfig DB corrompu (Json arbitraire) | mitigate | `parseEscalationConfig` coerce vers valeurs sûres (08-01) — cron utilise la version parsée |
| T-08-18 | Tampering | delayHours malformé soumis via UI | mitigate | Validation côté client (1 ≤ x ≤ 168) + côté Server Action `updateEscalationConfig` (08-01 T-08-03) |
| T-08-19 | Information Disclosure | Email contient lien direct vers BT sans auth check | accept | Le lien `appUrl/bons-de-travail/{id}` est protégé par Clerk middleware ; seuls les membres de l'org connectés peuvent voir le BT |
| T-08-20 | Repudiation | Pas de log persistant des escalades envoyées | accept | `escalationSentAt` sert de trace minimale ; logs console suffisent pour MVP — pas de log table dédié |
| T-08-21 | Denial of Service | Org avec 10k WOs urgents anciens à l'activation | accept | Premier run enverra un email par WO ; volume contenu en pratique (admin Korvia + role admin/manager). Si problème survient en prod → batching à ajouter ultérieurement |
</threat_model>

<verification>
- TypeScript : `npx tsc --noEmit` exit 0
- Build : `npx next build` exit 0
- vercel.json valide : `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"`
- Cron testable en local avec curl (étape 21 du checkpoint)
- Checkpoint humain entièrement validé
</verification>

<success_criteria>
1. PROD-03 livré : cron `/api/cron/urgent-escalation` planifié horaire, idempotent, scoped par org config
2. UI EscalationConfigSection accessible à admin/manager, dirty-check, validation 1-168h
3. Email envoyé via Resend avec sujet `[Korvia] BT urgent #N non résolu — escalade`
4. Phase 8 entière validée par checkpoint humain (PROD-01 + PROD-02 + PROD-03)
</success_criteria>

<output>
After completion, create `.planning/phases/08-productivit-technicien/08-03-SUMMARY.md` documenting:
- Architecture cron + idempotence (escalationSentAt)
- Configuration Vercel ajoutée (vercel.json)
- Résultats du checkpoint humain (notes, captures éventuelles, anomalies traitées)
- Toute friction notée pour la rétrospective milestone
</output>
