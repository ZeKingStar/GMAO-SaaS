---
phase: 08-productivit-technicien
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/escalation-config.ts
  - src/lib/escalation-config.test.ts
  - src/actions/maintenance.ts
  - src/actions/work-orders.ts
  - src/actions/settings.ts
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03]
must_haves:
  truths:
    - "La base de données contient les nouvelles tables WorkOrderChecklistItem et MaintenancePlanPart"
    - "La colonne WorkOrder.maintenancePlanId existe et est nullable"
    - "La colonne WorkOrder.escalationSentAt existe et est nullable"
    - "La colonne Organization.escalationConfig (Json?) existe"
    - "Le module escalation-config parse et valide une configuration JSON arbitraire en EscalationConfig typé"
    - "Les Server Actions Phase 8 (generateWorkOrderFromPlan, addPlanPart, deletePlanPart, toggleChecklistItem, setChecklistMeasure, updateEscalationConfig) sont exportées et type-safe"
    - "La suite de tests Vitest passe (npx vitest run)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Schéma étendu pour Phase 8"
      contains: "model WorkOrderChecklistItem"
    - path: "src/lib/escalation-config.ts"
      provides: "Type EscalationConfig + parser + valeur par défaut"
      exports: ["EscalationConfig", "DEFAULT_ESCALATION_CONFIG", "parseEscalationConfig"]
    - path: "src/lib/escalation-config.test.ts"
      provides: "Tests unitaires Wave 0 PROD-01 + PROD-03"
      contains: "parseEscalationConfig"
    - path: "src/actions/maintenance.ts"
      provides: "generateWorkOrderFromPlan, addPlanPart, deletePlanPart"
      contains: "generateWorkOrderFromPlan"
    - path: "src/actions/work-orders.ts"
      provides: "toggleChecklistItem, setChecklistMeasure"
      contains: "toggleChecklistItem"
    - path: "src/actions/settings.ts"
      provides: "updateEscalationConfig"
      contains: "updateEscalationConfig"
  key_links:
    - from: "src/actions/maintenance.ts:generateWorkOrderFromPlan"
      to: "prisma WorkOrder.create avec checklistItems"
      via: "nested create"
      pattern: "checklistItems:\\s*plan\\.tasks"
    - from: "src/actions/work-orders.ts:toggleChecklistItem"
      to: "prisma workOrderChecklistItem"
      via: "findFirst avec workOrder.organizationId"
      pattern: "workOrder:\\s*\\{\\s*organizationId"
    - from: "src/actions/settings.ts:updateEscalationConfig"
      to: "Organization.escalationConfig"
      via: "db.organization.update"
      pattern: "escalationConfig"
---

<objective>
Poser les fondations Phase 8 : étendre le schéma Prisma (nouveaux modèles + champs), pousser la migration vers la base, créer le module typé `escalation-config` avec ses tests Wave 0, et implémenter les Server Actions partagées par les deux plans aval (génération BT depuis PM, manipulation checklist, config escalade).

Purpose: Ce plan est **bloquant** — il met en place toutes les surfaces de données et les actions serveur dont dépendent les Plans 08-02 (UI job plans + checklists) et 08-03 (cron + UI config escalade). Toute UI Phase 8 doit pouvoir importer un type stable et appeler des actions dont le contrat est figé.
Output: Schéma DB synchronisé, types et helpers prêts, Server Actions exportées, suite Vitest verte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-productivit-technicien/08-RESEARCH.md

<interfaces>
<!-- Patrons existants à respecter — extraits du codebase, pas d'exploration nécessaire -->

From src/lib/closure-requirements.ts (patron à dupliquer pour escalation-config.ts):
```typescript
export type ClosureRequirements = { faultCode: boolean; timeSpent: boolean; partsUsed: boolean }
export const DEFAULT_CLOSURE_REQUIREMENTS: ClosureRequirements = { ... }
export function parseClosureRequirements(raw: unknown): ClosureRequirements { ... }
```

From src/actions/maintenance.ts (helper auth à réutiliser tel quel):
```typescript
async function getOrgAndMembership(): Promise<{ organizationId: string; organizationName: string; membershipId: string }>
```

From src/actions/settings.ts (helper admin à réutiliser tel quel):
```typescript
async function getAdminOrg(): Promise<string> // retourne organizationId ; vérifie role admin|manager ; throw sinon
```

From src/actions/work-orders.ts (helper auth à réutiliser tel quel):
```typescript
async function getOrgAndMembership(): Promise<{ organizationId: string; organizationName: string; membershipId: string }>
```

From prisma/schema.prisma — modèles existants pertinents:
```prisma
model Organization { id String @id ... closureRequirements Json? ... }
model WorkOrder { id String @id ... organizationId String ... number Int ... priority WorkOrderPriority @default(medium) status WorkOrderStatus @default(open) ... createdAt DateTime @default(now()) ... parts WorkOrderPart[] ... @@unique([organizationId, number]) }
model WorkOrderPart { id String @id ... workOrderId String ... sparePartId String? ... name String ... quantity Float ... unitCost Float? ... }
model MaintenancePlan { id String @id ... organizationId String ... assetId String? ... estimatedHours Float? ... priority WorkOrderPriority ... lastGeneratedAt DateTime? ... tasks MaintenanceTask[] ... }
model MaintenanceTask { id String @id ... maintenancePlanId String ... order Int ... description String ... }
model SparePart { id String @id ... organizationId String ... }
```

From src/lib/email.ts (pattern email Resend):
```typescript
import { render } from '@react-email/render'
export async function sendMaintenanceReminderEmail(params: { to: string[]; planName: string; ... }): Promise<void>
```

From src/actions/work-orders.ts (pattern de création BT avec numérotation):
```typescript
// 1. const last = await db.workOrder.findFirst({ where: { organizationId }, orderBy: { number: 'desc' }, select: { number: true } })
// 2. const number = (last?.number ?? 0) + 1
// 3. db.workOrder.create({ data: { organizationId, number, ... } })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer src/lib/escalation-config.ts + tests Wave 0</name>
  <files>src/lib/escalation-config.ts, src/lib/escalation-config.test.ts</files>
  <read_first>
    - src/lib/closure-requirements.ts (patron exact à dupliquer pour la forme et le style)
    - src/lib/closure-requirements.test.ts (patron exact à dupliquer pour les tests)
    - .planning/phases/08-productivit-technicien/08-RESEARCH.md (section Pattern 3)
  </read_first>
  <behavior>
    - `parseEscalationConfig(undefined)` retourne `DEFAULT_ESCALATION_CONFIG` (`{ enabled: false, delayHours: 4 }`)
    - `parseEscalationConfig(null)` retourne `DEFAULT_ESCALATION_CONFIG`
    - `parseEscalationConfig({})` retourne `DEFAULT_ESCALATION_CONFIG`
    - `parseEscalationConfig({ enabled: true, delayHours: 8 })` retourne `{ enabled: true, delayHours: 8 }`
    - `parseEscalationConfig({ enabled: 'yes', delayHours: 8 })` retourne `{ enabled: false, delayHours: 8 }` (enabled non-bool → false)
    - `parseEscalationConfig({ enabled: true, delayHours: 0 })` retourne `{ enabled: true, delayHours: 4 }` (delayHours ≤ 0 → défaut)
    - `parseEscalationConfig({ enabled: true, delayHours: -5 })` retourne `{ enabled: true, delayHours: 4 }`
    - `parseEscalationConfig({ enabled: true, delayHours: 'abc' })` retourne `{ enabled: true, delayHours: 4 }`
    - `parseEscalationConfig('garbage')` retourne `DEFAULT_ESCALATION_CONFIG`
  </behavior>
  <action>
    Créer `src/lib/escalation-config.ts` avec EXACTEMENT ce contenu (types et fonctions):

    ```typescript
    export type EscalationConfig = {
      enabled: boolean
      delayHours: number
    }

    export const DEFAULT_ESCALATION_CONFIG: EscalationConfig = {
      enabled: false,
      delayHours: 4,
    }

    export const ESCALATION_FIELD_LABELS = {
      enabled: "Activer l'escalade des bons urgents",
      delayHours: "Délai avant escalade (heures)",
    } as const

    export function parseEscalationConfig(raw: unknown): EscalationConfig {
      if (!raw || typeof raw !== 'object') return DEFAULT_ESCALATION_CONFIG
      const r = raw as Record<string, unknown>
      const delayHours =
        typeof r.delayHours === 'number' && Number.isFinite(r.delayHours) && r.delayHours > 0
          ? r.delayHours
          : DEFAULT_ESCALATION_CONFIG.delayHours
      return {
        enabled: typeof r.enabled === 'boolean' ? r.enabled : false,
        delayHours,
      }
    }
    ```

    Créer `src/lib/escalation-config.test.ts` en suivant le patron exact de `src/lib/closure-requirements.test.ts`. Inclure tous les cas listés dans `<behavior>` ci-dessus avec `describe('parseEscalationConfig', ...)`. Utiliser `import { describe, it, expect } from 'vitest'`.

    NE PAS ajouter d'autres fonctions à `escalation-config.ts` dans cette tâche (le helper `computeEscalationThreshold` sera ajouté en Task 4 si nécessaire — ne pas anticiper).
  </action>
  <verify>
    <automated>npx vitest run src/lib/escalation-config.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/escalation-config.ts` exporte: `EscalationConfig`, `DEFAULT_ESCALATION_CONFIG`, `ESCALATION_FIELD_LABELS`, `parseEscalationConfig`
    - `grep -q "export type EscalationConfig" src/lib/escalation-config.ts` retourne 0
    - `grep -q "export const DEFAULT_ESCALATION_CONFIG" src/lib/escalation-config.ts` retourne 0
    - `grep -q "export function parseEscalationConfig" src/lib/escalation-config.ts` retourne 0
    - `npx vitest run src/lib/escalation-config.test.ts` exit code 0, au moins 8 tests passent
    - `npx tsc --noEmit` n'introduit pas d'erreur nouvelle sur ces deux fichiers
  </acceptance_criteria>
  <done>Module escalation-config créé, suite de tests Wave 0 verte, type sûr et conforme au patron closure-requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Étendre prisma/schema.prisma + db push + generate</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (état actuel — surtout les modèles WorkOrder lignes 282-318, MaintenancePlan lignes 395-421, MaintenanceTask lignes 423-433, Organization lignes 74-98)
    - .planning/phases/08-productivit-technicien/08-RESEARCH.md (section Pattern 1)
  </read_first>
  <action>
    Modifier `prisma/schema.prisma` avec les changements EXACTS suivants. Ne PAS toucher aux autres modèles.

    **1. Dans `model Organization`** (après la ligne `closureRequirements Json?`), ajouter:
    ```prisma
      escalationConfig    Json?
    ```

    **2. Dans `model WorkOrder`** (avant la ligne `createdAt DateTime @default(now())`), ajouter ces champs:
    ```prisma
      maintenancePlanId String?
      escalationSentAt  DateTime?
    ```
    Puis dans le bloc des relations (après `parts WorkOrderPart[]`), ajouter:
    ```prisma
      maintenancePlan MaintenancePlan?         @relation(fields: [maintenancePlanId], references: [id], onDelete: SetNull)
      checklistItems  WorkOrderChecklistItem[]
    ```
    Puis ajouter l'index dans le bloc des `@@index`:
    ```prisma
      @@index([maintenancePlanId])
      @@index([priority, status, escalationSentAt])
    ```

    **3. Dans `model MaintenancePlan`** (dans le bloc des relations, après `tasks MaintenanceTask[]`), ajouter:
    ```prisma
      planParts   MaintenancePlanPart[]
      workOrders  WorkOrder[]
    ```

    **4. Ajouter deux nouveaux modèles** juste après `model MaintenanceTask { ... }` (avant la section `// ─── Inventaire ───`):
    ```prisma
    model MaintenancePlanPart {
      id                String   @id @default(cuid())
      maintenancePlanId String
      sparePartId       String?
      name              String
      quantity          Float
      createdAt         DateTime @default(now())
      updatedAt         DateTime @updatedAt

      maintenancePlan MaintenancePlan @relation(fields: [maintenancePlanId], references: [id], onDelete: Cascade)
      sparePart       SparePart?      @relation(fields: [sparePartId], references: [id], onDelete: SetNull)

      @@index([maintenancePlanId])
      @@index([sparePartId])
    }

    model WorkOrderChecklistItem {
      id           String    @id @default(cuid())
      workOrderId  String
      order        Int
      description  String
      checked      Boolean   @default(false)
      checkedAt    DateTime?
      measureValue String?
      photoUrl     String?
      createdAt    DateTime  @default(now())
      updatedAt    DateTime  @updatedAt

      workOrder WorkOrder @relation(fields: [workOrderId], references: [id], onDelete: Cascade)

      @@index([workOrderId])
    }
    ```

    **5. Dans `model SparePart`** (dans le bloc des relations, après `workOrderParts WorkOrderPart[]`), ajouter:
    ```prisma
      planParts MaintenancePlanPart[]
    ```
  </action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "escalationConfig\s\+Json?" prisma/schema.prisma` retourne 0
    - `grep -q "maintenancePlanId\s\+String?" prisma/schema.prisma` retourne 0
    - `grep -q "escalationSentAt\s\+DateTime?" prisma/schema.prisma` retourne 0
    - `grep -q "^model MaintenancePlanPart" prisma/schema.prisma` retourne 0
    - `grep -q "^model WorkOrderChecklistItem" prisma/schema.prisma` retourne 0
    - `grep -q "checklistItems\s\+WorkOrderChecklistItem\[\]" prisma/schema.prisma` retourne 0
    - `grep -q "planParts\s\+MaintenancePlanPart\[\]" prisma/schema.prisma` retourne 0
    - `grep -q "workOrders\s\+WorkOrder\[\]" prisma/schema.prisma` retourne 0
    - `npx prisma validate` exit code 0
  </acceptance_criteria>
  <done>Schéma Prisma étendu et validé syntaxiquement. La migration DB suit en Task 3.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: npx prisma db push + prisma generate</name>
  <files></files>
  <read_first>
    - prisma/schema.prisma (vérifier que Task 2 a bien appliqué tous les changements)
  </read_first>
  <action>
    Exécuter dans cet ordre EXACT, depuis la racine du projet :

    1. `npx prisma db push` — applique les nouveaux modèles + champs vers PostgreSQL. Si la commande échoue avec une erreur de perte de données (improbable, tous les nouveaux champs sont nullable ou ont un défaut), utiliser `npx prisma db push --accept-data-loss` en repli.
    2. `npx prisma generate` — régénère le client Prisma dans `src/generated/prisma/` pour rendre les nouveaux types et relations disponibles à TypeScript.

    Ne pas créer de migration (`prisma migrate dev` interdit dans ce projet — toutes les phases précédentes utilisent `db push`).

    Cette tâche est BLOQUANTE : les Tasks 4-6 (Server Actions) ne compilent pas sans `prisma generate`.
  </action>
  <verify>
    <automated>npx prisma generate && node -e "const { PrismaClient } = require('./src/generated/prisma'); const p = new PrismaClient(); console.log(typeof p.workOrderChecklistItem.findFirst, typeof p.maintenancePlanPart.findFirst); process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exit code 0
    - `src/generated/prisma/models/WorkOrderChecklistItem.ts` (ou équivalent type fichier) existe ; vérifier via `grep -rq "WorkOrderChecklistItem" src/generated/prisma/`
    - `grep -rq "MaintenancePlanPart" src/generated/prisma/` retourne 0
    - La commande de vérification automated retourne `function function` et exit 0
  </acceptance_criteria>
  <done>Base de données synchronisée avec le schéma. Le client Prisma reconnaît `workOrderChecklistItem`, `maintenancePlanPart`, et les nouvelles relations sur `workOrder` et `maintenancePlan`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Server Actions maintenance — generateWorkOrderFromPlan, addPlanPart, deletePlanPart</name>
  <files>src/actions/maintenance.ts</files>
  <read_first>
    - src/actions/maintenance.ts (état actuel, conserver toutes les fonctions existantes)
    - src/actions/work-orders.ts lignes 29-91 (patron createWorkOrder + numérotation)
    - .planning/phases/08-productivit-technicien/08-RESEARCH.md (section Pattern 2)
  </read_first>
  <behavior>
    - `generateWorkOrderFromPlan(planId)` crée un nouveau WorkOrder avec `maintenancePlanId = planId`, type='preventive', priorité copiée du plan
    - Le BT généré a `number = max(number) + 1` pour l'organisation
    - Le BT généré inclut `checklistItems` clonés depuis `MaintenanceTask` du plan (order + description)
    - Le BT généré inclut `parts` clonés depuis `MaintenancePlanPart` (name + quantity + sparePartId) SANS décrémenter le stock
    - `MaintenancePlan.lastGeneratedAt` est mis à jour
    - Si le plan appartient à une autre org → throw 'Plan introuvable'
    - `addPlanPart(planId, { sparePartId?, name, quantity })` ajoute une ligne à MaintenancePlanPart
    - `deletePlanPart(partId)` supprime après vérification org-scoping via la relation plan
  </behavior>
  <action>
    Étendre `src/actions/maintenance.ts` en AJOUTANT (ne pas modifier les fonctions existantes) trois fonctions exportées. Importer `revalidatePath` depuis `next/cache` si pas déjà fait.

    ```typescript
    export async function generateWorkOrderFromPlan(planId: string) {
      const { organizationId } = await getOrgAndMembership()

      const plan = await db.maintenancePlan.findFirst({
        where: { id: planId, organizationId },
        include: {
          tasks: { orderBy: { order: 'asc' } },
          planParts: true,
        },
      })
      if (!plan) throw new Error('Plan introuvable')

      const last = await db.workOrder.findFirst({
        where: { organizationId },
        orderBy: { number: 'desc' },
        select: { number: true },
      })
      const number = (last?.number ?? 0) + 1

      const wo = await db.workOrder.create({
        data: {
          organizationId,
          number,
          title: plan.name,
          description: plan.description ?? undefined,
          type: 'preventive',
          priority: plan.priority,
          estimatedHours: plan.estimatedHours ?? undefined,
          assetId: plan.assetId ?? undefined,
          maintenancePlanId: planId,
          checklistItems: plan.tasks.length > 0 ? {
            create: plan.tasks.map(t => ({
              order: t.order,
              description: t.description,
            })),
          } : undefined,
          parts: plan.planParts.length > 0 ? {
            create: plan.planParts.map(p => ({
              sparePartId: p.sparePartId ?? null,
              name: p.name,
              quantity: p.quantity,
            })),
          } : undefined,
        },
        select: { id: true, number: true },
      })

      await db.maintenancePlan.update({
        where: { id: planId },
        data: { lastGeneratedAt: new Date() },
      })

      revalidatePath('/bons-de-travail')
      revalidatePath('/maintenance')
      return wo
    }

    export async function addPlanPart(planId: string, data: {
      sparePartId?: string | null
      name: string
      quantity: number
    }) {
      const { organizationId } = await getOrgAndMembership()

      const plan = await db.maintenancePlan.findFirst({
        where: { id: planId, organizationId },
        select: { id: true },
      })
      if (!plan) throw new Error('Plan introuvable')

      if (!data.name.trim()) throw new Error('Nom de pièce requis')
      if (!Number.isFinite(data.quantity) || data.quantity <= 0) throw new Error('Quantité invalide')

      await db.maintenancePlanPart.create({
        data: {
          maintenancePlanId: planId,
          sparePartId: data.sparePartId || null,
          name: data.name.trim(),
          quantity: data.quantity,
        },
      })
      revalidatePath('/maintenance')
    }

    export async function deletePlanPart(partId: string) {
      const { organizationId } = await getOrgAndMembership()

      const part = await db.maintenancePlanPart.findFirst({
        where: { id: partId, maintenancePlan: { organizationId } },
        select: { id: true },
      })
      if (!part) throw new Error('Pièce introuvable')

      await db.maintenancePlanPart.delete({ where: { id: partId } })
      revalidatePath('/maintenance')
    }
    ```

    Si le projet utilise un autre nom d'enum (par exemple `WorkOrderType.preventive` vs string `'preventive'`), conserver la chaîne — Prisma 7 accepte la coercition string pour les enums string. Si la build échoue avec une erreur d'enum, utiliser `import { WorkOrderType } from '@/generated/prisma/enums'` et passer `type: WorkOrderType.preventive`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function generateWorkOrderFromPlan" src/actions/maintenance.ts` retourne 0
    - `grep -q "export async function addPlanPart" src/actions/maintenance.ts` retourne 0
    - `grep -q "export async function deletePlanPart" src/actions/maintenance.ts` retourne 0
    - `grep -q "maintenancePlanId: planId" src/actions/maintenance.ts` retourne 0
    - `grep -q "lastGeneratedAt: new Date()" src/actions/maintenance.ts` retourne 0
    - `grep -q "checklistItems:" src/actions/maintenance.ts` retourne 0
    - `npx tsc --noEmit` exit code 0
    - Les fonctions existantes (`createMaintenancePlan`, `updateMaintenancePlan`, `deleteMaintenancePlan`, `toggleMaintenancePlan`, `addMaintenanceTask`, `deleteMaintenanceTask`) sont toujours présentes — `grep -c "^export async function" src/actions/maintenance.ts` ≥ 9
  </acceptance_criteria>
  <done>Trois nouvelles Server Actions ajoutées et compilées. La génération d'un BT depuis un plan copie tasks → checklistItems et planParts → parts sans décompte de stock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Server Actions work-orders — toggleChecklistItem + setChecklistMeasure</name>
  <files>src/actions/work-orders.ts</files>
  <read_first>
    - src/actions/work-orders.ts (état actuel, conserver toutes les fonctions existantes ; étudier surtout lignes 1-15 pour les imports et le helper)
    - .planning/phases/08-productivit-technicien/08-RESEARCH.md (section Exemples de code → toggleChecklistItem)
  </read_first>
  <behavior>
    - `toggleChecklistItem(itemId, checked)` met `checked` à la valeur passée et `checkedAt` à `new Date()` si checked sinon `null`
    - L'org-scoping est vérifié via `workOrder.organizationId` — un item d'une autre org → throw 'Item introuvable'
    - `setChecklistMeasure(itemId, measureValue)` met `measureValue` (string|null, trim, max 200 chars)
    - Les deux fonctions revalident `/bons-de-travail/${workOrderId}`
  </behavior>
  <action>
    AJOUTER à la fin de `src/actions/work-orders.ts` (ne pas modifier les fonctions existantes) :

    ```typescript
    export async function toggleChecklistItem(itemId: string, checked: boolean) {
      const { organizationId } = await getOrgAndMembership()

      const item = await db.workOrderChecklistItem.findFirst({
        where: { id: itemId, workOrder: { organizationId } },
        select: { id: true, workOrderId: true },
      })
      if (!item) throw new Error('Item introuvable')

      await db.workOrderChecklistItem.update({
        where: { id: itemId },
        data: { checked, checkedAt: checked ? new Date() : null },
      })
      revalidatePath(`/bons-de-travail/${item.workOrderId}`)
    }

    export async function setChecklistMeasure(itemId: string, measureValue: string | null) {
      const { organizationId } = await getOrgAndMembership()

      const item = await db.workOrderChecklistItem.findFirst({
        where: { id: itemId, workOrder: { organizationId } },
        select: { id: true, workOrderId: true },
      })
      if (!item) throw new Error('Item introuvable')

      const cleaned = measureValue?.trim() ?? null
      if (cleaned && cleaned.length > 200) {
        throw new Error('Mesure trop longue (max 200 caractères)')
      }

      await db.workOrderChecklistItem.update({
        where: { id: itemId },
        data: { measureValue: cleaned || null },
      })
      revalidatePath(`/bons-de-travail/${item.workOrderId}`)
    }
    ```

    Vérifier que `revalidatePath` est déjà importé depuis `next/cache` dans ce fichier (il l'est, d'après le pattern des autres actions du fichier). Si non, ajouter l'import.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function toggleChecklistItem" src/actions/work-orders.ts` retourne 0
    - `grep -q "export async function setChecklistMeasure" src/actions/work-orders.ts` retourne 0
    - `grep -q "workOrder: { organizationId }" src/actions/work-orders.ts` retourne 0
    - `grep -q "checkedAt: checked ? new Date() : null" src/actions/work-orders.ts` retourne 0
    - `npx tsc --noEmit` exit code 0
    - Toutes les fonctions existantes sont préservées — `grep -c "^export async function" src/actions/work-orders.ts` ≥ 14 (count actuel + 2)
  </acceptance_criteria>
  <done>Server Actions checklist disponibles avec garde org-scoped et revalidation correcte du BT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Server Action settings — updateEscalationConfig</name>
  <files>src/actions/settings.ts</files>
  <read_first>
    - src/actions/settings.ts (état actuel, surtout lignes 9-25 pour `getAdminOrg`, lignes 55-70 pour le patron `updateClosureRequirements`)
    - src/lib/escalation-config.ts (créé en Task 1 — type EscalationConfig)
  </read_first>
  <behavior>
    - `updateEscalationConfig({ enabled, delayHours })` met à jour `Organization.escalationConfig` (Json)
    - Vérifie role admin|manager via `getAdminOrg()` (throw 'Accès refusé' sinon)
    - Coerce `enabled` en boolean, valide `delayHours` ∈ ]0, 168] (max 1 semaine)
    - Revalide `/parametres/organisation`
  </behavior>
  <action>
    AJOUTER à la fin de `src/actions/settings.ts` (ne pas modifier les fonctions existantes) :

    ```typescript
    import { type EscalationConfig } from '@/lib/escalation-config'

    export async function updateEscalationConfig(cfg: EscalationConfig) {
      const organizationId = await getAdminOrg()

      const enabled = !!cfg.enabled
      const delayHours = Number(cfg.delayHours)
      if (!Number.isFinite(delayHours) || delayHours <= 0 || delayHours > 168) {
        throw new Error('Délai invalide (entre 1 et 168 heures)')
      }

      const sanitized: EscalationConfig = { enabled, delayHours }

      await db.organization.update({
        where: { id: organizationId },
        data: { escalationConfig: sanitized },
      })
      revalidatePath('/parametres/organisation')
    }
    ```

    Si `revalidatePath` n'est pas déjà importé en haut du fichier, déplacer l'import en haut (ligne 4 actuellement contient `import { revalidatePath } from 'next/cache'`).
    Si l'import `EscalationConfig` est déjà placé en haut du fichier groupé avec les autres imports, le déplacer dans le bloc d'imports plutôt qu'en ligne avec la fonction.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function updateEscalationConfig" src/actions/settings.ts` retourne 0
    - `grep -q "from '@/lib/escalation-config'" src/actions/settings.ts` retourne 0
    - `grep -q "delayHours > 168" src/actions/settings.ts` retourne 0
    - `grep -q "escalationConfig: sanitized" src/actions/settings.ts` retourne 0
    - `npx tsc --noEmit` exit code 0
    - Fonctions existantes préservées — `grep -c "^export async function" src/actions/settings.ts` ≥ 5
  </acceptance_criteria>
  <done>Server Action de config escalade exportée, validée, scoped admin/manager. Utilisable par l'UI Plan 08-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action | Input non-fiable (planId, itemId, escalationConfig payload) traverse la barrière |
| Org A → Org B | Tentative d'accès cross-tenant via IDs forgés (multi-tenant Korvia) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | toggleChecklistItem, setChecklistMeasure | mitigate | `findFirst({ where: { id, workOrder: { organizationId } } })` avant tout update — empêche un utilisateur d'une autre org de cocher des items qui ne lui appartiennent pas |
| T-08-02 | Spoofing | generateWorkOrderFromPlan, addPlanPart, deletePlanPart | mitigate | Vérification `organizationId` du plan via `findFirst` org-scoped avant toute opération |
| T-08-03 | Tampering | updateEscalationConfig (delayHours malformé) | mitigate | `Number.isFinite(delayHours) && delayHours > 0 && delayHours <= 168`, throw sinon |
| T-08-04 | Tampering | updateEscalationConfig (enabled coerce) | mitigate | `enabled = !!cfg.enabled` côté serveur (defense-in-depth) |
| T-08-05 | Tampering | parseEscalationConfig (config lue depuis DB) | mitigate | Coerce strict des types ; valeurs hors-bornes → défauts |
| T-08-06 | Elevation of Privilege | updateEscalationConfig | mitigate | `getAdminOrg()` vérifie membership role admin|manager, throw sinon |
| T-08-07 | Information Disclosure | generateWorkOrderFromPlan (assetId leak via plan) | accept | L'assetId est déjà restreint à l'org du plan — pas de fuite cross-org possible |
| T-08-08 | Denial of Service | addPlanPart spam | accept | Pas de rate limit dans Phase 8 — risque faible (admin/manager uniquement, faible volume métier attendu) |
</threat_model>

<verification>
- Schéma Prisma validé : `npx prisma validate`
- DB synchronisée : nouvelles tables et colonnes présentes ; `npx prisma generate` régénère le client
- Tests Vitest verts : `npx vitest run src/lib/escalation-config.test.ts`
- Compilation TypeScript : `npx tsc --noEmit` exit 0
- Build Next.js (sanity check) : `npx next build` exit 0 (ou au minimum aucune erreur de type sur les fichiers modifiés)
</verification>

<success_criteria>
1. Toutes les vérifications automatisées des Tasks 1-6 passent
2. Aucune régression sur les fonctions existantes des fichiers modifiés (count d'exports vérifié)
3. Les Plans 08-02 et 08-03 peuvent importer `EscalationConfig`, appeler `generateWorkOrderFromPlan`, `addPlanPart`, `deletePlanPart`, `toggleChecklistItem`, `setChecklistMeasure`, `updateEscalationConfig`, et accéder aux nouvelles relations Prisma (`workOrder.checklistItems`, `workOrder.maintenancePlan`, `maintenancePlan.planParts`, `organization.escalationConfig`)
</success_criteria>

<output>
After completion, create `.planning/phases/08-productivit-technicien/08-01-SUMMARY.md` documenting:
- Tables et champs ajoutés (exact list)
- Nouvelles Server Actions exportées avec leurs signatures
- Contrat de `EscalationConfig` et valeurs par défaut
- Tout point de friction rencontré pour informer Plan 08-02 et 08-03
</output>
