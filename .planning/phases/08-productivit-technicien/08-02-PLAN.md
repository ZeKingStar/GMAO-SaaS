---
phase: 08-productivit-technicien
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/maintenance/maintenance-plan-form-dialog.tsx
  - src/components/maintenance/maintenance-plan-list.tsx
  - src/components/maintenance/plan-parts-section.tsx
  - src/components/maintenance/generate-work-order-button.tsx
  - src/components/work-orders/work-order-checklist.tsx
  - src/components/work-orders/work-order-detail.tsx
  - src/app/(app)/maintenance/page.tsx
  - src/app/(app)/bons-de-travail/[id]/page.tsx
autonomous: true
requirements: [PROD-01, PROD-02]
must_haves:
  truths:
    - "Sur la fiche d'un plan de maintenance, l'admin peut ajouter une pièce requise (nom + quantité, optionnellement liée à une SparePart)"
    - "Sur la fiche d'un plan de maintenance, l'admin peut supprimer une pièce requise"
    - "Sur la fiche d'un plan de maintenance, l'admin voit un bouton 'Générer un BT' qui crée un BT pré-rempli (titre = nom du plan, type = preventive, priorité copiée, tasks → checklistItems, pieces → parts)"
    - "Sur le détail d'un BT issu d'un plan (maintenancePlanId non null OU checklistItems.length > 0), une section 'Checklist' s'affiche"
    - "Dans la section Checklist, le technicien peut cocher/décocher chaque item — la coche persiste après refresh"
    - "Dans la section Checklist, chaque item a un champ texte libre pour une mesure (sauvegarde au blur)"
    - "Sur les BTs sans plan de maintenance, AUCUNE section Checklist ne s'affiche"
  artifacts:
    - path: "src/components/maintenance/plan-parts-section.tsx"
      provides: "UI gestion pièces requises d'un job plan"
      contains: "addPlanPart"
    - path: "src/components/maintenance/generate-work-order-button.tsx"
      provides: "Bouton de génération BT depuis plan"
      contains: "generateWorkOrderFromPlan"
    - path: "src/components/work-orders/work-order-checklist.tsx"
      provides: "Liste interactive cases à cocher + mesures"
      contains: "toggleChecklistItem"
    - path: "src/components/maintenance/maintenance-plan-form-dialog.tsx"
      provides: "Form dialog avec section pièces"
      contains: "PlanPartsSection"
    - path: "src/components/work-orders/work-order-detail.tsx"
      provides: "Affichage conditionnel de WorkOrderChecklist"
      contains: "WorkOrderChecklist"
  key_links:
    - from: "GenerateWorkOrderButton"
      to: "src/actions/maintenance.ts:generateWorkOrderFromPlan"
      via: "Server Action via useTransition"
      pattern: "generateWorkOrderFromPlan\\("
    - from: "WorkOrderChecklist (toggle)"
      to: "src/actions/work-orders.ts:toggleChecklistItem"
      via: "Server Action onChange"
      pattern: "toggleChecklistItem\\("
    - from: "WorkOrderChecklist (mesure)"
      to: "src/actions/work-orders.ts:setChecklistMeasure"
      via: "Server Action onBlur"
      pattern: "setChecklistMeasure\\("
    - from: "WorkOrderDetail"
      to: "WorkOrderChecklist"
      via: "rendu conditionnel sur workOrder.maintenancePlanId || checklistItems.length > 0"
      pattern: "checklistItems"
---

<objective>
Livrer les UI PROD-01 (job plans : pièces requises + bouton de génération de BT) et PROD-02 (checklist interactive sur un BT issu d'un plan, avec cases à cocher et champs de mesure).

Purpose: Donner aux gestionnaires un template réutilisable (job plan = nom + tâches + pièces + durée) et aux techniciens une checklist mobile-first pour exécuter sans rien oublier. C'est le cœur du gain de productivité Phase 8.
Output: Deux nouveaux composants client (`PlanPartsSection`, `WorkOrderChecklist`), un bouton de génération, et l'intégration dans les pages maintenance et BT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-productivit-technicien/08-RESEARCH.md
@.planning/phases/08-productivit-technicien/08-01-PLAN.md

<interfaces>
<!-- Contrats issus de Plan 08-01 ; pas d'exploration nécessaire -->

From src/actions/maintenance.ts (créés en 08-01):
```typescript
export async function generateWorkOrderFromPlan(planId: string): Promise<{ id: string; number: number }>
export async function addPlanPart(planId: string, data: { sparePartId?: string | null; name: string; quantity: number }): Promise<void>
export async function deletePlanPart(partId: string): Promise<void>
```

From src/actions/work-orders.ts (créés en 08-01):
```typescript
export async function toggleChecklistItem(itemId: string, checked: boolean): Promise<void>
export async function setChecklistMeasure(itemId: string, measureValue: string | null): Promise<void>
```

From prisma/schema.prisma (étendu en 08-01):
```prisma
model WorkOrderChecklistItem {
  id String; workOrderId String; order Int; description String;
  checked Boolean; checkedAt DateTime?; measureValue String?; photoUrl String?
}
model MaintenancePlanPart {
  id String; maintenancePlanId String; sparePartId String?; name String; quantity Float
}
// WorkOrder a maintenant: maintenancePlanId String?, maintenancePlan, checklistItems[]
// MaintenancePlan a maintenant: planParts MaintenancePlanPart[]
```

From src/components/maintenance/maintenance-plan-form-dialog.tsx (à étendre):
```typescript
export function MaintenancePlanFormDialog({ plan, assets, categories, children }: Props): JSX.Element
// Le dialog gère création + édition d'un plan. Sections actuelles: infos générales, fréquence, tasks.
// On ajoute: section "Pièces requises" (visible uniquement en mode édition — plan existant).
```

From src/components/work-orders/work-order-detail.tsx (à étendre):
```typescript
type Props = {
  workOrder: { id: string; status: WorkOrderStatus; maintenancePlanId: string | null;
                checklistItems: Array<{ id: string; order: number; description: string;
                                        checked: boolean; measureValue: string | null }>;
                /* ... champs existants ... */ }
  // ... autres props
}
// Le composant rend déjà WorkOrderTimer (ligne ~238) et WorkOrderParts (ligne ~360).
// On insère <WorkOrderChecklist /> conditionnellement.
```

From src/components/work-orders/work-order-parts.tsx (patron pour formulaire add/delete):
```typescript
// useTransition + sonner.toast + revalidate via Server Action
// Pattern: <Button disabled={pending} onClick={() => startTransition(async () => { ... })}>
```

From src/components/settings/closure-requirements-section.tsx (patron UI checkbox + state):
```typescript
// useState + useTransition + toast — patron exact à dupliquer
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlanPartsSection — UI pièces requises d'un job plan</name>
  <files>src/components/maintenance/plan-parts-section.tsx</files>
  <read_first>
    - src/components/work-orders/work-order-parts.tsx (patron exact à dupliquer pour le formulaire d'ajout + delete + pending state + toast)
    - src/components/maintenance/maintenance-plan-form-dialog.tsx (où le composant sera intégré — étudier comment les tasks sont gérées)
    - src/actions/maintenance.ts (signatures addPlanPart et deletePlanPart créées en 08-01)
  </read_first>
  <behavior>
    - Affiche la liste des `MaintenancePlanPart` du plan (lecture initiale via props)
    - Formulaire d'ajout: combobox/select de SparePart existante OU champ "nom libre" si pièce hors inventaire, + champ quantité (number ≥ 0.01)
    - Bouton "Ajouter la pièce" appelle `addPlanPart` puis affiche toast + revalide
    - Chaque ligne a un bouton "Supprimer" qui appelle `deletePlanPart`
    - État pending pendant les Server Actions (boutons disabled, label "Ajout…" / "Suppression…")
  </behavior>
  <action>
    Créer `src/components/maintenance/plan-parts-section.tsx` (Client Component, `'use client'`).

    Props:
    ```typescript
    type Props = {
      planId: string
      parts: Array<{
        id: string
        sparePartId: string | null
        name: string
        quantity: number
        sparePart?: { id: string; name: string; partNumber: string | null } | null
      }>
      spareParts: Array<{ id: string; name: string; partNumber: string | null }>
    }
    export function PlanPartsSection({ planId, parts, spareParts }: Props)
    ```

    Structure UI (utiliser le patron exact de `work-order-parts.tsx`) :
    - Titre `<h4>Pièces requises</h4>` + helper text "Ces pièces seront pré-remplies sur les BT générés depuis ce plan. Aucun décompte de stock à la création."
    - Liste des `parts` triée par `createdAt` (déjà ordonnée côté serveur) :
      - Une ligne par pièce : icône, nom (si lié à SparePart, afficher `${sparePart.name} (${sparePart.partNumber})`), quantité, bouton Supprimer
    - Formulaire en bas :
      - `<select>` ou Combobox pour choisir une SparePart (option "— Pièce hors inventaire —")
      - Si "hors inventaire" sélectionné, afficher un `<Input>` pour le nom libre
      - `<Input type="number" min="0.01" step="0.01">` pour quantité
      - `<Button type="submit">Ajouter la pièce</Button>`

    Logique Server Actions :
    ```typescript
    import { addPlanPart, deletePlanPart } from '@/actions/maintenance'
    import { useTransition, useState } from 'react'
    import { toast } from 'sonner'
    ```

    Au submit du formulaire :
    ```typescript
    startTransition(async () => {
      try {
        await addPlanPart(planId, {
          sparePartId: selectedSparePartId || null,
          name: selectedSparePartId
            ? spareParts.find(s => s.id === selectedSparePartId)!.name
            : freeName,
          quantity: parsedQty,
        })
        toast.success('Pièce ajoutée')
        // reset form
      } catch (e) {
        toast.error(e instanceof Error ? e.message : 'Erreur')
      }
    })
    ```

    Validation client :
    - Si "hors inventaire" et `freeName.trim() === ''` → toast d'erreur, ne pas appeler l'action
    - Si `parsedQty <= 0` ou non-fini → toast d'erreur

    Utiliser Tailwind, composants `Button`, `Input` de `@/components/ui/`. Style cohérent avec `work-order-parts.tsx`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/maintenance/plan-parts-section.tsx` existe et démarre par `'use client'`
    - `grep -q "export function PlanPartsSection" src/components/maintenance/plan-parts-section.tsx` retourne 0
    - `grep -q "addPlanPart" src/components/maintenance/plan-parts-section.tsx` retourne 0
    - `grep -q "deletePlanPart" src/components/maintenance/plan-parts-section.tsx` retourne 0
    - `grep -q "useTransition" src/components/maintenance/plan-parts-section.tsx` retourne 0
    - `grep -q "sonner" src/components/maintenance/plan-parts-section.tsx` retourne 0
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>Composant créé, type-safe, prêt à être intégré dans MaintenancePlanFormDialog en Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WorkOrderChecklist — UI checklist interactive (PROD-02)</name>
  <files>src/components/work-orders/work-order-checklist.tsx</files>
  <read_first>
    - src/actions/work-orders.ts (signatures toggleChecklistItem et setChecklistMeasure créées en 08-01)
    - src/components/work-orders/work-order-parts.tsx (patron useTransition + sonner + composants UI)
    - src/components/work-orders/work-order-detail.tsx lignes 1-50 (style d'en-tête de section utilisé dans le détail BT)
    - .planning/phases/08-productivit-technicien/08-RESEARCH.md (section Piège 3 — affichage conditionnel)
  </read_first>
  <behavior>
    - Affiche la liste des items triés par `order` ASC
    - Chaque item : checkbox + numéro d'ordre + description + champ texte mesure (optionnel)
    - Toggle checkbox appelle `toggleChecklistItem(item.id, e.target.checked)` immédiatement (optimistic via `useTransition`)
    - Champ mesure sauvegarde au `onBlur` via `setChecklistMeasure(item.id, value)` si valeur a changé
    - Toast d'erreur si l'action échoue
    - Compteur "X/Y complétés" en en-tête
    - Si `items.length === 0`, le composant retourne `null` (ne pas afficher de section vide)
  </behavior>
  <action>
    Créer `src/components/work-orders/work-order-checklist.tsx` (Client Component, `'use client'`).

    Props:
    ```typescript
    type ChecklistItem = {
      id: string
      order: number
      description: string
      checked: boolean
      measureValue: string | null
    }
    type Props = {
      workOrderId: string
      items: ChecklistItem[]
      readOnly?: boolean  // true si BT closed/resolved — désactive interactions
    }
    export function WorkOrderChecklist({ workOrderId, items, readOnly = false }: Props)
    ```

    Si `items.length === 0`, `return null` immédiatement (PROD-02 + Piège 3).

    Structure UI :
    - En-tête : `<h3>Checklist</h3>` + badge `<span>{completed}/{total} complétés</span>` où `completed = items.filter(i => i.checked).length`
    - Liste : pour chaque item (trié par order ASC) :
      ```jsx
      <li className="flex flex-col gap-2 p-3 rounded-lg border bg-card">
        <label className="flex items-start gap-3 cursor-pointer">
          <input
            type="checkbox"
            checked={item.checked}
            onChange={(e) => handleToggle(item.id, e.target.checked)}
            disabled={readOnly || pending}
            className="mt-1 h-5 w-5 rounded border-input"
          />
          <span className="flex-1">
            <span className="text-xs text-muted-foreground">Étape {item.order}</span>
            <span className={`block text-sm ${item.checked ? 'line-through text-muted-foreground' : ''}`}>
              {item.description}
            </span>
          </span>
        </label>
        <Input
          type="text"
          placeholder="Mesure / valeur (optionnel)"
          defaultValue={item.measureValue ?? ''}
          disabled={readOnly}
          onBlur={(e) => handleMeasureBlur(item.id, e.target.value, item.measureValue)}
          maxLength={200}
        />
      </li>
      ```

    Handlers :
    ```typescript
    const [pending, startTransition] = useTransition()

    function handleToggle(itemId: string, checked: boolean) {
      startTransition(async () => {
        try { await toggleChecklistItem(itemId, checked) }
        catch (e) { toast.error(e instanceof Error ? e.message : 'Erreur') }
      })
    }

    function handleMeasureBlur(itemId: string, newValue: string, oldValue: string | null) {
      const cleaned = newValue.trim()
      const oldCleaned = (oldValue ?? '').trim()
      if (cleaned === oldCleaned) return  // no-op si inchangé
      startTransition(async () => {
        try {
          await setChecklistMeasure(itemId, cleaned || null)
          toast.success('Mesure enregistrée')
        } catch (e) { toast.error(e instanceof Error ? e.message : 'Erreur') }
      })
    }
    ```

    Imports requis :
    ```typescript
    'use client'
    import { useTransition } from 'react'
    import { toast } from 'sonner'
    import { Input } from '@/components/ui/input'
    import { toggleChecklistItem, setChecklistMeasure } from '@/actions/work-orders'
    import { CheckSquare } from 'lucide-react'
    ```

    Mobile-first : la liste utilise `flex flex-col gap-3`, les checkboxes sont 5×5 (cible tactile suffisante). Les inputs sont full-width sur mobile.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/work-orders/work-order-checklist.tsx` existe et commence par `'use client'`
    - `grep -q "export function WorkOrderChecklist" src/components/work-orders/work-order-checklist.tsx` retourne 0
    - `grep -q "toggleChecklistItem" src/components/work-orders/work-order-checklist.tsx` retourne 0
    - `grep -q "setChecklistMeasure" src/components/work-orders/work-order-checklist.tsx` retourne 0
    - `grep -q "items.length === 0" src/components/work-orders/work-order-checklist.tsx` retourne 0
    - `grep -q "type=\"checkbox\"" src/components/work-orders/work-order-checklist.tsx` retourne 0
    - `grep -q "onBlur" src/components/work-orders/work-order-checklist.tsx` retourne 0
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>Composant checklist mobile-first créé, gère toggle (transition optimiste) + mesure (sauvegarde au blur), retourne `null` si vide.</done>
</task>

<task type="auto">
  <name>Task 3: Intégrer PlanPartsSection + GenerateWorkOrderButton dans MaintenancePlan UI</name>
  <files>src/components/maintenance/maintenance-plan-form-dialog.tsx, src/components/maintenance/maintenance-plan-list.tsx, src/components/maintenance/generate-work-order-button.tsx, src/app/(app)/maintenance/page.tsx</files>
  <read_first>
    - src/components/maintenance/maintenance-plan-form-dialog.tsx (lire complet — section tasks doit être préservée, on ajoute après)
    - src/components/maintenance/maintenance-plan-list.tsx (état actuel — l'accordéon affiche tasks ; on affichera aussi planParts)
    - src/app/(app)/maintenance/page.tsx (loader qui passe `assets`, `categories` à `MaintenancePlanList` — on ajoute `spareParts`)
    - src/actions/maintenance.ts (generateWorkOrderFromPlan signature 08-01)
  </read_first>
  <action>
    **A. Créer `src/components/maintenance/generate-work-order-button.tsx`** (Client Component) :

    ```typescript
    'use client'
    import { useTransition } from 'react'
    import { useRouter } from 'next/navigation'
    import { Button } from '@/components/ui/button'
    import { toast } from 'sonner'
    import { Wrench } from 'lucide-react'
    import { generateWorkOrderFromPlan } from '@/actions/maintenance'

    type Props = { planId: string; planName: string; disabled?: boolean }

    export function GenerateWorkOrderButton({ planId, planName, disabled }: Props) {
      const [pending, startTransition] = useTransition()
      const router = useRouter()

      function handleClick() {
        if (!confirm(`Générer un bon de travail pré-rempli depuis le plan « ${planName} » ?`)) return
        startTransition(async () => {
          try {
            const wo = await generateWorkOrderFromPlan(planId)
            toast.success(`Bon de travail #${wo.number} créé`)
            router.push(`/bons-de-travail/${wo.id}`)
          } catch (e) {
            toast.error(e instanceof Error ? e.message : 'Erreur')
          }
        })
      }

      return (
        <Button variant="default" size="sm" onClick={handleClick} disabled={disabled || pending}>
          <Wrench className="mr-2 h-4 w-4" />
          {pending ? 'Génération…' : 'Générer un BT'}
        </Button>
      )
    }
    ```

    **B. Étendre `src/components/maintenance/maintenance-plan-form-dialog.tsx`** :

    1. Étendre les props : ajouter `spareParts: Array<{ id: string; name: string; partNumber: string | null }>` (passé depuis `maintenance-plan-list.tsx` qui le recevra de la page).
    2. Étendre le type `plan` (prop existante en mode édition) pour inclure `planParts: Array<{ id, sparePartId, name, quantity, sparePart?: ... }>`.
    3. Importer `PlanPartsSection` : `import { PlanPartsSection } from './plan-parts-section'`.
    4. En mode édition uniquement (`plan` est défini), après la section des tasks et avant la section "Soumettre", ajouter :
    ```tsx
    {plan && (
      <div className="space-y-3 pt-4 border-t">
        <PlanPartsSection
          planId={plan.id}
          parts={plan.planParts ?? []}
          spareParts={spareParts}
        />
      </div>
    )}
    ```

    **C. Étendre `src/components/maintenance/maintenance-plan-list.tsx`** :

    1. Étendre les props : `spareParts: Array<{ id: string; name: string; partNumber: string | null }>`.
    2. Importer `GenerateWorkOrderButton` : `import { GenerateWorkOrderButton } from './generate-work-order-button'`.
    3. Dans chaque carte/accordéon de plan, à côté du bouton "Modifier" (ligne ~193 actuellement avec `<MaintenancePlanFormDialog ...>`), ajouter le bouton :
    ```tsx
    <GenerateWorkOrderButton planId={plan.id} planName={plan.name} disabled={!plan.isActive} />
    ```
    4. Passer `spareParts` à `MaintenancePlanFormDialog` aux deux endroits où il est rendu (édition ligne ~193 ET création lignes ~281 + ~294).
    5. Étendre l'affichage de chaque plan pour montrer les pièces requises (sous les tasks) :
    ```tsx
    {plan.planParts && plan.planParts.length > 0 && (
      <div className="mt-3">
        <p className="text-xs font-medium text-muted-foreground">Pièces requises ({plan.planParts.length}) :</p>
        <ul className="text-xs text-muted-foreground list-disc list-inside">
          {plan.planParts.map(p => (
            <li key={p.id}>{p.name} × {p.quantity}</li>
          ))}
        </ul>
      </div>
    )}
    ```
    6. Étendre le type `plan` props pour inclure `planParts`.

    **D. Étendre `src/app/(app)/maintenance/page.tsx`** :

    1. Dans le `Promise.all` (ou les requêtes Prisma de la page), inclure `planParts` dans les plans chargés :
    ```typescript
    const plans = await db.maintenancePlan.findMany({
      where: { organizationId: org.id },
      include: {
        asset: { select: { id: true, name: true } },
        tasks: { orderBy: { order: 'asc' } },
        planParts: {
          include: { sparePart: { select: { id: true, name: true, partNumber: true } } },
          orderBy: { createdAt: 'asc' },
        },
      },
      orderBy: { createdAt: 'desc' },
    })
    ```
    2. Charger les SpareParts :
    ```typescript
    const spareParts = await db.sparePart.findMany({
      where: { organizationId: org.id },
      select: { id: true, name: true, partNumber: true },
      orderBy: { name: 'asc' },
    })
    ```
    3. Passer `spareParts` au composant `<MaintenancePlanList ... spareParts={spareParts} />`.

    Si les chemins/noms exacts dans `maintenance/page.tsx` diffèrent (par exemple noms de variables `plans` vs autre), adapter en conservant la sémantique (charger planParts + spareParts, les transmettre).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | grep -E "Failed|error" | head -5 || true</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/maintenance/generate-work-order-button.tsx` existe avec `'use client'` et appelle `generateWorkOrderFromPlan`
    - `grep -q "generateWorkOrderFromPlan" src/components/maintenance/generate-work-order-button.tsx` retourne 0
    - `grep -q "GenerateWorkOrderButton" src/components/maintenance/maintenance-plan-list.tsx` retourne 0
    - `grep -q "PlanPartsSection" src/components/maintenance/maintenance-plan-form-dialog.tsx` retourne 0
    - `grep -q "planParts" src/app/\\(app\\)/maintenance/page.tsx` retourne 0
    - `grep -q "spareParts" src/app/\\(app\\)/maintenance/page.tsx` retourne 0
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>UI Job Plans complète : pièces ajoutables/supprimables dans le dialog, affichées dans la liste, bouton Générer BT fonctionnel. PROD-01 livré.</done>
</task>

<task type="auto">
  <name>Task 4: Intégrer WorkOrderChecklist dans WorkOrderDetail (PROD-02)</name>
  <files>src/components/work-orders/work-order-detail.tsx, src/app/(app)/bons-de-travail/[id]/page.tsx</files>
  <read_first>
    - src/components/work-orders/work-order-detail.tsx (lire complet pour comprendre où insérer la section — autour des sections existantes Timer / Parts / Comments)
    - src/app/(app)/bons-de-travail/[id]/page.tsx (loader Prisma — il faut inclure `checklistItems` et `maintenancePlanId`)
    - .planning/phases/08-productivit-technicien/08-RESEARCH.md (section Piège 3 — affichage conditionnel uniquement si BT issu de PM)
  </read_first>
  <action>
    **A. Étendre `src/app/(app)/bons-de-travail/[id]/page.tsx`** :

    Dans le `findFirst({ where: { id, organizationId }, include: { ... } })`, ajouter au `include` :
    ```typescript
    checklistItems: {
      select: { id: true, order: true, description: true, checked: true, measureValue: true },
      orderBy: { order: 'asc' },
    },
    ```

    Le champ `maintenancePlanId` est scalar — pas besoin de l'inclure explicitement, il est déjà dans le retour par défaut. Si la page utilise `select: { ... }` au lieu de `include`, ajouter `maintenancePlanId: true` et le sous-select `checklistItems: { ... }`.

    Pas besoin de passer une prop supplémentaire — `workOrder.checklistItems` est désormais disponible sur l'objet déjà passé à `<WorkOrderDetail workOrder={workOrder} ... />`.

    **B. Étendre `src/components/work-orders/work-order-detail.tsx`** :

    1. Import en haut du fichier :
    ```typescript
    import { WorkOrderChecklist } from './work-order-checklist'
    ```

    2. Étendre le type Props pour inclure `checklistItems` et `maintenancePlanId` sur `workOrder` (suivre le style des autres champs déjà typés dans le fichier) :
    ```typescript
    workOrder: {
      // ... champs existants
      maintenancePlanId: string | null
      checklistItems: Array<{
        id: string
        order: number
        description: string
        checked: boolean
        measureValue: string | null
      }>
    }
    ```

    3. Insérer le rendu conditionnel **juste après** la section `WorkOrderTimer` (ligne ~238) et **avant** la section `WorkOrderParts` (ligne ~360). Le placement exact : la Checklist passe en deuxième position (après Timer, avant Parts) — c'est l'ordre d'exécution naturel du technicien (lance timer → coche checklist → ajoute pièces).

    ```tsx
    {(workOrder.maintenancePlanId || workOrder.checklistItems.length > 0) && (
      <WorkOrderChecklist
        workOrderId={workOrder.id}
        items={workOrder.checklistItems}
        readOnly={workOrder.status === 'closed' || workOrder.status === 'resolved'}
      />
    )}
    ```

    Si le composant utilise un layout grid/card spécifique (ex : `<Card>`), envelopper de la même manière que `<WorkOrderParts>` pour cohérence visuelle.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "WorkOrderChecklist" src/components/work-orders/work-order-detail.tsx` retourne 0
    - `grep -q "workOrder.maintenancePlanId || workOrder.checklistItems.length > 0" src/components/work-orders/work-order-detail.tsx` retourne 0
    - `grep -q "checklistItems" src/app/\\(app\\)/bons-de-travail/\\[id\\]/page.tsx` retourne 0
    - `grep -q "orderBy: { order: 'asc' }" src/app/\\(app\\)/bons-de-travail/\\[id\\]/page.tsx` retourne 0
    - `npx tsc --noEmit` exit code 0
  </acceptance_criteria>
  <done>WorkOrderChecklist affichée conditionnellement sur les BTs issus de PM, en lecture seule sur les BTs clos. PROD-02 livré.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → Server Action | Saisies utilisateur (texte mesure, quantité pièce, IDs) traversent vers les actions 08-01 |
| Mode lecture seule → mutation | BT clos doit empêcher modifications de checklist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | WorkOrderChecklist mesure input | mitigate | `maxLength={200}` côté UI + clipping serveur dans `setChecklistMeasure` (Plan 08-01 T-08-03 cousin) |
| T-08-10 | Spoofing | Toggle item d'un autre BT via DevTools | mitigate | Server Action 08-01 vérifie `workOrder.organizationId` — UI ne fait que déclencher |
| T-08-11 | Tampering | Mode readOnly contourné côté client (DevTools) | mitigate | UI désactive contrôles mais sécurité véritable côté Server Action ; ici accept que UI soit "soft" — l'action serveur autorise toujours le toggle (statut clos n'est pas un blocage métier) |
| T-08-12 | Information Disclosure | Affichage planParts d'un autre plan via spareParts mal scopés | mitigate | Le loader `maintenance/page.tsx` charge `spareParts` strictement filtrés `where: { organizationId: org.id }` |
| T-08-13 | Confused Deputy | GenerateWorkOrderButton appelé sur un planId arbitraire | mitigate | `generateWorkOrderFromPlan` 08-01 vérifie `plan.organizationId` ; l'UI ne fait que passer l'ID |
</threat_model>

<verification>
- TypeScript compile : `npx tsc --noEmit` exit 0
- Build Next.js : `npx next build` exit 0 (sans nouveau warning bloquant)
- Visuel : la liste maintenance affiche pièces requises et bouton Générer BT (sera testé en Plan 08-03 lors du checkpoint humain)
- Visuel : la page d'un BT généré depuis un plan affiche la section Checklist (sera testé en Plan 08-03)
</verification>

<success_criteria>
1. PROD-01 : Sur la fiche d'un plan de maintenance, l'admin peut ajouter, voir, supprimer des pièces requises ET cliquer sur "Générer un BT" pour obtenir un nouveau BT pré-rempli (tasks → checklist, pieces → parts).
2. PROD-02 : Sur le détail d'un BT issu d'un plan, le technicien voit une section Checklist, peut cocher/décocher chaque item, et entrer une mesure libre par item.
3. Aucune section Checklist ne s'affiche sur les BTs correctifs créés à la main (sans `maintenancePlanId` et sans items).
4. La build complète (`npx next build`) reste verte.
</success_criteria>

<output>
After completion, create `.planning/phases/08-productivit-technicien/08-02-SUMMARY.md` documenting:
- Composants client créés et leurs props
- Modifications apportées aux loaders de pages
- Décisions UX prises (ordre d'affichage Timer/Checklist/Parts, mode readOnly, etc.)
- Points à valider lors du checkpoint humain de Plan 08-03
</output>
