---
phase: 08-productivit-technicien
plan: "04"
type: execute
wave: 3
gap_closure: true
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/components/maintenance/plan-parts-section.tsx
  - src/components/maintenance/maintenance-plan-form-dialog.tsx
autonomous: true
requirements: [PROD-01]
must_haves:
  truths:
    - "La section 'Pièces requises' est visible immédiatement à l'ouverture du dialog d'édition d'un plan"
    - "Cliquer 'Ajouter la pièce' dans le formulaire de pièce confirme uniquement la pièce (toast), ne ferme pas le dialog du plan"
    - "Après ajout d'une pièce, le formulaire se réinitialise pour permettre un ajout immédiat suivant (GAP-3)"
    - "Un bouton 'Ajouter et continuer' permet d'enchaîner les ajouts sans rouvrir le formulaire"
  artifacts:
    - path: "src/components/maintenance/plan-parts-section.tsx"
      provides: "Formulaire pièce standalone (pas un <form> imbriqué)"
      contains: "keepOpen"
    - path: "src/components/maintenance/maintenance-plan-form-dialog.tsx"
      provides: "Dialog plan sans bubbling de submit depuis PlanPartsSection"
      contains: "PlanPartsSection"
  key_links:
    - from: "PlanPartsSection submit handler"
      to: "addPlanPart server action"
      via: "onClick handler sur <button> — pas via <form onSubmit>"
      pattern: "onClick.*addPlanPart\\|handleAddPart"
---

<objective>
Corriger le bug critique GAP-1 : la section "Pièces requises" non visible par défaut et le bouton "Enregistrer" qui ferme le dialog entier au lieu de confirmer uniquement la pièce. Ajouter simultanément le mode ajout séquentiel (GAP-3).

Purpose: Les bugs rendent le workflow pièces inutilisable — un technicien ne peut pas gérer les pièces d'un plan de maintenance sans fermer le dialog.
Output: PlanPartsSection sans form imbriqué, avec mode "Ajouter et continuer".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-productivit-technicien/08-02-SUMMARY.md
@src/components/maintenance/plan-parts-section.tsx
@src/components/maintenance/maintenance-plan-form-dialog.tsx
</context>

<interfaces>
<!-- Contrats actuels à préserver -->

From src/components/maintenance/plan-parts-section.tsx:
```typescript
type Props = {
  planId: string
  parts: PlanPart[]
  spareParts: SparePart[]
}
// Utilisé dans maintenance-plan-form-dialog.tsx ligne 341-345:
// <PlanPartsSection planId={plan.id} parts={plan.planParts ?? []} spareParts={spareParts} />
```

From src/actions/maintenance.ts (08-01-SUMMARY):
```typescript
addPlanPart(planId: string, { sparePartId?: string | null, name: string, quantity: number }): Promise<void>
deletePlanPart(partId: string): Promise<void>
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Tâche 1 : Corriger le bubbling de submit et ajouter le mode séquentiel dans PlanPartsSection</name>
  <files>src/components/maintenance/plan-parts-section.tsx</files>
  <read_first>
    - src/components/maintenance/plan-parts-section.tsx (fichier complet — lire AVANT toute modification)
    - src/components/maintenance/maintenance-plan-form-dialog.tsx (lignes 339-350 — contexte d'intégration)
  </read_first>
  <action>
**Cause racine du bug GAP-1-B :** Le formulaire `<form onSubmit={handleSubmit}>` dans `PlanPartsSection` est rendu à l'intérieur d'un `<form onSubmit={handleSubmit}>` du dialog parent (`maintenance-plan-form-dialog.tsx` ligne 183). L'événement `submit` du formulaire enfant bulle jusqu'au parent, déclenchant `handleSubmit` du dialog qui appelle `setOpen(false)`.

**Correction :** Remplacer le `<form onSubmit={handleSubmit}>` par un `<div>` avec un bouton `onClick`. L'appel à `e.preventDefault()` dans `handleSubmit` devient inutile — mais garder la signature compatible.

**Changements précis dans `plan-parts-section.tsx` :**

1. Supprimer `function handleSubmit(e: React.FormEvent)` — remplacer par `function handleAddPart()` (sans event parameter).

2. Remplacer la validation d'event par la même logique sans `e.preventDefault()`.

3. Ajouter un state `keepOpen` : `const [keepOpen, setKeepOpen] = useState(false)`.

4. Dans `handleAddPart`, après `toast.success('Pièce ajoutée')` :
   - Si `keepOpen` est true : ne pas appeler `setShowForm(false)` — appeler à la place `setSelectedSparePartId('')` + `setFreeName('')` + `setQuantity('1')` (reset partiel, garde le formulaire ouvert)
   - Si `keepOpen` est false : appeler `resetForm()` (comportement actuel)

5. Remplacer `<form onSubmit={handleSubmit} ...>` par `<div className="border rounded-lg p-3 space-y-3 bg-muted/30">`.

6. Remplacer les deux boutons actuels (Annuler + "Ajouter la pièce") par trois éléments :
   ```tsx
   <Button type="button" variant="ghost" size="sm" onClick={resetForm}>Annuler</Button>
   <Button
     type="button"
     variant="outline"
     size="sm"
     onClick={() => { setKeepOpen(true); handleAddPart() }}
     disabled={isPending}
   >
     + Continuer
   </Button>
   <Button
     type="button"
     size="sm"
     onClick={() => { setKeepOpen(false); handleAddPart() }}
     disabled={isPending}
   >
     Ajouter la pièce
   </Button>
   ```
   Note : `setKeepOpen` doit être mis à jour AVANT d'appeler `handleAddPart`. Pour éviter la race condition, refactorer `handleAddPart` pour prendre un paramètre `keepFormOpen: boolean` et supprimer le state `keepOpen`.

   **Implémentation finale recommandée :**
   ```typescript
   function handleAddPart(keepFormOpen: boolean) {
     const parsedQty = parseFloat(quantity)
     if (!Number.isFinite(parsedQty) || parsedQty <= 0) {
       toast.error('Quantité invalide (doit être > 0)')
       return
     }
     if (isHorsInventaire) {
       if (!freeName.trim()) { toast.error('Le nom de la pièce est requis'); return }
     } else if (!selectedSparePartId) {
       toast.error('Choisissez une pièce ou sélectionnez "Pièce hors inventaire"')
       return
     }
     const sparePartId = isHorsInventaire ? null : selectedSparePartId
     const name = isHorsInventaire
       ? freeName.trim()
       : (spareParts.find(s => s.id === selectedSparePartId)?.name ?? freeName.trim())

     startTransition(async () => {
       try {
         await addPlanPart(planId, { sparePartId, name, quantity: parsedQty })
         toast.success('Pièce ajoutée')
         if (keepFormOpen) {
           setSelectedSparePartId('')
           setFreeName('')
           setQuantity('1')
         } else {
           resetForm()
         }
       } catch (e) {
         toast.error(e instanceof Error ? e.message : 'Erreur')
       }
     })
   }
   ```

7. Ne pas modifier les imports ni la structure de la liste de pièces existante (partie basse du composant).

8. Renommer le bouton label : "Ajouter la pièce" → "Ajouter" (plus compact) et "+ Continuer" pour le mode séquentiel.
  </action>
  <verify>
    <automated>grep -n "form onSubmit\|&lt;form " /home/deploy/gmao-saas/src/components/maintenance/plan-parts-section.tsx | wc -l</automated>
    Le résultat doit être 0 — aucun élément `<form` dans le fichier.
    Vérification supplémentaire : `grep -n "handleAddPart\|keepFormOpen" /home/deploy/gmao-saas/src/components/maintenance/plan-parts-section.tsx` doit retourner des lignes.
    `npx tsc --noEmit 2>&1 | grep plan-parts-section` doit retourner vide.
  </verify>
  <acceptance_criteria>
    - Aucun élément `<form` dans plan-parts-section.tsx (grep retourne 0 ligne)
    - `handleAddPart(keepFormOpen: boolean)` est présent et appelé depuis deux boutons onClick
    - Bouton "Ajouter" appelle `handleAddPart(false)` → ferme le formulaire, conserve le dialog
    - Bouton "+ Continuer" appelle `handleAddPart(true)` → remet les champs à zéro, garde le formulaire ouvert
    - `npx tsc --noEmit` ne génère aucune nouvelle erreur sur ce fichier
  </acceptance_criteria>
  <done>PlanPartsSection sans form imbriqué — le submit ne bulle plus vers le dialog parent. Mode séquentiel opérationnel.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Corriger la visibilité initiale de la section "Pièces requises" dans le dialog</name>
  <files>src/components/maintenance/maintenance-plan-form-dialog.tsx</files>
  <read_first>
    - src/components/maintenance/maintenance-plan-form-dialog.tsx (fichier complet — lire AVANT toute modification)
  </read_first>
  <action>
**Cause racine du bug GAP-1-A :** Le composant `MaintenancePlanFormDialog` initialise son state `form` dans `useState` à la construction du composant React — pas à chaque ouverture du dialog. Quand le dialog s'ouvre pour la première fois sur un plan existant, les props `plan.planParts` sont bien présentes mais le rendu conditionnel `{plan && (<PlanPartsSection .../>)}` dépend de la prop `plan` passée au composant, pas du state.

Le vrai problème : après une Server Action de type `revalidatePath` ou mutation, Next.js re-rend la page serveur et passe de nouvelles props au composant. Mais si le composant est monté avec `plan.planParts = []` et que les parts sont ajoutées plus tard, le composant ne voit pas les nouvelles parts car il utilise `plan.planParts ?? []` directement dans les props de `PlanPartsSection` sans state local de synchronisation.

**Correction dans `maintenance-plan-form-dialog.tsx` :**

1. Lire les lignes 83-101 (`handleOpen`) et la ligne 64 (signature du composant).

2. Dans `handleOpen`, quand `isOpen === true` et que `plan` existe, forcer un reset explicite pour garantir que le dialog affiche l'état frais des props. Ajouter :
   ```typescript
   function handleOpen(isOpen: boolean) {
     if (isOpen && plan) {
       // Forcer re-sync avec les props courantes à chaque ouverture
       setForm({
         name: plan.name,
         description: plan.description ?? '',
         triggerType: plan.triggerType,
         frequency: plan.frequency ?? 'monthly',
         customDays: plan.customDays?.toString() ?? '',
         meterThreshold: plan.meterThreshold?.toString() ?? '',
         estimatedHours: plan.estimatedHours?.toString() ?? '',
         priority: plan.priority,
         assetId: plan.assetId ?? '',
         categoryId: plan.categoryId ?? '',
         nextDueAt: plan.nextDueAt ? new Date(plan.nextDueAt).toISOString().slice(0, 10) : '',
         tasks: plan.tasks?.map(t => t.description) ?? [],
       })
       setTaskInput('')
     }
     // ... le reste du code existant (cas !plan déjà géré)
     setOpen(isOpen)
   }
   ```

3. Vérifier que la section `{plan && (<div ...><PlanPartsSection .../></div>)}` passe bien `plan.planParts ?? []` comme prop `parts` — c'est déjà le cas (ligne 342), ne pas modifier.

4. S'assurer que le `<DialogContent>` a `key={plan?.id ?? 'new'}` pour forcer un remontage complet du composant Dialog quand on passe d'un plan à l'autre :
   ```tsx
   <DialogContent key={plan?.id ?? 'new'} className="sm:max-w-lg max-h-[90vh] overflow-y-auto">
   ```
   Cela garantit que React recrée le composant (et donc son state) à chaque changement de plan cible.

5. Ne modifier que `handleOpen` et l'attribut `key` sur `DialogContent` — ne pas toucher le reste du composant.
  </action>
  <verify>
    <automated>grep -n "key={plan\|handleOpen\|isOpen && plan" /home/deploy/gmao-saas/src/components/maintenance/maintenance-plan-form-dialog.tsx</automated>
    Doit montrer `key={plan?.id ?? 'new'}` sur DialogContent et la branche `isOpen && plan` dans handleOpen.
    `npx tsc --noEmit 2>&1 | grep maintenance-plan-form-dialog` doit retourner vide.
  </verify>
  <acceptance_criteria>
    - `DialogContent` a l'attribut `key={plan?.id ?? 'new'}`
    - `handleOpen` contient une branche `if (isOpen && plan)` qui re-synchronise le form avec les props
    - `npx tsc --noEmit` ne génère aucune nouvelle erreur sur ce fichier
    - Visuellement : ouvrir le dialog d'édition d'un plan → section "Pièces requises" visible immédiatement sans fermer/rouvrir
  </acceptance_criteria>
  <done>Dialog plan synchronisé à l'ouverture — section pièces visible immédiatement, state cohérent avec les props serveur.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server Action | addPlanPart/deletePlanPart — entrées non filtrées côté client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | handleAddPart onClick | accept | Validation quantité et nom déjà présente dans la fonction — identique à l'implémentation précédente |
| T-08-04-02 | Elevation | PlanPartsSection | accept | Server Action addPlanPart vérifie l'appartenance org (implémentée en 08-01) |
</threat_model>

<verification>
```bash
# 1. Vérifier qu'aucun <form> imbriqué dans plan-parts-section.tsx
grep -c "<form" /home/deploy/gmao-saas/src/components/maintenance/plan-parts-section.tsx
# Attendu : 0

# 2. Vérifier la présence du key sur DialogContent
grep "key={plan" /home/deploy/gmao-saas/src/components/maintenance/maintenance-plan-form-dialog.tsx
# Attendu : 1 ligne contenant key={plan?.id ?? 'new'}

# 3. TypeScript clean
cd /home/deploy/gmao-saas && npx tsc --noEmit 2>&1 | grep -E "plan-parts-section|maintenance-plan-form-dialog"
# Attendu : aucune sortie
```
</verification>

<success_criteria>
- Section "Pièces requises" visible immédiatement à l'ouverture du dialog d'édition (GAP-1-A)
- Bouton "Ajouter" confirme uniquement la pièce (toast), le dialog reste ouvert (GAP-1-B)
- Bouton "+ Continuer" réinitialise le formulaire et reste ouvert pour un ajout immédiat (GAP-3)
- Zéro régression TypeScript sur les deux fichiers modifiés
</success_criteria>

<output>
Après complétion, créer `.planning/phases/08-productivit-technicien/08-04-SUMMARY.md`
</output>
