---
phase: 08-productivit-technicien
plan: "06"
type: execute
wave: 3
gap_closure: true
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - src/components/work-orders/work-order-list.tsx
  - src/components/maintenance/maintenance-plan-list.tsx
autonomous: true
requirements: [PROD-01, PROD-02]
must_haves:
  truths:
    - "Les listes BT et plans de maintenance ont des en-têtes de colonnes cliquables qui trient la liste"
    - "Un indicateur visuel (flèche) montre la colonne active et le sens du tri (ASC/DESC)"
    - "Cliquer une deuxième fois sur un en-tête inverse le tri"
    - "Les boutons de filtre (statut, type) ont un état hover visuellement distinct de l'état normal et de l'état sélectionné"
  artifacts:
    - path: "src/components/work-orders/work-order-list.tsx"
      provides: "Liste BT avec tri par colonnes + hover amélioré"
      contains: "sortKey\|SortHeader\|ChevronUp"
    - path: "src/components/maintenance/maintenance-plan-list.tsx"
      provides: "Liste plans avec tri par colonnes + hover amélioré"
      contains: "sortKey\|SortHeader"
  key_links:
    - from: "SortHeader onClick"
      to: "sortKey + sortDir state"
      via: "useState local — tri client-side"
      pattern: "setSortKey\\|setSortDir"
---

<objective>
Ajouter des en-têtes de colonnes triables aux listes principales (BT et plans de maintenance) et améliorer l'état hover des boutons de filtre (GAP-4 + GAP-5).

Purpose: L'utilisateur veut un comportement Maximo — cliquer un en-tête pour trier. Le tri client-side est suffisant pour les volumes attendus (< 500 enregistrements par org en MVP).
Output: Listes triables avec SortHeader inline + hover CSS corrigé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/components/work-orders/work-order-list.tsx
@src/components/maintenance/maintenance-plan-list.tsx
</context>

<interfaces>
<!-- Pattern hover identifié dans work-order-list.tsx ligne 93-96 -->
```typescript
// ÉTAT ACTUEL (problème GAP-5) :
// Normal   : 'bg-muted text-muted-foreground'
// Hover    : 'hover:bg-muted/80'           ← trop proche du normal
// Sélect.  : 'bg-primary text-primary-foreground'

// CIBLE :
// Normal   : 'bg-muted text-muted-foreground'
// Hover    : 'hover:bg-muted/60 hover:text-foreground'   ← contraste intermédiaire distinct
// Sélect.  : 'bg-primary text-primary-foreground'
```

<!-- Types WorkOrder et Plan disponibles dans les fichiers existants -->
<!-- WorkOrder : number, title, type, status, priority, dueDate, createdAt, asset -->
<!-- Plan : name, triggerType, frequency, priority, isActive, nextDueAt, tasks.length -->
</interfaces>

<tasks>

<task type="auto">
  <name>Tâche 1 : Ajouter tri par colonnes et corriger hover dans WorkOrderList</name>
  <files>src/components/work-orders/work-order-list.tsx</files>
  <read_first>
    - src/components/work-orders/work-order-list.tsx (fichier complet — lire AVANT toute modification)
  </read_first>
  <action>
**Étape A — Ajouter un helper de tri local SortHeader :**

Définir un composant inline `SortHeader` (non exporté) dans le fichier, AVANT `WorkOrderList` :

```tsx
import { ChevronUp, ChevronDown, ChevronsUpDown } from 'lucide-react'

type SortDir = 'asc' | 'desc'

function SortHeader({
  label,
  sortKey,
  activeKey,
  dir,
  onSort,
}: {
  label: string
  sortKey: string
  activeKey: string
  dir: SortDir
  onSort: (key: string) => void
}) {
  const isActive = sortKey === activeKey
  return (
    <button
      type="button"
      onClick={() => onSort(sortKey)}
      className="flex items-center gap-1 text-xs font-medium text-muted-foreground hover:text-foreground transition-colors group"
    >
      {label}
      <span className="text-muted-foreground/50 group-hover:text-muted-foreground">
        {isActive ? (
          dir === 'asc' ? <ChevronUp className="h-3 w-3" /> : <ChevronDown className="h-3 w-3" />
        ) : (
          <ChevronsUpDown className="h-3 w-3" />
        )}
      </span>
    </button>
  )
}
```

**Étape B — Ajouter states de tri dans WorkOrderList :**

Juste après les deux useState existants (`statusFilter`, `typeFilter`) :
```typescript
const [sortKey, setSortKey] = useState<string>('number')
const [sortDir, setSortDir] = useState<SortDir>('desc')

function handleSort(key: string) {
  if (sortKey === key) {
    setSortDir(d => d === 'asc' ? 'desc' : 'asc')
  } else {
    setSortKey(key)
    setSortDir('asc')
  }
}
```

**Étape C — Modifier le pipeline `filtered` pour trier après le filtrage :**

Remplacer la const `filtered` par :
```typescript
const filtered = useMemo(() => {
  const base = workOrders.filter(wo => {
    if (statusFilter !== 'all' && wo.status !== statusFilter) return false
    if (typeFilter !== 'all' && wo.type !== typeFilter) return false
    return true
  })

  return [...base].sort((a, b) => {
    let cmp = 0
    switch (sortKey) {
      case 'number': cmp = a.number - b.number; break
      case 'title': cmp = a.title.localeCompare(b.title, 'fr'); break
      case 'status': cmp = a.status.localeCompare(b.status); break
      case 'priority': {
        const order = { low: 0, medium: 1, high: 2, urgent: 3 }
        cmp = (order[a.priority] ?? 0) - (order[b.priority] ?? 0)
        break
      }
      case 'dueDate': {
        const da = a.dueDate ? new Date(a.dueDate).getTime() : Infinity
        const db2 = b.dueDate ? new Date(b.dueDate).getTime() : Infinity
        cmp = da - db2
        break
      }
      case 'createdAt': cmp = new Date(a.createdAt).getTime() - new Date(b.createdAt).getTime(); break
      default: cmp = 0
    }
    return sortDir === 'asc' ? cmp : -cmp
  })
}, [workOrders, statusFilter, typeFilter, sortKey, sortDir])
```

Ajouter `useMemo` aux imports de React : `import { useState, useTransition, useMemo } from 'react'`.

**Étape D — Ajouter une barre d'en-têtes au-dessus de la liste de BT :**

Juste avant le `filtered.map(...)`, ajouter une ligne d'en-têtes (visible uniquement si `filtered.length > 0`) :

```tsx
{filtered.length > 0 && (
  <div className="hidden sm:grid grid-cols-[auto_1fr_auto_auto_auto_auto] gap-3 items-center px-4 py-2 border rounded-lg bg-muted/30 text-xs">
    <SortHeader label="#" sortKey="number" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
    <SortHeader label="Titre" sortKey="title" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
    <SortHeader label="Statut" sortKey="status" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
    <SortHeader label="Priorité" sortKey="priority" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
    <SortHeader label="Échéance" sortKey="dueDate" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
    <SortHeader label="Créé" sortKey="createdAt" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
  </div>
)}
```

**Étape E — Corriger l'état hover des boutons de filtre (GAP-5) :**

Dans `STATUS_FILTERS.map(...)`, ligne 96, remplacer :
```typescript
'bg-muted text-muted-foreground hover:bg-muted/80'
```
Par :
```typescript
'bg-muted text-muted-foreground hover:bg-accent hover:text-accent-foreground'
```

`bg-accent` est la couleur intermédiaire du design system Shadcn — distincte de `bg-muted` (normal) et de `bg-primary` (sélectionné). Vérifier que `accent` est défini dans globals.css/tailwind.config. Si absent, utiliser `hover:bg-muted/50 hover:text-foreground` comme fallback.
  </action>
  <verify>
    <automated>cd /home/deploy/gmao-saas && npx tsc --noEmit 2>&1 | grep work-order-list</automated>
    Doit retourner vide.
    `grep -n "SortHeader\|sortKey\|sortDir\|useMemo" /home/deploy/gmao-saas/src/components/work-orders/work-order-list.tsx | wc -l`
    Doit retourner ≥ 8 lignes.
    `grep "hover:bg-muted/80" /home/deploy/gmao-saas/src/components/work-orders/work-order-list.tsx | wc -l`
    Doit retourner 0 (pattern remplacé).
  </verify>
  <acceptance_criteria>
    - `SortHeader` défini comme composant local dans le fichier
    - `useMemo` utilisé pour le pipeline filtrage + tri
    - États : `sortKey` initialisé à `'number'`, `sortDir` à `'desc'`
    - Barre d'en-têtes visible avec 6 colonnes triables
    - Pattern `hover:bg-muted/80` absent du fichier (remplacé par contrast intermédiaire)
    - `npx tsc --noEmit` sans erreur sur ce fichier
  </acceptance_criteria>
  <done>WorkOrderList avec tri par colonnes et hover amélioré.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Ajouter tri par colonnes dans MaintenancePlanList</name>
  <files>src/components/maintenance/maintenance-plan-list.tsx</files>
  <read_first>
    - src/components/maintenance/maintenance-plan-list.tsx (fichier complet — lire AVANT toute modification)
  </read_first>
  <action>
Le composant `MaintenancePlanList` affiche les plans en deux groupes (actifs / inactifs). Le tri s'applique à chacun des groupes indépendamment.

**Étape A — Importer les icônes et useMemo :**
Ajouter dans les imports : `ChevronUp, ChevronDown, ChevronsUpDown` depuis `lucide-react`.
Ajouter `useMemo` aux imports React.

**Étape B — Définir SortHeader (même implémentation que Tâche 1) :**
Copier exactement le composant `SortHeader` et le type `SortDir` définis dans la Tâche 1 — les placer avant `PlanCard` dans maintenance-plan-list.tsx.

**Étape C — Ajouter states dans MaintenancePlanList :**
Dans `MaintenancePlanList`, remplacer le code qui sépare `activePlans` et `inactivePlans` :

```typescript
// Remplacer
const activePlans = plans.filter(p => p.isActive)
const inactivePlans = plans.filter(p => !p.isActive)

// Par
const [sortKey, setSortKey] = useState<string>('name')
const [sortDir, setSortDir] = useState<SortDir>('asc')

function handleSort(key: string) {
  if (sortKey === key) {
    setSortDir(d => d === 'asc' ? 'desc' : 'asc')
  } else {
    setSortKey(key)
    setSortDir('asc')
  }
}

function sortPlans(arr: Plan[]) {
  return [...arr].sort((a, b) => {
    let cmp = 0
    switch (sortKey) {
      case 'name': cmp = a.name.localeCompare(b.name, 'fr'); break
      case 'priority': {
        const order = { low: 0, medium: 1, high: 2, urgent: 3 }
        cmp = (order[a.priority] ?? 0) - (order[b.priority] ?? 0)
        break
      }
      case 'nextDueAt': {
        const da = a.nextDueAt ? new Date(a.nextDueAt).getTime() : Infinity
        const db2 = b.nextDueAt ? new Date(b.nextDueAt).getTime() : Infinity
        cmp = da - db2
        break
      }
      case 'tasks': cmp = a.tasks.length - b.tasks.length; break
      default: cmp = 0
    }
    return sortDir === 'asc' ? cmp : -cmp
  })
}

const activePlans = sortPlans(plans.filter(p => p.isActive))
const inactivePlans = sortPlans(plans.filter(p => !p.isActive))
```

**Étape D — Ajouter la barre d'en-têtes dans le JSX :**
Juste après le compteur `<p className="text-sm text-muted-foreground">...` et avant le premier `{activePlans.length > 0 && ...}`, ajouter :

```tsx
{plans.length > 0 && (
  <div className="hidden sm:flex gap-4 items-center px-2 py-1.5 text-xs border-b">
    <SortHeader label="Nom" sortKey="name" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
    <SortHeader label="Priorité" sortKey="priority" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
    <SortHeader label="Prochaine échéance" sortKey="nextDueAt" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
    <SortHeader label="Tâches" sortKey="tasks" activeKey={sortKey} dir={sortDir} onSort={handleSort} />
  </div>
)}
```

**Note :** La liste affiche des cartes (`PlanCard`) et non une table. Les en-têtes de tri sont donc une barre légère au-dessus des cartes, pas des colonnes alignées pixel-perfect — c'est intentionnel et cohérent avec le design card existant.

Ne pas modifier `PlanCard` ni les actions existantes.
  </action>
  <verify>
    <automated>cd /home/deploy/gmao-saas && npx tsc --noEmit 2>&1 | grep maintenance-plan-list</automated>
    Doit retourner vide.
    `grep -n "SortHeader\|sortKey\|handleSort" /home/deploy/gmao-saas/src/components/maintenance/maintenance-plan-list.tsx | wc -l`
    Doit retourner ≥ 6 lignes.
  </verify>
  <acceptance_criteria>
    - `SortHeader` présent dans maintenance-plan-list.tsx
    - `sortKey` initialisé à `'name'`, `sortDir` à `'asc'`
    - Fonction `sortPlans()` appliquée aux deux groupes (actifs et inactifs)
    - Barre d'en-têtes visible avec 4 colonnes triables (Nom, Priorité, Échéance, Tâches)
    - `npx tsc --noEmit` sans erreur sur ce fichier
  </acceptance_criteria>
  <done>MaintenancePlanList avec tri par colonnes — comportement Maximo minimal livré.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client state → rendu | Tri local uniquement — aucune donnée ne quitte le navigateur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-01 | Denial of Service | useMemo sort | accept | Tri O(n log n) sur données déjà chargées — pas de requête serveur supplémentaire, volume MVP < 500 items |
</threat_model>

<verification>
```bash
# 1. SortHeader présent dans les deux fichiers
grep -l "SortHeader" /home/deploy/gmao-saas/src/components/work-orders/work-order-list.tsx \
                      /home/deploy/gmao-saas/src/components/maintenance/maintenance-plan-list.tsx
# Attendu : 2 fichiers

# 2. Hover amélioré — plus de hover:bg-muted/80
grep "hover:bg-muted/80" /home/deploy/gmao-saas/src/components/work-orders/work-order-list.tsx
# Attendu : vide

# 3. TypeScript clean
cd /home/deploy/gmao-saas && npx tsc --noEmit 2>&1 | grep -E "work-order-list|maintenance-plan-list"
# Attendu : vide
```
</verification>

<success_criteria>
- En-têtes cliquables dans les deux listes avec indicateur tri ASC/DESC (GAP-4)
- Tri par défaut : BT par numéro décroissant, plans par nom croissant
- Hover des boutons de filtre visuellement distinct (GAP-5) — pattern `hover:bg-muted/80` remplacé
- Zero erreur TypeScript
</success_criteria>

<output>
Après complétion, créer `.planning/phases/08-productivit-technicien/08-06-SUMMARY.md`
</output>
