---
phase: 09-maintenance-conditionnelle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/meter-utils.ts
  - src/lib/meter-utils.test.ts
  - src/actions/work-orders.ts
  - src/components/assets/asset-meter-section.tsx
  - src/app/(app)/actifs/page.tsx
autonomous: true
requirements: [COND-01]
must_haves:
  truths:
    - "Un technicien peut saisir un relevé de compteur directement sur un actif depuis la page /actifs (hors contexte BT)"
    - "La saisie d'un relevé met à jour AssetMeter.value avec la nouvelle valeur"
    - "La saisie d'un relevé recalcule nextMeterValue pour tous les plans meter_based liés à l'actif"
    - "Une valeur négative ou non finie est rejetée avec une erreur"
    - "Un actif d'une autre organisation ne peut pas être modifié"
  artifacts:
    - path: "src/lib/meter-utils.ts"
      provides: "Helper updateMeterAndPlans partagé (compteur + recalcul nextMeterValue)"
      exports: ["updateMeterAndPlans"]
    - path: "src/lib/meter-utils.test.ts"
      provides: "Tests Vitest du helper (update value + recalcul plans)"
      contains: "updateMeterAndPlans"
    - path: "src/actions/work-orders.ts"
      provides: "Server Action recordMeterReadingOnAsset"
      exports: ["recordMeterReadingOnAsset"]
    - path: "src/components/assets/asset-meter-section.tsx"
      provides: "UI de saisie compteur sur la page actif"
      contains: "recordMeterReadingOnAsset"
    - path: "src/app/(app)/actifs/page.tsx"
      provides: "Query actifs incluant meters + rendu AssetMeterSection"
      contains: "meters"
  key_links:
    - from: "src/components/assets/asset-meter-section.tsx"
      to: "recordMeterReadingOnAsset"
      via: "appel Server Action au submit du formulaire"
      pattern: "recordMeterReadingOnAsset\\("
    - from: "src/actions/work-orders.ts"
      to: "src/lib/meter-utils.ts"
      via: "import updateMeterAndPlans"
      pattern: "updateMeterAndPlans"
    - from: "src/app/(app)/actifs/page.tsx"
      to: "asset.meters"
      via: "include meters dans query Prisma"
      pattern: "meters:\\s*true"
---

<objective>
Compléter COND-01 : permettre la saisie directe d'un relevé de compteur sur un actif, en dehors du contexte d'un bon de travail.

La Phase 8 a déjà livré la saisie de compteur à la clôture d'un BT (`WorkOrderMeterReading` + `recordMeterReading`). Ce plan ajoute le chaînon manquant : un technicien ouvre la page `/actifs`, voit la valeur courante du compteur d'un actif, et peut saisir un nouveau relevé directement.

Purpose: Fermer la moitié manquante de COND-01 (entrée directe sur actif). Sans cela, le technicien doit ouvrir un BT pour mettre à jour un compteur.
Output:
- `src/lib/meter-utils.ts` — helper `updateMeterAndPlans` (extrait de la logique dupliquée)
- `src/lib/meter-utils.test.ts` — tests Vitest du helper
- `recordMeterReadingOnAsset` Server Action dans `work-orders.ts`
- `AssetMeterSection` composant client
- Query `/actifs/page.tsx` incluant `meters` + rendu du composant
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-maintenance-conditionnelle/09-RESEARCH.md

<interfaces>
<!-- Extrait vérifié de RESEARCH.md (HIGH confidence, codebase grep ligne par ligne). -->
<!-- L'executor DOIT lire les fichiers réels (voir read_first) pour confirmer les signatures avant édition. -->

Schéma Prisma (prisma/schema.prisma, vérifié):
```
model AssetMeter {           // ligne 255
  id        String   @id
  assetId   String
  name      String
  unit      String           // "h", "km", "cycles" — À AFFICHER, ne pas coder en dur
  value     Float
  updatedAt DateTime @updatedAt
}

model MaintenancePlan {      // champs vérifiés
  meterThreshold  Float?     // ligne 414 — intervalle (ex: 500)
  nextMeterValue  Float?     // ligne 415 — valeur cible calculée
  triggerType     MaintenanceTriggerType  // enum: time_based | meter_based (ligne 56)
  assetId         String?
  organizationId  String
}
```

Action existante (src/actions/work-orders.ts:464, à RÉPLIQUER en pattern):
```typescript
export async function recordMeterReading(workOrderId: string, reading: number)
// Met à jour: WorkOrder.meterReading, AssetMeter.value, recalcule nextMeterValue
// pour tous les plans meter_based de l'actif. Validation: Number.isFinite(reading) && reading >= 0.
// Auth: getOrgAndMembership() en début.
```

Helper org (utilisé partout dans work-orders.ts):
```typescript
const { organizationId } = await getOrgAndMembership()
```

Composant modèle à RÉPLIQUER (src/components/work-orders/work-order-meter-reading.tsx):
```
'use client'
// Structure: Input numérique + Button + toast (sonner). Affiche valeur courante + unité.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper updateMeterAndPlans + tests Vitest (RED→GREEN)</name>
  <read_first>
    - src/actions/work-orders.ts (lire recordMeterReading aux alentours de la ligne 464 — c'est la logique source à extraire ; confirmer signature exacte et nom du client Prisma importé, ex `db` depuis @/lib/db)
    - prisma/schema.prisma (confirmer champs AssetMeter.value, MaintenancePlan.meterThreshold/nextMeterValue/triggerType/assetId/organizationId)
    - src/lib/db.ts (confirmer le nom exporté du client Prisma : `db` ou `prisma`)
    - vitest.config.mts (confirmer config jsdom + alias @/)
    - un test Vitest existant de Phase 8, ex src/app/api/cron/*/route.test.ts si présent (répliquer le pattern de mock Prisma)
  </read_first>
  <behavior>
    - Test 1: updateMeterAndPlans(assetId, orgId, reading) appelle db.assetMeter.update avec { value: reading } pour le premier compteur de l'actif.
    - Test 2: updateMeterAndPlans recalcule nextMeterValue = reading + meterThreshold pour chaque plan meter_based ayant un meterThreshold non nul.
    - Test 3: si l'actif n'a aucun compteur (db.assetMeter.findFirst retourne null), retourne null sans throw et n'appelle pas update.
    - Test 4: un plan meter_based avec meterThreshold null est ignoré (pas d'update de nextMeterValue).
  </behavior>
  <action>
    Créer `src/lib/meter-utils.ts` exportant exactement:
    ```typescript
    import { db } from '@/lib/db'  // ADAPTER au nom réel confirmé dans src/lib/db.ts

    export async function updateMeterAndPlans(
      assetId: string,
      organizationId: string,
      reading: number,
    ) {
      const meter = await db.assetMeter.findFirst({ where: { assetId } })
      if (!meter) return null

      await db.assetMeter.update({ where: { id: meter.id }, data: { value: reading } })

      const plans = await db.maintenancePlan.findMany({
        where: { assetId, organizationId, triggerType: 'meter_based' },
        select: { id: true, meterThreshold: true },
      })
      for (const plan of plans) {
        if (plan.meterThreshold != null) {
          await db.maintenancePlan.update({
            where: { id: plan.id },
            data: { nextMeterValue: reading + plan.meterThreshold },
          })
        }
      }
      return meter
    }
    ```
    Créer `src/lib/meter-utils.test.ts` avec Vitest : mocker `@/lib/db` (vi.mock) en exposant `db.assetMeter.findFirst`, `db.assetMeter.update`, `db.maintenancePlan.findMany`, `db.maintenancePlan.update` comme vi.fn(). Écrire les 4 tests décrits dans <behavior>. Les tests DOIVENT échouer avant la création du helper (RED), puis passer (GREEN).
    NE PAS dupliquer la logique de recalcul dans recordMeterReading sans la centraliser ici — la Task 2 réutilisera ce helper.
  </action>
  <verify>
    <automated>npx vitest run src/lib/meter-utils.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function updateMeterAndPlans" src/lib/meter-utils.ts` retourne 0
    - `grep -q "triggerType: 'meter_based'" src/lib/meter-utils.ts` retourne 0
    - `grep -q "nextMeterValue: reading + plan.meterThreshold" src/lib/meter-utils.ts` retourne 0
    - `npx vitest run src/lib/meter-utils.test.ts` : 4 tests passent, 0 échec
    - Le fichier de test contient au moins 4 blocs `it(` ou `test(`
  </acceptance_criteria>
  <done>Le helper updateMeterAndPlans existe, est testé (4 tests verts), et centralise la logique compteur + recalcul nextMeterValue.</done>
</task>

<task type="auto">
  <name>Task 2: Server Action recordMeterReadingOnAsset</name>
  <read_first>
    - src/actions/work-orders.ts (lire l'en-tête du fichier pour 'use server', les imports — getOrgAndMembership, revalidatePath, db — et recordMeterReading comme modèle exact d'auth + validation)
    - src/lib/meter-utils.ts (le helper créé en Task 1 — importer updateMeterAndPlans)
  </read_first>
  <action>
    Dans `src/actions/work-orders.ts`, ajouter l'export suivant (réutiliser le helper de Task 1) :
    ```typescript
    import { updateMeterAndPlans } from '@/lib/meter-utils'

    export async function recordMeterReadingOnAsset(assetId: string, reading: number) {
      const { organizationId } = await getOrgAndMembership()

      if (!Number.isFinite(reading) || reading < 0) {
        throw new Error('Valeur de compteur invalide')
      }

      // Scoping org — bloque le tampering cross-org (V4 Access Control)
      const asset = await db.asset.findFirst({
        where: { id: assetId, organizationId },
        select: { id: true },
      })
      if (!asset) throw new Error('Actif introuvable')

      const meter = await updateMeterAndPlans(assetId, organizationId, reading)
      if (!meter) throw new Error('Aucun compteur configuré pour cet actif')

      revalidatePath('/actifs')
      revalidatePath('/maintenance')
    }
    ```
    Confirmer le nom du client Prisma (`db` vs `prisma`) et de `getOrgAndMembership` à partir des imports existants du fichier — réutiliser exactement les mêmes. NE PAS appeler `auth()` directement (passer par le helper org existant).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "work-orders.ts" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function recordMeterReadingOnAsset" src/actions/work-orders.ts` retourne 0
    - `grep -q "Number.isFinite(reading)" src/actions/work-orders.ts` retourne 0 (validation présente)
    - `grep -q "findFirst({ where: { id: assetId, organizationId }" src/actions/work-orders.ts` ou équivalent grep `organizationId` dans le scoping retourne 0 (scoping org présent)
    - `grep -q "updateMeterAndPlans" src/actions/work-orders.ts` retourne 0 (réutilisation du helper, pas de duplication)
    - `npx tsc --noEmit` ne signale aucune erreur dans work-orders.ts
  </acceptance_criteria>
  <done>recordMeterReadingOnAsset existe, valide l'entrée, scope par org, réutilise updateMeterAndPlans, et compile sans erreur.</done>
</task>

<task type="auto">
  <name>Task 3: Composant AssetMeterSection + intégration page /actifs</name>
  <read_first>
    - src/components/work-orders/work-order-meter-reading.tsx (modèle EXACT à répliquer : Input + Button + toast sonner, affichage valeur courante + unité)
    - src/app/(app)/actifs/page.tsx (le fichier à modifier — repérer la query db.asset.findMany existante et où la liste/le détail des actifs est rendu)
    - src/actions/work-orders.ts (confirmer la signature de recordMeterReadingOnAsset créée en Task 2)
    - node_modules/next/dist/docs/ (lire le guide pertinent Server/Client Components avant d'écrire le composant — cette version de Next.js a des breaking changes)
  </read_first>
  <action>
    1. Créer `src/components/assets/asset-meter-section.tsx` (`'use client'`). Props :
    ```typescript
    type Meter = { id: string; name: string; unit: string; value: number }
    interface AssetMeterSectionProps {
      assetId: string
      meters: Meter[]
      canEdit?: boolean
    }
    ```
    Comportement : si `meters.length === 0`, ne rien afficher (ou afficher "Aucun compteur"). Sinon afficher `meters[0]` : la valeur courante avec son unité (`{meter.value} {meter.unit}` — NE PAS coder "h" en dur), un Input numérique (defaultValue = meter.value) et un Button "Enregistrer le relevé". Au submit : appeler `recordMeterReadingOnAsset(assetId, Number(value))` dans un try/catch, afficher `toast.success('Relevé enregistré')` ou `toast.error(err.message)`. Utiliser le même import de toast (sonner) que work-order-meter-reading.tsx.

    2. Modifier `src/app/(app)/actifs/page.tsx` :
       - Ajouter `meters: true` au `include` de la query `db.asset.findMany` (voir RESEARCH.md ligne 304 pour la query exacte).
       - Rendre `<AssetMeterSection assetId={asset.id} meters={asset.meters} />` à l'endroit où chaque actif est affiché (liste ou modal/drawer de détail — Claude's Discretion : intégrer dans le pattern existant le plus simple, modal/drawer cohérent avec Phase 8).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "asset-meter-section|actifs/page" | grep -c . | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'use client'" src/components/assets/asset-meter-section.tsx` retourne 0
    - `grep -q "recordMeterReadingOnAsset" src/components/assets/asset-meter-section.tsx` retourne 0
    - `grep -q "meter.unit\|meters\[0\].unit\|{.*unit}" src/components/assets/asset-meter-section.tsx` retourne 0 (unité affichée, non codée en dur)
    - `grep -qE "meters:\s*true" "src/app/(app)/actifs/page.tsx"` retourne 0 (include meters)
    - `grep -q "AssetMeterSection" "src/app/(app)/actifs/page.tsx"` retourne 0 (composant rendu)
    - `npx tsc --noEmit` ne signale aucune erreur dans asset-meter-section.tsx ni actifs/page.tsx
    - `grep -c "\"h\"\|'h'" src/components/assets/asset-meter-section.tsx` ne contient pas d'unité codée en dur "h" comme libellé d'affichage
  </acceptance_criteria>
  <done>La page /actifs charge les compteurs et affiche AssetMeterSection ; le technicien peut saisir un relevé qui appelle recordMeterReadingOnAsset ; l'unité est dynamique.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur (technicien) → Server Action | Entrée utilisateur non fiable (assetId, reading) traverse ici |
| Server Action → DB (Prisma) | Toute écriture doit être scopée à l'organisation de l'utilisateur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | recordMeterReadingOnAsset (assetId) | mitigate | `db.asset.findFirst({ where: { id: assetId, organizationId } })` avant toute écriture — throw si introuvable (Task 2) |
| T-09-02 | Tampering | recordMeterReadingOnAsset (reading) | mitigate | `Number.isFinite(reading) && reading >= 0` — throw 'Valeur invalide' (Task 2) |
| T-09-03 | Elevation of Privilege | Server Action sans auth | mitigate | `getOrgAndMembership()` en première ligne (V2 Auth, pattern existant) (Task 2) |
| T-09-04 | Information Disclosure | message d'erreur 'Actif introuvable' | accept | Message générique, ne révèle pas l'existence d'un actif d'une autre org |
</threat_model>

<verification>
- `npx vitest run src/lib/meter-utils.test.ts` : 4 tests verts
- `npx tsc --noEmit` : aucune erreur dans les fichiers modifiés
- Saisie manuelle d'un relevé sur /actifs met à jour AssetMeter.value et recalcule nextMeterValue (vérifiable via Prisma Studio ou test d'intégration)
</verification>

<success_criteria>
- COND-01 complété : un technicien peut saisir un relevé de compteur directement sur un actif depuis /actifs
- AssetMeter.value est mis à jour et nextMeterValue recalculé pour les plans meter_based liés
- Validation (>= 0, finite) et scoping org en place
- Helper updateMeterAndPlans centralisé et testé — prêt à être réutilisé par 09-02
</success_criteria>

<output>
After completion, create `.planning/phases/09-maintenance-conditionnelle/09-01-SUMMARY.md`
</output>
