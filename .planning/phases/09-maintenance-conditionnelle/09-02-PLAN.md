---
phase: 09-maintenance-conditionnelle
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/actions/maintenance.ts
  - src/app/api/cron/meter-threshold-check/route.ts
  - src/app/api/cron/meter-threshold-check/route.test.ts
  - vercel.json
autonomous: true
requirements: [COND-02]
must_haves:
  truths:
    - "Un cron horaire vérifie chaque plan meter_based actif et compare AssetMeter.value à nextMeterValue"
    - "Quand AssetMeter.value >= nextMeterValue, un bon de travail est généré automatiquement"
    - "Aucun BT en double n'est créé si un BT open/in_progress/on_hold lié au plan existe déjà (idempotence)"
    - "Après génération, nextMeterValue est avancé à meter.value + meterThreshold"
    - "Le cron rejette toute requête sans Bearer CRON_SECRET valide"
    - "Les plans meter_based sans assetId ou sans nextMeterValue sont ignorés"
  artifacts:
    - path: "src/actions/maintenance.ts"
      provides: "Helper generateWorkOrderFromPlanInternal(planId, organizationId) sans dépendance auth()"
      exports: ["generateWorkOrderFromPlanInternal"]
    - path: "src/app/api/cron/meter-threshold-check/route.ts"
      provides: "Route Handler GET cron de détection de seuil compteur"
      exports: ["GET"]
    - path: "src/app/api/cron/meter-threshold-check/route.test.ts"
      provides: "Tests Vitest : détection, idempotence, avancement seuil"
      contains: "meter-threshold-check"
    - path: "vercel.json"
      provides: "Entrée cron meter-threshold-check à 0 * * * *"
      contains: "meter-threshold-check"
  key_links:
    - from: "src/app/api/cron/meter-threshold-check/route.ts"
      to: "generateWorkOrderFromPlanInternal"
      via: "appel direct du helper (contexte cron, pas d'auth)"
      pattern: "generateWorkOrderFromPlanInternal\\("
    - from: "src/app/api/cron/meter-threshold-check/route.ts"
      to: "db.workOrder.findFirst (guard idempotence)"
      via: "vérifie BT actif avant création"
      pattern: "status:\\s*\\{\\s*in:\\s*\\[.*open.*in_progress.*on_hold"
    - from: "vercel.json"
      to: "/api/cron/meter-threshold-check"
      via: "entrée crons schedule"
      pattern: "/api/cron/meter-threshold-check"
---

<objective>
Compléter COND-02 : générer automatiquement un bon de travail quand un actif atteint le seuil de compteur configuré sur un PM meter_based.

`nextMeterValue` est déjà calculé (Phase 8 + plan 09-01). Le chaînon manquant est un cron Vercel horaire qui compare `AssetMeter.value >= nextMeterValue` et déclenche `generateWorkOrderFromPlan`. Comme la version actuelle de `generateWorkOrderFromPlan` appelle `auth()` (incompatible avec un contexte cron), on extrait d'abord la logique pure dans `generateWorkOrderFromPlanInternal(planId, organizationId)`.

Purpose: Fermer la boucle de la maintenance conditionnelle — zéro intervention manuelle.
Output:
- Refactor : `generateWorkOrderFromPlanInternal(planId, organizationId)` dans `maintenance.ts`
- Cron `src/app/api/cron/meter-threshold-check/route.ts` avec guard d'idempotence
- Tests Vitest (détection + idempotence + avancement seuil)
- Entrée `vercel.json`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-maintenance-conditionnelle/09-RESEARCH.md
@.planning/phases/09-maintenance-conditionnelle/09-01-PLAN.md

<interfaces>
<!-- Extrait vérifié de RESEARCH.md (HIGH confidence). L'executor DOIT lire les fichiers réels (read_first). -->

Action existante à REFACTORER (src/actions/maintenance.ts:136):
```typescript
export async function generateWorkOrderFromPlan(planId: string)
// Appelle auth() via getOrgAndMembership() — INCOMPATIBLE contexte cron.
// Logique de création de BT depuis un plan (copie étapes/pièces/durée du job plan).
// Met à jour lastGeneratedAt sur le plan.
```

Pattern cron de référence (src/app/api/cron/urgent-escalation/route.ts):
```typescript
export async function GET(request: NextRequest) {
  const authHeader = request.headers.get('authorization')
  if (!process.env.CRON_SECRET || authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: 'Non autorisé' }, { status: 401 })
  }
  // ... boucle métier ...
  return NextResponse.json({ ok: true, ... })
}
```

vercel.json actuel (VÉRIFIÉ, 2 crons existants):
```json
{
  "crons": [
    { "path": "/api/cron/maintenance-reminder", "schedule": "0 * * * *" },
    { "path": "/api/cron/urgent-escalation", "schedule": "0 * * * *" }
  ]
}
```

Schéma vérifié:
```
MaintenancePlan: { id, isActive, triggerType, nextMeterValue (Float?), meterThreshold (Float?), assetId (String?), organizationId, lastGeneratedAt }
WorkOrder: { maintenancePlanId (String?), status }  // status valeurs incluent: open | in_progress | on_hold
Asset.meters: AssetMeter[]  // utiliser meters[0]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor generateWorkOrderFromPlanInternal (sans auth)</name>
  <read_first>
    - src/actions/maintenance.ts (lire generateWorkOrderFromPlan aux alentours de la ligne 136 — identifier OÙ auth()/getOrgAndMembership() est appelé et OÙ commence la logique pure de création de BT)
    - src/actions/work-orders.ts (confirmer le nom du client Prisma `db` et le pattern recordMeterReading qui appelle Prisma sans auth — modèle d'une fonction sans contexte session)
  </read_first>
  <action>
    Refactorer `src/actions/maintenance.ts` pour séparer auth et logique métier :

    1. Créer un export `generateWorkOrderFromPlanInternal(planId: string, organizationId: string)` qui contient TOUTE la logique actuelle de création de BT de `generateWorkOrderFromPlan` MAIS sans aucun appel `auth()` / `getOrgAndMembership()`. Elle prend `organizationId` en paramètre. Charger le plan avec `db.maintenancePlan.findFirst({ where: { id: planId, organizationId } })` (scoping org explicite par paramètre). Conserver la copie des étapes/pièces/durée et la mise à jour de `lastGeneratedAt`. Retourner le BT créé.

    2. Réécrire `generateWorkOrderFromPlan(planId: string)` (la Server Action publique existante) pour qu'elle :
       - appelle `const { organizationId } = await getOrgAndMembership()`
       - délègue à `return generateWorkOrderFromPlanInternal(planId, organizationId)`
       - conserve les `revalidatePath` existants.

    NE PAS dupliquer la logique de création : la Server Action doit devenir un simple wrapper auth autour du helper interne. Confirmer la signature de retour réelle avant édition.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "maintenance.ts" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function generateWorkOrderFromPlanInternal" src/actions/maintenance.ts` retourne 0
    - `grep -q "generateWorkOrderFromPlanInternal(planId" src/actions/maintenance.ts` retourne 0 (la Server Action délègue au helper)
    - Le corps de generateWorkOrderFromPlanInternal ne contient PAS d'appel `auth(` ni `getOrgAndMembership(` : `sed -n '/generateWorkOrderFromPlanInternal/,/^}/p' src/actions/maintenance.ts | grep -c "auth(\|getOrgAndMembership(" ` retourne 0
    - `grep -q "where: { id: planId, organizationId }" src/actions/maintenance.ts` ou équivalent scoping org par paramètre retourne 0
    - `npx tsc --noEmit` ne signale aucune erreur dans maintenance.ts
  </acceptance_criteria>
  <done>generateWorkOrderFromPlanInternal(planId, organizationId) existe sans dépendance auth ; la Server Action publique en est un wrapper ; aucune logique dupliquée ; compile sans erreur.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cron meter-threshold-check + tests (détection + idempotence + avancement)</name>
  <read_first>
    - src/app/api/cron/urgent-escalation/route.ts (modèle EXACT : auth Bearer CRON_SECRET, structure GET, réponse JSON)
    - src/actions/maintenance.ts (confirmer la signature de generateWorkOrderFromPlanInternal créée en Task 1)
    - src/lib/db.ts (confirmer le nom exporté du client Prisma)
    - un test cron existant de Phase 8 si présent (src/app/api/cron/urgent-escalation/route.test.ts) pour répliquer le pattern de mock NextRequest + Prisma
    - node_modules/next/dist/docs/ (lire le guide Route Handlers — cette version de Next.js a des breaking changes ; confirmer signature GET(request: NextRequest) et NextResponse)
  </read_first>
  <behavior>
    - Test 1 (auth): GET sans header Authorization (ou Bearer incorrect) retourne 401.
    - Test 2 (détection): un plan meter_based actif avec meter.value (1500) >= nextMeterValue (1000) et aucun BT actif → generateWorkOrderFromPlanInternal est appelé une fois.
    - Test 3 (idempotence): même plan MAIS un BT status 'in_progress' lié existe déjà → generateWorkOrderFromPlanInternal n'est PAS appelé (triggered = 0).
    - Test 4 (sous le seuil): meter.value (500) < nextMeterValue (1000) → pas de génération.
    - Test 5 (avancement): après génération, db.maintenancePlan.update est appelé avec nextMeterValue = meter.value + meterThreshold.
    - Test 6 (filtre): plan sans assetId ou nextMeterValue null est ignoré (filtré par la query).
  </behavior>
  <action>
    1. Créer `src/app/api/cron/meter-threshold-check/route.ts` :
    ```typescript
    import { NextRequest, NextResponse } from 'next/server'
    import { db } from '@/lib/db'  // ADAPTER au nom réel
    import { generateWorkOrderFromPlanInternal } from '@/actions/maintenance'

    export async function GET(request: NextRequest) {
      const authHeader = request.headers.get('authorization')
      if (!process.env.CRON_SECRET || authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
        return NextResponse.json({ error: 'Non autorisé' }, { status: 401 })
      }

      const plans = await db.maintenancePlan.findMany({
        where: {
          isActive: true,
          triggerType: 'meter_based',
          nextMeterValue: { not: null },
          assetId: { not: null },
        },
        include: { asset: { include: { meters: { take: 1 } } } },
      })

      let triggered = 0
      const errors: string[] = []

      for (const plan of plans) {
        const meter = plan.asset?.meters[0]
        if (!meter || plan.nextMeterValue == null) continue
        if (meter.value < plan.nextMeterValue) continue

        // Guard idempotence — CRITIQUE (sinon 1 BT/heure en double)
        const existing = await db.workOrder.findFirst({
          where: {
            maintenancePlanId: plan.id,
            status: { in: ['open', 'in_progress', 'on_hold'] },
          },
          select: { id: true },
        })
        if (existing) continue

        try {
          await generateWorkOrderFromPlanInternal(plan.id, plan.organizationId)
          if (plan.meterThreshold != null) {
            await db.maintenancePlan.update({
              where: { id: plan.id },
              data: { nextMeterValue: meter.value + plan.meterThreshold },
            })
          }
          triggered++
        } catch (err) {
          errors.push(`Plan ${plan.id}: ${err instanceof Error ? err.message : String(err)}`)
        }
      }

      return NextResponse.json({ ok: true, plansChecked: plans.length, triggered, errors })
    }
    ```
    Confirmer les valeurs exactes de l'enum status ('open' / 'in_progress' / 'on_hold') dans schema.prisma avant édition.

    2. Créer `src/app/api/cron/meter-threshold-check/route.test.ts` : mocker `@/lib/db`, mocker `@/actions/maintenance` (vi.mock pour generateWorkOrderFromPlanInternal en vi.fn()), construire un NextRequest avec/sans header Authorization, setter `process.env.CRON_SECRET`. Écrire les 6 tests décrits dans <behavior>. RED avant route.ts, GREEN après.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/cron/meter-threshold-check/route.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function GET" src/app/api/cron/meter-threshold-check/route.ts` retourne 0
    - `grep -q "Bearer \${process.env.CRON_SECRET}" src/app/api/cron/meter-threshold-check/route.ts` retourne 0 (auth cron)
    - `grep -qE "status:\s*\{\s*in:\s*\[" src/app/api/cron/meter-threshold-check/route.ts` ET la ligne contient open/in_progress/on_hold (guard idempotence présent)
    - `grep -q "generateWorkOrderFromPlanInternal" src/app/api/cron/meter-threshold-check/route.ts` retourne 0
    - `grep -q "nextMeterValue: meter.value + plan.meterThreshold" src/app/api/cron/meter-threshold-check/route.ts` retourne 0 (avancement seuil)
    - `grep -qE "assetId:\s*\{\s*not:\s*null" src/app/api/cron/meter-threshold-check/route.ts` retourne 0 (filtre plans sans actif)
    - `npx vitest run src/app/api/cron/meter-threshold-check/route.test.ts` : 6 tests passent, 0 échec
  </acceptance_criteria>
  <done>Le cron détecte les dépassements de seuil, génère un BT via le helper interne, empêche les doublons par guard d'idempotence, avance nextMeterValue, et rejette les requêtes non authentifiées. 6 tests verts.</done>
</task>

<task type="auto">
  <name>Task 3: Enregistrer le cron dans vercel.json</name>
  <read_first>
    - vercel.json (lire le tableau crons existant — 2 entrées à 0 * * * * ; conserver l'ordre et le format exact)
  </read_first>
  <action>
    Dans `vercel.json`, ajouter une troisième entrée au tableau `crons`, après les deux existantes :
    ```json
    { "path": "/api/cron/meter-threshold-check", "schedule": "0 * * * *" }
    ```
    Résultat attendu :
    ```json
    {
      "crons": [
        { "path": "/api/cron/maintenance-reminder", "schedule": "0 * * * *" },
        { "path": "/api/cron/urgent-escalation", "schedule": "0 * * * *" },
        { "path": "/api/cron/meter-threshold-check", "schedule": "0 * * * *" }
      ]
    }
    ```
    NE PAS modifier les deux crons existants. Conserver un JSON valide.
  </action>
  <verify>
    <automated>node -e "const c=require('./vercel.json'); const ok=c.crons.some(x=>x.path==='/api/cron/meter-threshold-check' && x.schedule==='0 * * * *'); if(!ok) process.exit(1); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/api/cron/meter-threshold-check" vercel.json` retourne 0
    - `node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"` ne lève pas d'erreur (JSON valide)
    - Le tableau crons contient exactement 3 entrées : `node -e "console.log(require('./vercel.json').crons.length)"` affiche 3
    - Les 2 crons existants (maintenance-reminder, urgent-escalation) sont toujours présents
  </acceptance_criteria>
  <done>vercel.json contient 3 crons valides incluant meter-threshold-check à 0 * * * * ; les crons existants sont préservés.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Route Handler cron | Toute requête externe peut tenter de déclencher le cron |
| cron → DB (Prisma) | Génération de BT en masse possible si non bornée |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation of Privilege | GET /api/cron/meter-threshold-check | mitigate | `Bearer ${process.env.CRON_SECRET}` — 401 si absent/incorrect (Task 2, pattern identique aux crons existants) |
| T-09-06 | Denial of Service | génération de BT en boucle horaire | mitigate | Guard d'idempotence : skip si BT open/in_progress/on_hold lié au plan existe déjà (Task 2) — empêche 1 BT/heure |
| T-09-07 | Tampering | plan meter_based sans assetId | mitigate | Filtre `assetId: { not: null }` dans la query — plans sans actif ignorés (Task 2) |
| T-09-08 | Information Disclosure | tableau errors dans la réponse JSON | accept | Endpoint authentifié par CRON_SECRET ; messages d'erreur internes acceptables car non exposés publiquement |
</threat_model>

<verification>
- `npx vitest run src/app/api/cron/meter-threshold-check/route.test.ts` : 6 tests verts (auth, détection, idempotence, sous-seuil, avancement, filtre)
- `npx tsc --noEmit` : aucune erreur dans maintenance.ts ni route.ts
- `node -e "require('./vercel.json').crons.length"` : 3
- Test d'idempotence explicitement vérifié : un second passage du cron sur le même plan avec BT actif ne crée pas de doublon
</verification>

<success_criteria>
- COND-02 complété : un PM meter_based dont l'actif atteint le seuil génère automatiquement un BT via cron horaire
- Aucun doublon (guard d'idempotence vérifié par test)
- nextMeterValue avancé après chaque génération
- Cron authentifié par CRON_SECRET et enregistré dans vercel.json
- generateWorkOrderFromPlanInternal réutilisable hors contexte auth
</success_criteria>

<output>
After completion, create `.planning/phases/09-maintenance-conditionnelle/09-02-SUMMARY.md`
</output>
