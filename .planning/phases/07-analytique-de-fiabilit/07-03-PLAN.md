---
phase: 07-analytique-de-fiabilit
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/components/rapports/mttr-tab.tsx
  - src/components/rapports/cost-tab.tsx
  - src/app/(app)/rapports/page.tsx
autonomous: true
requirements:
  - FIAB-02
  - FIAB-03
must_haves:
  truths:
    - "Le tab MTTR affiche le MTTR (heures) par actif pour les BTs correctifs clôturés sur la période, trié décroissant"
    - "Le MTTR est calculé comme sum(timeLogs.minutes) / count(BT) / 60 par actif"
    - "Le tab Coût actifs affiche pour chaque actif : coût main-d'œuvre + coût pièces sur la période, trié décroissant"
    - "Le coût MO = sum(timeLogs.minutes / 60 × hourlyRate du technicien) — null hourlyRate compte 0 et l'actif est marqué"
    - "Le coût pièces = sum(unitCost × quantity) pour les pièces consommées sur des BTs clôturés"
    - "Si aucune donnée disponible, chaque tab affiche un message d'état vide actionnable"
  artifacts:
    - path: "src/components/rapports/mttr-tab.tsx"
      provides: "Server Component FIAB-02"
      exports: ["MttrTab"]
    - path: "src/components/rapports/cost-tab.tsx"
      provides: "Server Component FIAB-03"
      exports: ["CostTab"]
  key_links:
    - from: "src/components/rapports/mttr-tab.tsx"
      to: "db.workOrder.findMany type=corrective + timeLogs"
      via: "Prisma findMany avec select timeLogs.minutes"
      pattern: "type: 'corrective'"
    - from: "src/components/rapports/cost-tab.tsx"
      to: "db.workOrderPart + db.workOrderTimeLog"
      via: "Deux requêtes Prisma + agrégation JS par assetId"
      pattern: "membership.*hourlyRate"
    - from: "src/app/(app)/rapports/page.tsx"
      to: "MttrTab et CostTab"
      via: "Remplacer placeholders Plan 02 par composants réels"
      pattern: "MttrTab orgId={orgId} period={period}"
---

<objective>
Phase 7 — Plan 03 : Implémenter les rapports **MTTR par actif** (FIAB-02) et **Coût de maintenance par actif** (FIAB-03), puis câbler ces deux Server Components dans la page `/rapports` à la place des placeholders posés par Plan 02.

Purpose: FIAB-02 répond à "Quel actif tombe en panne le plus souvent et combien de temps pour le réparer ?". FIAB-03 répond à "Quel actif coûte le plus cher en maintenance ?". Les deux sont essentiels pour les décisions d'achat/remplacement.

Output: Tabs MTTR et Coût actifs fonctionnels avec données réelles, états vides actionnables, table triée par valeur décroissante.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-analytique-de-fiabilit/07-CONTEXT.md
@.planning/phases/07-analytique-de-fiabilit/07-RESEARCH.md
@.planning/phases/07-analytique-de-fiabilit/07-01-SUMMARY.md
@.planning/phases/07-analytique-de-fiabilit/07-02-SUMMARY.md
@src/lib/report-utils.ts
@src/components/rapports/top-faults-tab.tsx
@src/app/(app)/rapports/page.tsx
@prisma/schema.prisma

<interfaces>
<!-- Contrats Wave 1 et Wave 2 consommés ici -->

Depuis src/lib/report-utils.ts (Plan 01):
```typescript
export function getPeriodStart(period: Period): Date
export function formatCurrency(amount: number | null | undefined): string  // ex: '1 234,56 $' fr-CA
export const PERIOD_LABELS: Record<Period, string>
export type Period
```

Depuis prisma/schema.prisma (post Plan 01):
```prisma
model WorkOrder {
  id              String
  organizationId  String
  assetId         String?
  type            WorkOrderType    // 'corrective' | 'preventive' | 'inspection' | 'service_request'
  completedAt     DateTime?
  asset           Asset?
  timeLogs        WorkOrderTimeLog[]
  parts           WorkOrderPart[]
}
model WorkOrderTimeLog {
  workOrderId   String
  membershipId  String
  minutes       Int?
  membership    Membership
}
model WorkOrderPart {
  workOrderId  String
  quantity     Int        // [VERIFIED Phase 6]
  unitCost     Float?     // [VERIFIED Phase 6]
}
model Membership {
  id          String
  hourlyRate  Float?
}
model Asset { id: String, name: String }
```

Signature unifiée des tabs (établie par Plan 02):
```typescript
type Props = { orgId: string; period: Period }
export async function MttrTab(props: Props): Promise<JSX.Element>
export async function CostTab(props: Props): Promise<JSX.Element>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer MttrTab (FIAB-02)</name>
  <files>src/components/rapports/mttr-tab.tsx</files>
  <read_first>
    - src/lib/report-utils.ts (signatures getPeriodStart, PERIOD_LABELS, type Period)
    - src/components/rapports/top-faults-tab.tsx (pattern Server Component + état vide + Card)
    - prisma/schema.prisma (confirmer enum WorkOrderType corrective et relation timeLogs)
  </read_first>
  <action>
    Créer `src/components/rapports/mttr-tab.tsx` comme Server Component (pas de 'use client') avec EXACTEMENT cette structure :

    ```tsx
    import { db } from '@/lib/db'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Badge } from '@/components/ui/badge'
    import { Clock } from 'lucide-react'
    import { getPeriodStart, PERIOD_LABELS, type Period } from '@/lib/report-utils'

    type Props = { orgId: string; period: Period }

    type AssetStat = {
      assetId: string
      assetName: string
      totalMinutes: number
      btCount: number
      mttrHours: number
    }

    export async function MttrTab({ orgId, period }: Props) {
      const periodStart = getPeriodStart(period)

      // [Pattern 4 RESEARCH.md] — pas de groupBy car on doit sommer timeLogs.minutes par assetId
      const orders = await db.workOrder.findMany({
        where: {
          organizationId: orgId,
          type: 'corrective',
          assetId: { not: null },
          completedAt: { gte: periodStart },
          timeLogs: { some: { minutes: { not: null } } },
        },
        select: {
          assetId: true,
          asset: { select: { id: true, name: true } },
          timeLogs: { select: { minutes: true } },
        },
      })

      // Agrégation JS par assetId
      const byAsset = new Map<string, AssetStat>()
      for (const o of orders) {
        if (!o.assetId || !o.asset) continue
        const minutes = o.timeLogs.reduce((sum, tl) => sum + (tl.minutes ?? 0), 0)
        if (minutes <= 0) continue
        const existing = byAsset.get(o.assetId)
        if (existing) {
          existing.totalMinutes += minutes
          existing.btCount += 1
        } else {
          byAsset.set(o.assetId, {
            assetId: o.assetId,
            assetName: o.asset.name,
            totalMinutes: minutes,
            btCount: 1,
            mttrHours: 0,
          })
        }
      }

      // MTTR = totalMinutes / btCount / 60 (heures)
      const stats: AssetStat[] = Array.from(byAsset.values())
        .map(s => ({ ...s, mttrHours: s.totalMinutes / s.btCount / 60 }))
        .sort((a, b) => b.mttrHours - a.mttrHours)
        .slice(0, 20)

      if (stats.length === 0) {
        return (
          <Card>
            <CardContent className="py-12 text-center">
              <Clock className="h-10 w-10 mx-auto text-muted-foreground mb-3" />
              <p className="font-medium">Aucune session de temps enregistrée sur des BTs correctifs</p>
              <p className="text-sm text-muted-foreground mt-1">
                Période : {PERIOD_LABELS[period]}. Le MTTR nécessite que les techniciens démarrent
                la minuterie sur leurs bons de travail correctifs.
              </p>
            </CardContent>
          </Card>
        )
      }

      const maxMttr = Math.max(...stats.map(s => s.mttrHours), 0.01)

      return (
        <Card>
          <CardHeader>
            <CardTitle className="text-base">MTTR par actif — {PERIOD_LABELS[period]}</CardTitle>
            <p className="text-xs text-muted-foreground mt-1">
              Mean Time To Repair = temps total de réparation ÷ nombre de BTs correctifs clôturés
            </p>
          </CardHeader>
          <CardContent>
            <div className="overflow-x-auto">
              <table className="w-full text-sm">
                <thead>
                  <tr className="border-b">
                    <th className="text-left py-2 font-medium text-muted-foreground">Actif</th>
                    <th className="text-right py-2 font-medium text-muted-foreground">BTs correctifs</th>
                    <th className="text-right py-2 font-medium text-muted-foreground">Total heures</th>
                    <th className="text-right py-2 font-medium text-muted-foreground">MTTR (h)</th>
                    <th className="py-2 px-2 font-medium text-muted-foreground" style={{ minWidth: 120 }}>Visuel</th>
                  </tr>
                </thead>
                <tbody>
                  {stats.map((s) => (
                    <tr key={s.assetId} className="border-b last:border-0">
                      <td className="py-2 font-medium truncate max-w-[40%]">{s.assetName}</td>
                      <td className="py-2 text-right"><Badge variant="secondary">{s.btCount}</Badge></td>
                      <td className="py-2 text-right tabular-nums">{(s.totalMinutes / 60).toFixed(1)} h</td>
                      <td className="py-2 text-right font-bold tabular-nums">{s.mttrHours.toFixed(2)} h</td>
                      <td className="py-2 px-2">
                        <div className="h-2 rounded-full bg-muted overflow-hidden">
                          <div
                            className="h-full rounded-full transition-all bg-amber-500"
                            style={{ width: `${Math.round((s.mttrHours / maxMttr) * 100)}%` }}
                          />
                        </div>
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          </CardContent>
        </Card>
      )
    }
    ```

    Comportement clé:
    - `type: 'corrective'` (FIAB-02 explicite "BTs correctifs")
    - `assetId: { not: null }` (Piège 6 RESEARCH.md — exclure BTs sans actif)
    - `timeLogs: { some: { minutes: { not: null } } }` (exclure BTs sans temps loggé)
    - `completedAt: { gte: periodStart }` (fenêtre temporelle)
    - Agrégation mémoire JS (Hypothèse A3 RESEARCH.md, < 10k BTs)
    - Top 20 actifs triés par MTTR décroissant
    - État vide actionnable
  </action>
  <verify>
    <automated>test -f src/components/rapports/mttr-tab.tsx && grep -q "type: 'corrective'" src/components/rapports/mttr-tab.tsx && grep -q "assetId: { not: null }" src/components/rapports/mttr-tab.tsx && grep -q "timeLogs: { some:" src/components/rapports/mttr-tab.tsx && grep -q "Aucune session de temps" src/components/rapports/mttr-tab.tsx && grep -q "totalMinutes / s.btCount / 60" src/components/rapports/mttr-tab.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function MttrTab" src/components/rapports/mttr-tab.tsx` exit 0
    - `grep -q "type: 'corrective'" src/components/rapports/mttr-tab.tsx` exit 0
    - `grep -q "completedAt: { gte: periodStart }" src/components/rapports/mttr-tab.tsx` exit 0
    - `grep -q "totalMinutes / s.btCount / 60" src/components/rapports/mttr-tab.tsx` exit 0 (formule MTTR exacte)
    - `grep -q "Aucune session de temps" src/components/rapports/mttr-tab.tsx` exit 0 (état vide)
    - `grep -q ".slice(0, 20)" src/components/rapports/mttr-tab.tsx` exit 0 (limite 20 actifs)
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>MttrTab calcule et affiche le MTTR par actif (top 20) pour BTs correctifs clôturés sur la période.</done>
</task>

<task type="auto">
  <name>Task 2: Créer CostTab (FIAB-03)</name>
  <files>src/components/rapports/cost-tab.tsx</files>
  <read_first>
    - src/lib/report-utils.ts (formatCurrency, getPeriodStart, type Period)
    - src/components/rapports/mttr-tab.tsx (pattern table + agrégation mémoire)
    - prisma/schema.prisma (modèles WorkOrderPart unitCost/quantity, WorkOrderTimeLog membership.hourlyRate)
  </read_first>
  <action>
    Créer `src/components/rapports/cost-tab.tsx` comme Server Component :

    ```tsx
    import { db } from '@/lib/db'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Badge } from '@/components/ui/badge'
    import { DollarSign, AlertTriangle } from 'lucide-react'
    import { getPeriodStart, PERIOD_LABELS, formatCurrency, type Period } from '@/lib/report-utils'

    type Props = { orgId: string; period: Period }

    type AssetCost = {
      assetId: string
      assetName: string
      laborCost: number
      partsCost: number
      total: number
      hasMissingRate: boolean
    }

    export async function CostTab({ orgId, period }: Props) {
      const periodStart = getPeriodStart(period)

      // [Pattern 5 RESEARCH.md] — deux requêtes parallèles + agrégation JS par assetId
      const [parts, timeLogs] = await Promise.all([
        db.workOrderPart.findMany({
          where: {
            workOrder: {
              organizationId: orgId,
              assetId: { not: null },
              completedAt: { gte: periodStart },
            },
          },
          select: {
            quantity: true,
            unitCost: true,
            workOrder: { select: { assetId: true, asset: { select: { name: true } } } },
          },
        }),
        db.workOrderTimeLog.findMany({
          where: {
            minutes: { not: null },
            workOrder: {
              organizationId: orgId,
              assetId: { not: null },
              completedAt: { gte: periodStart },
            },
          },
          select: {
            minutes: true,
            membership: { select: { hourlyRate: true } },
            workOrder: { select: { assetId: true, asset: { select: { name: true } } } },
          },
        }),
      ])

      const byAsset = new Map<string, AssetCost>()

      function getOrInit(assetId: string, assetName: string): AssetCost {
        let entry = byAsset.get(assetId)
        if (!entry) {
          entry = { assetId, assetName, laborCost: 0, partsCost: 0, total: 0, hasMissingRate: false }
          byAsset.set(assetId, entry)
        }
        return entry
      }

      // Coût pièces : unitCost × quantity (null unitCost → 0)
      for (const p of parts) {
        const aid = p.workOrder.assetId
        if (!aid || !p.workOrder.asset) continue
        const cost = (p.unitCost ?? 0) * p.quantity
        const entry = getOrInit(aid, p.workOrder.asset.name)
        entry.partsCost += cost
      }

      // Coût main-d'œuvre : (minutes / 60) × hourlyRate (Piège 4 — null → 0 + flag)
      for (const tl of timeLogs) {
        const aid = tl.workOrder.assetId
        if (!aid || !tl.workOrder.asset || tl.minutes == null) continue
        const rate = tl.membership?.hourlyRate
        const hours = tl.minutes / 60
        const cost = (rate ?? 0) * hours
        const entry = getOrInit(aid, tl.workOrder.asset.name)
        entry.laborCost += cost
        if (rate == null && tl.minutes > 0) {
          entry.hasMissingRate = true
        }
      }

      const stats: AssetCost[] = Array.from(byAsset.values())
        .map(s => ({ ...s, total: s.laborCost + s.partsCost }))
        .filter(s => s.total > 0 || s.hasMissingRate)
        .sort((a, b) => b.total - a.total)
        .slice(0, 20)

      if (stats.length === 0) {
        return (
          <Card>
            <CardContent className="py-12 text-center">
              <DollarSign className="h-10 w-10 mx-auto text-muted-foreground mb-3" />
              <p className="font-medium">Aucun bon de travail complété avec un actif associé</p>
              <p className="text-sm text-muted-foreground mt-1">
                Période : {PERIOD_LABELS[period]}. Le coût par actif nécessite des BTs clôturés
                avec pièces ou temps enregistrés.
              </p>
            </CardContent>
          </Card>
        )
      }

      const maxTotal = Math.max(...stats.map(s => s.total), 0.01)
      const hasAnyMissingRate = stats.some(s => s.hasMissingRate)

      return (
        <div className="space-y-4">
          {hasAnyMissingRate && (
            <div className="flex items-start gap-2 rounded-md border border-amber-200 bg-amber-50 p-3 text-sm dark:bg-amber-950/30 dark:border-amber-900">
              <AlertTriangle className="h-4 w-4 text-amber-600 mt-0.5 shrink-0" />
              <p>
                Certains techniciens n'ont pas de taux horaire configuré — leur main-d'œuvre est
                comptée à 0 $. Configurez les taux dans Paramètres &gt; Équipe.
              </p>
            </div>
          )}
          <Card>
            <CardHeader>
              <CardTitle className="text-base">Coût de maintenance par actif — {PERIOD_LABELS[period]}</CardTitle>
              <p className="text-xs text-muted-foreground mt-1">Main-d'œuvre + pièces, top 20 actifs</p>
            </CardHeader>
            <CardContent>
              <div className="overflow-x-auto">
                <table className="w-full text-sm">
                  <thead>
                    <tr className="border-b">
                      <th className="text-left py-2 font-medium text-muted-foreground">Actif</th>
                      <th className="text-right py-2 font-medium text-muted-foreground">Main-d'œuvre</th>
                      <th className="text-right py-2 font-medium text-muted-foreground">Pièces</th>
                      <th className="text-right py-2 font-medium text-muted-foreground">Total</th>
                      <th className="py-2 px-2 font-medium text-muted-foreground" style={{ minWidth: 120 }}>Visuel</th>
                    </tr>
                  </thead>
                  <tbody>
                    {stats.map((s) => (
                      <tr key={s.assetId} className="border-b last:border-0">
                        <td className="py-2 font-medium truncate max-w-[35%]">
                          <span>{s.assetName}</span>
                          {s.hasMissingRate && (
                            <Badge variant="outline" className="ml-2 border-amber-500 text-amber-700">
                              taux manquant
                            </Badge>
                          )}
                        </td>
                        <td className="py-2 text-right tabular-nums">{formatCurrency(s.laborCost)}</td>
                        <td className="py-2 text-right tabular-nums">{formatCurrency(s.partsCost)}</td>
                        <td className="py-2 text-right font-bold tabular-nums">{formatCurrency(s.total)}</td>
                        <td className="py-2 px-2">
                          <div className="h-2 rounded-full bg-muted overflow-hidden">
                            <div
                              className="h-full rounded-full transition-all bg-green-600"
                              style={{ width: `${Math.round((s.total / maxTotal) * 100)}%` }}
                            />
                          </div>
                        </td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            </CardContent>
          </Card>
        </div>
      )
    }
    ```

    Comportement clé :
    - Deux requêtes Prisma parallèles (Promise.all) — parts et timeLogs
    - `assetId: { not: null }` sur les deux pour exclure les BTs sans actif
    - Agrégation JS par assetId : laborCost + partsCost
    - `(rate ?? 0) * hours` (Piège 4) + flag `hasMissingRate` pour avertir l'utilisateur
    - Top 20 actifs triés par total décroissant
    - Banner d'alerte si au moins un actif a des heures loggées par un technicien sans taux
    - `formatCurrency` utilisé partout pour l'affichage monétaire
  </action>
  <verify>
    <automated>test -f src/components/rapports/cost-tab.tsx && grep -q "Promise.all" src/components/rapports/cost-tab.tsx && grep -q "rate ?? 0" src/components/rapports/cost-tab.tsx && grep -q "hasMissingRate" src/components/rapports/cost-tab.tsx && grep -q "formatCurrency" src/components/rapports/cost-tab.tsx && grep -q "Aucun bon de travail complété avec un actif" src/components/rapports/cost-tab.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function CostTab" src/components/rapports/cost-tab.tsx` exit 0
    - `grep -q "Promise.all" src/components/rapports/cost-tab.tsx` exit 0
    - `grep -q "db.workOrderPart.findMany" src/components/rapports/cost-tab.tsx` exit 0
    - `grep -q "db.workOrderTimeLog.findMany" src/components/rapports/cost-tab.tsx` exit 0
    - `grep -q "rate ?? 0" src/components/rapports/cost-tab.tsx` exit 0 (gestion null hourlyRate)
    - `grep -q "hasMissingRate" src/components/rapports/cost-tab.tsx` exit 0 (flag d'avertissement)
    - `grep -q "formatCurrency" src/components/rapports/cost-tab.tsx` exit 0
    - `grep -q ".slice(0, 20)" src/components/rapports/cost-tab.tsx` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>CostTab agrège le coût main-d'œuvre + pièces par actif sur la période, avec banner d'avertissement pour les techniciens sans taux configuré.</done>
</task>

<task type="auto">
  <name>Task 3: Câbler MttrTab et CostTab dans rapports/page.tsx</name>
  <files>src/app/(app)/rapports/page.tsx</files>
  <read_first>
    - src/app/(app)/rapports/page.tsx (état après Plan 02 — chercher les `<TabsContent value="mttr">` et `<TabsContent value="cost">` qui contiennent un placeholder Card)
    - src/components/rapports/mttr-tab.tsx (créé Task 1)
    - src/components/rapports/cost-tab.tsx (créé Task 2)
  </read_first>
  <action>
    Dans `src/app/(app)/rapports/page.tsx` :

    1. Ajouter les imports en haut du fichier (à côté des imports OverviewTab et TopFaultsTab) :
       ```typescript
       import { MttrTab } from '@/components/rapports/mttr-tab'
       import { CostTab } from '@/components/rapports/cost-tab'
       ```

    2. Remplacer le `<TabsContent value="mttr">` placeholder par :
       ```tsx
       <TabsContent value="mttr" className="mt-4">
         <MttrTab orgId={orgId} period={period} />
       </TabsContent>
       ```

    3. Remplacer le `<TabsContent value="cost">` placeholder par :
       ```tsx
       <TabsContent value="cost" className="mt-4">
         <CostTab orgId={orgId} period={period} />
       </TabsContent>
       ```

    4. NE PAS toucher au `<TabsContent value="planned-vs-real">` placeholder — il sera remplacé par Plan 04.
    5. NE PAS modifier la structure Tabs/TabsList/TabsTrigger ni la logique de validation `isTab`/`isPeriod`.

    Lancer `npx tsc --noEmit` — DOIT passer.
  </action>
  <verify>
    <automated>grep -q "import { MttrTab } from '@/components/rapports/mttr-tab'" src/app/(app)/rapports/page.tsx && grep -q "import { CostTab } from '@/components/rapports/cost-tab'" src/app/(app)/rapports/page.tsx && grep -q "<MttrTab orgId={orgId} period={period}" src/app/(app)/rapports/page.tsx && grep -q "<CostTab orgId={orgId} period={period}" src/app/(app)/rapports/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { MttrTab }" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -q "import { CostTab }" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -q "<MttrTab orgId={orgId} period={period}" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -q "<CostTab orgId={orgId} period={period}" src/app/(app)/rapports/page.tsx` exit 0
    - Le placeholder MTTR original ("disponible après livraison Plan 03") n'apparaît plus : `grep -c "Rapport MTTR" src/app/(app)/rapports/page.tsx` === 0
    - Le placeholder Coût original n'apparaît plus : `grep -c "Rapport Coût par actif" src/app/(app)/rapports/page.tsx` === 0
    - Le placeholder Planifié vs Réel EST TOUJOURS présent : `grep -q "Plan 04" src/app/(app)/rapports/page.tsx` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Tabs MTTR et Coût actifs sont câblés et rendent les Server Components avec les vraies données. Tab Planifié vs Réel reste un placeholder pour Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (URL ?period=) | Période validée par Plan 02 via `isPeriod()` |
| server→PostgreSQL | Agrégations filtrées par `organizationId` (multi-tenancy) |
| RH data → CostTab | `Membership.hourlyRate` exposé indirectement via le total coût |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | CostTab — `Membership.hourlyRate` est une donnée RH sensible | mitigate | (a) Page `/rapports` gated par `requirePlan(['growth', 'enterprise'])` (Plan 02). (b) Le `hourlyRate` individuel n'est JAMAIS rendu — seul l'agrégat `laborCost` par actif est affiché. Un manager peut estimer le taux moyen d'un tech sur un actif, mais ne peut pas voir un salaire individuel. (c) `requireOrgMember` ou équivalent applique le multi-tenancy via `organizationId` partout. |
| T-07-03-02 | Information Disclosure | MttrTab et CostTab — fuite cross-tenant si filter manquant | mitigate | Filtre `organizationId: orgId` posé sur les 2 requêtes principales (workOrder direct ou via `workOrder: { organizationId: orgId }`). Vérifier par grep que chaque `findMany` contient `organizationId`. |
| T-07-03-03 | Tampering | Manipulation de `?period=` pour récupérer données hors période | mitigate | `period` validé par `isPeriod()` (Plan 02) qui force la valeur dans un whitelist `['month', '3m', '6m', 'year']`. `getPeriodStart()` calcule une Date — pas de chaîne libre injectée dans la query Prisma. |
| T-07-03-04 | Denial of Service | Volume de WorkOrders énorme → agrégation mémoire JS lente | accept | Hypothèse A3 RESEARCH.md : < 10k BTs par org. Si volume explose en production, basculer vers raw SQL avec GROUP BY. Pas de mitigation Phase 7. Mesurer via logs si nécessaire. |
| T-07-03-05 | Authorization | Plan starter accède directement à /rapports?tab=mttr | mitigate | `requirePlan(['growth', 'enterprise'])` exécuté AVANT toute lecture/agrégation. `UpgradeGate` wrappe le contenu. Pas de fuite possible — vérifié par Plan 02. |
</threat_model>

<verification>
- `npx tsc --noEmit` GREEN sur tout le projet
- `npx next build` retourne 0 sans erreur sur /rapports
- Navigation manuelle : ouvrir `/rapports?tab=mttr` → tableau MTTR ou message vide selon les données
- Ouvrir `/rapports?tab=cost` → tableau coûts main-d'œuvre + pièces ou message vide
- Changer la période via le selector → URL `?period=3m` → re-fetch automatique des deux tabs
- Si un BT a `assetId: null`, il n'apparaît dans aucun des 2 rapports
- Si un technicien sans `hourlyRate` a des minutes loggées sur un actif → banner d'avertissement "taux manquant" + flag sur la ligne actif
- Multi-tenant : créer un BT dans une autre organisation, ne doit JAMAIS apparaître dans les rapports
</verification>

<success_criteria>
1. Tab MTTR affiche le MTTR par actif (heures, top 20) pour BTs correctifs clôturés sur la période, trié décroissant
2. Formule MTTR = somme(timeLogs.minutes) / count(BT) / 60 (heures) par actif
3. Tab Coût actifs affiche pour chaque actif (top 20) : main-d'œuvre + pièces + total sur la période, trié par total décroissant
4. Coût main-d'œuvre = sum((minutes/60) × (hourlyRate ?? 0)) par actif
5. Coût pièces = sum((unitCost ?? 0) × quantity) par actif
6. Banner d'avertissement si un technicien sans `hourlyRate` a contribué à un actif affiché
7. État vide actionnable pour chaque tab (message + lien vers Paramètres)
8. Filtrage par `organizationId` strict sur toutes les requêtes (multi-tenancy)
9. `requirePlan(['growth', 'enterprise'])` toujours en place (hérité Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytique-de-fiabilit/07-03-SUMMARY.md` documenting:
- Requête Prisma exacte de MttrTab (findMany + select + agrégation)
- Requête Prisma exacte de CostTab (les deux findMany parallèles)
- Comportement du flag `hasMissingRate`
- Confirmation que Plan 04 doit remplacer uniquement le placeholder `planned-vs-real`
</output>
