---
phase: 07-analytique-de-fiabilit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql
  - src/lib/closure-requirements.ts
  - src/lib/closure-requirements.test.ts
  - src/actions/work-orders.ts
  - src/components/work-orders/work-order-fault-form.tsx
  - src/components/work-orders/work-order-detail.tsx
  - src/components/work-orders/work-order-parts.tsx
  - src/lib/report-utils.ts
  - src/lib/report-utils.test.ts
autonomous: true
requirements:
  - FIAB-01
must_haves:
  truths:
    - "La colonne WorkOrder.faultDescription est renommée en faultProblem dans la base de données sans perte de données"
    - "Les colonnes WorkOrder.faultCause et WorkOrder.faultRemedy existent et acceptent NULL"
    - "Le code existant Phase 6 (closure-requirements, work-orders action, fault form, detail) compile et utilise faultProblem"
    - "Le formulaire de clôture saisit Problème + Cause + Remède"
    - "Les utilitaires getPeriodStart() et formatCurrency() sont disponibles pour les rapports"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Modèle WorkOrder avec faultProblem/faultCause/faultRemedy"
      contains: "faultProblem"
    - path: "prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql"
      provides: "Migration SQL ALTER TABLE rename + ADD COLUMN"
      contains: "RENAME COLUMN"
    - path: "src/lib/report-utils.ts"
      provides: "getPeriodStart() et formatCurrency()"
      exports: ["getPeriodStart", "formatCurrency", "PERIOD_VALUES", "type Period"]
    - path: "src/lib/closure-requirements.ts"
      provides: "ClosureCheckInput utilise faultProblem"
      contains: "faultProblem"
    - path: "src/components/work-orders/work-order-fault-form.tsx"
      provides: "Formulaire P/C/R complet"
      contains: "faultRemedy"
  key_links:
    - from: "prisma/schema.prisma"
      to: "Base de données PostgreSQL"
      via: "prisma migrate deploy"
      pattern: "RENAME COLUMN.*faultDescription.*TO.*faultProblem"
    - from: "src/actions/work-orders.ts setWorkOrderFault"
      to: "WorkOrder.faultProblem/faultCause/faultRemedy"
      via: "prisma.workOrder.update data"
      pattern: "faultProblem.*faultCause.*faultRemedy"
---

<objective>
Phase 7 — Plan 01 : Migrer le schéma Prisma pour structurer les codes de panne Problème/Cause/Remède (D-01, D-02), mettre à jour le code Phase 6 qui référence `faultDescription`, et créer les utilitaires de rapport partagés (`getPeriodStart`, `formatCurrency`) consommés par les Plans 02/03/04.

**Purpose:** Sans cette migration, les rapports FIAB-01 (Top pannes) ne peuvent pas grouper par `faultProblem`+`faultCategory` séparément, et le formulaire de clôture ne peut pas saisir Cause+Remède. Sans `report-utils.ts`, chaque tab dupliquerait la logique de période.

**Output:** Schéma migré, code Phase 6 mis à jour pour utiliser `faultProblem`, utilitaires partagés disponibles pour Wave 2/3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-analytique-de-fiabilit/07-CONTEXT.md
@.planning/phases/07-analytique-de-fiabilit/07-RESEARCH.md
@prisma/schema.prisma
@src/lib/closure-requirements.ts
@src/lib/closure-requirements.test.ts
@src/actions/work-orders.ts
@src/components/work-orders/work-order-fault-form.tsx
@src/components/work-orders/work-order-detail.tsx
@src/components/work-orders/work-order-parts.tsx

<interfaces>
<!-- Contrats existants et nouveaux. Les Plans 02/03/04 utiliseront getPeriodStart et le champ faultProblem. -->

État courant — prisma/schema.prisma (lignes 282-316):
```prisma
model WorkOrder {
  id               String  @id @default(cuid())
  organizationId   String
  ...
  faultCategory    String?
  faultDescription String?   // ← À RENOMMER en faultProblem (D-01)
  // ← AJOUTER : faultCause String?  (D-02)
  // ← AJOUTER : faultRemedy String? (D-02)
  ...
}
```

État courant — src/lib/closure-requirements.ts (lignes 29-34):
```typescript
export type ClosureCheckInput = {
  faultCategory: string | null
  faultDescription: string | null   // ← À RENOMMER en faultProblem
  timeLogsMinutesTotal: number
  partsCount: number
}
```

Nouveau contrat — src/lib/report-utils.ts (à créer):
```typescript
export const PERIOD_VALUES = ['month', '3m', '6m', 'year'] as const
export type Period = typeof PERIOD_VALUES[number]
export const PERIOD_LABELS: Record<Period, string>  // 'Ce mois', '3 mois', '6 mois', 'Cette année'

export function getPeriodStart(period: Period): Date
export function isPeriod(value: string | undefined): value is Period
export function formatCurrency(amount: number): string   // '1 234,56 $' format CAD
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Créer src/lib/report-utils.ts + tests</name>
  <files>src/lib/report-utils.ts, src/lib/report-utils.test.ts</files>
  <read_first>
    - src/lib/report-utils.ts (vérifier s'il existe déjà — il NE devrait PAS exister)
    - src/lib/closure-requirements.test.ts (pattern de test Vitest utilisé dans le projet)
    - package.json (vérifier que vitest est installé et la commande de test exacte)
  </read_first>
  <behavior>
    - Test 1: getPeriodStart('month') retourne une Date au 1er du mois courant à minuit (locale)
    - Test 2: getPeriodStart('3m') retourne une Date 3 mois en arrière à la date courante
    - Test 3: getPeriodStart('6m') retourne une Date 6 mois en arrière à la date courante
    - Test 4: getPeriodStart('year') retourne une Date un an en arrière à la date courante
    - Test 5: isPeriod('month') === true, isPeriod('foo') === false, isPeriod(undefined) === false
    - Test 6: formatCurrency(1234.56) retourne une chaîne contenant '1' '234' ',' '56' et '$' (locale fr-CA)
    - Test 7: formatCurrency(0) retourne une chaîne contenant '0,00' et '$'
    - Test 8: formatCurrency(null as any) ne lance pas — retourne '0,00 $' ou équivalent (traiter null/undefined comme 0)
  </behavior>
  <action>
    Créer src/lib/report-utils.ts avec exports suivants (les valeurs concrètes EXACTES) :

    ```typescript
    export const PERIOD_VALUES = ['month', '3m', '6m', 'year'] as const
    export type Period = typeof PERIOD_VALUES[number]

    export const PERIOD_LABELS: Record<Period, string> = {
      month: 'Ce mois',
      '3m': '3 mois',
      '6m': '6 mois',
      year: 'Cette année',
    }

    export const DEFAULT_PERIOD: Period = 'month'

    export function isPeriod(value: string | undefined | null): value is Period {
      return typeof value === 'string' && (PERIOD_VALUES as readonly string[]).includes(value)
    }

    export function getPeriodStart(period: Period): Date {
      const now = new Date()
      switch (period) {
        case 'month':
          return new Date(now.getFullYear(), now.getMonth(), 1, 0, 0, 0, 0)
        case '3m':
          return new Date(now.getFullYear(), now.getMonth() - 3, now.getDate(), 0, 0, 0, 0)
        case '6m':
          return new Date(now.getFullYear(), now.getMonth() - 6, now.getDate(), 0, 0, 0, 0)
        case 'year':
          return new Date(now.getFullYear() - 1, now.getMonth(), now.getDate(), 0, 0, 0, 0)
      }
    }

    export function formatCurrency(amount: number | null | undefined): string {
      const value = typeof amount === 'number' && Number.isFinite(amount) ? amount : 0
      return new Intl.NumberFormat('fr-CA', {
        style: 'currency',
        currency: 'CAD',
        minimumFractionDigits: 2,
        maximumFractionDigits: 2,
      }).format(value)
    }
    ```

    Créer src/lib/report-utils.test.ts en utilisant le pattern de closure-requirements.test.ts (import { describe, it, expect } from 'vitest'). Implémenter les 8 tests décrits dans <behavior>.

    Lancer `npx vitest run src/lib/report-utils.test.ts` — DOIT passer GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/lib/report-utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export function getPeriodStart" src/lib/report-utils.ts` exit 0
    - `grep -q "export function formatCurrency" src/lib/report-utils.ts` exit 0
    - `grep -q "PERIOD_VALUES = \['month', '3m', '6m', 'year'\]" src/lib/report-utils.ts` exit 0
    - `grep -q "fr-CA" src/lib/report-utils.ts` exit 0
    - `npx vitest run src/lib/report-utils.test.ts` exit 0 avec au moins 8 tests passés
  </acceptance_criteria>
  <done>Module utilitaire créé avec 8 tests verts. Plans 02/03/04 peuvent importer getPeriodStart, formatCurrency, PERIOD_LABELS, isPeriod et le type Period.</done>
</task>

<task type="auto">
  <name>Task 2: Modifier prisma/schema.prisma — rename + 2 nouveaux champs</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (lignes 280-316 — modèle WorkOrder)
  </read_first>
  <action>
    Dans prisma/schema.prisma, modifier le modèle WorkOrder (ligne ~282-316) comme suit :

    1. RENOMMER ligne 299 :
       AVANT : `faultDescription String?`
       APRÈS : `faultProblem     String?`

    2. AJOUTER deux lignes JUSTE APRÈS la ligne `faultProblem` (D-02) :
       ```prisma
         faultCause       String?
         faultRemedy      String?
       ```

    Le bloc final WorkOrder doit contenir (extrait) :
    ```prisma
      closureNotes     String?
      faultCategory    String?
      faultProblem     String?
      faultCause       String?
      faultRemedy      String?
      createdAt        DateTime          @default(now())
    ```

    NE PAS lancer `prisma db push` ni `prisma migrate dev` dans cette task — c'est l'objet de la Task 3 ([BLOCKING]).
    NE PAS modifier d'autre modèle ni d'autre champ.
  </action>
  <verify>
    <automated>grep -q "faultProblem.*String?" prisma/schema.prisma && grep -q "faultCause.*String?" prisma/schema.prisma && grep -q "faultRemedy.*String?" prisma/schema.prisma && ! grep -q "faultDescription" prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "faultProblem" prisma/schema.prisma` === 1
    - `grep -c "faultCause" prisma/schema.prisma` === 1
    - `grep -c "faultRemedy" prisma/schema.prisma` === 1
    - `grep -c "faultDescription" prisma/schema.prisma` === 0 (zéro occurrence — entièrement remplacé)
    - Le modèle WorkOrder contient encore `faultCategory String?` (non modifié)
  </acceptance_criteria>
  <done>Schéma Prisma reflète la structure P/C/R sans plus aucune référence à faultDescription.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Migration formelle Prisma rename + ADD COLUMN + génération client</name>
  <files>prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql, src/generated/prisma/*</files>
  <read_first>
    - prisma/migrations/migration_lock.toml (vérifier provider postgresql)
    - prisma/migrations/20260517220402_add_spare_parts/migration.sql (pattern de migration existant pour référence)
    - prisma/schema.prisma (état modifié par Task 2)
    - .env.example (vérifier que DATABASE_URL existe)
  </read_first>
  <action>
    Cette task est BLOCKING (schema_push_requirement) : un renommage de colonne DOIT passer par une migration SQL formelle, PAS par `prisma db push` (qui drop+create et perd les données).

    1. Créer le répertoire de migration :
       `mkdir -p prisma/migrations/20260526000000_phase7_fault_pcr`

    2. Créer le fichier `prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql` avec EXACTEMENT ce contenu :
       ```sql
       -- Phase 7 — Structure P/C/R (Problème / Cause / Remède) pour les codes de panne
       -- D-01 : rename faultDescription → faultProblem (préserve les données existantes)
       -- D-02 : ajout des champs faultCause et faultRemedy (nullable)

       ALTER TABLE "WorkOrder" RENAME COLUMN "faultDescription" TO "faultProblem";
       ALTER TABLE "WorkOrder" ADD COLUMN "faultCause" TEXT;
       ALTER TABLE "WorkOrder" ADD COLUMN "faultRemedy" TEXT;
       ```

    3. Appliquer la migration :
       - Mode interactif (préféré dev) : `npx prisma migrate dev --name phase7_fault_pcr --create-only` PUIS `npx prisma migrate deploy`
       - Mode non-TTY (CI/auto) : `DATABASE_URL="$DATABASE_URL" npx prisma migrate deploy`
       - Si `prisma migrate dev` veut RECRÉER la migration (parce que `db push` a précédé) : utiliser `npx prisma migrate resolve --applied 20260526000000_phase7_fault_pcr` pour synchroniser sans réexécuter.

    4. Régénérer le client Prisma : `npx prisma generate`

    5. Vérifier que la migration est appliquée : `npx prisma migrate status` doit afficher la migration `20260526000000_phase7_fault_pcr` comme "Database schema is up to date" ou équivalent.

    6. Vérifier que le client généré expose `faultProblem` : `grep -q "faultProblem" src/generated/prisma/internal/class.ts` doit exit 0.

    Cette task NE PEUT PAS être skippée — sans elle, les tasks 4-7 (qui référencent `faultProblem`) compileront mais échoueront en runtime contre la base de données.
  </action>
  <verify>
    <automated>test -f prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql && grep -q "RENAME COLUMN \"faultDescription\" TO \"faultProblem\"" prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql && grep -q "ADD COLUMN \"faultCause\"" prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql && grep -q "ADD COLUMN \"faultRemedy\"" prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql && grep -q "faultProblem" src/generated/prisma/internal/class.ts && npx prisma migrate status 2>&1 | grep -qiE "up to date|in sync|no pending"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f prisma/migrations/20260526000000_phase7_fault_pcr/migration.sql` exit 0
    - migration.sql contient EXACTEMENT : `ALTER TABLE "WorkOrder" RENAME COLUMN "faultDescription" TO "faultProblem";`
    - migration.sql contient : `ALTER TABLE "WorkOrder" ADD COLUMN "faultCause" TEXT;`
    - migration.sql contient : `ALTER TABLE "WorkOrder" ADD COLUMN "faultRemedy" TEXT;`
    - `grep -q "faultProblem" src/generated/prisma/internal/class.ts` exit 0
    - `! grep -q "faultDescription" src/generated/prisma/internal/class.ts` exit 0
    - `npx prisma migrate status` retourne 0 sans pending migration
  </acceptance_criteria>
  <done>Migration SQL formelle créée, appliquée à PostgreSQL, client Prisma régénéré, plus aucune référence à faultDescription dans le client généré.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Adapter src/lib/closure-requirements.ts (faultDescription → faultProblem)</name>
  <files>src/lib/closure-requirements.ts, src/lib/closure-requirements.test.ts</files>
  <read_first>
    - src/lib/closure-requirements.ts (état actuel — ClosureCheckInput.faultDescription)
    - src/lib/closure-requirements.test.ts (tests existants à mettre à jour)
  </read_first>
  <behavior>
    - Test (mis à jour) : validateClosure avec `{ faultCategory: 'mecanique', faultProblem: 'Moteur HS', ... }` retourne `[]` (valide) quand `req.faultCode = true`
    - Test (mis à jour) : validateClosure avec `{ faultCategory: 'mecanique', faultProblem: null, ... }` retourne `['faultCode']` quand `req.faultCode = true`
    - Test (mis à jour) : validateClosure avec `{ faultCategory: null, faultProblem: 'Moteur HS', ... }` retourne `['faultCode']` quand `req.faultCode = true`
    - Tests existants timeSpent/partsUsed restent inchangés sur le plan logique
  </behavior>
  <action>
    Dans src/lib/closure-requirements.ts :

    1. Ligne 31 — RENOMMER :
       AVANT : `faultDescription: string | null`
       APRÈS : `faultProblem: string | null`

    2. Lignes 39-41 — METTRE À JOUR le bloc `if (req.faultCode)` :
       AVANT :
       ```typescript
       const desc = input.faultDescription?.trim()
       if (!cat || !desc) missing.push('faultCode')
       ```
       APRÈS :
       ```typescript
       const problem = input.faultProblem?.trim()
       if (!cat || !problem) missing.push('faultCode')
       ```

    3. Mettre à jour CLOSURE_FIELD_LABELS (ligne 23-27) pour refléter le nouveau vocabulaire :
       AVANT : `faultCode: 'Code de panne (catégorie + description)'`
       APRÈS : `faultCode: 'Code de panne (catégorie + problème)'`

    Dans src/lib/closure-requirements.test.ts :
    - Remplacer toutes les occurrences de `faultDescription:` par `faultProblem:` dans les objets de test
    - Garder les mêmes valeurs de chaîne (ex: 'Moteur HS' reste 'Moteur HS')
    - Si un test attend le label exact, le mettre à jour avec le nouveau libellé

    Lancer `npx vitest run src/lib/closure-requirements.test.ts` — DOIT passer GREEN.
  </action>
  <verify>
    <automated>grep -q "faultProblem: string" src/lib/closure-requirements.ts && ! grep -q "faultDescription" src/lib/closure-requirements.ts && ! grep -q "faultDescription" src/lib/closure-requirements.test.ts && npx vitest run src/lib/closure-requirements.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "faultProblem" src/lib/closure-requirements.ts` >= 2 (type + usage)
    - `grep -c "faultDescription" src/lib/closure-requirements.ts` === 0
    - `grep -c "faultDescription" src/lib/closure-requirements.test.ts` === 0
    - `npx vitest run src/lib/closure-requirements.test.ts` exit 0
  </acceptance_criteria>
  <done>Module de validation de clôture utilise faultProblem partout, tests verts.</done>
</task>

<task type="auto">
  <name>Task 5: Mettre à jour src/actions/work-orders.ts (setWorkOrderFault)</name>
  <files>src/actions/work-orders.ts</files>
  <read_first>
    - src/actions/work-orders.ts (chercher la fonction setWorkOrderFault et toutes les références à faultDescription)
    - prisma/schema.prisma (confirmer les noms exacts : faultProblem, faultCause, faultRemedy)
  </read_first>
  <action>
    Dans src/actions/work-orders.ts :

    1. Trouver la fonction `setWorkOrderFault` (Server Action) et étendre sa signature pour accepter les 3 champs :

       AVANT (probable) :
       ```typescript
       export async function setWorkOrderFault(input: {
         workOrderId: string
         faultCategory: string | null
         faultDescription: string | null
       })
       ```

       APRÈS :
       ```typescript
       export async function setWorkOrderFault(input: {
         workOrderId: string
         faultCategory: string | null
         faultProblem: string | null
         faultCause: string | null
         faultRemedy: string | null
       })
       ```

    2. Mettre à jour le `prisma.workOrder.update({ data: ... })` pour persister les 3 nouveaux champs :
       ```typescript
       data: {
         faultCategory: input.faultCategory,
         faultProblem: input.faultProblem,
         faultCause: input.faultCause,
         faultRemedy: input.faultRemedy,
       }
       ```

    3. Si la fonction utilise un schema Zod pour valider l'input, mettre à jour pour inclure `faultProblem`, `faultCause`, `faultRemedy` (tous `z.string().nullable()`).

    4. Chercher toutes les autres occurrences de `faultDescription` dans le fichier (validation de clôture, select Prisma, transformations) :
       - Dans tout `select: { ... faultDescription: true }` → remplacer par `faultProblem: true, faultCause: true, faultRemedy: true`
       - Dans tout appel à `validateClosure` qui passe `faultDescription` → renommer en `faultProblem`

    5. NE PAS modifier la signature des autres actions (`startTimer`, `stopTimer`, `addPart`, etc.).

    Vérifier la compilation : `npx tsc --noEmit` doit passer pour ce fichier (les erreurs ailleurs seront corrigées dans Tasks 6-7).
  </action>
  <verify>
    <automated>grep -q "faultProblem" src/actions/work-orders.ts && grep -q "faultCause" src/actions/work-orders.ts && grep -q "faultRemedy" src/actions/work-orders.ts && ! grep -q "faultDescription" src/actions/work-orders.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "faultProblem" src/actions/work-orders.ts` >= 2 (input + data)
    - `grep -c "faultCause" src/actions/work-orders.ts` >= 2
    - `grep -c "faultRemedy" src/actions/work-orders.ts` >= 2
    - `grep -c "faultDescription" src/actions/work-orders.ts` === 0
  </acceptance_criteria>
  <done>setWorkOrderFault accepte et persiste faultProblem/faultCause/faultRemedy. Aucune référence à faultDescription dans ce fichier.</done>
</task>

<task type="auto">
  <name>Task 6: Étendre work-order-fault-form.tsx avec Cause + Remède</name>
  <files>src/components/work-orders/work-order-fault-form.tsx</files>
  <read_first>
    - src/components/work-orders/work-order-fault-form.tsx (composant actuel — props et structure du formulaire)
    - src/components/work-orders/work-order-detail.tsx (pour comprendre comment les props sont passées)
  </read_first>
  <action>
    Dans src/components/work-orders/work-order-fault-form.tsx :

    1. Mettre à jour les props du composant :
       AVANT (probable) :
       ```typescript
       type Props = {
         workOrderId: string
         initialCategory: string | null
         initialDescription: string | null
       }
       ```
       APRÈS :
       ```typescript
       type Props = {
         workOrderId: string
         initialCategory: string | null
         initialProblem: string | null
         initialCause: string | null
         initialRemedy: string | null
       }
       ```

    2. Mettre à jour les `useState` :
       - `description` → `problem`
       - AJOUTER `const [cause, setCause] = useState(initialCause ?? '')`
       - AJOUTER `const [remedy, setRemedy] = useState(initialRemedy ?? '')`

    3. Mettre à jour l'appel à `setWorkOrderFault` (handler de soumission) pour passer les 3 nouveaux champs :
       ```typescript
       await setWorkOrderFault({
         workOrderId,
         faultCategory: category || null,
         faultProblem: problem.trim() || null,
         faultCause: cause.trim() || null,
         faultRemedy: remedy.trim() || null,
       })
       ```

    4. Mettre à jour le JSX — remplacer le champ "Description" existant par 3 champs :
       - Champ existant "Description" → renommer le label en "Problème observé" et la valeur liée à `problem`
       - AJOUTER un nouveau Textarea ou Input "Cause identifiée" (placeholder: "Ex: roulement usé, fusible grillé...")
       - AJOUTER un nouveau Textarea ou Input "Remède appliqué" (placeholder: "Ex: remplacement roulement, reset disjoncteur...")
       - Conserver le `Select` pour `faultCategory` (mecanique/electrique/hydraulique/autre)

    5. Garder la même structure visuelle (Card, espacement) que l'existant — ne pas refondre le design.
  </action>
  <verify>
    <automated>grep -q "initialProblem" src/components/work-orders/work-order-fault-form.tsx && grep -q "initialCause" src/components/work-orders/work-order-fault-form.tsx && grep -q "initialRemedy" src/components/work-orders/work-order-fault-form.tsx && grep -q "faultProblem:" src/components/work-orders/work-order-fault-form.tsx && grep -q "faultCause:" src/components/work-orders/work-order-fault-form.tsx && grep -q "faultRemedy:" src/components/work-orders/work-order-fault-form.tsx && ! grep -q "initialDescription\|faultDescription" src/components/work-orders/work-order-fault-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "initialProblem\|initialCause\|initialRemedy" src/components/work-orders/work-order-fault-form.tsx` === 3
    - `grep -q "Problème observé\|Problème" src/components/work-orders/work-order-fault-form.tsx` exit 0
    - `grep -q "Cause" src/components/work-orders/work-order-fault-form.tsx` exit 0
    - `grep -q "Remède" src/components/work-orders/work-order-fault-form.tsx` exit 0
    - `grep -c "faultDescription\|initialDescription" src/components/work-orders/work-order-fault-form.tsx` === 0
  </acceptance_criteria>
  <done>Formulaire de panne propose 3 champs P/C/R avec catégorie. Soumission appelle setWorkOrderFault avec les 4 valeurs.</done>
</task>

<task type="auto">
  <name>Task 7: Mettre à jour work-order-detail.tsx et work-order-parts.tsx (callers)</name>
  <files>src/components/work-orders/work-order-detail.tsx, src/components/work-orders/work-order-parts.tsx</files>
  <read_first>
    - src/components/work-orders/work-order-detail.tsx (chercher comment WorkOrderFaultForm est instancié et quelles props lui sont passées)
    - src/components/work-orders/work-order-parts.tsx (chercher toute référence à faultDescription)
    - prisma/schema.prisma (confirmer les types Prisma fournis)
  </read_first>
  <action>
    Dans src/components/work-orders/work-order-detail.tsx :

    1. Chercher l'instanciation `<WorkOrderFaultForm ...>` et mettre à jour les props :
       AVANT (probable) :
       ```tsx
       <WorkOrderFaultForm
         workOrderId={workOrder.id}
         initialCategory={workOrder.faultCategory}
         initialDescription={workOrder.faultDescription}
       />
       ```
       APRÈS :
       ```tsx
       <WorkOrderFaultForm
         workOrderId={workOrder.id}
         initialCategory={workOrder.faultCategory}
         initialProblem={workOrder.faultProblem}
         initialCause={workOrder.faultCause}
         initialRemedy={workOrder.faultRemedy}
       />
       ```

    2. Si le composant lit `workOrder.faultDescription` ailleurs (affichage en mode lecture seule, banner de clôture, etc.) :
       - Remplacer par `workOrder.faultProblem`
       - Si un affichage P/C/R combiné est pertinent (ex: ClosureBanner qui résume), afficher les 3 valeurs séparément avec labels.

    3. Si la query Prisma qui charge le WorkOrder côté serveur a un `select` explicite mentionnant `faultDescription: true`, le remplacer par :
       ```typescript
       faultProblem: true,
       faultCause: true,
       faultRemedy: true,
       ```

    Dans src/components/work-orders/work-order-parts.tsx :

    4. Chercher toute occurrence de `faultDescription` et la remplacer par `faultProblem` (ce fichier ne devrait pas en avoir mais le grep dans git status le suggère — vérifier).

    5. Pour ClosureBanner ou validateClosure call : passer `faultProblem` au lieu de `faultDescription` dans l'input ClosureCheckInput.

    Lancer `npx tsc --noEmit` à la fin — DOIT passer sans erreur TypeScript dans tout le projet (le client Prisma est régénéré depuis Task 3, plus aucune référence à faultDescription n'existe).
  </action>
  <verify>
    <automated>grep -q "initialProblem={workOrder.faultProblem}" src/components/work-orders/work-order-detail.tsx && ! grep -rn "faultDescription" src/components/work-orders/ && ! grep -rn "initialDescription" src/components/work-orders/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "initialProblem" src/components/work-orders/work-order-detail.tsx` exit 0
    - `grep -q "initialCause" src/components/work-orders/work-order-detail.tsx` exit 0
    - `grep -q "initialRemedy" src/components/work-orders/work-order-detail.tsx` exit 0
    - `grep -rn "faultDescription" src/components/work-orders/` retourne 0 ligne
    - `grep -rn "faultDescription" src/` retourne 0 ligne (excluant src/generated/)
    - `npx tsc --noEmit` exit 0 (compilation TypeScript GREEN)
  </acceptance_criteria>
  <done>Tous les consommateurs de WorkOrderFaultForm utilisent les nouveaux props P/C/R. La codebase compile sans erreur TypeScript. Plus aucune référence à faultDescription nulle part (hors src/generated/ régénéré).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server action `setWorkOrderFault` | Saisie utilisateur des champs P/C/R par techniciens authentifiés (Clerk) |
| application→PostgreSQL | Migration SQL DDL exécutée avec credentials DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | Migration SQL phase7_fault_pcr | mitigate | Migration formelle versionnée (pas db push) avec ALTER RENAME qui préserve les données. Procédure de rollback documentée : `ALTER TABLE "WorkOrder" RENAME COLUMN "faultProblem" TO "faultDescription"; DROP COLUMN "faultCause"; DROP COLUMN "faultRemedy";` |
| T-07-01-02 | Information Disclosure | setWorkOrderFault Server Action | mitigate | L'action existante vérifie déjà l'auth via `requireOrgMember()` ou pattern équivalent dans work-orders.ts. Préserver ce check — ne pas l'enlever pendant la mise à jour. Filtrer par `organizationId` avant tout update Prisma. |
| T-07-01-03 | Input Validation | Champs faultProblem/Cause/Remedy (String?) | mitigate | Trim côté serveur (déjà fait dans setWorkOrderFault), longueur max raisonnable via Zod (ex: `z.string().max(2000).nullable()`). Pas de HTML rendu — les valeurs sont affichées en texte brut dans la UI. |
| T-07-01-04 | Repudiation | Modification d'un code de panne après clôture | accept | Pas de log d'audit prévu Phase 7. Les BTs clôturés peuvent toujours être édités (pas de blocage). Risque faible — usage interne uniquement. |
</threat_model>

<verification>
- Migration `20260526000000_phase7_fault_pcr` apparaît dans `npx prisma migrate status` comme appliquée
- `npx prisma studio` (manuel optionnel) montre `faultProblem`, `faultCause`, `faultRemedy` sur le modèle WorkOrder, plus aucun `faultDescription`
- `npx tsc --noEmit` GREEN sur tout le projet
- `npx vitest run` GREEN au moins pour `report-utils.test.ts` et `closure-requirements.test.ts`
- `grep -rn "faultDescription" src/ --exclude-dir=generated` retourne 0 ligne
- Une création/édition manuelle de WorkOrder dans la UI persiste les 3 champs (à valider en Wave 2/3 par checkpoint humain — pas dans ce plan)
</verification>

<success_criteria>
1. Migration SQL `20260526000000_phase7_fault_pcr/migration.sql` créée, appliquée, et listée par `prisma migrate status`
2. Schéma Prisma contient `faultProblem`, `faultCause`, `faultRemedy` sans plus aucune référence à `faultDescription`
3. Client Prisma régénéré expose les 3 nouveaux champs
4. Code Phase 6 (closure-requirements, work-orders action, fault form, detail, parts) compile et utilise `faultProblem`
5. Formulaire de panne propose 3 champs P/C/R + sélecteur de catégorie
6. Utilitaires `getPeriodStart`, `formatCurrency`, `isPeriod`, `PERIOD_LABELS` exportés depuis `src/lib/report-utils.ts` et testés
7. `npx tsc --noEmit` GREEN, tous les tests Vitest existants GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytique-de-fiabilit/07-01-SUMMARY.md` documenting:
- Migration SQL exacte appliquée + comment vérifier
- Nouveaux exports de `report-utils.ts` (signatures complètes)
- Nouvelle signature de `setWorkOrderFault` (4 champs au lieu de 3)
- Nouveaux props de `WorkOrderFaultForm` (initialProblem/Cause/Remedy)
- Confirmation que `faultDescription` n'existe plus nulle part dans src/
</output>
