---
phase: 07-analytique-de-fiabilit
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - src/components/rapports/planned-vs-real-tab.tsx
  - src/app/(app)/rapports/page.tsx
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - FIAB-04
must_haves:
  truths:
    - "Le tab Planifié vs Réel affiche 3 sous-onglets : Par BT | Par technicien | Par type d'actif (D-06)"
    - "Le sous-onglet actif est piloté par searchParams `?subtab=`"
    - "Pour chaque vue, le delta = realHours - estimatedHours où realHours = sum(timeLogs.minutes) / 60"
    - "Seuls les BTs avec estimatedHours non null et completedAt dans la période sont comptés"
    - "Si aucun BT avec estimatedHours, le tab affiche un message d'état vide actionnable"
    - "Un humain valide visuellement les 5 tabs de /rapports et signe le checkpoint"
  artifacts:
    - path: "src/components/rapports/planned-vs-real-tab.tsx"
      provides: "Server Component FIAB-04 avec 3 sous-onglets"
      exports: ["PlannedVsRealTab", "VALID_SUBTABS", "isSubTab", "type SubTab"]
  key_links:
    - from: "src/components/rapports/planned-vs-real-tab.tsx"
      to: "db.workOrder.findMany estimatedHours not null"
      via: "Prisma findMany avec select timeLogs.minutes + assignees + asset.category"
      pattern: "estimatedHours: { not: null }"
    - from: "src/app/(app)/rapports/page.tsx"
      to: "PlannedVsRealTab"
      via: "Remplacer placeholder Plan 02 par composant réel + transmettre subtab"
      pattern: "PlannedVsRealTab orgId={orgId} period={period} subtab={subtab}"
---

<objective>
Phase 7 — Plan 04 : Implémenter le rapport **Planifié vs Réel** (FIAB-04) avec ses 3 sous-onglets (D-06) — Par BT / Par technicien / Par type d'actif —, câbler le composant dans `/rapports`, et obtenir le checkpoint humain de validation visuelle de l'ensemble Phase 7.

Purpose: FIAB-04 permet aux gestionnaires d'identifier les sources de variance entre estimation et exécution. C'est le dernier rapport, donc le bon moment pour vérifier visuellement l'intégralité de l'analytique Phase 7.

Output: Tab Planifié vs Réel fonctionnel avec 3 sous-vues, validation visuelle humaine complète, ROADMAP.md mise à jour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-analytique-de-fiabilit/07-CONTEXT.md
@.planning/phases/07-analytique-de-fiabilit/07-RESEARCH.md
@.planning/phases/07-analytique-de-fiabilit/07-01-SUMMARY.md
@.planning/phases/07-analytique-de-fiabilit/07-02-SUMMARY.md
@src/lib/report-utils.ts
@src/components/rapports/mttr-tab.tsx
@src/components/rapports/top-faults-tab.tsx
@src/components/ui/tabs.tsx
@src/app/(app)/rapports/page.tsx
@prisma/schema.prisma

<interfaces>
<!-- Contrats consommés -->

Depuis src/lib/report-utils.ts (Plan 01):
```typescript
export function getPeriodStart(period: Period): Date
export const PERIOD_LABELS: Record<Period, string>
export type Period
```

Depuis prisma/schema.prisma (Plan 01):
```prisma
model WorkOrder {
  id              String
  number          Int
  title           String
  organizationId  String
  estimatedHours  Float?
  completedAt     DateTime?
  type            WorkOrderType
  assetId         String?
  asset           Asset?      // Asset.categoryId nullable, Asset.category AssetCategory?
  assignees       WorkOrderAssignee[]
  timeLogs        WorkOrderTimeLog[]
}
model WorkOrderAssignee {
  membership Membership   // a id, firstName, lastName
}
// AssetCategory { id, name }
```

Signature à respecter (cohérente Plans 02/03):
```typescript
type Props = { orgId: string; period: Period; subtab: SubTab }
type SubTab = 'by-wo' | 'by-tech' | 'by-asset-type'
export async function PlannedVsRealTab(props: Props): Promise<JSX.Element>
```

Validation de subtab depuis page.tsx (à étendre):
```typescript
export const VALID_SUBTABS = ['by-wo', 'by-tech', 'by-asset-type'] as const
export type SubTab = typeof VALID_SUBTABS[number]
export function isSubTab(value: string | undefined): value is SubTab
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer PlannedVsRealTab avec 3 sous-onglets (FIAB-04)</name>
  <files>src/components/rapports/planned-vs-real-tab.tsx</files>
  <read_first>
    - src/lib/report-utils.ts (getPeriodStart, PERIOD_LABELS, type Period)
    - src/components/rapports/mttr-tab.tsx (pattern Server Component + table + agrégation)
    - src/components/rapports/top-faults-tab.tsx (pattern badge + état vide)
    - src/components/ui/tabs.tsx (sous-onglets imbriqués)
    - prisma/schema.prisma (relations Asset.category, WorkOrderAssignee.membership)
  </read_first>
  <action>
    Créer `src/components/rapports/planned-vs-real-tab.tsx` comme Server Component avec 3 sous-vues pilotées par `subtab` :

    ```tsx
    import { db } from '@/lib/db'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Badge } from '@/components/ui/badge'
    import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'
    import { Target, TrendingUp, TrendingDown } from 'lucide-react'
    import { getPeriodStart, PERIOD_LABELS, type Period } from '@/lib/report-utils'

    export const VALID_SUBTABS = ['by-wo', 'by-tech', 'by-asset-type'] as const
    export type SubTab = typeof VALID_SUBTABS[number]
    export function isSubTab(value: string | undefined | null): value is SubTab {
      return typeof value === 'string' && (VALID_SUBTABS as readonly string[]).includes(value)
    }

    type Props = { orgId: string; period: Period; subtab: SubTab }

    type ByWo = {
      id: string
      number: number
      title: string
      estimatedHours: number
      realHours: number
      deltaHours: number
      deltaPct: number
    }
    type ByGroup = {
      key: string
      label: string
      btCount: number
      totalEstimated: number
      totalReal: number
      deltaHours: number
      deltaPct: number
    }

    function formatDelta(delta: number): { text: string; positive: boolean } {
      const sign = delta >= 0 ? '+' : ''
      return { text: `${sign}${delta.toFixed(2)} h`, positive: delta >= 0 }
    }

    export async function PlannedVsRealTab({ orgId, period, subtab }: Props) {
      const periodStart = getPeriodStart(period)

      // Une seule requête sert les 3 vues — on dispose des assignees et asset.category
      const orders = await db.workOrder.findMany({
        where: {
          organizationId: orgId,
          estimatedHours: { not: null },
          completedAt: { gte: periodStart },
        },
        select: {
          id: true,
          number: true,
          title: true,
          estimatedHours: true,
          type: true,
          asset: {
            select: {
              category: { select: { id: true, name: true } },
            },
          },
          assignees: {
            select: {
              membership: { select: { id: true, firstName: true, lastName: true, email: true } },
            },
          },
          timeLogs: { select: { minutes: true } },
        },
      })

      if (orders.length === 0) {
        return (
          <Card>
            <CardContent className="py-12 text-center">
              <Target className="h-10 w-10 mx-auto text-muted-foreground mb-3" />
              <p className="font-medium">Aucun bon de travail avec durée estimée sur la période</p>
              <p className="text-sm text-muted-foreground mt-1">
                Période : {PERIOD_LABELS[period]}. Renseignez le champ "Heures estimées" sur vos
                bons de travail pour activer cette comparaison.
              </p>
            </CardContent>
          </Card>
        )
      }

      // Calculer pour chaque BT : realHours, delta
      const enriched = orders.map(o => {
        const realMinutes = o.timeLogs.reduce((s, tl) => s + (tl.minutes ?? 0), 0)
        const realHours = realMinutes / 60
        const estimated = o.estimatedHours ?? 0
        const deltaHours = realHours - estimated
        const deltaPct = estimated > 0 ? (deltaHours / estimated) * 100 : 0
        return { ...o, realHours, deltaHours, deltaPct }
      })

      // Vue Par BT
      const byWo: ByWo[] = enriched
        .map(o => ({
          id: o.id,
          number: o.number,
          title: o.title,
          estimatedHours: o.estimatedHours ?? 0,
          realHours: o.realHours,
          deltaHours: o.deltaHours,
          deltaPct: o.deltaPct,
        }))
        .sort((a, b) => Math.abs(b.deltaHours) - Math.abs(a.deltaHours))
        .slice(0, 30)

      // Vue Par technicien : agrégation par membershipId (un BT avec N assignees compte pour chaque)
      const byTechMap = new Map<string, ByGroup>()
      for (const o of enriched) {
        for (const a of o.assignees) {
          const m = a.membership
          if (!m) continue
          const label = [m.firstName, m.lastName].filter(Boolean).join(' ') || m.email || m.id
          let entry = byTechMap.get(m.id)
          if (!entry) {
            entry = { key: m.id, label, btCount: 0, totalEstimated: 0, totalReal: 0, deltaHours: 0, deltaPct: 0 }
            byTechMap.set(m.id, entry)
          }
          entry.btCount += 1
          entry.totalEstimated += o.estimatedHours ?? 0
          entry.totalReal += o.realHours
        }
      }
      const byTech: ByGroup[] = Array.from(byTechMap.values()).map(g => ({
        ...g,
        deltaHours: g.totalReal - g.totalEstimated,
        deltaPct: g.totalEstimated > 0 ? ((g.totalReal - g.totalEstimated) / g.totalEstimated) * 100 : 0,
      })).sort((a, b) => Math.abs(b.deltaHours) - Math.abs(a.deltaHours))

      // Vue Par type d'actif : groupé par asset.category.id (BTs sans catégorie → bucket "Sans catégorie")
      const byCatMap = new Map<string, ByGroup>()
      for (const o of enriched) {
        const cat = o.asset?.category
        const key = cat?.id ?? '__none__'
        const label = cat?.name ?? "Sans catégorie d'actif"
        let entry = byCatMap.get(key)
        if (!entry) {
          entry = { key, label, btCount: 0, totalEstimated: 0, totalReal: 0, deltaHours: 0, deltaPct: 0 }
          byCatMap.set(key, entry)
        }
        entry.btCount += 1
        entry.totalEstimated += o.estimatedHours ?? 0
        entry.totalReal += o.realHours
      }
      const byCat: ByGroup[] = Array.from(byCatMap.values()).map(g => ({
        ...g,
        deltaHours: g.totalReal - g.totalEstimated,
        deltaPct: g.totalEstimated > 0 ? ((g.totalReal - g.totalEstimated) / g.totalEstimated) * 100 : 0,
      })).sort((a, b) => Math.abs(b.deltaHours) - Math.abs(a.deltaHours))

      return (
        <div className="space-y-4">
          <Tabs defaultValue={subtab}>
            <TabsList variant="line">
              <TabsTrigger value="by-wo" render={<a href={`?tab=planned-vs-real&period=${period}&subtab=by-wo`}>Par BT</a>} />
              <TabsTrigger value="by-tech" render={<a href={`?tab=planned-vs-real&period=${period}&subtab=by-tech`}>Par technicien</a>} />
              <TabsTrigger value="by-asset-type" render={<a href={`?tab=planned-vs-real&period=${period}&subtab=by-asset-type`}>Par type d'actif</a>} />
            </TabsList>

            <TabsContent value="by-wo" className="mt-4">
              <Card>
                <CardHeader>
                  <CardTitle className="text-base">Planifié vs Réel — Par BT ({PERIOD_LABELS[period]})</CardTitle>
                  <p className="text-xs text-muted-foreground mt-1">Top 30 BTs triés par écart absolu</p>
                </CardHeader>
                <CardContent>
                  <div className="overflow-x-auto">
                    <table className="w-full text-sm">
                      <thead>
                        <tr className="border-b">
                          <th className="text-left py-2 font-medium text-muted-foreground">N°</th>
                          <th className="text-left py-2 font-medium text-muted-foreground">Titre</th>
                          <th className="text-right py-2 font-medium text-muted-foreground">Estimé</th>
                          <th className="text-right py-2 font-medium text-muted-foreground">Réel</th>
                          <th className="text-right py-2 font-medium text-muted-foreground">Écart</th>
                          <th className="text-right py-2 font-medium text-muted-foreground">%</th>
                        </tr>
                      </thead>
                      <tbody>
                        {byWo.map(r => {
                          const d = formatDelta(r.deltaHours)
                          return (
                            <tr key={r.id} className="border-b last:border-0">
                              <td className="py-2 font-medium tabular-nums">#{r.number}</td>
                              <td className="py-2 truncate max-w-[40%]">{r.title}</td>
                              <td className="py-2 text-right tabular-nums">{r.estimatedHours.toFixed(1)} h</td>
                              <td className="py-2 text-right tabular-nums">{r.realHours.toFixed(1)} h</td>
                              <td className={`py-2 text-right font-bold tabular-nums ${d.positive ? 'text-red-600' : 'text-green-600'}`}>{d.text}</td>
                              <td className={`py-2 text-right tabular-nums ${d.positive ? 'text-red-600' : 'text-green-600'}`}>{r.deltaPct >= 0 ? '+' : ''}{r.deltaPct.toFixed(0)}%</td>
                            </tr>
                          )
                        })}
                      </tbody>
                    </table>
                  </div>
                </CardContent>
              </Card>
            </TabsContent>

            <TabsContent value="by-tech" className="mt-4">
              <GroupTable title={`Par technicien — ${PERIOD_LABELS[period]}`} rows={byTech} groupLabel="Technicien" />
            </TabsContent>

            <TabsContent value="by-asset-type" className="mt-4">
              <GroupTable title={`Par type d'actif — ${PERIOD_LABELS[period]}`} rows={byCat} groupLabel="Catégorie" />
            </TabsContent>
          </Tabs>
        </div>
      )
    }

    function GroupTable({ title, rows, groupLabel }: { title: string; rows: ByGroup[]; groupLabel: string }) {
      if (rows.length === 0) {
        return (
          <Card>
            <CardContent className="py-8 text-center text-sm text-muted-foreground">
              Aucune donnée à agréger pour cette vue.
            </CardContent>
          </Card>
        )
      }
      return (
        <Card>
          <CardHeader>
            <CardTitle className="text-base">{title}</CardTitle>
          </CardHeader>
          <CardContent>
            <div className="overflow-x-auto">
              <table className="w-full text-sm">
                <thead>
                  <tr className="border-b">
                    <th className="text-left py-2 font-medium text-muted-foreground">{groupLabel}</th>
                    <th className="text-right py-2 font-medium text-muted-foreground">BTs</th>
                    <th className="text-right py-2 font-medium text-muted-foreground">Total estimé</th>
                    <th className="text-right py-2 font-medium text-muted-foreground">Total réel</th>
                    <th className="text-right py-2 font-medium text-muted-foreground">Écart</th>
                    <th className="text-right py-2 font-medium text-muted-foreground">%</th>
                  </tr>
                </thead>
                <tbody>
                  {rows.map(r => {
                    const positive = r.deltaHours >= 0
                    const Icon = positive ? TrendingUp : TrendingDown
                    return (
                      <tr key={r.key} className="border-b last:border-0">
                        <td className="py-2 font-medium truncate max-w-[35%]">{r.label}</td>
                        <td className="py-2 text-right"><Badge variant="secondary">{r.btCount}</Badge></td>
                        <td className="py-2 text-right tabular-nums">{r.totalEstimated.toFixed(1)} h</td>
                        <td className="py-2 text-right tabular-nums">{r.totalReal.toFixed(1)} h</td>
                        <td className={`py-2 text-right font-bold tabular-nums ${positive ? 'text-red-600' : 'text-green-600'}`}>
                          <span className="inline-flex items-center gap-1">
                            <Icon className="h-3 w-3" />
                            {positive ? '+' : ''}{r.deltaHours.toFixed(2)} h
                          </span>
                        </td>
                        <td className={`py-2 text-right tabular-nums ${positive ? 'text-red-600' : 'text-green-600'}`}>{r.deltaPct >= 0 ? '+' : ''}{r.deltaPct.toFixed(0)}%</td>
                      </tr>
                    )
                  })}
                </tbody>
              </table>
            </div>
          </CardContent>
        </Card>
      )
    }
    ```

    Comportement clé :
    - `estimatedHours: { not: null }` (FIAB-04 — exclure BTs sans estimation)
    - `completedAt: { gte: periodStart }` (fenêtre temporelle)
    - 3 vues calculées depuis un même findMany (1 requête DB)
    - Par BT : top 30 par écart absolu
    - Par technicien : agrégation par `membership.id` via `assignees`
    - Par type d'actif : agrégation par `asset.category.id` (BTs sans catégorie → bucket "Sans catégorie d'actif")
    - Écarts positifs (réel > estimé = dépassement) en rouge, négatifs (réel < estimé = sous-utilisation) en vert
    - Exporte `VALID_SUBTABS`, `isSubTab`, `type SubTab` pour réutilisation dans page.tsx
  </action>
  <verify>
    <automated>test -f src/components/rapports/planned-vs-real-tab.tsx && grep -q "export async function PlannedVsRealTab" src/components/rapports/planned-vs-real-tab.tsx && grep -q "VALID_SUBTABS = \['by-wo', 'by-tech', 'by-asset-type'\]" src/components/rapports/planned-vs-real-tab.tsx && grep -q "estimatedHours: { not: null }" src/components/rapports/planned-vs-real-tab.tsx && grep -q "Aucun bon de travail avec durée estimée" src/components/rapports/planned-vs-real-tab.tsx && grep -q "export function isSubTab" src/components/rapports/planned-vs-real-tab.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function PlannedVsRealTab" src/components/rapports/planned-vs-real-tab.tsx` exit 0
    - `grep -q "export const VALID_SUBTABS" src/components/rapports/planned-vs-real-tab.tsx` exit 0
    - `grep -q "export function isSubTab" src/components/rapports/planned-vs-real-tab.tsx` exit 0
    - `grep -q "estimatedHours: { not: null }" src/components/rapports/planned-vs-real-tab.tsx` exit 0
    - `grep -q "completedAt: { gte: periodStart }" src/components/rapports/planned-vs-real-tab.tsx` exit 0
    - `grep -c "TabsTrigger" src/components/rapports/planned-vs-real-tab.tsx` === 3 (3 sous-onglets)
    - `grep -q "by-wo\|by-tech\|by-asset-type" src/components/rapports/planned-vs-real-tab.tsx` exit 0
    - `grep -q "Aucun bon de travail avec durée estimée" src/components/rapports/planned-vs-real-tab.tsx` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>PlannedVsRealTab affiche 3 sous-vues (BT/Tech/Type) avec calcul du delta estimé-réel, état vide actionnable, 1 seule requête Prisma.</done>
</task>

<task type="auto">
  <name>Task 2: Câbler PlannedVsRealTab + validation subtab dans rapports/page.tsx</name>
  <files>src/app/(app)/rapports/page.tsx</files>
  <read_first>
    - src/app/(app)/rapports/page.tsx (état après Plans 02 + 03 — chercher le placeholder `<TabsContent value="planned-vs-real">`)
    - src/components/rapports/planned-vs-real-tab.tsx (créé Task 1 — exports VALID_SUBTABS, isSubTab, type SubTab)
  </read_first>
  <action>
    Dans `src/app/(app)/rapports/page.tsx` :

    1. Ajouter import :
       ```typescript
       import { PlannedVsRealTab, isSubTab, type SubTab } from '@/components/rapports/planned-vs-real-tab'
       ```

    2. Ajouter une constante de défaut près des autres :
       ```typescript
       const DEFAULT_SUBTAB: SubTab = 'by-wo'
       ```

    3. Lire et valider le sous-tab depuis searchParams (après lecture du tab et period) :
       ```typescript
       const subtab: SubTab = isSubTab(sp.subtab) ? sp.subtab : DEFAULT_SUBTAB
       ```

    4. Remplacer le `<TabsContent value="planned-vs-real">` placeholder par :
       ```tsx
       <TabsContent value="planned-vs-real" className="mt-4">
         <PlannedVsRealTab orgId={orgId} period={period} subtab={subtab} />
       </TabsContent>
       ```

    5. NE PAS toucher aux autres TabsContent (overview, top-faults, mttr, cost — déjà câblés par Plans 02/03).

    Lancer `npx tsc --noEmit` — DOIT passer.
  </action>
  <verify>
    <automated>grep -q "import { PlannedVsRealTab, isSubTab, type SubTab }" src/app/(app)/rapports/page.tsx && grep -q "const subtab: SubTab" src/app/(app)/rapports/page.tsx && grep -q "isSubTab(sp.subtab)" src/app/(app)/rapports/page.tsx && grep -q "<PlannedVsRealTab orgId={orgId} period={period} subtab={subtab}" src/app/(app)/rapports/page.tsx && ! grep -q "Plan 04" src/app/(app)/rapports/page.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { PlannedVsRealTab" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -q "isSubTab(sp.subtab)" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -q "<PlannedVsRealTab orgId={orgId} period={period} subtab={subtab}" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -c "Plan 04\|Plan 03" src/app/(app)/rapports/page.tsx` === 0 (plus aucun placeholder)
    - `npx tsc --noEmit` exit 0
    - `npx next build` ne produit pas d'erreur sur /rapports
  </acceptance_criteria>
  <done>Tab Planifié vs Réel câblé avec son composant réel + validation `subtab` via searchParams. Plus aucun placeholder dans /rapports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humain — Validation visuelle des 5 tabs Phase 7</name>
  <files>src/app/(app)/rapports/page.tsx, src/components/rapports/*.tsx</files>
  <read_first>
    - src/app/(app)/rapports/page.tsx (page complète post Task 2)
    - src/components/rapports/planned-vs-real-tab.tsx (composant créé Task 1)
  </read_first>
  <action>
    [CHECKPOINT HUMAIN — Validation visuelle bloquante avant clôture Phase 7]

    Pause l'exécution et présente à l'utilisateur l'inventaire suivant + procédure de test :

    **Ce qui a été construit (Phase 7 complète, Plans 01-04) :**
    - Tab "Vue générale" : contenu Phase 6 préservé (KPIs, statuts, priorités, top actifs, stock faible)
    - Tab "Top pannes" : FIAB-01 — top 10 pannes groupées par (Problème + Catégorie) sur la période
    - Tab "MTTR" : FIAB-02 — MTTR (heures) par actif top 20 sur BTs correctifs
    - Tab "Coût actifs" : FIAB-03 — coût main-d'œuvre + pièces par actif top 20 avec banner taux manquant
    - Tab "Planifié vs Réel" : FIAB-04 — 3 sous-onglets (Par BT / Par technicien / Par type d'actif)
    - Sélecteur de période : Ce mois / 3 mois / 6 mois / Cette année (met à jour `?period=` dans l'URL)
    - Migration Prisma `20260526000000_phase7_fault_pcr` appliquée (faultDescription → faultProblem + faultCause + faultRemedy)
    - Formulaire de panne sur les BTs propose 3 champs P/C/R + sélecteur de catégorie

    **Procédure de test à exécuter par l'humain (12 vérifications) :**
    1. Démarrer le serveur : `npm run dev`
    2. Se connecter comme manager d'une org avec plan growth/enterprise (au besoin god mode)
    3. Aller à `/rapports` : doit afficher 5 tabs + un sélecteur de période à droite de l'entête
    4. Tab "Vue générale" : contenu identique à l'ancienne page (4 KPI cards, distributions statut/priorité, Plans de maintenance, Top actifs, Pièces stock faible si pertinent)
    5. Tab "Top pannes" : si des BTs ont un `faultProblem` non null clôturés sur la période, groupement (Problème + Catégorie) trié par occurrence ; sinon état vide "Aucun code de panne saisi sur la période"
    6. Tab "MTTR" : si BTs correctifs avec timeLogs sur la période, table top 20 par MTTR décroissant ; sinon "Aucune session de temps enregistrée sur des BTs correctifs"
    7. Tab "Coût actifs" : si pièces/temps existent, table top 20 par total décroissant ; si technicien sans `hourlyRate` a contribué, banner amber d'avertissement + badge "taux manquant" sur la ligne actif
    8. Tab "Planifié vs Réel" : vérifier 3 sous-onglets (Par BT / Par technicien / Par type d'actif), URL change avec `?subtab=`, table mise à jour ; sinon état vide
    9. Formulaire P/C/R : ouvrir un BT non clôturé, voir 3 champs Problème/Cause/Remède + sélecteur catégorie, saisir + sauver + recharger → persistance OK
    10. Multi-tenancy : se connecter à une autre org, aucune donnée org 1 ne fuit
    11. Plan starter : se connecter avec user starter sur `/rapports` → écran UpgradeGate, aucune donnée de rapport ne fuit
    12. Changer "Ce mois" → "Cette année" via sélecteur → URL `?period=year` → données mises à jour partout

    NE PAS marquer la phase comme complète tant que l'utilisateur n'a pas répondu "approved" ou listé les problèmes spécifiques.
    Si problèmes : capturer exactement quel tab/sous-tab, comportement attendu vs observé. La phase repassera en planning de gaps.
  </action>
  <verify>
    <automated>echo "Manual verification — see <action> for the 12 steps. Resume after user responds 'approved' or lists issues."; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - L'utilisateur répond "approved" après avoir parcouru les 12 étapes
    - OU l'utilisateur liste les problèmes EXACTS (tab/sous-tab concerné, comportement attendu, observé) → déclenche gaps closure planning
    - L'utilisateur a explicitement vérifié les 5 tabs visuellement
    - L'utilisateur a explicitement vérifié le formulaire P/C/R sur un BT existant
  </acceptance_criteria>
  <done>Checkpoint humain signé "approved" — Phase 7 visuellement validée par l'humain. Sinon, problèmes documentés pour gaps closure.</done>
</task>

<task type="auto">
  <name>Task 4: Mettre à jour ROADMAP.md Phase 7</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (chercher la section "Phase 7" entre les lignes 158-170)
  </read_first>
  <action>
    Dans `.planning/ROADMAP.md`, mettre à jour la section Phase 7 (après "**Requirements:** FIAB-01, FIAB-02, FIAB-03, FIAB-04" et avant le séparateur `---`) :

    AJOUTER (s'ils n'existent pas déjà) — placer ces lignes après les "Success criteria" et avant le `---` final de la section Phase 7 :

    ```
    **Plans:** 4 plans

    Plans:
    - [x] 07-01-PLAN.md — Migration schema P/C/R + utilitaires rapports + fix Phase 6 callers (Wave 1)
    - [x] 07-02-PLAN.md — Restructuration /rapports en tabs + period-selector + FIAB-01 Top pannes (Wave 2)
    - [x] 07-03-PLAN.md — FIAB-02 MTTR par actif + FIAB-03 Coût par actif (Wave 3)
    - [x] 07-04-PLAN.md — FIAB-04 Planifié vs Réel + checkpoint humain (Wave 3)
    ```

    NE PAS modifier les autres sections (Phases 1-6 ou 8-9).
    NE PAS modifier le tableau Milestone 3 ligne 112 (les critères de succès y restent inchangés).
  </action>
  <verify>
    <automated>grep -q "07-01-PLAN.md.*Migration schema P/C/R" .planning/ROADMAP.md && grep -q "07-02-PLAN.md" .planning/ROADMAP.md && grep -q "07-03-PLAN.md" .planning/ROADMAP.md && grep -q "07-04-PLAN.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "07-0[1-4]-PLAN.md" .planning/ROADMAP.md` === 4
    - `grep -q "FIAB-01, FIAB-02, FIAB-03, FIAB-04" .planning/ROADMAP.md` exit 0 (préservé)
  </acceptance_criteria>
  <done>ROADMAP.md liste les 4 plans Phase 7. La phase est traçable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (URL ?subtab=) | Validé par `isSubTab()` whitelist 3 valeurs |
| server→PostgreSQL | Une seule findMany filtrée par `organizationId` |
| Données RH (firstName/lastName via assignees) | Affichées dans la vue Par technicien (multi-tenancy strict) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | `searchParams.subtab` | mitigate | Validation `isSubTab()` — whitelist exact ['by-wo', 'by-tech', 'by-asset-type'] avec fallback `DEFAULT_SUBTAB`. Pas de chaîne arbitraire injectée dans Prisma. |
| T-07-04-02 | Information Disclosure | Vue "Par technicien" affiche firstName/lastName/email | mitigate | (a) `requirePlan(['growth', 'enterprise'])` gate la page (hérité Plan 02). (b) Multi-tenancy strict via `organizationId: orgId` — un manager ne voit que les techniciens de son org. (c) Pas de salaire affiché ici (seulement nom + écart heures). |
| T-07-04-03 | Information Disclosure | Liste BTs (titre, numéro) cross-tenant | mitigate | `where: { organizationId: orgId }` strict sur la findMany. Vérifier par grep que cette clause existe. |
| T-07-04-04 | Denial of Service | Findmany sur 5000+ BTs avec assignees + timeLogs imbriqués | accept | Hypothèse A3 RESEARCH.md — < 10k BTs/org. Les select sont sliminés (id/number/title + minutes only). Pas de mitigation Phase 7. |
| T-07-04-05 | Repudiation | Manager altère artificiellement les estimations pour cacher les retards | accept | Pas de log d'audit Phase 7. Acceptable car usage interne et les `estimatedHours` sont par BT (modifications visibles dans l'historique du BT en Phase 8). |
</threat_model>

<verification>
- `npx tsc --noEmit` GREEN sur tout le projet
- `npx next build` retourne 0 sans erreur
- Checkpoint humain (Task 3) signé "approved" par l'utilisateur
- Les 5 tabs de `/rapports` rendent du contenu (réel ou état vide actionnable)
- ROADMAP.md liste les 4 plans Phase 7 cochés
</verification>

<success_criteria>
1. Tab "Planifié vs Réel" affiche 3 sous-onglets : Par BT | Par technicien | Par type d'actif (D-06)
2. Chaque sous-onglet calcule correctement : `delta = realHours - estimatedHours` où `realHours = sum(timeLogs.minutes) / 60`
3. Seuls les BTs avec `estimatedHours` non null ET `completedAt` dans la période sont comptés
4. Vue Par BT : top 30 par écart absolu, rouge si dépassement, vert si sous-utilisation
5. Vue Par technicien : agrégation correcte quand un BT a plusieurs assignees
6. Vue Par type d'actif : agrégation par `asset.category.id`, bucket "Sans catégorie d'actif" pour BTs sans cat
7. État vide actionnable si aucun BT avec estimation
8. Checkpoint humain validé "approved" — les 5 tabs fonctionnent visuellement et le formulaire P/C/R sauvegarde
9. ROADMAP.md mise à jour avec les 4 plans Phase 7 cochés
10. Aucune référence à `faultDescription` ne subsiste dans la codebase (vérifié par grep récursif)
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytique-de-fiabilit/07-04-SUMMARY.md` documenting:
- Requête Prisma de PlannedVsRealTab (1 findMany, 3 vues calculées en mémoire)
- Constantes exportées : VALID_SUBTABS, type SubTab, isSubTab()
- Résultats du checkpoint humain (problèmes identifiés ou "approved")
- Confirmation que la Phase 7 est complète : 4 plans, 4 requirements (FIAB-01 à FIAB-04), 5 tabs fonctionnels
</output>
