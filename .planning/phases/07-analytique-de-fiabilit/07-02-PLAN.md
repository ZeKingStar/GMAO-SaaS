---
phase: 07-analytique-de-fiabilit
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/(app)/rapports/page.tsx
  - src/components/rapports/period-selector.tsx
  - src/components/rapports/top-faults-tab.tsx
  - src/components/rapports/overview-tab.tsx
autonomous: true
requirements:
  - FIAB-01
must_haves:
  truths:
    - "La page /rapports affiche 5 tabs : Vue générale | Top pannes | MTTR | Coût actifs | Planifié vs Réel"
    - "Le sélecteur de période propose Ce mois / 3 mois / 6 mois / Cette année et met à jour l'URL via ?period="
    - "Le tab Vue générale conserve le contenu actuel (KPIs, statuts, priorités, top actifs, stock faible)"
    - "Le tab Top pannes affiche les pannes groupées par (faultProblem, faultCategory) triées par occurrence décroissante"
    - "Si aucun BT n'a de faultProblem sur la période, le tab Top pannes affiche un message d'état vide actionnable"
    - "Les tabs MTTR/Coût/Planifié vs Réel affichent un placeholder 'Bientôt disponible' (rempli par Plans 03 et 04)"
  artifacts:
    - path: "src/app/(app)/rapports/page.tsx"
      provides: "Page rapports refactorisée avec Tabs + searchParams"
      contains: "TabsList"
    - path: "src/components/rapports/period-selector.tsx"
      provides: "Client Component sélecteur de période"
      exports: ["PeriodSelector"]
    - path: "src/components/rapports/top-faults-tab.tsx"
      provides: "Server Component rapport FIAB-01"
      exports: ["TopFaultsTab"]
    - path: "src/components/rapports/overview-tab.tsx"
      provides: "Server Component extrait du contenu actuel /rapports"
      exports: ["OverviewTab"]
  key_links:
    - from: "src/app/(app)/rapports/page.tsx"
      to: "searchParams.period et searchParams.tab"
      via: "await searchParams (Next.js 16 App Router)"
      pattern: "await searchParams"
    - from: "src/components/rapports/period-selector.tsx"
      to: "Next.js router.push"
      via: "useRouter() + URLSearchParams"
      pattern: "router.push"
    - from: "src/components/rapports/top-faults-tab.tsx"
      to: "db.workOrder.groupBy by faultProblem + faultCategory"
      via: "Prisma agrégation"
      pattern: "by: \\['faultProblem', 'faultCategory'\\]"
---

<objective>
Phase 7 — Plan 02 : Restructurer la page `/rapports` en navigation tabs (D-04), ajouter le sélecteur de période (D-05) qui pilote tous les rapports via `?period=` dans l'URL, extraire le contenu actuel dans `OverviewTab`, et implémenter le tab **Top pannes récurrentes** (FIAB-01, D-03) qui groupe par `faultProblem` + `faultCategory`.

**Purpose:** FIAB-01 exige que les pannes saisies à la clôture (Plan 01) soient agrégées et visibles par les gestionnaires. La structure tabs + period-selector établie ici est consommée par les Plans 03 et 04.

**Output:** Page /rapports avec 5 tabs fonctionnels — Vue générale (existant) + Top pannes (FIAB-01) en GREEN, 3 placeholders pour MTTR/Coût/Planifié remplis en Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-analytique-de-fiabilit/07-CONTEXT.md
@.planning/phases/07-analytique-de-fiabilit/07-RESEARCH.md
@.planning/phases/07-analytique-de-fiabilit/07-01-SUMMARY.md
@src/app/(app)/rapports/page.tsx
@src/components/ui/tabs.tsx
@src/components/ui/select.tsx
@src/lib/report-utils.ts
@src/lib/auth.ts

<interfaces>
<!-- Contrats de Plan 01 (Wave 1) que ce plan consomme -->

Depuis src/lib/report-utils.ts (créé par Plan 01) :
```typescript
export const PERIOD_VALUES: readonly ['month', '3m', '6m', 'year']
export type Period = 'month' | '3m' | '6m' | 'year'
export const PERIOD_LABELS: Record<Period, string>
export const DEFAULT_PERIOD: Period
export function getPeriodStart(period: Period): Date
export function isPeriod(value: string | undefined | null): value is Period
```

Depuis src/components/ui/tabs.tsx (existant, base-ui) :
```typescript
// Pattern d'usage :
<Tabs defaultValue="overview" value={activeTab} onValueChange={...}>
  <TabsList>
    <TabsTrigger value="overview">Vue générale</TabsTrigger>
    ...
  </TabsList>
  <TabsContent value="overview">...</TabsContent>
</Tabs>
```

Depuis src/lib/auth.ts (existant) :
```typescript
export async function requirePlan(plans: SubscriptionPlan[]): Promise<{ membership, hasAccess }>
```

Depuis prisma/schema.prisma (post-migration Plan 01) :
```prisma
model WorkOrder {
  faultCategory  String?
  faultProblem   String?   // ← renommé depuis faultDescription
  faultCause     String?   // ← nouveau
  faultRemedy    String?   // ← nouveau
  completedAt    DateTime?
  organizationId String
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer PeriodSelector (Client Component)</name>
  <files>src/components/rapports/period-selector.tsx</files>
  <read_first>
    - src/lib/report-utils.ts (signatures Period, PERIOD_VALUES, PERIOD_LABELS depuis Plan 01)
    - src/components/ui/select.tsx (composant Select disponible)
    - src/components/ui/tabs.tsx (référence pour le pattern Client Component du projet)
  </read_first>
  <action>
    Créer `src/components/rapports/period-selector.tsx` avec EXACTEMENT cette structure :

    ```tsx
    'use client'

    import { useRouter, usePathname, useSearchParams } from 'next/navigation'
    import { useTransition } from 'react'
    import { PERIOD_VALUES, PERIOD_LABELS, DEFAULT_PERIOD, type Period } from '@/lib/report-utils'
    import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'

    type Props = { value: Period }

    export function PeriodSelector({ value }: Props) {
      const router = useRouter()
      const pathname = usePathname()
      const searchParams = useSearchParams()
      const [isPending, startTransition] = useTransition()

      function onChange(next: string) {
        const params = new URLSearchParams(searchParams.toString())
        if (next === DEFAULT_PERIOD) {
          params.delete('period')
        } else {
          params.set('period', next)
        }
        const query = params.toString()
        startTransition(() => {
          router.push(query ? `${pathname}?${query}` : pathname)
        })
      }

      return (
        <Select value={value} onValueChange={onChange} disabled={isPending}>
          <SelectTrigger className="w-[180px]" aria-label="Période d'analyse">
            <SelectValue />
          </SelectTrigger>
          <SelectContent>
            {PERIOD_VALUES.map((p) => (
              <SelectItem key={p} value={p}>
                {PERIOD_LABELS[p]}
              </SelectItem>
            ))}
          </SelectContent>
        </Select>
      )
    }
    ```

    NE PAS modifier `pathname` directement, TOUJOURS passer par `router.push` (Next.js 16 App Router).
    NE PAS utiliser `useState` pour la valeur — la source de vérité est l'URL (prop `value` venant du serveur).
  </action>
  <verify>
    <automated>test -f src/components/rapports/period-selector.tsx && grep -q "'use client'" src/components/rapports/period-selector.tsx && grep -q "useRouter" src/components/rapports/period-selector.tsx && grep -q "PeriodSelector" src/components/rapports/period-selector.tsx && grep -q "PERIOD_VALUES" src/components/rapports/period-selector.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^'use client'" src/components/rapports/period-selector.tsx` exit 0 (première ligne)
    - `grep -q "export function PeriodSelector" src/components/rapports/period-selector.tsx` exit 0
    - `grep -q "from '@/lib/report-utils'" src/components/rapports/period-selector.tsx` exit 0
    - `grep -q "router.push" src/components/rapports/period-selector.tsx` exit 0
    - `grep -q "useTransition" src/components/rapports/period-selector.tsx` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Sélecteur de période fonctionnel : changement de valeur → URL mise à jour → rerender server.</done>
</task>

<task type="auto">
  <name>Task 2: Extraire OverviewTab depuis page.tsx actuel</name>
  <files>src/components/rapports/overview-tab.tsx</files>
  <read_first>
    - src/app/(app)/rapports/page.tsx (page actuelle complète — l'intégralité du contenu après requirePlan devient OverviewTab)
  </read_first>
  <action>
    Créer `src/components/rapports/overview-tab.tsx` comme **Server Component** (pas de 'use client') qui contient l'INTÉGRALITÉ du contenu UI actuel de /rapports/page.tsx (lignes 51-365 environ).

    Signature :
    ```tsx
    import { db } from '@/lib/db'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Badge } from '@/components/ui/badge'
    import { ClipboardList, CheckCircle2, Clock, Package, Calendar, AlertTriangle } from 'lucide-react'

    type Props = { orgId: string }

    export async function OverviewTab({ orgId }: Props) {
      // ... TOUT le contenu actuel de la page (Promise.all, KPIs, distribution charts, etc.)
      // Adapté pour recevoir orgId en prop au lieu de l'extraire de requirePlan().
      // Le return JSX reproduit EXACTEMENT le contenu actuel SANS le wrapper UpgradeGate ni le titre H1.
    }
    ```

    Étapes :
    1. Copier les constantes `statusLabel`, `statusColor`, `priorityLabel`, `priorityColor`, `statusOrder`, `priorityOrder` (lignes 11-42) DANS overview-tab.tsx — ces constantes restent locales à ce composant.
    2. Copier les requêtes Promise.all (lignes 51-106 — workOrdersByStatus, workOrdersByPriority, workOrdersThisMonth, completedThisMonth, totalTimeLogs, maintenancePlansTotal, activePlansCount, spareParts, topAssetGroups) en utilisant `orgId` depuis les props.
    3. Copier le traitement des données (lignes 108-139) : assetIds, assetNames, topAssets, activeWorkOrders, totalWorkOrders, resolutionRate, totalHours, lowStockParts, maxStatusCount, maxPriorityCount, sortedStatuses, sortedPriorities.
    4. Copier le JSX retourné (lignes 141-365) — SANS le wrapper `<UpgradeGate>` et SANS le bloc titre `<div><h1>...</h1>...</div>` (lignes 142-149). Conserver uniquement le contenu interne de `<div className="p-6 max-w-6xl mx-auto space-y-6">` — mais sans ce div parent (le parent sera dans page.tsx).
    5. Le composant retourne directement `<div className="space-y-6">{kpis}{distributionCharts}{maintenancePlansAndTopAssets}{lowStockTable}</div>`.

    NE PAS ajouter de nouveaux KPI. Strict equivalent fonctionnel du contenu actuel.
  </action>
  <verify>
    <automated>test -f src/components/rapports/overview-tab.tsx && grep -q "export async function OverviewTab" src/components/rapports/overview-tab.tsx && grep -q "workOrdersByStatus" src/components/rapports/overview-tab.tsx && grep -q "lowStockParts" src/components/rapports/overview-tab.tsx && ! grep -q "'use client'" src/components/rapports/overview-tab.tsx && ! grep -q "UpgradeGate" src/components/rapports/overview-tab.tsx && ! grep -q "<h1>" src/components/rapports/overview-tab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function OverviewTab" src/components/rapports/overview-tab.tsx` exit 0
    - `grep -q "type Props = { orgId: string }" src/components/rapports/overview-tab.tsx` exit 0
    - `grep -q "workOrdersByStatus" src/components/rapports/overview-tab.tsx` exit 0
    - `grep -q "topAssetGroups" src/components/rapports/overview-tab.tsx` exit 0
    - `grep -q "lowStockParts" src/components/rapports/overview-tab.tsx` exit 0
    - `grep -c "'use client'" src/components/rapports/overview-tab.tsx` === 0 (Server Component)
    - `grep -c "UpgradeGate" src/components/rapports/overview-tab.tsx` === 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>OverviewTab encapsule l'intégralité du contenu actuel /rapports comme Server Component prenant orgId en prop.</done>
</task>

<task type="auto">
  <name>Task 3: Créer TopFaultsTab (FIAB-01)</name>
  <files>src/components/rapports/top-faults-tab.tsx</files>
  <read_first>
    - src/lib/report-utils.ts (getPeriodStart, formatCurrency depuis Plan 01)
    - src/lib/closure-requirements.ts (FAULT_CATEGORY_LABELS pour libellés français)
    - src/app/(app)/rapports/page.tsx (pattern pour les barres CSS de progression, lignes 222-227)
    - prisma/schema.prisma (modèle WorkOrder confirmé post-Plan 01)
  </read_first>
  <action>
    Créer `src/components/rapports/top-faults-tab.tsx` comme Server Component implémentant FIAB-01 (D-03) :

    ```tsx
    import { db } from '@/lib/db'
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
    import { Badge } from '@/components/ui/badge'
    import { AlertCircle } from 'lucide-react'
    import { getPeriodStart, PERIOD_LABELS, type Period } from '@/lib/report-utils'
    import { FAULT_CATEGORY_LABELS, type FaultCategory } from '@/lib/closure-requirements'

    type Props = { orgId: string; period: Period }

    const CATEGORY_COLORS: Record<string, string> = {
      mecanique: 'bg-blue-500',
      electrique: 'bg-amber-500',
      hydraulique: 'bg-cyan-500',
      autre: 'bg-gray-400',
    }

    export async function TopFaultsTab({ orgId, period }: Props) {
      const periodStart = getPeriodStart(period)

      // D-03 : grouper par faultProblem + faultCategory
      const groups = await db.workOrder.groupBy({
        by: ['faultProblem', 'faultCategory'],
        where: {
          organizationId: orgId,
          faultProblem: { not: null },
          completedAt: { gte: periodStart },
        },
        _count: { _all: true },
        orderBy: { _count: { _all: 'desc' } },
        take: 10,
      })

      const total = groups.reduce((sum, g) => sum + g._count._all, 0)
      const maxCount = Math.max(...groups.map(g => g._count._all), 1)

      if (groups.length === 0) {
        return (
          <Card>
            <CardContent className="py-12 text-center">
              <AlertCircle className="h-10 w-10 mx-auto text-muted-foreground mb-3" />
              <p className="font-medium">Aucun code de panne saisi sur la période</p>
              <p className="text-sm text-muted-foreground mt-1">
                Période : {PERIOD_LABELS[period]}. Activez la saisie obligatoire dans
                Paramètres &gt; Organisation &gt; Exigences de clôture pour collecter ces données.
              </p>
            </CardContent>
          </Card>
        )
      }

      return (
        <div className="space-y-4">
          <Card>
            <CardHeader>
              <CardTitle className="text-base">Top {groups.length} pannes récurrentes — {PERIOD_LABELS[period]}</CardTitle>
              <p className="text-xs text-muted-foreground mt-1">{total} bon(s) de travail clôturé(s) avec code de panne</p>
            </CardHeader>
            <CardContent className="space-y-3">
              {groups.map((g, i) => {
                const cat = g.faultCategory as FaultCategory | null
                const catLabel = cat ? FAULT_CATEGORY_LABELS[cat] ?? cat : 'Sans catégorie'
                const catColor = cat ? CATEGORY_COLORS[cat] ?? 'bg-gray-400' : 'bg-gray-400'
                return (
                  <div key={`${g.faultProblem}-${g.faultCategory}-${i}`} className="space-y-1">
                    <div className="flex items-center justify-between gap-2 text-sm">
                      <div className="flex items-center gap-2 min-w-0 flex-1">
                        <Badge variant="outline" className={`${catColor} text-white border-0 shrink-0`}>{catLabel}</Badge>
                        <span className="truncate font-medium">{g.faultProblem}</span>
                      </div>
                      <span className="font-bold tabular-nums shrink-0">{g._count._all}</span>
                    </div>
                    <div className="h-2 rounded-full bg-muted overflow-hidden">
                      <div
                        className={`h-full rounded-full transition-all ${catColor}`}
                        style={{ width: `${Math.round((g._count._all / maxCount) * 100)}%` }}
                      />
                    </div>
                  </div>
                )
              })}
            </CardContent>
          </Card>
        </div>
      )
    }
    ```

    Comportement clé :
    - groupBy par `faultProblem` + `faultCategory` (D-03 verrouillé)
    - Filtre `completedAt: { gte: periodStart }` (BTs clôturés dans la fenêtre)
    - Filtre `faultProblem: { not: null }` (seulement BTs avec panne renseignée)
    - take: 10 (top 10 pannes)
    - État vide : message actionnable pointant vers Paramètres > Exigences de clôture
    - Badge coloré par catégorie + barre de progression CSS pattern existant
  </action>
  <verify>
    <automated>test -f src/components/rapports/top-faults-tab.tsx && grep -q "by: \['faultProblem', 'faultCategory'\]" src/components/rapports/top-faults-tab.tsx && grep -q "faultProblem: { not: null }" src/components/rapports/top-faults-tab.tsx && grep -q "completedAt: { gte: periodStart }" src/components/rapports/top-faults-tab.tsx && grep -q "Aucun code de panne" src/components/rapports/top-faults-tab.tsx && grep -q "take: 10" src/components/rapports/top-faults-tab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export async function TopFaultsTab" src/components/rapports/top-faults-tab.tsx` exit 0
    - `grep -q "by: \['faultProblem', 'faultCategory'\]" src/components/rapports/top-faults-tab.tsx` exit 0
    - `grep -q "take: 10" src/components/rapports/top-faults-tab.tsx` exit 0
    - `grep -q "orderBy: { _count: { _all: 'desc' } }" src/components/rapports/top-faults-tab.tsx` exit 0
    - `grep -q "Aucun code de panne saisi" src/components/rapports/top-faults-tab.tsx` exit 0
    - `grep -q "FAULT_CATEGORY_LABELS" src/components/rapports/top-faults-tab.tsx` exit 0
    - `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>TopFaultsTab agrège les 10 pannes les plus fréquentes par (faultProblem, faultCategory) sur la période, avec état vide actionnable.</done>
</task>

<task type="auto">
  <name>Task 4: Refondre src/app/(app)/rapports/page.tsx avec Tabs + searchParams</name>
  <files>src/app/(app)/rapports/page.tsx</files>
  <read_first>
    - src/app/(app)/rapports/page.tsx (état actuel — sera entièrement remplacé)
    - src/components/rapports/overview-tab.tsx (créé en Task 2)
    - src/components/rapports/top-faults-tab.tsx (créé en Task 3)
    - src/components/rapports/period-selector.tsx (créé en Task 1)
    - src/components/ui/tabs.tsx (API base-ui — TabsList, TabsTrigger, TabsContent)
  </read_first>
  <action>
    Réécrire INTÉGRALEMENT `src/app/(app)/rapports/page.tsx` avec cette structure :

    ```tsx
    import type { Metadata } from 'next'
    import { requirePlan } from '@/lib/auth'
    import { UpgradeGate } from '@/components/upgrade-gate/upgrade-gate'
    import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs'
    import { Card, CardContent } from '@/components/ui/card'
    import { OverviewTab } from '@/components/rapports/overview-tab'
    import { TopFaultsTab } from '@/components/rapports/top-faults-tab'
    import { PeriodSelector } from '@/components/rapports/period-selector'
    import { DEFAULT_PERIOD, isPeriod, type Period } from '@/lib/report-utils'

    export const metadata: Metadata = { title: 'Rapports' }

    const VALID_TABS = ['overview', 'top-faults', 'mttr', 'cost', 'planned-vs-real'] as const
    type TabValue = typeof VALID_TABS[number]

    function isTab(value: string | undefined): value is TabValue {
      return typeof value === 'string' && (VALID_TABS as readonly string[]).includes(value)
    }

    type SearchParams = Promise<{ tab?: string; period?: string; subtab?: string }>

    export default async function RapportsPage({ searchParams }: { searchParams: SearchParams }) {
      const { membership, hasAccess } = await requirePlan(['growth', 'enterprise'])
      const orgId = membership.organization.id

      const sp = await searchParams
      const tab: TabValue = isTab(sp.tab) ? sp.tab : 'overview'
      const period: Period = isPeriod(sp.period) ? sp.period : DEFAULT_PERIOD

      return (
        <UpgradeGate hasAccess={hasAccess} requiredPlan="growth">
          <div className="p-6 max-w-6xl mx-auto space-y-6">
            <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
              <div>
                <h1 className="text-2xl font-bold">Rapports & Indicateurs</h1>
                <p className="text-muted-foreground text-sm mt-1">
                  Analytique de fiabilité — pannes, MTTR, coûts, planifié vs réel
                </p>
              </div>
              {tab !== 'overview' && <PeriodSelector value={period} />}
            </div>

            <Tabs defaultValue={tab}>
              <TabsList>
                <TabsTrigger value="overview" render={<a href="?tab=overview">Vue générale</a>} />
                <TabsTrigger value="top-faults" render={<a href={`?tab=top-faults&period=${period}`}>Top pannes</a>} />
                <TabsTrigger value="mttr" render={<a href={`?tab=mttr&period=${period}`}>MTTR</a>} />
                <TabsTrigger value="cost" render={<a href={`?tab=cost&period=${period}`}>Coût actifs</a>} />
                <TabsTrigger value="planned-vs-real" render={<a href={`?tab=planned-vs-real&period=${period}`}>Planifié vs Réel</a>} />
              </TabsList>

              <TabsContent value="overview" className="mt-4">
                <OverviewTab orgId={orgId} />
              </TabsContent>

              <TabsContent value="top-faults" className="mt-4">
                <TopFaultsTab orgId={orgId} period={period} />
              </TabsContent>

              <TabsContent value="mttr" className="mt-4">
                <Card><CardContent className="py-12 text-center text-sm text-muted-foreground">Rapport MTTR — disponible après livraison Plan 03</CardContent></Card>
              </TabsContent>

              <TabsContent value="cost" className="mt-4">
                <Card><CardContent className="py-12 text-center text-sm text-muted-foreground">Rapport Coût par actif — disponible après livraison Plan 03</CardContent></Card>
              </TabsContent>

              <TabsContent value="planned-vs-real" className="mt-4">
                <Card><CardContent className="py-12 text-center text-sm text-muted-foreground">Rapport Planifié vs Réel — disponible après livraison Plan 04</CardContent></Card>
              </TabsContent>
            </Tabs>
          </div>
        </UpgradeGate>
      )
    }
    ```

    **IMPORTANT — Composabilité base-ui Tabs:**
    Le composant TabsTrigger de base-ui supporte la prop `render` pour fournir un élément personnalisé (un `<a>` natif déclenche la navigation server). Si la prop `render` n'est PAS supportée dans la version installée de @base-ui/react, fallback :
    - Wrapper chaque TabsTrigger dans un `<a href="?tab=..." className="contents">` parent. Lire `node_modules/@base-ui/react/package.json` pour confirmer la version, et `node_modules/@base-ui/react/tabs/dist/index.d.ts` pour confirmer la prop `render`.
    - Si même le fallback `<a>` casse l'accessibilité, importer dynamiquement un Client Component wrapper qui appelle `router.push` au `onValueChange`.

    Tester la navigation manuellement : `curl -I "http://localhost:3000/rapports?tab=top-faults&period=3m"` doit retourner 200 (ou 307 vers Clerk si non auth). Ce n'est PAS un test automatisé bloquant — la vérification réelle se fait à l'étape `tsc --noEmit` et au checkpoint visuel du Plan 04.

    NE PAS dupliquer le contenu d'OverviewTab inline — tout passe par les composants extraits.
    NE PAS ajouter de bouton/badge tier inline — UpgradeGate gère déjà cela.
  </action>
  <verify>
    <automated>grep -q "import { OverviewTab }" src/app/(app)/rapports/page.tsx && grep -q "import { TopFaultsTab }" src/app/(app)/rapports/page.tsx && grep -q "import { PeriodSelector }" src/app/(app)/rapports/page.tsx && grep -q "await searchParams" src/app/(app)/rapports/page.tsx && grep -q "TabsList" src/app/(app)/rapports/page.tsx && grep -c "TabsTrigger" src/app/(app)/rapports/page.tsx | grep -q "^5$" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "await searchParams" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -q "isPeriod\|isTab" src/app/(app)/rapports/page.tsx` exit 0 (validation des inputs URL)
    - `grep -c "TabsTrigger" src/app/(app)/rapports/page.tsx` === 5 (5 tabs)
    - `grep -c "TabsContent" src/app/(app)/rapports/page.tsx` === 5
    - `grep -q "OverviewTab orgId={orgId}" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -q "TopFaultsTab orgId={orgId} period={period}" src/app/(app)/rapports/page.tsx` exit 0
    - `grep -q "requirePlan(\['growth', 'enterprise'\])" src/app/(app)/rapports/page.tsx` exit 0
    - `npx tsc --noEmit` exit 0
    - `npx next build` ne produit PAS d'erreur sur /rapports (warnings acceptables)
  </acceptance_criteria>
  <done>/rapports affiche 5 tabs, sélecteur de période fonctionnel, OverviewTab et TopFaultsTab rendus selon le tab actif. Les 3 autres tabs affichent un placeholder explicite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (URL searchParams) | Utilisateur authentifié pousse `?tab=` et `?period=` |
| server→PostgreSQL (groupBy WorkOrder) | Agrégation filtrée par organizationId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | searchParams.period et .tab | mitigate | Validation stricte via `isPeriod()` et `isTab()` — toute valeur non-whitelist tombe sur défaut (`'overview'`, `'month'`). Pas de SQL injection possible car le champ est typé et utilisé dans switch/case. |
| T-07-02-02 | Information Disclosure | TopFaultsTab — agrégation faultProblem | mitigate | `where: { organizationId: orgId }` (multi-tenancy strict) + `requirePlan(['growth', 'enterprise'])` gating sur la page parent. Un user starter ne peut pas atteindre le composant. |
| T-07-02-03 | Elevation of Privilege | Accès direct à /rapports?tab=top-faults sans plan growth | mitigate | `requirePlan` est appelé AVANT toute lecture de searchParams. `UpgradeGate` wrappe tout le contenu. Si `hasAccess === false`, le bloc UpgradeGate affiche l'écran d'upgrade et n'expose pas les données. |
| T-07-02-04 | Information Disclosure | faultProblem peut contenir du PII technique (nom de serveur, IP) | accept | Considéré opérationnel, pas de PII personnel. Accès limité aux managers/admins du tenant. |
</threat_model>

<verification>
- `npx tsc --noEmit` GREEN sur tout le projet
- `npx next build` retourne 0 sans erreur sur /rapports
- Navigation manuelle : ouvrir `/rapports` → tab par défaut "Vue générale" → contenu identique à avant Phase 7
- Clic sur "Top pannes" → URL devient `/rapports?tab=top-faults&period=month` → affiche les pannes ou l'état vide selon les données
- Changement de période → URL `?period=3m` → tab Top pannes re-fetch avec nouvelle fenêtre
- Clic sur MTTR/Coût/Planifié → affiche le placeholder "disponible après livraison Plan 03/04"
</verification>

<success_criteria>
1. Page /rapports affiche 5 tabs visuellement (Vue générale, Top pannes, MTTR, Coût actifs, Planifié vs Réel)
2. Le tab par défaut est "Vue générale" et son contenu est identique à l'ancienne page /rapports
3. Le tab "Top pannes" affiche le top 10 des `(faultProblem, faultCategory)` clôturés dans la période, triés par occurrence décroissante
4. Si aucun BT n'a `faultProblem` non null dans la période, le tab "Top pannes" affiche l'état vide actionnable
5. Le sélecteur de période propose Ce mois / 3 mois / 6 mois / Cette année et met à jour l'URL
6. Les tabs MTTR/Coût/Planifié vs Réel affichent un placeholder (rempli par Plans 03 et 04)
7. `requirePlan(['growth', 'enterprise'])` et `UpgradeGate` toujours en place — un user starter voit l'écran d'upgrade
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytique-de-fiabilit/07-02-SUMMARY.md` documenting:
- Structure tabs choisie (variant default ou line, navigation par lien `<a>` ou par Client Component)
- Comportement exact du `PeriodSelector` (Server Component prop value, push URL via `useTransition`)
- Requête Prisma exacte de `TopFaultsTab` (groupBy, where, orderBy, take)
- Contrats consommés par Plans 03/04 (signature des Server Components `props: { orgId, period }`)
</output>
