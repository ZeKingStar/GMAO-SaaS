---
phase: 999.2-invitation-membres-depuis-app
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/invitation-roles.ts
  - src/lib/invitation-roles.test.ts
  - src/actions/settings.ts
  - src/actions/settings.test.ts
autonomous: true
requirements:
  - D-06
  - D-07
  - D-03
  - D-04
  - D-05
user_setup: []

must_haves:
  truths:
    - "Un admin peut inviter un membre avec n'importe quel rôle (admin, manager, technician, requester, viewer)"
    - "Un manager ne peut PAS inviter un admin (rôle supérieur refusé côté serveur)"
    - "Une invitation avec un email vide ou invalide est refusée avant tout appel Clerk"
    - "Seuls admin/manager peuvent inviter ou révoquer (getAdminOrg guard)"
    - "La liste des invitations en attente est lisible côté serveur, filtrée sur status pending"
  artifacts:
    - path: "src/lib/invitation-roles.ts"
      provides: "ROLE_ORDER + getAvailableRoles(currentRole) + canInviteWithRole(currentRole, target)"
      exports: ["getAvailableRoles", "canInviteWithRole", "ROLE_ORDER"]
    - path: "src/lib/invitation-roles.test.ts"
      provides: "Tests unitaires du scoping de rôles (D-06)"
      contains: "getAvailableRoles"
    - path: "src/actions/settings.ts"
      provides: "inviteMember, revokeInvitation, listPendingInvitations"
      exports: ["inviteMember", "revokeInvitation", "listPendingInvitations"]
    - path: "src/actions/settings.test.ts"
      provides: "Tests unitaires inviteMember (happy/email invalide/rôle escalade) + revokeInvitation (happy/non autorisé)"
      contains: "inviteMember"
  key_links:
    - from: "src/actions/settings.ts inviteMember"
      to: "src/lib/invitation-roles.ts canInviteWithRole"
      via: "import + appel avant createOrganizationInvitation"
      pattern: "canInviteWithRole"
    - from: "src/actions/settings.ts inviteMember"
      to: "clerkClient().organizations.createOrganizationInvitation"
      via: "await clerkClient() puis .organizations"
      pattern: "createOrganizationInvitation"
---

<objective>
Créer la couche logique serveur de la fonctionnalité d'invitation de membres : un utilitaire de scoping de rôles (`src/lib/invitation-roles.ts`) et trois server actions dans `src/actions/settings.ts` (`inviteMember`, `revokeInvitation`, `listPendingInvitations`), le tout couvert par des tests Vitest écrits AVANT l'implémentation (TDD, Wave 0 puis GREEN).

Purpose: Toute la logique de sécurité (D-06 scoping de rôles, validation email, guard admin/manager) DOIT vivre côté serveur, pas seulement dans l'UI. Un appel direct à la server action ne doit jamais permettre d'escalader un rôle ou d'inviter sans droits.

Output:
- `src/lib/invitation-roles.ts` + `src/lib/invitation-roles.test.ts`
- 3 nouvelles fonctions exportées dans `src/actions/settings.ts` + `src/actions/settings.test.ts`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-invitation-membres-depuis-app/999.2-CONTEXT.md
@.planning/phases/999.2-invitation-membres-depuis-app/999.2-RESEARCH.md
@.planning/phases/999.2-invitation-membres-depuis-app/999.2-VALIDATION.md

<interfaces>
<!-- Contrats clés. Extraits du codebase. Utiliser directement, ne pas réexplorer. -->

Pattern getAdminOrg() existant — src/actions/settings.ts (lignes 10-26). À RÉUTILISER tel quel.
Il appelle auth(), vérifie que le membre est admin OU manager, et retourne l'UUID Prisma `org.id`.
ATTENTION: getAdminOrg() retourne l'UUID Prisma, PAS le Clerk orgId. Les appels Clerk Backend
nécessitent le Clerk `orgId` (format `org_xxx`) obtenu via `auth()`.

Enum MemberRole — src/generated/prisma/enums.ts :
  type MemberRole = 'admin' | 'manager' | 'technician' | 'requester' | 'viewer'

Signatures Clerk Backend v7 (VERIFIED dans RESEARCH.md) :
  // clerkClient est une FONCTION ASYNC en v7 — toujours `const client = await clerkClient()`
  import { clerkClient } from '@clerk/nextjs/server'

  client.organizations.createOrganizationInvitation({
    organizationId: string,   // = Clerk orgId de auth()
    emailAddress: string,
    role: string,             // voir note format rôle ci-dessous
    inviterUserId?: string,
  }): Promise<OrganizationInvitation>

  client.organizations.getOrganizationInvitationList({
    organizationId: string,
    status?: ('pending'|'accepted'|'revoked'|'expired')[],
  }): Promise<{ data: OrganizationInvitation[] }>

  client.organizations.revokeOrganizationInvitation({
    organizationId: string,
    invitationId: string,
    requestingUserId?: string,
  }): Promise<OrganizationInvitation>

  OrganizationInvitation : { id, emailAddress, role, roleName, organizationId, createdAt: number (ms), status }

Infra test — vitest.config.ts : setupFiles ['./src/test/setup.ts'], environment jsdom, globals true.
src/test/setup.ts ne mocke PAS clerkClient ni auth → mock à faire au niveau du fichier de test.
Commande rapide : `npx vitest run src/actions/settings.test.ts`
</interfaces>

<role_format_note>
FORMAT DU RÔLE CLERK (Pitfall 3 de RESEARCH.md — ASSUMED, à confirmer en dev) :
Clerk attend possiblement le préfixe `org:` (ex: `org:technician`). Le projet utilise les valeurs
MemberRole Prisma sans préfixe. Pour gérer les deux cas SANS coder en dur une hypothèse fausse :
- Créer dans invitation-roles.ts un helper `toClerkRole(role: MemberRole): string` qui retourne `org:${role}`.
- Dans inviteMember, passer `toClerkRole(role)` à Clerk.
- Si l'instance Clerk Korvia n'utilise PAS le préfixe, ce sera corrigé en une ligne dans toClerkRole.
- Documenter ce choix dans le SUMMARY pour vérification manuelle en dev (voir Assumptions A1/A2 RESEARCH.md).
</role_format_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0 RED): Tests du scoping de rôles invitation-roles.test.ts</name>
  <read_first>
    - src/components/settings/team-table.tsx (voir ROLE_LABELS/ROLE_VARIANTS et l'ordre admin→viewer)
    - src/generated/prisma/enums.ts (valeurs exactes de MemberRole)
    - src/test/setup.ts (infra de test existante)
    - .planning/phases/999.2-invitation-membres-depuis-app/999.2-RESEARCH.md (section "Filtrage des rôles selon D-06")
  </read_first>
  <behavior>
    Fonctions testées (à implémenter en Task 1) dans src/lib/invitation-roles.ts :
    - ROLE_ORDER = ['admin','manager','technician','requester','viewer'] (du plus puissant au moins puissant)
    - getAvailableRoles(currentRole): MemberRole[] → renvoie les rôles de rang ≤ courant (slice à partir de l'index courant)
    - canInviteWithRole(currentRole, target): boolean → true si index(target) >= index(currentRole)
    - toClerkRole(role): string → `org:${role}`

    Cas de test exacts :
    - Test 1: getAvailableRoles('admin') retourne ['admin','manager','technician','requester','viewer'] (5 rôles)
    - Test 2: getAvailableRoles('manager') retourne ['manager','technician','requester','viewer'] (4 rôles, PAS admin)
    - Test 3: getAvailableRoles('technician') retourne ['technician','requester','viewer'] (3 rôles)
    - Test 4: canInviteWithRole('manager','admin') === false (escalade refusée, D-06)
    - Test 5: canInviteWithRole('manager','technician') === true
    - Test 6: canInviteWithRole('admin','admin') === true
    - Test 7: toClerkRole('technician') === 'org:technician'
  </behavior>
  <action>
    Créer le fichier src/lib/invitation-roles.test.ts. Importer depuis './invitation-roles' :
    `import { ROLE_ORDER, getAvailableRoles, canInviteWithRole, toClerkRole } from './invitation-roles'`.
    Importer `import { describe, it, expect } from 'vitest'`.
    Écrire les 7 cas de test décrits dans <behavior> avec des assertions `toEqual` (tableaux) et `toBe` (booléens/strings).
    NE PAS créer invitation-roles.ts encore — ce test DOIT échouer à la compilation/import (RED).
    Lancer `npx vitest run src/lib/invitation-roles.test.ts` : il DOIT échouer (module introuvable).
    Commit: `test(999.2-01): failing tests for invitation role scoping`
  </action>
  <verify>
    <automated>npx vitest run src/lib/invitation-roles.test.ts; test $? -ne 0 && echo "RED OK (échec attendu)"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/invitation-roles.test.ts existe et contient `getAvailableRoles`
    - le fichier contient `canInviteWithRole('manager', 'admin')` et l'assertion `false`
    - le fichier contient les 7 cas (grep `it(` retourne au moins 7 occurrences)
    - `npx vitest run src/lib/invitation-roles.test.ts` sort en échec (code ≠ 0) car le module n'existe pas encore
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (GREEN): Implémenter invitation-roles.ts</name>
  <read_first>
    - src/lib/invitation-roles.test.ts (créé en Task 0 — contrat exact à satisfaire)
    - src/generated/prisma/enums.ts (type MemberRole)
  </read_first>
  <behavior>
    Le fichier src/lib/invitation-roles.ts doit faire passer les 7 tests de Task 0.
  </behavior>
  <action>
    Créer src/lib/invitation-roles.ts (PAS de directive 'use server' — utilitaire pur, importable client ET serveur) :

    ```typescript
    import type { MemberRole } from '@/generated/prisma/enums'

    // Du plus puissant (index 0) au moins puissant. Aligné sur ROLE_LABELS de team-table.tsx.
    export const ROLE_ORDER: MemberRole[] = ['admin', 'manager', 'technician', 'requester', 'viewer']

    // D-06 : un invitant ne peut attribuer que des rôles de rang ≤ au sien.
    export function getAvailableRoles(currentRole: MemberRole): MemberRole[] {
      const idx = ROLE_ORDER.indexOf(currentRole)
      if (idx === -1) return []
      return ROLE_ORDER.slice(idx)
    }

    // D-06 enforcement (utilisé côté serveur ET pour filtrer le select UI).
    export function canInviteWithRole(currentRole: MemberRole, target: MemberRole): boolean {
      const cur = ROLE_ORDER.indexOf(currentRole)
      const tgt = ROLE_ORDER.indexOf(target)
      if (cur === -1 || tgt === -1) return false
      return tgt >= cur
    }

    // Pitfall 3 RESEARCH.md : Clerk attend possiblement le préfixe `org:`.
    export function toClerkRole(role: MemberRole): string {
      return `org:${role}`
    }
    ```

    Lancer `npx vitest run src/lib/invitation-roles.test.ts` : DOIT passer (GREEN).
    Commit: `feat(999.2-01): invitation role scoping utility`
  </action>
  <verify>
    <automated>npx vitest run src/lib/invitation-roles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/invitation-roles.ts contient `export function getAvailableRoles(`
    - src/lib/invitation-roles.ts contient `export function canInviteWithRole(`
    - src/lib/invitation-roles.ts contient `export function toClerkRole(` retournant `` `org:${role}` ``
    - src/lib/invitation-roles.ts NE contient PAS `'use server'`
    - `npx vitest run src/lib/invitation-roles.test.ts` sort en succès (code 0)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (RED→GREEN): Server actions inviteMember / revokeInvitation / listPendingInvitations + tests</name>
  <read_first>
    - src/actions/settings.ts (pattern getAdminOrg, updateMemberRole, revalidatePath — fichier à étendre)
    - src/lib/invitation-roles.ts (canInviteWithRole, toClerkRole créés en Task 1)
    - src/test/setup.ts (infra test — ne mocke ni auth ni clerkClient)
    - .planning/phases/999.2-invitation-membres-depuis-app/999.2-RESEARCH.md (sections "Code Examples" et "Mock Clerk pour tests vitest")
  </read_first>
  <behavior>
    Comportements de inviteMember(emailAddress: string, role: MemberRole) :
    - Test A (happy path admin) : auth() = {orgId:'org_1', userId:'u_admin'}, membre admin → appelle
      createOrganizationInvitation avec { organizationId:'org_1', emailAddress:'jean@exemple.com',
      role:'org:technician', inviterUserId:'u_admin' } et déclenche revalidatePath.
    - Test B (email invalide) : emailAddress='   ' ou 'pasun-email' → throw `Error('Adresse e-mail invalide')`
      AVANT tout appel Clerk (createOrganizationInvitation NON appelé).
    - Test C (escalade rôle, D-06) : membre manager invite role='admin' → throw
      `Error('Vous ne pouvez pas attribuer un rôle supérieur au vôtre')`, Clerk NON appelé.
    - Test D (non autorisé) : getAdminOrg throw (membre technician) → inviteMember propage l'erreur, Clerk NON appelé.

    Comportements de revokeInvitation(invitationId: string) :
    - Test E (happy path) : admin → appelle revokeOrganizationInvitation avec { organizationId, invitationId, requestingUserId } + revalidatePath.
    - Test F (non autorisé) : getAdminOrg throw → revokeInvitation propage, Clerk NON appelé.

    listPendingInvitations() : retourne le tableau `data` de getOrganizationInvitationList({ status:['pending'] }) — testé indirectement (mock retourne data), au moins 1 test du happy path.
  </behavior>
  <action>
    PARTIE 1 — RED. Créer src/actions/settings.test.ts.
    Mock complet en tête de fichier (copié de RESEARCH.md, adapté) :

    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest'

    const mockCreate = vi.fn()
    const mockRevoke = vi.fn()
    const mockList = vi.fn()
    const mockAuth = vi.fn()

    vi.mock('@clerk/nextjs/server', () => ({
      auth: () => mockAuth(),
      clerkClient: vi.fn().mockResolvedValue({
        organizations: {
          createOrganizationInvitation: (...a: unknown[]) => mockCreate(...a),
          getOrganizationInvitationList: (...a: unknown[]) => mockList(...a),
          revokeOrganizationInvitation: (...a: unknown[]) => mockRevoke(...a),
        },
      }),
    }))

    vi.mock('next/cache', () => ({ revalidatePath: vi.fn() }))

    // Mock de la couche DB pour piloter le rôle du membre courant (getAdminOrg)
    const mockOrgFind = vi.fn()
    const mockMembershipFind = vi.fn()
    vi.mock('@/lib/db', () => ({
      db: {
        organization: { findUnique: (...a: unknown[]) => mockOrgFind(...a) },
        membership: { findFirst: (...a: unknown[]) => mockMembershipFind(...a) },
      },
    }))

    import { inviteMember, revokeInvitation } from './settings'
    ```

    Helper d'arrangement par défaut dans beforeEach : auth={orgId:'org_1',userId:'u_admin'},
    org.findUnique→{id:'orgUuid'}, membership.findFirst→{role:'admin'}. Surcharger par test.
    Écrire les tests A–F décrits dans <behavior>.
    - Pour Test A : `mockList`/`mockCreate` resolved ; assert `expect(mockCreate).toHaveBeenCalledWith(expect.objectContaining({ organizationId:'org_1', emailAddress:'jean@exemple.com', role:'org:technician', inviterUserId:'u_admin' }))`.
    - Pour Test B/C/D/F : `await expect(inviteMember(...)).rejects.toThrow('...')` puis `expect(mockCreate).not.toHaveBeenCalled()`.
    Lancer `npx vitest run src/actions/settings.test.ts` → DOIT échouer (fonctions inexistantes). RED.
    Commit: `test(999.2-01): failing tests for invite/revoke server actions`

    PARTIE 2 — GREEN. Étendre src/actions/settings.ts. Ajouter en tête les imports manquants :
    `import { clerkClient } from '@clerk/nextjs/server'` (auth est déjà importé),
    `import { canInviteWithRole, toClerkRole } from '@/lib/invitation-roles'`.

    Modifier getAdminOrg pour retourner aussi le rôle courant (sans casser les appels existants) :
    le faire retourner `{ organizationId: org.id, orgId, userId, role: membership.role }`. ATTENTION :
    updateMemberRole, updateClosureRequirements, updateMemberHourlyRate, updateEscalationConfig font
    `const organizationId = await getAdminOrg()`. Pour ne pas tous les casser, créer un NOUVEAU helper
    `getAdminContext()` qui retourne l'objet complet, et garder getAdminOrg() en wrapper :
    `async function getAdminOrg() { return (await getAdminContext()).organizationId }`.

    ```typescript
    async function getAdminContext() {
      const { orgId, userId } = await auth()
      if (!orgId || !userId) throw new Error('Non autorisé')
      const org = await db.organization.findUnique({ where: { clerkId: orgId }, select: { id: true } })
      if (!org) throw new Error('Organisation introuvable')
      const membership = await db.membership.findFirst({
        where: { clerkUserId: userId, organizationId: org.id },
        select: { role: true },
      })
      if (!membership || (membership.role !== 'admin' && membership.role !== 'manager')) {
        throw new Error('Accès refusé')
      }
      return { organizationId: org.id, orgId, userId, role: membership.role }
    }
    async function getAdminOrg() { return (await getAdminContext()).organizationId }
    ```

    Ajouter les 3 actions :

    ```typescript
    const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/

    export async function inviteMember(emailAddress: string, role: MemberRole) {
      const { orgId, userId, role: currentRole } = await getAdminContext()
      const email = emailAddress.trim().toLowerCase()
      if (!email || !EMAIL_RE.test(email)) throw new Error('Adresse e-mail invalide')
      if (!canInviteWithRole(currentRole, role)) {
        throw new Error('Vous ne pouvez pas attribuer un rôle supérieur au vôtre')
      }
      const client = await clerkClient()
      await client.organizations.createOrganizationInvitation({
        organizationId: orgId,
        emailAddress: email,
        role: toClerkRole(role),
        inviterUserId: userId,
      })
      revalidatePath('/parametres/organisation')
    }

    export async function revokeInvitation(invitationId: string) {
      const { orgId, userId } = await getAdminContext()
      const client = await clerkClient()
      await client.organizations.revokeOrganizationInvitation({
        organizationId: orgId,
        invitationId,
        requestingUserId: userId,
      })
      revalidatePath('/parametres/organisation')
    }

    export async function listPendingInvitations() {
      const { orgId } = await getAdminContext()
      const client = await clerkClient()
      const { data } = await client.organizations.getOrganizationInvitationList({
        organizationId: orgId,
        status: ['pending'],
      })
      return data
    }
    ```

    Lancer `npx vitest run src/actions/settings.test.ts` → DOIT passer (GREEN).
    Commit: `feat(999.2-01): inviteMember/revokeInvitation/listPendingInvitations actions`
  </action>
  <verify>
    <automated>npx vitest run src/actions/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/actions/settings.test.ts contient `inviteMember` et au moins 6 occurrences de `it(`
    - src/actions/settings.test.ts vérifie `rejects.toThrow('Vous ne pouvez pas attribuer un rôle supérieur au vôtre')`
    - src/actions/settings.test.ts vérifie `expect(mockCreate).not.toHaveBeenCalled()` pour le cas email invalide
    - src/actions/settings.ts contient `export async function inviteMember(`
    - src/actions/settings.ts contient `export async function revokeInvitation(`
    - src/actions/settings.ts contient `export async function listPendingInvitations(`
    - src/actions/settings.ts contient `canInviteWithRole(currentRole, role)` AVANT l'appel `createOrganizationInvitation`
    - src/actions/settings.ts contient `await clerkClient()` (jamais `clerkClient.organizations` direct)
    - src/actions/settings.ts contient `emailAddress.trim().toLowerCase()` ou `emailAddress.trim()`
    - getAdminOrg() existant reste présent et fonctionnel (grep `function getAdminOrg`)
    - `npx vitest run src/actions/settings.test.ts` sort en succès (code 0)
    - `npx vitest run` (suite complète) sort en succès (code 0)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server action | L'UI peut être contournée ; toute server action reçoit une entrée non fiable (email, role, invitationId) |
| server action → Clerk Backend API | orgId/userId doivent provenir de `auth()`, jamais du client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.2-01 | Elevation of Privilege | inviteMember(role) | mitigate | `canInviteWithRole(currentRole, role)` enforcé côté serveur AVANT createOrganizationInvitation (D-06). currentRole lu depuis getAdminContext() (DB), pas du client. Test C couvre l'escalade manager→admin. |
| T-999.2-02 | Tampering / Injection | inviteMember(emailAddress) | mitigate | `emailAddress.trim().toLowerCase()` + validation regex EMAIL_RE avant appel Clerk. Email vide/whitespace/malformé → throw. Test B couvre. |
| T-999.2-03 | Broken Access Control | inviteMember / revokeInvitation / listPendingInvitations | mitigate | `getAdminContext()` (admin/manager requis) en tête de chaque action. Non-admin/manager → throw 'Accès refusé'. Tests D et F couvrent. |
| T-999.2-04 | Information Disclosure / IDOR | revokeInvitation(invitationId) | mitigate | organizationId passé à Clerk vient de auth() ; Clerk refuse une invitation n'appartenant pas à cet orgId. requestingUserId fourni pour audit. |
| T-999.2-05 | Spoofing | organizationId Clerk | mitigate | orgId provient exclusivement de `auth()` (jamais d'un paramètre client) ; UUID Prisma jamais envoyé à Clerk (Pitfall 2). |
</threat_model>

<verification>
- `npx vitest run src/lib/invitation-roles.test.ts` passe (scoping de rôles)
- `npx vitest run src/actions/settings.test.ts` passe (actions invite/revoke)
- `npx vitest run` (suite complète) verte — aucun test existant cassé par la refactorisation getAdminOrg→getAdminContext
- Vérification manuelle dev (à noter dans SUMMARY) : confirmer le format de rôle Clerk (`org:` ou non) via les valeurs retournées par getOrganizationMembershipList — ajuster toClerkRole si nécessaire (Assumptions A1/A2)
</verification>

<success_criteria>
- src/lib/invitation-roles.ts exporte getAvailableRoles, canInviteWithRole, toClerkRole, ROLE_ORDER
- src/actions/settings.ts exporte inviteMember, revokeInvitation, listPendingInvitations
- D-06 (scoping de rôles) enforcé côté serveur, pas seulement UI
- Email validé/normalisé avant appel Clerk
- Guard admin/manager sur les 3 actions
- getAdminOrg() existant préservé (aucune régression sur les actions settings existantes)
- Tous les tests verts
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-invitation-membres-depuis-app/999.2-01-SUMMARY.md`.
Inclure dans le SUMMARY : la note de vérification du format de rôle Clerk (org: préfixe) pour le dev.
</output>
