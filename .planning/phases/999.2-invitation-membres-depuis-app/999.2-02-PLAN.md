---
phase: 999.2-invitation-membres-depuis-app
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/components/settings/invite-dialog.tsx
  - src/components/settings/pending-invitations-section.tsx
  - src/components/settings/team-table.tsx
  - src/app/(app)/parametres/organisation/page.tsx
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
user_setup: []

must_haves:
  truths:
    - "Un admin/manager voit un bouton 'Inviter un membre' dans l'en-tête de la section Équipe"
    - "Un technician/requester/viewer ne voit PAS le bouton (canManage)"
    - "Le clic ouvre un dialog modal avec champ email + select rôle (défaut technician) + 2 boutons"
    - "Le select rôle ne propose que les rôles ≤ au rôle courant (D-06)"
    - "Succès → dialog fermé + toast 'Invitation envoyée à {email}'"
    - "Les invitations en attente s'affichent sous le TeamTable ; section masquée si 0 invitation"
    - "Chaque invitation affiche email, badge rôle, date d'envoi, bouton Révoquer"
  artifacts:
    - path: "src/components/settings/invite-dialog.tsx"
      provides: "Dialog client d'invitation (email + select rôle scopé + submit)"
      min_lines: 60
    - path: "src/components/settings/pending-invitations-section.tsx"
      provides: "Liste des invitations en attente avec révocation, masquée si vide"
      min_lines: 40
    - path: "src/app/(app)/parametres/organisation/page.tsx"
      provides: "Intégration bouton + section ; lecture des invitations côté serveur"
      contains: "listPendingInvitations"
  key_links:
    - from: "src/components/settings/invite-dialog.tsx"
      to: "src/actions/settings.ts inviteMember"
      via: "import + appel dans useTransition"
      pattern: "inviteMember"
    - from: "src/components/settings/pending-invitations-section.tsx"
      to: "src/actions/settings.ts revokeInvitation"
      via: "import + appel dans useTransition"
      pattern: "revokeInvitation"
    - from: "src/app/(app)/parametres/organisation/page.tsx"
      to: "src/actions/settings.ts listPendingInvitations"
      via: "appel serveur + passage en props à PendingInvitationsSection"
      pattern: "listPendingInvitations"
    - from: "src/app/(app)/parametres/organisation/page.tsx"
      to: "src/components/settings/invite-dialog.tsx"
      via: "rendu conditionnel dans l'en-tête section Équipe (canManage)"
      pattern: "InviteDialog"
---

<objective>
Construire l'interface d'invitation : un dialog modal `InviteDialog` (email + select rôle scopé par D-06, défaut technician) et une section `PendingInvitationsSection` (liste des invitations en attente avec révocation, masquée si vide), puis les intégrer dans la page `/parametres/organisation` (bouton dans l'en-tête Équipe, lecture des invitations côté serveur).

Purpose: Permettre aux admins/managers d'inviter et de gérer les invitations sans quitter l'app, conformément au contrat UI approuvé (999.2-UI-SPEC.md).

Output:
- src/components/settings/invite-dialog.tsx (nouveau)
- src/components/settings/pending-invitations-section.tsx (nouveau)
- src/app/(app)/parametres/organisation/page.tsx (modifié : bouton + section + lecture invitations)
- src/components/settings/team-table.tsx (modifié si besoin : exporter ROLE_LABELS/ROLE_VARIANTS pour réutilisation)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-invitation-membres-depuis-app/999.2-CONTEXT.md
@.planning/phases/999.2-invitation-membres-depuis-app/999.2-UI-SPEC.md
@.planning/phases/999.2-invitation-membres-depuis-app/999.2-01-SUMMARY.md

<interfaces>
<!-- Contrats clés du Plan 01 et du codebase. Utiliser directement. -->

Server actions (Plan 01 — src/actions/settings.ts) :
  inviteMember(emailAddress: string, role: MemberRole): Promise<void>   // throw avec message fr en cas d'erreur
  revokeInvitation(invitationId: string): Promise<void>
  listPendingInvitations(): Promise<OrganizationInvitation[]>           // status pending, lecture serveur

Utilitaire rôles (Plan 01 — src/lib/invitation-roles.ts) :
  getAvailableRoles(currentRole: MemberRole): MemberRole[]   // D-06 — admin: tous ; manager: sans admin
  ROLE_ORDER: MemberRole[]

OrganizationInvitation (@clerk/backend) — champs utiles :
  { id: string, emailAddress: string, role: string, roleName: string, createdAt: number /* ms */ }

team-table.tsx expose actuellement (NON exportés — voir Task 0) :
  ROLE_LABELS: Record<MemberRole,string> = { admin:'Admin', manager:'Gestionnaire', technician:'Technicien', requester:'Demandeur', viewer:'Lecteur' }
  ROLE_VARIANTS: Record<MemberRole,'default'|'secondary'|'outline'> = { admin:'default', manager:'default', technician:'secondary', requester:'outline', viewer:'outline' }
  canManage = currentRole === 'admin' || currentRole === 'manager'

Composants UI disponibles (ne pas réinstaller) :
  @/components/ui/dialog : Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle
  @/components/ui/button : Button   (variants: default, outline, ghost, destructive ; size: sm)
  @/components/ui/input  : Input
  @/components/ui/label  : Label
  @/components/ui/badge  : Badge
  @/components/ui/select : Select, SelectContent, SelectItem, SelectTrigger, SelectValue
  lucide-react : UserPlus
  sonner : toast

Pattern dialog de référence : src/components/assets/asset-form-dialog.tsx
  (useState open + useTransition pending + DialogContent sm:max-w-* + DialogFooter)

Page actuelle (Server Component) src/app/(app)/parametres/organisation/page.tsx :
  - lignes 95-114 : section Équipe avec en-tête flex justify-between, <TeamTable .../>, et un
    <p> "Pour inviter des membres, utilisez le tableau de bord Clerk..." (lignes 111-113) à SUPPRIMER.
  - currentMembership.role disponible ; canManage = ['admin','manager'].includes(currentMembership.role)
</interfaces>

<copywriting>
Toutes les chaînes en français (fr-CA), tirées de 999.2-UI-SPEC.md Copywriting Contract :
- Bouton trigger / titre dialog : "Inviter un membre"
- Label email : "Adresse e-mail" — placeholder : "prenom.nom@exemple.com"
- Label rôle : "Rôle"
- Bouton submit : "Envoyer l'invitation"  / Bouton annuler : "Annuler l'invitation"
- Toast succès : `Invitation envoyée à ${email}`
- Titre section : "Invitations en attente"
- Label date : `Envoyé le ${date}`
- Bouton révoquer : "Révoquer l'invitation"
- Toast révocation succès : "Invitation révoquée" / erreur : "Erreur lors de la révocation"
- Erreur générique fallback : "Erreur lors de l'envoi de l'invitation"
</copywriting>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Exporter ROLE_LABELS et ROLE_VARIANTS depuis team-table.tsx</name>
  <read_first>
    - src/components/settings/team-table.tsx (lignes 11-25 : ROLE_LABELS, ROLE_VARIANTS actuellement locaux)
  </read_first>
  <action>
    Dans src/components/settings/team-table.tsx, ajouter le mot-clé `export` devant les déclarations
    existantes (lignes 11 et 19) :
    - `const ROLE_LABELS` → `export const ROLE_LABELS`
    - `const ROLE_VARIANTS` → `export const ROLE_VARIANTS`
    Ne RIEN changer d'autre dans ce fichier. Le but : permettre la réutilisation dans
    pending-invitations-section.tsx sans dupliquer les libellés/variants (cohérence garantie).
  </action>
  <verify>
    <automated>grep -q "export const ROLE_LABELS" src/components/settings/team-table.tsx && grep -q "export const ROLE_VARIANTS" src/components/settings/team-table.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - team-table.tsx contient `export const ROLE_LABELS`
    - team-table.tsx contient `export const ROLE_VARIANTS`
    - team-table.tsx compile (le composant TeamTable n'est pas modifié par ailleurs)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 1: Créer InviteDialog (email + select rôle scopé D-06, défaut technician)</name>
  <read_first>
    - src/components/assets/asset-form-dialog.tsx (pattern open/setOpen + useTransition + DialogFooter)
    - src/components/ui/select.tsx (API Select/SelectTrigger/SelectValue/SelectContent/SelectItem)
    - src/lib/invitation-roles.ts (getAvailableRoles)
    - src/components/settings/team-table.tsx (ROLE_LABELS exporté en Task 0)
    - src/actions/settings.ts (signature inviteMember)
    - .planning/phases/999.2-invitation-membres-depuis-app/999.2-UI-SPEC.md (sections InviteDialog + Interaction States)
  </read_first>
  <action>
    Créer src/components/settings/invite-dialog.tsx (Client Component). Spécifications exactes (UI-SPEC) :

    - `'use client'` en tête.
    - Props : `{ currentRole: MemberRole }`.
    - État : `open` (useState false), `pending` (useTransition), `email` (useState ''),
      `role` (useState<MemberRole>('technician')) — défaut technician (D-07).
    - Rôles disponibles : `const roles = getAvailableRoles(currentRole)` (D-06).
    - Bouton trigger : `<Button variant="default" size="sm" onClick={() => setOpen(true)}>` avec
      icône `<UserPlus className="h-4 w-4" />` (16px) avant le texte "Inviter un membre".
    - Dialog : `<Dialog open={open} onOpenChange={setOpen}>` → `<DialogContent className="sm:max-w-sm">`.
      Structure : DialogHeader > DialogTitle "Inviter un membre" ; puis un `<form onSubmit={handleSubmit}>`
      avec un stack vertical `className="space-y-4"` :
        - Champ email : `<Label htmlFor="invite-email">Adresse e-mail</Label>` +
          `<Input id="invite-email" type="email" required value={email} onChange={...}
           placeholder="prenom.nom@exemple.com" autoFocus />`
        - Champ rôle : `<Label>Rôle</Label>` + Select shadcn contrôlé :
          `<Select value={role} onValueChange={(v) => setRole(v as MemberRole)}>`
          `<SelectTrigger><SelectValue /></SelectTrigger>`
          `<SelectContent>{roles.map(r => <SelectItem key={r} value={r}>{ROLE_LABELS[r]}</SelectItem>)}</SelectContent>`
      - DialogFooter avec 2 boutons :
        `<Button type="button" variant="outline" onClick={() => setOpen(false)}>Annuler l'invitation</Button>`
        `<Button type="submit" variant="default" disabled={pending}>Envoyer l'invitation</Button>`
    - handleSubmit(e) : `e.preventDefault()` ; `startTransition(async () => { try {
        await inviteMember(email, role); toast.success(\`Invitation envoyée à ${email}\`);
        setOpen(false); setEmail(''); setRole('technician') }
        catch (err) { toast.error(err instanceof Error ? err.message : "Erreur lors de l'envoi de l'invitation") } })`.
      NOTE: D-04 du CONTEXT mentionne une erreur inline ; le contrat UI-SPEC approuvé (sections
      InviteDialog "On error" + Interaction States) tranche pour un toast.error. Suivre l'UI-SPEC (toast).
    - Imports : Button, Input, Label, Dialog* depuis @/components/ui/*, Select* depuis @/components/ui/select,
      UserPlus de lucide-react, toast de sonner, inviteMember de '@/actions/settings',
      getAvailableRoles de '@/lib/invitation-roles', ROLE_LABELS de '@/components/settings/team-table',
      type MemberRole de '@/generated/prisma/enums'.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "invite-dialog" || echo "no type errors in invite-dialog"</automated>
  </verify>
  <acceptance_criteria>
    - invite-dialog.tsx contient `'use client'`
    - invite-dialog.tsx contient `getAvailableRoles(currentRole)`
    - invite-dialog.tsx contient `useState<MemberRole>('technician')` ou équivalent défaut technician
    - invite-dialog.tsx contient `await inviteMember(email, role)`
    - invite-dialog.tsx contient `` toast.success(`Invitation envoyée à ${email}`) `` (template literal)
    - invite-dialog.tsx contient `"Envoyer l'invitation"` et `"Annuler l'invitation"`
    - invite-dialog.tsx contient `className="sm:max-w-sm"`
    - invite-dialog.tsx contient `UserPlus`
    - invite-dialog.tsx contient `disabled={pending}` sur le bouton submit
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Créer PendingInvitationsSection (liste + révocation, masquée si vide)</name>
  <read_first>
    - src/components/settings/team-table.tsx (ROLE_LABELS, ROLE_VARIANTS exportés en Task 0 ; format Badge ; Intl.DateTimeFormat fr-CA ligne 157)
    - src/components/ui/badge.tsx (variants)
    - src/components/ui/button.tsx (variant ghost, size sm)
    - src/actions/settings.ts (signature revokeInvitation)
    - .planning/phases/999.2-invitation-membres-depuis-app/999.2-UI-SPEC.md (section PendingInvitationsSection + Destructive Actions)
  </read_first>
  <action>
    Créer src/components/settings/pending-invitations-section.tsx (Client Component). Spécifications (UI-SPEC) :

    - `'use client'` en tête.
    - Type prop invitation (sérialisable — données extraites côté serveur, voir Task 3) :
      `type Invitation = { id: string; emailAddress: string; role: MemberRole; createdAt: number }`
      Props : `{ invitations: Invitation[] }`.
    - D-05 : si `invitations.length === 0` → `return null` (section entièrement masquée, aucun message vide).
    - useTransition pour la révocation : `const [revoking, startTransition] = useTransition()`.
      Tracker l'id en cours optionnellement via un state `revokingId`.
    - Structure quand visible :
      `<div className="mt-8">` (32px de séparation au-dessus — xl)
        `<h3 className="text-base font-medium mb-4">Invitations en attente</h3>`
        `<div>` listant chaque invitation :
          `<div key={inv.id} className="flex items-center justify-between py-2 border-b border-border last:border-b-0">`
            Gauche : `<div className="flex items-center gap-2">`
              `<span className="text-sm">{inv.emailAddress}</span>`
              `<Badge variant={ROLE_VARIANTS[inv.role]} className="text-xs">{ROLE_LABELS[inv.role]}</Badge>`
            Droite : `<div className="flex items-center gap-4">`
              `<span className="text-xs text-muted-foreground">Envoyé le {formatDate(inv.createdAt)}</span>`
              `<Button variant="ghost" size="sm" className="text-destructive hover:text-destructive"
                disabled={revoking} onClick={() => handleRevoke(inv.id)}>Révoquer l'invitation</Button>`
    - formatDate : `new Intl.DateTimeFormat('fr-CA', { day: '2-digit', month: 'short', year: 'numeric' }).format(new Date(createdAt))`
      (createdAt est un timestamp ms — Pitfall 4 RESEARCH.md).
    - handleRevoke(id) : `startTransition(async () => { try { await revokeInvitation(id);
        toast.success('Invitation révoquée') } catch { toast.error('Erreur lors de la révocation') } })`.
      Pas de dialog de confirmation (UI-SPEC Destructive Actions : toast simple, pas d'undo).
    - Imports : Badge de @/components/ui/badge, Button de @/components/ui/button,
      ROLE_LABELS + ROLE_VARIANTS de '@/components/settings/team-table', revokeInvitation de '@/actions/settings',
      toast de sonner, type MemberRole de '@/generated/prisma/enums', useTransition/useState de react.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "pending-invitations" || echo "no type errors in pending-invitations-section"</automated>
  </verify>
  <acceptance_criteria>
    - pending-invitations-section.tsx contient `'use client'`
    - pending-invitations-section.tsx contient `if (invitations.length === 0) return null` (ou équivalent `=== 0` + `return null`)
    - pending-invitations-section.tsx contient `"Invitations en attente"`
    - pending-invitations-section.tsx contient `await revokeInvitation(`
    - pending-invitations-section.tsx contient `toast.success('Invitation révoquée')`
    - pending-invitations-section.tsx contient `Intl.DateTimeFormat('fr-CA'`
    - pending-invitations-section.tsx contient `ROLE_VARIANTS[` et `ROLE_LABELS[`
    - pending-invitations-section.tsx contient `text-destructive` et `mt-8`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Intégrer bouton + section dans la page organisation (lecture serveur des invitations)</name>
  <read_first>
    - src/app/(app)/parametres/organisation/page.tsx (Server Component complet ; section Équipe lignes 95-114)
    - src/components/settings/invite-dialog.tsx (props currentRole — créé Task 1)
    - src/components/settings/pending-invitations-section.tsx (props invitations — créé Task 2)
    - src/actions/settings.ts (listPendingInvitations — Plan 01)
  </read_first>
  <action>
    Modifier src/app/(app)/parametres/organisation/page.tsx (Server Component) :

    1. Imports en tête :
       `import { InviteDialog } from '@/components/settings/invite-dialog'`
       `import { PendingInvitationsSection } from '@/components/settings/pending-invitations-section'`
       `import { listPendingInvitations } from '@/actions/settings'`
       `import type { MemberRole } from '@/generated/prisma/enums'`

    2. Calculer canManage Équipe (réutiliser la logique existante) :
       `const canManageTeam = !!currentMembership && ['admin','manager'].includes(currentMembership.role)`

    3. Charger les invitations en attente côté serveur, uniquement si canManageTeam (sinon []) :
       ```typescript
       const pendingInvitations = canManageTeam
         ? (await listPendingInvitations()).map(inv => ({
             id: inv.id,
             emailAddress: inv.emailAddress,
             // dé-préfixer le rôle Clerk 'org:technician' → 'technician' pour matcher MemberRole/ROLE_LABELS
             role: (inv.role.startsWith('org:') ? inv.role.slice(4) : inv.role) as MemberRole,
             createdAt: inv.createdAt,
           }))
         : []
       ```
       (Ajouter ce bloc après le calcul de currentMembership, dans le corps async de la page.)

    4. Modifier l'en-tête de la section Équipe (actuellement lignes 96-105) pour ajouter le bouton
       d'invitation à droite, visible seulement si canManageTeam. Remplacer le bloc en-tête par :
       ```tsx
       <div className="flex items-center justify-between">
         <div className="flex items-center gap-2">
           <Users className="h-4 w-4 text-muted-foreground" />
           <h2 className="font-semibold">Équipe</h2>
         </div>
         <div className="flex items-center gap-3">
           <span className="text-xs text-muted-foreground">
             {members.length} membre{members.length !== 1 ? 's' : ''}
           </span>
           {canManageTeam && (
             <InviteDialog currentRole={currentMembership!.role as MemberRole} />
           )}
         </div>
       </div>
       ```

    5. SUPPRIMER le paragraphe obsolète (lignes 111-113 actuelles) :
       `<p className="text-xs text-muted-foreground">Pour inviter des membres, utilisez le tableau de bord Clerk de votre organisation.</p>`

    6. Ajouter la section invitations en attente JUSTE APRÈS le <TeamTable .../> (toujours dans la
       section Équipe, avant la fermeture `</section>`) :
       `<PendingInvitationsSection invitations={pendingInvitations} />`
       (Le composant renvoie null lui-même si la liste est vide — D-05.)

    Ne pas toucher aux autres sections de la page.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -iE "organisation/page" || echo "page typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contient `import { InviteDialog }` et `import { PendingInvitationsSection }`
    - page.tsx contient `listPendingInvitations()`
    - page.tsx contient `<InviteDialog currentRole={` à l'intérieur d'un bloc `canManageTeam &&`
    - page.tsx contient `<PendingInvitationsSection invitations={pendingInvitations}`
    - page.tsx NE contient PLUS `utilisez le tableau de bord Clerk`
    - page.tsx contient le dé-préfixage `startsWith('org:')` pour le rôle
    - `npx vitest run` (suite complète) sort en succès (code 0)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verification visuelle et fonctionnelle de l invitation</name>
  <action>
    Cette tâche est un checkpoint de vérification humaine. Avant de demander la confirmation,
    l'exécuteur DOIT : (1) confirmer que `npx vitest run` est vert, (2) lancer le dev server
    (`npm run dev`) et confirmer qu'il démarre sans erreur de compilation sur /parametres/organisation,
    (3) présenter à l'utilisateur les étapes de <how-to-verify> ci-dessous. Aucune modification de code
    n'est attendue ici — uniquement la validation visuelle/fonctionnelle du flux d'invitation.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <what-built>
    Bouton "Inviter un membre" dans l'en-tête Équipe (visible admin/manager), dialog d'invitation
    (email + select rôle scopé + défaut technician), toast de succès, et section "Invitations en attente"
    sous le tableau (masquée si vide) avec révocation.
  </what-built>
  <how-to-verify>
    1. Démarrer le dev server, se connecter comme admin, aller sur /parametres/organisation.
    2. Vérifier que le bouton "Inviter un membre" apparaît à droite de l'en-tête "Équipe".
    3. Cliquer : le dialog s'ouvre. Vérifier : champ "Adresse e-mail", select "Rôle" pré-réglé sur
       "Technicien", et que le select propose admin..viewer (admin connecté).
    4. Saisir une adresse e-mail valide, envoyer → le dialog se ferme + toast "Invitation envoyée à {email}".
    5. Vérifier qu'une section "Invitations en attente" apparaît sous le tableau avec l'email, le badge
       de rôle, "Envoyé le ..." et un bouton "Révoquer l'invitation".
    6. Cliquer "Révoquer l'invitation" → toast "Invitation révoquée" ; si c'était la dernière, la section disparaît.
    7. (Si possible) Se connecter comme manager : vérifier que le select rôle NE propose PAS "Admin" (D-06).
    8. (Si possible) Se connecter comme technician : vérifier que le bouton "Inviter un membre" est absent.
  </how-to-verify>
  <resume-signal>Tapez "approved" ou décrivez les écarts constatés</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigateur → server action | L'UI (select scopé) est une commodité, pas une protection. L'enforcement réel est dans le Plan 01 (serveur). |
| Server Component → Clerk | listPendingInvitations lit côté serveur avec orgId de auth() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.2-01 | Elevation of Privilege | InviteDialog select rôle | mitigate | Le select n'affiche que `getAvailableRoles(currentRole)` (D-06) — défense en profondeur UI. L'enforcement décisif reste server-side (Plan 01 canInviteWithRole). |
| T-999.2-03 | Broken Access Control | bouton Inviter + listPendingInvitations | mitigate | Bouton rendu seulement si canManageTeam ; listPendingInvitations appelée seulement si canManageTeam et protégée par getAdminContext() côté serveur (Plan 01). |
| T-999.2-06 | Injection (XSS) | rendu email/role dans la liste | accept | React échappe par défaut le contenu textuel ; emailAddress/role rendus comme texte, pas dangerouslySetInnerHTML. Risque résiduel négligeable. |
| T-999.2-04 | IDOR | bouton Révoquer (invitationId) | mitigate | invitationId provient de la liste retournée pour l'org courant ; revokeInvitation (Plan 01) revérifie via getAdminContext() + orgId de auth(). |
</threat_model>

<verification>
- `npx vitest run` (suite complète) verte après intégration
- `npx tsc --noEmit` sans erreur sur les nouveaux composants et la page
- Vérification humaine (Task 4) : flux complet invite → toast → liste → révocation ; scoping rôles manager ; absence du bouton pour technician
</verification>

<success_criteria>
- InviteDialog : email + select rôle scopé (D-06) + défaut technician (D-07) + toast succès (D-03), boutons "Envoyer/Annuler l'invitation"
- PendingInvitationsSection : email + badge rôle + date + révocation (D-04), masquée si vide (D-05)
- Page intégrée : bouton dans l'en-tête Équipe visible admin/manager (D-01, D-02), section sous le TeamTable (D-03)
- Paragraphe "tableau de bord Clerk" supprimé
- Suite de tests verte, typecheck propre
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-invitation-membres-depuis-app/999.2-02-SUMMARY.md`.
</output>
