---
phase: 05-portail-de-demandes-de-travail
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/api/portal/[siteToken]/route.ts
  - src/app/api/portal/[siteToken]/route.test.ts
  - src/app/(public)/portail/[siteToken]/page.tsx
  - src/app/(public)/portail/[siteToken]/portal-form.tsx
  - src/app/(public)/layout.tsx
autonomous: true
requirements:
  - PORTAL-01
  - PORTAL-02
must_haves:
  truths:
    - "GET /portail/{token-valide} retourne 200 et affiche le nom du site + formulaire"
    - "GET /portail/{token-invalide} retourne 404"
    - "GET /portail/{token-valide-mais-portalEnabled-false} retourne 404"
    - "POST /api/portal/{token} avec body valide crée un BT en DB et retourne 201 + numéro BT"
    - "POST /api/portal/{token} avec body invalide retourne 400 + issues Zod"
    - "POST /api/portal/{token-invalide} retourne 404"
    - "POST /api/portal/{token} avec honeypot rempli retourne 204 sans rien créer (anti-spam)"
    - "Le formulaire client utilise useActionState et affiche le numéro BT après succès"
    - "organizationId et siteId du BT créé proviennent de la lookup DB par token, jamais du body"
  artifacts:
    - path: "src/app/api/portal/[siteToken]/route.ts"
      provides: "POST handler public — création BT + email"
      exports: ["POST"]
    - path: "src/app/api/portal/[siteToken]/route.test.ts"
      provides: "Tests Vitest pour PORTAL-01 + PORTAL-02"
      contains: "describe"
    - path: "src/app/(public)/portail/[siteToken]/page.tsx"
      provides: "Server Component qui résout le site par token + rend le formulaire"
      contains: "notFound"
    - path: "src/app/(public)/portail/[siteToken]/portal-form.tsx"
      provides: "Client Component avec useActionState, honeypot, états de soumission"
      contains: "useActionState"
    - path: "src/app/(public)/layout.tsx"
      provides: "Layout minimal sans Clerk pour le groupe (public)"
      exports: ["default"]
  key_links:
    - from: "src/app/(public)/portail/[siteToken]/page.tsx"
      to: "db.site.findUnique({ where: { portalToken, portalEnabled: true } })"
      via: "lookup direct sans auth"
      pattern: "portalToken:\\s*siteToken"
    - from: "src/app/api/portal/[siteToken]/route.ts"
      to: "createPortalWorkOrder + sendPortalConfirmationEmail"
      via: "fire-and-forget email après création BT"
      pattern: "sendPortalConfirmationEmail"
    - from: "src/app/(public)/portail/[siteToken]/portal-form.tsx"
      to: "/api/portal/[siteToken]"
      via: "fetch POST multipart ou JSON"
      pattern: "/api/portal/"
---

<objective>
Surface publique de la Phase 5 : page SSR `/portail/[siteToken]`, formulaire client React 19 avec `useActionState`, Route Handler POST `/api/portal/[siteToken]` qui crée le BT et envoie l'email, tests Vitest pour PORTAL-01 et PORTAL-02.

Purpose: Plan 01 a livré les fondations (schema, proxy, helpers). Ce plan branche la route HTTP + l'UI publique. Plan 03 ajoute l'UI admin pour générer les liens.
Output: Une URL `/portail/{token}` fonctionnelle accessible sans Clerk, formulaire de soumission, BT créé + email envoyé.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-portail-de-demandes-de-travail/05-RESEARCH.md
@.planning/phases/05-portail-de-demandes-de-travail/05-01-PLAN.md

@src/app/api/v1/work-orders/route.ts
@src/app/api/qr/[code]/route.ts
@src/lib/portal-validation.ts
@src/lib/portal-work-order.ts
@src/lib/email.ts
@src/app/(app)/layout.tsx
@src/proxy.ts

<interfaces>
<!-- Contrats livrés par Plan 01 -->

Schéma Zod (src/lib/portal-validation.ts) :
```typescript
export const portalSubmitSchema: z.ZodObject<{
  requesterName: z.ZodString,
  requesterEmail: z.ZodString,
  description: z.ZodString,
  locationDescription: z.ZodOptional<z.ZodString>,
  honeypot: z.ZodOptional<z.ZodString>,
}>
export type PortalSubmitInput
```

Helper BT (src/lib/portal-work-order.ts) :
```typescript
export interface CreatePortalWorkOrderParams {
  organizationId: string
  siteId: string
  input: Omit<PortalSubmitInput, 'honeypot'>
}
export async function createPortalWorkOrder(params): Promise<{
  id: string; number: number; title: string; description: string;
  status: string; priority: string; type: string;
  siteId: string; organizationId: string; createdAt: Date;
}>
```

Email (src/lib/email.ts) :
```typescript
export async function sendPortalConfirmationEmail(params: {
  to: string; requesterName: string; workOrderNumber: number;
  siteName: string; organizationName: string;
}): Promise<void>
```

Next.js 16 conventions :
- `params: Promise<{ siteToken: string }>` puis `const { siteToken } = await params`
- Groupes de routes : `(public)` ne hérite pas du `(app)` layout
- React 19 : `useActionState` (pas `useFormState`)

Pattern test Route Handler — voir `src/app/api/v1/work-orders/route.test.ts` (mocks vi.mock pour @/lib/db, @/lib/email).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Layout public minimal pour le groupe (public)</name>
  <files>src/app/(public)/layout.tsx</files>
  <read_first>
    - src/app/(app)/layout.tsx (voir ce qu'il NE faut PAS reproduire : Clerk, sidebar, header)
    - src/app/layout.tsx (le root layout, à hériter implicitement)
  </read_first>
  <action>
    Créer `src/app/(public)/layout.tsx` — un layout extrêmement simple qui n'importe AUCUN composant Clerk et AUCUN composant de chrome auth :

    ```typescript
    import type { ReactNode } from 'react'

    export default function PublicLayout({ children }: { children: ReactNode }) {
      return (
        <div className="min-h-screen bg-background">
          {children}
        </div>
      )
    }
    ```

    Ne PAS importer `@clerk/nextjs`, ne PAS importer la sidebar, ne PAS importer le header.

    Vérifier qu'aucun `metadata` ou `viewport` n'est exporté ici (ils sont hérités du root layout `src/app/layout.tsx`).
  </action>
  <verify>
    <automated>test -f "src/app/(public)/layout.tsx" && grep -E "^export default function PublicLayout" "src/app/(public)/layout.tsx" && ! grep -E "@clerk/nextjs" "src/app/(public)/layout.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Le fichier `src/app/(public)/layout.tsx` existe
    - `grep "export default function PublicLayout" src/app/(public)/layout.tsx` renvoie 1 ligne
    - `grep "@clerk/nextjs" src/app/(public)/layout.tsx` renvoie 0 ligne
    - `grep "Sidebar\|Header\|UserButton" src/app/(public)/layout.tsx` renvoie 0 ligne
  </acceptance_criteria>
  <done>Le groupe de routes `(public)` est créé avec un layout neutre.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route Handler POST /api/portal/[siteToken] + tests Vitest</name>
  <files>src/app/api/portal/[siteToken]/route.ts, src/app/api/portal/[siteToken]/route.test.ts</files>
  <read_first>
    - src/app/api/v1/work-orders/route.ts (pattern Response.json, dynamic='force-dynamic', validation Zod)
    - src/app/api/qr/[code]/route.ts (pattern `params: Promise<...>` Next 16)
    - src/lib/portal-validation.ts (Plan 01)
    - src/lib/portal-work-order.ts (Plan 01)
    - src/lib/email.ts (sendPortalConfirmationEmail, Plan 01)
    - src/lib/api-auth.test.ts ou src/actions/api-keys.test.ts (pattern de mock vi.mock pour @/lib/db et @/lib/email)
  </read_first>
  <behavior>
    - Test A1 (PORTAL-01): Token inexistant → response.status === 404, body { error: 'Portail introuvable' }
    - Test A2 (PORTAL-01): Token existant mais portalEnabled=false → response.status === 404
    - Test A3 (PORTAL-01): Body JSON invalide (pas du JSON) → response.status === 400, body.error contient 'JSON'
    - Test A4 (PORTAL-02): Token valide + body valide → response.status === 201, body.number est un entier > 0, createPortalWorkOrder appelé avec organizationId/siteId issus du mock DB
    - Test A5 (PORTAL-02): Token valide + body invalide (description trop courte) → response.status === 400, body.issues est un tableau Zod
    - Test A6 (sécurité): Body contenant `organizationId: 'org-pirate'` et `siteId: 'site-pirate'` est IGNORÉ — createPortalWorkOrder reçoit l'orgId du site DB, pas ceux du body
    - Test A7 (anti-spam): Honeypot rempli (`honeypot: 'spam'`) → response.status === 204, createPortalWorkOrder JAMAIS appelé
    - Test A8 (PORTAL-02): Sur succès, sendPortalConfirmationEmail est appelé avec to=requesterEmail, workOrderNumber=42 (mock), siteName et organizationName du site DB
    - Test A9 (résilience): Si sendPortalConfirmationEmail rejette, la réponse reste 201 (fire-and-forget — l'erreur est loggée mais ne bloque pas)
  </behavior>
  <action>
    1. Créer `src/app/api/portal/[siteToken]/route.ts` :

    ```typescript
    import type { NextRequest } from 'next/server'
    import { db } from '@/lib/db'
    import { portalSubmitSchema } from '@/lib/portal-validation'
    import { createPortalWorkOrder } from '@/lib/portal-work-order'
    import { sendPortalConfirmationEmail } from '@/lib/email'

    export const dynamic = 'force-dynamic'

    export async function POST(
      request: NextRequest,
      { params }: { params: Promise<{ siteToken: string }> }
    ) {
      const { siteToken } = await params

      const site = await db.site.findUnique({
        where: { portalToken: siteToken },
        select: {
          id: true,
          organizationId: true,
          name: true,
          portalEnabled: true,
          organization: { select: { name: true } },
        },
      })
      if (!site || !site.portalEnabled) {
        return Response.json({ error: 'Portail introuvable' }, { status: 404 })
      }

      let body: unknown
      try {
        body = await request.json()
      } catch {
        return Response.json({ error: 'Corps JSON invalide' }, { status: 400 })
      }

      const parsed = portalSubmitSchema.safeParse(body)
      if (!parsed.success) {
        return Response.json(
          { error: 'Validation', issues: parsed.error.issues },
          { status: 400 }
        )
      }

      // Anti-spam: honeypot rempli = bot. Réponse 204 silencieuse (pas de signal d'échec).
      if (parsed.data.honeypot && parsed.data.honeypot.length > 0) {
        return new Response(null, { status: 204 })
      }

      const { honeypot: _honeypot, ...input } = parsed.data

      // organizationId et siteId proviennent de la DB, JAMAIS du body
      const wo = await createPortalWorkOrder({
        organizationId: site.organizationId,
        siteId: site.id,
        input,
      })

      // Fire-and-forget — ne pas bloquer la réponse sur l'envoi email
      sendPortalConfirmationEmail({
        to: input.requesterEmail,
        requesterName: input.requesterName,
        workOrderNumber: wo.number,
        siteName: site.name,
        organizationName: site.organization.name,
      }).catch((err) => {
        console.error('[portal] sendPortalConfirmationEmail failed:', err)
      })

      return Response.json(
        { id: wo.id, number: wo.number, status: wo.status },
        { status: 201 }
      )
    }
    ```

    2. Créer `src/app/api/portal/[siteToken]/route.test.ts` avec Vitest. S'inspirer du style de `src/lib/api-auth.test.ts`. Mocker `@/lib/db`, `@/lib/portal-work-order`, `@/lib/email`. Construire NextRequest via `new Request(url, { method: 'POST', body: JSON.stringify(...) })` puis caster, ou utiliser le helper `new NextRequest(url, init)` si disponible.

    Pour chaque test, appeler `POST(req, { params: Promise.resolve({ siteToken: 'TOKEN-XYZ' }) })` et asserter sur le statut + le body parsé.

    Couvrir les 9 comportements A1-A9 listés dans `<behavior>`.

    Pour A9 spécifiquement : faire que le mock de `sendPortalConfirmationEmail` rejette avec une erreur, asserter que la promesse résolue par `POST(...)` retourne quand même statut 201 (NE PAS `await` la promesse email dans le code production — c'est le `.catch` qui gère).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/portal/\[siteToken\]/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "src/app/api/portal/[siteToken]/route.test.ts"` passe avec ≥ 9 tests verts
    - `grep "export async function POST" "src/app/api/portal/[siteToken]/route.ts"` renvoie 1 ligne
    - `grep "params: Promise" "src/app/api/portal/[siteToken]/route.ts"` renvoie 1 ligne (Next.js 16)
    - `grep "auth(" "src/app/api/portal/[siteToken]/route.ts"` renvoie 0 ligne (CRITIQUE — pas de Clerk)
    - `grep "site.organizationId" "src/app/api/portal/[siteToken]/route.ts"` renvoie au moins 1 ligne (organizationId vient bien du site DB)
    - `grep "honeypot" "src/app/api/portal/[siteToken]/route.ts"` renvoie au moins 2 lignes (lecture + condition de court-circuit)
    - `grep ".catch((err)" "src/app/api/portal/[siteToken]/route.ts"` renvoie au moins 1 ligne (fire-and-forget email)
  </acceptance_criteria>
  <done>Route Handler fonctionnel, tests verts pour PORTAL-01 et PORTAL-02, isolation tenant garantie côté serveur.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Page SSR /portail/[siteToken] + formulaire client useActionState</name>
  <files>src/app/(public)/portail/[siteToken]/page.tsx, src/app/(public)/portail/[siteToken]/portal-form.tsx</files>
  <read_first>
    - src/app/api/qr/[code]/route.ts (pattern await params Next 16)
    - src/app/(public)/layout.tsx (créé en Task 1)
    - src/components/ui/button.tsx, src/components/ui/input.tsx (composants shadcn disponibles)
    - node_modules/next/dist/docs/01-app/02-guides/forms.md (si présent — useActionState React 19)
  </read_first>
  <action>
    1. Créer `src/app/(public)/portail/[siteToken]/page.tsx` :

    ```typescript
    import { notFound } from 'next/navigation'
    import { db } from '@/lib/db'
    import { PortalForm } from './portal-form'

    export const dynamic = 'force-dynamic'

    export default async function PortalPage({
      params,
    }: {
      params: Promise<{ siteToken: string }>
    }) {
      const { siteToken } = await params
      const site = await db.site.findUnique({
        where: { portalToken: siteToken },
        select: {
          name: true,
          portalEnabled: true,
          organization: { select: { name: true } },
        },
      })
      if (!site || !site.portalEnabled) notFound()

      return (
        <main className="mx-auto max-w-xl px-4 py-12">
          <header className="mb-8">
            <h1 className="text-2xl font-bold">Demande de maintenance</h1>
            <p className="text-sm text-muted-foreground mt-1">
              {site.organization.name} — {site.name}
            </p>
          </header>
          <PortalForm siteToken={siteToken} />
        </main>
      )
    }
    ```

    2. Créer `src/app/(public)/portail/[siteToken]/portal-form.tsx` (Client Component) :

    ```typescript
    'use client'

    import { useActionState } from 'react'
    import { Button } from '@/components/ui/button'
    import { Input } from '@/components/ui/input'
    import { Label } from '@/components/ui/label'
    import { Textarea } from '@/components/ui/textarea'

    type ActionState =
      | { status: 'idle' }
      | { status: 'success'; workOrderNumber: number }
      | { status: 'error'; message: string; fieldErrors?: Record<string, string> }

    async function submitAction(
      _prev: ActionState,
      formData: FormData
    ): Promise<ActionState> {
      const siteToken = String(formData.get('siteToken') ?? '')
      const payload = {
        requesterName: String(formData.get('requesterName') ?? ''),
        requesterEmail: String(formData.get('requesterEmail') ?? ''),
        description: String(formData.get('description') ?? ''),
        locationDescription: String(formData.get('locationDescription') ?? '') || undefined,
        honeypot: String(formData.get('website') ?? ''), // champ piège nommé 'website'
      }
      const res = await fetch(`/api/portal/${siteToken}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      })
      if (res.status === 204) {
        // honeypot triggered — feindre le succès
        return { status: 'success', workOrderNumber: 0 }
      }
      if (!res.ok) {
        const data = await res.json().catch(() => ({ error: 'Erreur réseau' }))
        return { status: 'error', message: data.error ?? 'Erreur', fieldErrors: undefined }
      }
      const data = await res.json()
      return { status: 'success', workOrderNumber: data.number }
    }

    export function PortalForm({ siteToken }: { siteToken: string }) {
      const [state, formAction, pending] = useActionState<ActionState, FormData>(
        submitAction,
        { status: 'idle' }
      )

      if (state.status === 'success' && state.workOrderNumber > 0) {
        return (
          <div className="rounded-md border bg-card p-6">
            <h2 className="text-lg font-semibold">Demande reçue</h2>
            <p className="mt-2 text-sm">
              Votre demande a été enregistrée sous le numéro{' '}
              <strong>#{state.workOrderNumber}</strong>. Vous recevrez une confirmation
              par email.
            </p>
          </div>
        )
      }
      if (state.status === 'success') {
        // honeypot path — même UI que succès
        return (
          <div className="rounded-md border bg-card p-6">
            <h2 className="text-lg font-semibold">Demande reçue</h2>
            <p className="mt-2 text-sm">Votre demande a été enregistrée.</p>
          </div>
        )
      }

      return (
        <form action={formAction} className="space-y-4">
          <input type="hidden" name="siteToken" value={siteToken} />
          {/* Honeypot — nommé 'website' pour piéger les bots. Invisible aux humains. */}
          <input
            type="text"
            name="website"
            tabIndex={-1}
            autoComplete="off"
            aria-hidden="true"
            style={{ position: 'absolute', left: '-9999px', width: '1px', height: '1px' }}
          />

          <div className="space-y-1">
            <Label htmlFor="requesterName">Votre nom</Label>
            <Input id="requesterName" name="requesterName" required maxLength={100} />
          </div>
          <div className="space-y-1">
            <Label htmlFor="requesterEmail">Votre email</Label>
            <Input id="requesterEmail" name="requesterEmail" type="email" required maxLength={200} />
          </div>
          <div className="space-y-1">
            <Label htmlFor="locationDescription">Localisation (optionnel)</Label>
            <Input id="locationDescription" name="locationDescription" maxLength={200} placeholder="Ex: 2e étage, salle B-203" />
          </div>
          <div className="space-y-1">
            <Label htmlFor="description">Description du problème</Label>
            <Textarea id="description" name="description" required minLength={10} maxLength={1000} rows={5} />
          </div>

          {state.status === 'error' && (
            <p className="text-sm text-destructive">{state.message}</p>
          )}

          <Button type="submit" disabled={pending} className="w-full">
            {pending ? 'Envoi en cours…' : 'Envoyer la demande'}
          </Button>
        </form>
      )
    }
    ```

    Si `src/components/ui/textarea.tsx` n'existe pas, le créer en suivant le pattern shadcn (composant Textarea simple). Vérifier d'abord avec `ls src/components/ui/textarea.tsx` — si absent, ajouter le fichier minimal.
  </action>
  <verify>
    <automated>test -f "src/app/(public)/portail/[siteToken]/page.tsx" && test -f "src/app/(public)/portail/[siteToken]/portal-form.tsx" && grep "notFound" "src/app/(public)/portail/[siteToken]/page.tsx" && grep "useActionState" "src/app/(public)/portail/[siteToken]/portal-form.tsx" && grep "name=\"website\"" "src/app/(public)/portail/[siteToken]/portal-form.tsx" && grep "/api/portal/" "src/app/(public)/portail/[siteToken]/portal-form.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Les deux fichiers existent dans `src/app/(public)/portail/[siteToken]/`
    - `grep "await params" "src/app/(public)/portail/[siteToken]/page.tsx"` renvoie 1 ligne (Next 16)
    - `grep "notFound()" "src/app/(public)/portail/[siteToken]/page.tsx"` renvoie 1 ligne
    - `grep "portalEnabled" "src/app/(public)/portail/[siteToken]/page.tsx"` renvoie au moins 1 ligne (lookup filtré ou check explicite)
    - `grep "'use client'" "src/app/(public)/portail/[siteToken]/portal-form.tsx"` renvoie 1 ligne
    - `grep "useActionState" "src/app/(public)/portail/[siteToken]/portal-form.tsx"` renvoie au moins 1 ligne
    - `grep 'name="website"' "src/app/(public)/portail/[siteToken]/portal-form.tsx"` renvoie 1 ligne (honeypot)
    - `grep "fetch(\`/api/portal/" "src/app/(public)/portail/[siteToken]/portal-form.tsx"` renvoie 1 ligne
    - `grep "@clerk/nextjs" "src/app/(public)/portail/[siteToken]/page.tsx"` renvoie 0 ligne
    - `grep "@clerk/nextjs" "src/app/(public)/portail/[siteToken]/portal-form.tsx"` renvoie 0 ligne
  </acceptance_criteria>
  <done>Page publique fonctionnelle accessible sans authentification, formulaire interactif avec feedback de soumission et honeypot anti-spam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet anonyme → /portail/* (page SSR) | Tout le monde peut lire la page si le token est connu |
| Internet anonyme → /api/portal/* (POST) | Création BT déclenchée sans auth — seul le token sert d'identification du site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Spoofing | POST /api/portal organizationId/siteId | mitigate | Strictement dérivé de la lookup DB par token, JAMAIS pris dans le body. Test A6 vérifie. Code : `createPortalWorkOrder({ organizationId: site.organizationId, siteId: site.id, input })` |
| T-05-08 | Tampering | Input description / requesterName / requesterEmail | mitigate | portalSubmitSchema (Plan 01) impose .max() ; React échappe en SSR ; Email rendu via @react-email/render (HTML safe) |
| T-05-09 | Repudiation | Demande anonyme | accept | Email du demandeur est non-vérifié par design — c'est le compromis du portail public |
| T-05-10 | Information Disclosure | 404 vs 403 sur token invalide | mitigate | Toujours retourner 404 (jamais 403) pour ne pas révéler qu'un token existe mais est désactivé |
| T-05-11 | Denial of Service | Soumissions automatisées massives | mitigate (MVP) | Honeypot champ `website` côté formulaire ; bot remplit → 204 sans création BT. Rate-limit par IP différé hors MVP (Open Question #3 de la recherche) |
| T-05-12 | DoS | Requête JSON gigantesque | mitigate | Next.js limite le body à 1MB par défaut + Zod .max(1000) sur description |
| T-05-13 | Tampering | Bypass honeypot via JS modifié | accept | Honeypot est une mesure défensive ; un attaquant ciblé contournera. Le rate-limit IP est la mitigation suivante (futur) |
| T-05-14 | Information Disclosure | Énumération de tokens UUID v4 | accept | Espace de 2^122 — non énumérable pratiquement |
</threat_model>

<verification>
1. `npx vitest run src/app/api/portal/[siteToken]/route.test.ts` → 9+ tests verts
2. `npx tsc --noEmit` → pas de nouvelle erreur dans les fichiers modifiés
3. Inspection manuelle : `grep -r "@clerk" "src/app/(public)/"` → 0 résultat
4. Inspection manuelle : `grep "organizationId.*body\|body.*organizationId" "src/app/api/portal/"` → 0 résultat (tenant safety)
</verification>

<success_criteria>
- PORTAL-01 satisfait : URL publique `/portail/[token]` accessible sans Clerk, formulaire de soumission affiché, 404 si token invalide ou désactivé
- PORTAL-02 satisfait : POST crée un BT en DB, envoie un email de confirmation au demandeur, retourne le numéro de BT
- Tests Vitest verts couvrant : tokens invalides/désactivés, payload valide/invalide, isolation tenant, honeypot, résilience email
- Honeypot anti-spam fonctionnel (champ `website` invisible, court-circuite la création BT)
</success_criteria>

<output>
After completion, create `.planning/phases/05-portail-de-demandes-de-travail/05-02-SUMMARY.md`
</output>
