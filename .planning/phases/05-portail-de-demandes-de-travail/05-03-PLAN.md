---
phase: 05-portail-de-demandes-de-travail
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/settings/portal-sites-section.tsx
  - src/components/settings/portal-sites-section.test.tsx
  - src/app/(app)/parametres/organisation/page.tsx
autonomous: false
requirements:
  - PORTAL-01
user_setup: []
must_haves:
  truths:
    - "L'admin/manager voit la liste des sites de son organisation dans /parametres/organisation"
    - "Chaque site a un toggle 'Portail activé' qui appelle enablePortal/disablePortal"
    - "Quand le portail est activé, l'URL publique /portail/{token} est affichée avec un bouton 'Copier'"
    - "Un bouton 'Régénérer le lien' appelle regeneratePortalToken et met à jour l'affichage"
    - "Un utilisateur 'technician' ou 'viewer' ne voit PAS cette section (gating par rôle côté Server Component)"
    - "Un checkpoint humain confirme le flux end-to-end : activer portail → ouvrir URL en navigation privée → soumettre formulaire → vérifier BT créé + email reçu"
  artifacts:
    - path: "src/components/settings/portal-sites-section.tsx"
      provides: "Composant client de gestion des portails par site"
      contains: "PortalSitesSection"
    - path: "src/components/settings/portal-sites-section.test.tsx"
      provides: "Tests UI Vitest : rendu liste, toggles, copie URL"
      contains: "describe"
    - path: "src/app/(app)/parametres/organisation/page.tsx"
      provides: "Page admin intégrant PortalSitesSection avec gating de rôle"
      contains: "PortalSitesSection"
  key_links:
    - from: "src/components/settings/portal-sites-section.tsx"
      to: "src/actions/sites.ts (enablePortal/disablePortal/regeneratePortalToken)"
      via: "Server Actions via useTransition"
      pattern: "enablePortal|disablePortal|regeneratePortalToken"
    - from: "src/app/(app)/parametres/organisation/page.tsx"
      to: "PortalSitesSection"
      via: "Conditionnel sur membership.role IN (admin, manager)"
      pattern: "PortalSitesSection"
---

<objective>
UI admin de gestion du portail public par site, intégrée dans la page paramètres organisation. Liste des sites, toggle activé/désactivé, copie d'URL, régénération de token. Checkpoint humain final qui valide le flux complet end-to-end (Plan 02 + Plan 03 ensemble).

Purpose: Sans cette UI, l'admin ne peut pas générer/distribuer les URLs portail. C'est l'unique surface qui rend les Server Actions du Plan 01 atteignables.
Output: Section "Portails publics" dans /parametres/organisation, accessible aux rôles admin et manager.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-portail-de-demandes-de-travail/05-RESEARCH.md
@.planning/phases/05-portail-de-demandes-de-travail/05-01-PLAN.md
@.planning/phases/05-portail-de-demandes-de-travail/05-02-PLAN.md

@src/components/settings/api-keys-section.tsx
@src/components/settings/api-keys-section.test.tsx
@src/app/(app)/parametres/organisation/page.tsx
@src/actions/sites.ts

<interfaces>
<!-- Server Actions livrées par Plan 01 -->

```typescript
// src/actions/sites.ts
export async function enablePortal(siteId: string): Promise<{ portalToken: string }>
export async function disablePortal(siteId: string): Promise<void>
export async function regeneratePortalToken(siteId: string): Promise<{ portalToken: string }>
```

Site shape attendue (lecture depuis la page Server Component) :
```typescript
type PortalSiteRow = {
  id: string
  name: string
  portalToken: string | null
  portalEnabled: boolean
}
```

Pattern composant client à imiter — src/components/settings/api-keys-section.tsx :
- 'use client'
- useState + useTransition
- Bouton "Copier" avec icône Copy de lucide-react, état `copied` qui se reset après 2s
- Dialog shadcn pour confirmer la régénération (action destructive)
- Toast via sonner (`import { toast } from 'sonner'`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PortalSitesSection composant client + tests Vitest</name>
  <files>src/components/settings/portal-sites-section.tsx, src/components/settings/portal-sites-section.test.tsx</files>
  <read_first>
    - src/components/settings/api-keys-section.tsx (intégral — pattern à reproduire : useTransition, Dialog confirm, Copy button, toast)
    - src/components/settings/api-keys-section.test.tsx (intégral — pattern de test : vi.mock des Server Actions, render, fireEvent)
    - src/actions/sites.ts (signatures enablePortal/disablePortal/regeneratePortalToken)
  </read_first>
  <behavior>
    - Test B1: Avec `initialSites = []`, le composant affiche "Aucun site" (empty state)
    - Test B2: Avec un site `portalEnabled: false`, affiche le nom du site, un bouton "Activer le portail", PAS d'URL
    - Test B3: Avec un site `portalEnabled: true` + token, affiche l'URL `${origin}/portail/${token}`, bouton "Copier", bouton "Désactiver", bouton "Régénérer"
    - Test B4: Click sur "Activer" appelle enablePortal(siteId) mocké et met à jour l'UI avec le token retourné
    - Test B5: Click sur "Désactiver" appelle disablePortal(siteId) après confirmation dialog
    - Test B6: Click sur "Copier" appelle navigator.clipboard.writeText avec l'URL complète, puis affiche "Copié" pendant 2s
    - Test B7: Click sur "Régénérer" ouvre un Dialog, sur confirmation appelle regeneratePortalToken(siteId) et met à jour le token affiché
  </behavior>
  <action>
    1. Créer `src/components/settings/portal-sites-section.tsx` (Client Component) :

    ```typescript
    'use client'

    import { useState, useTransition } from 'react'
    import { toast } from 'sonner'
    import { Button } from '@/components/ui/button'
    import {
      Card,
      CardContent,
      CardDescription,
      CardHeader,
      CardTitle,
    } from '@/components/ui/card'
    import {
      Dialog,
      DialogContent,
      DialogDescription,
      DialogFooter,
      DialogHeader,
      DialogTitle,
    } from '@/components/ui/dialog'
    import {
      enablePortal,
      disablePortal,
      regeneratePortalToken,
    } from '@/actions/sites'
    import { Copy, Check, RotateCcw, Globe, Power, PowerOff } from 'lucide-react'

    export type PortalSiteRow = {
      id: string
      name: string
      portalToken: string | null
      portalEnabled: boolean
    }

    function portalUrl(token: string): string {
      const origin =
        typeof window !== 'undefined'
          ? window.location.origin
          : (process.env.NEXT_PUBLIC_APP_URL ?? '')
      return `${origin}/portail/${token}`
    }

    export function PortalSitesSection({ initialSites }: { initialSites: PortalSiteRow[] }) {
      const [sites, setSites] = useState<PortalSiteRow[]>(initialSites)
      const [isPending, startTransition] = useTransition()
      const [copiedId, setCopiedId] = useState<string | null>(null)
      const [regenerateTarget, setRegenerateTarget] = useState<PortalSiteRow | null>(null)

      function handleEnable(site: PortalSiteRow) {
        startTransition(async () => {
          try {
            const { portalToken } = await enablePortal(site.id)
            setSites((prev) =>
              prev.map((s) => (s.id === site.id ? { ...s, portalToken, portalEnabled: true } : s))
            )
            toast.success('Portail activé')
          } catch (err) {
            toast.error(err instanceof Error ? err.message : 'Erreur')
          }
        })
      }

      function handleDisable(site: PortalSiteRow) {
        startTransition(async () => {
          try {
            await disablePortal(site.id)
            setSites((prev) =>
              prev.map((s) => (s.id === site.id ? { ...s, portalEnabled: false } : s))
            )
            toast.success('Portail désactivé')
          } catch (err) {
            toast.error(err instanceof Error ? err.message : 'Erreur')
          }
        })
      }

      function handleRegenerate(site: PortalSiteRow) {
        startTransition(async () => {
          try {
            const { portalToken } = await regeneratePortalToken(site.id)
            setSites((prev) =>
              prev.map((s) => (s.id === site.id ? { ...s, portalToken } : s))
            )
            toast.success('Nouveau lien généré')
            setRegenerateTarget(null)
          } catch (err) {
            toast.error(err instanceof Error ? err.message : 'Erreur')
          }
        })
      }

      async function handleCopy(site: PortalSiteRow) {
        if (!site.portalToken) return
        await navigator.clipboard.writeText(portalUrl(site.portalToken))
        setCopiedId(site.id)
        setTimeout(() => setCopiedId(null), 2000)
      }

      return (
        <>
          <Card>
            <CardHeader>
              <CardTitle className="flex items-center gap-2">
                <Globe className="h-5 w-5" />
                Portails publics
              </CardTitle>
              <CardDescription>
                Générez une URL publique par site pour permettre à n'importe quel
                employé de soumettre une demande de maintenance sans compte Korvia.
              </CardDescription>
            </CardHeader>
            <CardContent className="space-y-4">
              {sites.length === 0 && (
                <p className="text-sm text-muted-foreground">Aucun site. Créez un site dans la section Actifs.</p>
              )}
              {sites.map((site) => (
                <div key={site.id} className="rounded-md border p-4 space-y-3">
                  <div className="flex items-center justify-between gap-4">
                    <div className="font-medium">{site.name}</div>
                    {site.portalEnabled ? (
                      <Button
                        variant="outline"
                        size="sm"
                        onClick={() => handleDisable(site)}
                        disabled={isPending}
                      >
                        <PowerOff className="h-4 w-4 mr-1" /> Désactiver
                      </Button>
                    ) : (
                      <Button
                        size="sm"
                        onClick={() => handleEnable(site)}
                        disabled={isPending}
                      >
                        <Power className="h-4 w-4 mr-1" /> Activer le portail
                      </Button>
                    )}
                  </div>
                  {site.portalEnabled && site.portalToken && (
                    <div className="flex items-center gap-2">
                      <code className="flex-1 text-xs bg-muted rounded px-2 py-1 truncate">
                        {portalUrl(site.portalToken)}
                      </code>
                      <Button
                        variant="outline"
                        size="sm"
                        onClick={() => handleCopy(site)}
                        aria-label="Copier l'URL"
                      >
                        {copiedId === site.id ? (
                          <>
                            <Check className="h-4 w-4 mr-1" /> Copié
                          </>
                        ) : (
                          <>
                            <Copy className="h-4 w-4 mr-1" /> Copier
                          </>
                        )}
                      </Button>
                      <Button
                        variant="outline"
                        size="sm"
                        onClick={() => setRegenerateTarget(site)}
                        aria-label="Régénérer le lien"
                      >
                        <RotateCcw className="h-4 w-4 mr-1" /> Régénérer
                      </Button>
                    </div>
                  )}
                </div>
              ))}
            </CardContent>
          </Card>

          <Dialog
            open={regenerateTarget !== null}
            onOpenChange={(open) => !open && setRegenerateTarget(null)}
          >
            <DialogContent>
              <DialogHeader>
                <DialogTitle>Régénérer le lien du portail ?</DialogTitle>
                <DialogDescription>
                  L'ancien lien deviendra immédiatement invalide. Toute personne
                  qui possède l'ancienne URL ne pourra plus l'utiliser.
                </DialogDescription>
              </DialogHeader>
              <DialogFooter>
                <Button variant="outline" onClick={() => setRegenerateTarget(null)}>
                  Annuler
                </Button>
                <Button
                  onClick={() => regenerateTarget && handleRegenerate(regenerateTarget)}
                  disabled={isPending}
                >
                  Régénérer
                </Button>
              </DialogFooter>
            </DialogContent>
          </Dialog>
        </>
      )
    }
    ```

    2. Créer `src/components/settings/portal-sites-section.test.tsx` en suivant la structure de `src/components/settings/api-keys-section.test.tsx`. Mocker :
       - `@/actions/sites` (les 3 functions enablePortal/disablePortal/regeneratePortalToken)
       - `sonner` (toast.success / toast.error)
       - `navigator.clipboard.writeText` via `Object.assign(navigator, { clipboard: { writeText: vi.fn() } })`

    Couvrir les comportements B1-B7. Pour B6, utiliser fake timers Vitest pour vérifier que `copiedId` revient à null après 2000ms.
  </action>
  <verify>
    <automated>npx vitest run src/components/settings/portal-sites-section.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/settings/portal-sites-section.test.tsx` passe avec ≥ 7 tests verts
    - `grep "export function PortalSitesSection" src/components/settings/portal-sites-section.tsx` renvoie 1 ligne
    - `grep "enablePortal\|disablePortal\|regeneratePortalToken" src/components/settings/portal-sites-section.tsx | wc -l` ≥ 3
    - `grep "navigator.clipboard.writeText" src/components/settings/portal-sites-section.tsx` renvoie 1 ligne
    - `grep "'use client'" src/components/settings/portal-sites-section.tsx` renvoie 1 ligne
    - `grep "Dialog" src/components/settings/portal-sites-section.tsx | wc -l` ≥ 3 (Dialog confirmation régénération)
  </acceptance_criteria>
  <done>Composant client fonctionnel testé, prêt à être intégré dans la page paramètres.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Intégrer PortalSitesSection dans /parametres/organisation avec gating de rôle</name>
  <files>src/app/(app)/parametres/organisation/page.tsx</files>
  <read_first>
    - src/app/(app)/parametres/organisation/page.tsx (intégral — voir le pattern existant pour ApiKeysSection)
    - src/components/settings/portal-sites-section.tsx (créé en Task 1 — vérifier le type exporté PortalSiteRow)
  </read_first>
  <action>
    Modifier `src/app/(app)/parametres/organisation/page.tsx` :

    1. Ajouter en haut, après les autres imports settings :
    ```typescript
    import { PortalSitesSection, type PortalSiteRow } from '@/components/settings/portal-sites-section'
    ```
    Et ajouter `Globe` à l'import lucide-react existant (déjà présent : `import { Building2, Users, CreditCard, KeyRound } from 'lucide-react'` → devient `import { Building2, Users, CreditCard, KeyRound, Globe } from 'lucide-react'`).

    2. Après la lecture des `members` et `subscription` (Promise.all), ajouter le calcul des permissions portail et la lecture des sites :
    ```typescript
    const canManagePortals =
      currentMembership && ['admin', 'manager'].includes(currentMembership.role)

    const portalSites: PortalSiteRow[] = canManagePortals
      ? await db.site.findMany({
          where: { organizationId: org.id },
          orderBy: { name: 'asc' },
          select: {
            id: true,
            name: true,
            portalToken: true,
            portalEnabled: true,
          },
        })
      : []
    ```

    3. Dans le JSX retourné, juste APRÈS la section `ApiKeysSection` existante (et avant `</div>` du wrapper), ajouter conditionnellement :
    ```tsx
    {canManagePortals && (
      <section className="space-y-4">
        <div className="flex items-center gap-2">
          <Globe className="h-5 w-5 text-muted-foreground" />
          <h2 className="text-lg font-semibold">Portails publics</h2>
        </div>
        <PortalSitesSection initialSites={portalSites} />
      </section>
    )}
    ```

    Ne pas modifier la logique de gating de ApiKeysSection ni les sections existantes.
  </action>
  <verify>
    <automated>grep "import { PortalSitesSection" "src/app/(app)/parametres/organisation/page.tsx" && grep "canManagePortals" "src/app/(app)/parametres/organisation/page.tsx" && grep "portalSites" "src/app/(app)/parametres/organisation/page.tsx" && grep "initialSites={portalSites}" "src/app/(app)/parametres/organisation/page.tsx" && npx tsc --noEmit 2>&1 | grep "parametres/organisation/page.tsx" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { PortalSitesSection" "src/app/(app)/parametres/organisation/page.tsx"` renvoie 1 ligne
    - `grep "Globe" "src/app/(app)/parametres/organisation/page.tsx"` renvoie au moins 2 lignes (import + JSX)
    - `grep "canManagePortals" "src/app/(app)/parametres/organisation/page.tsx"` renvoie au moins 2 lignes (déclaration + utilisation conditionnelle)
    - `grep "db.site.findMany" "src/app/(app)/parametres/organisation/page.tsx"` renvoie 1 ligne
    - `grep "portalToken: true" "src/app/(app)/parametres/organisation/page.tsx"` renvoie 1 ligne
    - `grep "<PortalSitesSection" "src/app/(app)/parametres/organisation/page.tsx"` renvoie 1 ligne
    - Aucune nouvelle erreur TypeScript dans ce fichier (les erreurs préexistantes du repo sont tolérées)
  </acceptance_criteria>
  <done>La section "Portails publics" apparaît dans /parametres/organisation pour les admins/managers, liste tous les sites, permet de gérer chaque portail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérification humaine end-to-end du flux portail complet</name>
  <files>(checkpoint — aucun fichier modifié, exécution humaine uniquement)</files>
  <read_first>
    - .planning/phases/05-portail-de-demandes-de-travail/05-01-SUMMARY.md (si présent)
    - .planning/phases/05-portail-de-demandes-de-travail/05-02-SUMMARY.md (si présent)
  </read_first>
  <action>
    Lancer un serveur de dev local puis suivre les 8 étapes décrites dans <how-to-verify>. Le détail intégral des étapes (URLs, formulaires à remplir, comportements attendus) est ci-dessous. Aucune modification de code attendue — uniquement de la vérification humaine.
  </action>
  <acceptance_criteria>
    - L'utilisateur a complété les 8 étapes décrites dans how-to-verify
    - L'utilisateur a répondu "approved" (succès) ou a décrit précisément les écarts observés (échec)
  </acceptance_criteria>
  <verify>
    <automated>echo "Checkpoint humain — vérification manuelle décrite dans how-to-verify (npm run dev requis)"</automated>
  </verify>
  <done>Le flux end-to-end (activation portail admin → soumission anonyme → BT créé → email reçu → désactivation/régénération → gating de rôle) est validé par l'utilisateur.</done>
  <what-built>
    Plan 01 + Plan 02 + Plan 03 ensemble livrent :
    - Schema Prisma migré (portalToken, portalEnabled sur Site)
    - Proxy Clerk whitelist /portail et /api/portal
    - Server Actions enable/disable/regenerate
    - Helper createPortalWorkOrder + email PortalConfirmationEmail
    - Route Handler POST /api/portal/[siteToken]
    - Page SSR publique /portail/[siteToken] avec formulaire React 19
    - Section "Portails publics" dans /parametres/organisation
  </what-built>
  <how-to-verify>
    Démarrer le serveur de dev : `npm run dev`

    **Étape 1 — Activer un portail (admin)**
    1. Se connecter en tant qu'admin via http://localhost:3000/sign-in
    2. Aller dans http://localhost:3000/parametres/organisation
    3. Vérifier que la section "Portails publics" s'affiche avec la liste des sites de l'organisation
    4. Cliquer sur "Activer le portail" pour un site (ex. le premier)
    5. Vérifier qu'une URL apparaît au format `http://localhost:3000/portail/{uuid}` et que le toast "Portail activé" s'affiche
    6. Cliquer sur "Copier" — vérifier que l'icône passe à "Copié" pendant 2s
    7. Coller l'URL quelque part pour pouvoir l'utiliser à l'étape 2

    **Étape 2 — Soumettre une demande (anonyme)**
    8. Ouvrir une fenêtre de navigation privée (Incognito) — IMPORTANT : pas de session Clerk
    9. Coller l'URL copiée → la page doit s'afficher SANS redirection vers /sign-in
    10. Vérifier que le formulaire affiche le nom de l'organisation et le nom du site
    11. Remplir le formulaire :
        - Nom : "Jean Test"
        - Email : VOTRE adresse email (pour vérifier la réception)
        - Localisation : "Salle B-203"
        - Description : "Ceci est une demande de test depuis le portail public"
    12. Cliquer "Envoyer la demande"
    13. Vérifier qu'un message de succès apparaît avec le numéro de BT (ex: "#42")

    **Étape 3 — Vérifier la création du BT (admin)**
    14. Retourner dans la fenêtre admin
    15. Aller dans http://localhost:3000/bons-de-travail
    16. Vérifier qu'un nouveau BT existe avec le numéro affiché à l'étape 13, statut "Ouvert", type "Demande de service" (ou équivalent)
    17. Ouvrir le BT et vérifier que :
        - Le titre contient le nom du demandeur
        - La description contient "Jean Test <votre.email@...>" et "Salle B-203"
        - Le site est correctement assigné

    **Étape 4 — Vérifier l'email**
    18. Consulter la boîte email utilisée à l'étape 11
    19. Vérifier qu'un email de "Korvia <notifications@korvia.app>" arrive avec :
        - Sujet : "[Korvia] Votre demande #{numéro} a été reçue"
        - Corps en français mentionnant le numéro de BT, le nom du site, le nom de l'organisation

    **Étape 5 — Tester la désactivation**
    20. Dans la fenêtre admin, cliquer "Désactiver" pour le même site
    21. Dans la fenêtre privée, rafraîchir l'URL du portail → doit afficher 404

    **Étape 6 — Tester la régénération**
    22. Réactiver le portail, noter le NOUVEAU token
    23. Cliquer "Régénérer" → confirmer le dialog
    24. Vérifier qu'un nouveau token apparaît, différent du précédent
    25. L'ancienne URL ne fonctionne plus (404), la nouvelle fonctionne

    **Étape 7 — Tester l'anti-spam (optionnel)**
    26. Ouvrir les DevTools de la page portail
    27. Inspecter le DOM, trouver le champ caché `<input name="website">`
    28. Le rendre visible (changer le `style.position`), remplir avec "bot"
    29. Soumettre → vérifier qu'aucun BT n'est créé (mais le message de succès s'affiche quand même)

    **Étape 8 — Tester le gating de rôle**
    30. Se connecter en tant que `technician` ou `viewer`
    31. Aller dans /parametres/organisation
    32. Vérifier que la section "Portails publics" N'EST PAS visible

    Si TOUTES les étapes passent → typer "approved" ou "valid".
    Si une étape échoue → décrire précisément l'écart constaté.
  </how-to-verify>
  <resume-signal>Typer "approved" si tout fonctionne, sinon décrire les écarts (numéro d'étape + observation).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → Server Action sites.ts | Admin authentifié déclenche enablePortal/disablePortal/regeneratePortalToken |
| Admin browser → page /parametres/organisation | SSR avec auth Clerk + check rôle membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Elevation of Privilege | Section PortalSitesSection visible aux non-admins | mitigate | Gating côté Server Component : `canManagePortals = ['admin','manager'].includes(currentMembership.role)` ; section non rendue sinon ; Server Actions Plan 01 valident à nouveau le rôle (defense-in-depth) |
| T-05-16 | Tampering | siteId envoyé au Server Action depuis client malveillant | mitigate | Server Actions Plan 01 valident `site.organizationId === auth().orgId` avant toute mutation |
| T-05-17 | Information Disclosure | Copie d'URL portail | accept | Comportement intentionnel — c'est le mécanisme de partage |
| T-05-18 | Repudiation | Action régénération token | accept | Action non destructive sur les données ; ancien token simplement remplacé. Logs Vercel suffisent pour audit basique en MVP |
</threat_model>

<verification>
1. `npx vitest run src/components/settings/portal-sites-section.test.tsx` → 7+ tests verts
2. `npx tsc --noEmit` → pas de nouvelle erreur introduite par les fichiers modifiés
3. Checkpoint humain Task 3 confirme le flux end-to-end (8 étapes)
</verification>

<success_criteria>
- Admin/manager peut activer, désactiver, régénérer le portail de chaque site depuis /parametres/organisation
- L'URL publique copiée fonctionne sans authentification
- Le formulaire soumis crée un BT en DB et envoie un email au demandeur
- Le gating de rôle empêche les technicians/viewers de voir la section
- Checkpoint humain approuvé
</success_criteria>

<output>
After completion, create `.planning/phases/05-portail-de-demandes-de-travail/05-03-SUMMARY.md`
</output>
