---
phase: 05-portail-de-demandes-de-travail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/proxy.ts
  - src/actions/sites.ts
  - src/lib/portal-validation.ts
  - src/lib/portal-work-order.ts
  - src/lib/portal-work-order.test.ts
  - src/lib/email.ts
  - src/emails/portal-confirmation.tsx
autonomous: true
requirements:
  - PORTAL-01
  - PORTAL-02
must_haves:
  truths:
    - "Le modèle Site possède portalToken (String? @unique) et portalEnabled (Boolean @default(false))"
    - "Le middleware Clerk laisse passer /portail(.*) et /api/portal(.*) sans authentification"
    - "Un admin/manager peut activer/désactiver et régénérer le portalToken d'un site via Server Action"
    - "Une fonction utilitaire crée un BT 'service_request' à partir d'un siteId/organizationId résolus par token, sans appel à auth()"
    - "Une fonction sendPortalConfirmationEmail envoie un email via Resend avec le numéro de BT au demandeur"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Champs portalToken et portalEnabled sur model Site"
      contains: "portalToken"
    - path: "src/proxy.ts"
      provides: "Whitelist des routes /portail(.*) et /api/portal(.*)"
      contains: "/portail(.*)"
    - path: "src/actions/sites.ts"
      provides: "enablePortal, disablePortal, regeneratePortalToken Server Actions"
      exports: ["enablePortal", "disablePortal", "regeneratePortalToken"]
    - path: "src/lib/portal-validation.ts"
      provides: "Schéma Zod portalSubmitSchema (requesterName, requesterEmail, description, locationDescription?, honeypot?)"
      exports: ["portalSubmitSchema"]
    - path: "src/lib/portal-work-order.ts"
      provides: "createPortalWorkOrder(siteId, organizationId, payload) — pas d'appel auth()"
      exports: ["createPortalWorkOrder"]
    - path: "src/lib/email.ts"
      provides: "sendPortalConfirmationEmail()"
      exports: ["sendPortalConfirmationEmail"]
    - path: "src/emails/portal-confirmation.tsx"
      provides: "Template email React (PortalConfirmationEmail) en français"
      exports: ["PortalConfirmationEmail"]
  key_links:
    - from: "src/actions/sites.ts"
      to: "prisma.site.update"
      via: "crypto.randomUUID() pour portalToken"
      pattern: "crypto\\.randomUUID"
    - from: "src/lib/portal-work-order.ts"
      to: "db.workOrder.create"
      via: "type='service_request', status='open'"
      pattern: "type:\\s*'service_request'"
    - from: "src/lib/email.ts"
      to: "PortalConfirmationEmail"
      via: "@react-email/render + Resend"
      pattern: "PortalConfirmationEmail"
---

<objective>
Fondations Phase 5 : extension du schema Prisma pour activer le portail public par site, whitelist du middleware Clerk, Server Actions admin (activer/désactiver/régénérer le token), helper pur de création BT sans Clerk, template + fonction email de confirmation.

Purpose: Tout ce qui n'est pas la route HTTP/UI. Plan 02 (Route Handler + page publique) et Plan 03 (UI admin) consomment ces artefacts.
Output: Schema migré (db pushé), proxy à jour, actions sites étendues, helpers `createPortalWorkOrder` + `sendPortalConfirmationEmail` testés.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-portail-de-demandes-de-travail/05-RESEARCH.md

@prisma/schema.prisma
@src/proxy.ts
@src/actions/sites.ts
@src/lib/email.ts
@src/emails/work-order-assigned.tsx
@src/app/api/v1/work-orders/route.ts

<interfaces>
<!-- Contrats clés que les tâches doivent respecter -->

Site model (avant modification) — prisma/schema.prisma L142-159:
```prisma
model Site {
  id             String   @id @default(cuid())
  organizationId String
  name           String
  address        String?
  city           String?
  province       String?
  postalCode     String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
  organization   Organization @relation(...)
  locations      Location[]
  assets         Asset[]
  workOrders     WorkOrder[]
  @@index([organizationId])
}
```

WorkOrder model — clé unique (organizationId, number), enum WorkOrderType inclut `service_request`, WorkOrderStatus inclut `open`, WorkOrderPriority inclut `medium`.

Email pattern (src/lib/email.ts):
```typescript
import { Resend } from 'resend'
import { render } from '@react-email/render'
const resend = new Resend(process.env.RESEND_API_KEY)
const FROM = process.env.RESEND_FROM_EMAIL ?? 'Korvia <notifications@korvia.app>'
// signature publique: async function send*(params): Promise<void>
```

Pattern Server Action sites.ts:
```typescript
'use server'
import { auth } from '@clerk/nextjs/server'
async function getOrganizationId() { /* clerkId → org.id */ }
// puis db.site.update({ where: { id, organizationId }, data })
// revalidatePath('/actifs/sites')
```

Pattern Route Handler API REST (à imiter dans Plan 02) — src/app/api/v1/work-orders/route.ts:
- `const last = await db.workOrder.findFirst({ where: { organizationId }, orderBy: { number: 'desc' }, select: { number: true } })`
- `const number = (last?.number ?? 0) + 1`
- organizationId TOUJOURS dérivé serveur, JAMAIS du body
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Étendre le modèle Site (portalToken + portalEnabled) et whitelister le proxy</name>
  <files>prisma/schema.prisma, src/proxy.ts</files>
  <read_first>
    - prisma/schema.prisma (lignes 142-159 : model Site)
    - src/proxy.ts (intégral — voir les routes publiques déjà déclarées)
    - .planning/phases/05-portail-de-demandes-de-travail/05-RESEARCH.md (sections "Pattern 1" et "Pattern 2")
  </read_first>
  <action>
    1. Dans `prisma/schema.prisma`, modifier `model Site` (lignes 142-159) en ajoutant exactement après le champ `updatedAt` et AVANT le bloc `organization Organization @relation(...)` :
    ```prisma
      portalToken    String?  @unique
      portalEnabled  Boolean  @default(false)
    ```
    Puis ajouter dans le bloc `@@index(...)` à la fin du modèle (après `@@index([organizationId])`) :
    ```prisma
      @@index([portalToken])
    ```
    2. Dans `src/proxy.ts`, dans le tableau passé à `createRouteMatcher([...])` (lignes 3-17), ajouter ces deux entrées juste après `"/api/qr(.*)"` et avant `"/api/cron(.*)"` :
    ```typescript
      "/portail(.*)",
      "/api/portal(.*)",
    ```
    Ne pas modifier le reste du fichier (la fonction `proxy`, la config `matcher`).
  </action>
  <verify>
    <automated>grep -E "portalToken\\s+String\\?\\s+@unique" prisma/schema.prisma && grep -E "portalEnabled\\s+Boolean\\s+@default\\(false\\)" prisma/schema.prisma && grep -E "@@index\\(\\[portalToken\\]\\)" prisma/schema.prisma && grep -E "/portail\\(\\.\\*\\)" src/proxy.ts && grep -E "/api/portal\\(\\.\\*\\)" src/proxy.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "portalToken    String?  @unique" prisma/schema.prisma` renvoie 1 ligne (dans le bloc model Site, entre lignes 142 et ~165)
    - `grep "portalEnabled  Boolean  @default(false)" prisma/schema.prisma` renvoie 1 ligne
    - `grep "@@index(\[portalToken\])" prisma/schema.prisma` renvoie 1 ligne
    - `grep '"/portail(.*)"' src/proxy.ts` renvoie 1 ligne dans `createRouteMatcher`
    - `grep '"/api/portal(.*)"' src/proxy.ts` renvoie 1 ligne dans `createRouteMatcher`
    - `npx prisma validate` retourne exit code 0
  </acceptance_criteria>
  <done>Schema validé par `prisma validate`, proxy compilable, deux nouvelles routes whitelistées.</done>
</task>

<task type="auto" tdd="false" blocking="true">
  <name>Task 2: [BLOCKING] Pousser le schema Prisma + régénérer le client</name>
  <files>prisma/schema.prisma (lecture seule)</files>
  <read_first>
    - prisma/schema.prisma (vérifier que Task 1 est appliqué)
  </read_first>
  <action>
    Exécuter dans cet ordre exact :
    1. `npx prisma db push --accept-data-loss` (les nouveaux champs sont nullable / ont des défauts → pas de perte réelle, le flag couvre les environnements non-TTY)
    2. `npx prisma generate`
    Si l'étape 1 échoue avec une erreur de connexion, vérifier que `DATABASE_URL` est défini puis retenter. Ne JAMAIS sauter cette tâche : Plan 02 et Plan 03 utilisent `db.site.update({ data: { portalToken, portalEnabled } })` qui ne typecheckera pas tant que le client Prisma n'est pas régénéré.
  </action>
  <verify>
    <automated>npx prisma db push --accept-data-loss && npx prisma generate && node -e "const {PrismaClient}=require('@prisma/client'); const p=new PrismaClient(); console.log(Object.keys(p.site.fields ?? {portalToken:1, portalEnabled:1}).includes('portalToken') || 'ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push --accept-data-loss` sort en exit 0 et imprime "Your database is now in sync with your Prisma schema" (ou équivalent)
    - `npx prisma generate` sort en exit 0
    - `grep "portalToken" node_modules/.prisma/client/index.d.ts` renvoie au moins 1 ligne (le type généré contient le champ)
    - `grep "portalEnabled" node_modules/.prisma/client/index.d.ts` renvoie au moins 1 ligne
  </acceptance_criteria>
  <done>Base de données et client Prisma synchronisés avec le nouveau schema. Plans 02 et 03 peuvent désormais utiliser `site.portalToken` et `site.portalEnabled` sans erreur de type.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Server Actions admin du portail dans sites.ts (enable/disable/regenerate)</name>
  <files>src/actions/sites.ts</files>
  <read_first>
    - src/actions/sites.ts (intégral — pattern getOrganizationId, auth, db.site.update)
    - .planning/phases/05-portail-de-demandes-de-travail/05-RESEARCH.md (section "Token opaque sur le modèle Site")
  </read_first>
  <action>
    À la fin du fichier `src/actions/sites.ts` (après `deleteLocation`), ajouter trois nouvelles Server Actions. Réutiliser le helper local `getOrganizationId()` déjà présent (lignes 7-16). Importer `crypto` n'est pas nécessaire : `crypto.randomUUID()` est globalement disponible sous Node 18+.

    Ajouter en plus un helper interne pour le contrôle de rôle (admin OU manager seulement — la recherche le recommande, Open Question #2) :

    ```typescript
    async function requireAdminOrManager(organizationId: string) {
      const { userId } = await auth()
      if (!userId) throw new Error('Non autorisé')
      const membership = await db.membership.findFirst({
        where: { organizationId, clerkUserId: userId },
        select: { role: true },
      })
      if (!membership || !['admin', 'manager'].includes(membership.role)) {
        throw new Error('Permissions insuffisantes')
      }
    }

    export async function enablePortal(siteId: string): Promise<{ portalToken: string }> {
      const organizationId = await getOrganizationId()
      await requireAdminOrManager(organizationId)
      const existing = await db.site.findUnique({
        where: { id: siteId },
        select: { organizationId: true, portalToken: true },
      })
      if (!existing || existing.organizationId !== organizationId) {
        throw new Error('Site introuvable')
      }
      const portalToken = existing.portalToken ?? crypto.randomUUID()
      await db.site.update({
        where: { id: siteId },
        data: { portalToken, portalEnabled: true },
      })
      revalidatePath('/parametres/organisation')
      revalidatePath('/actifs/sites')
      return { portalToken }
    }

    export async function disablePortal(siteId: string): Promise<void> {
      const organizationId = await getOrganizationId()
      await requireAdminOrManager(organizationId)
      const existing = await db.site.findUnique({
        where: { id: siteId },
        select: { organizationId: true },
      })
      if (!existing || existing.organizationId !== organizationId) {
        throw new Error('Site introuvable')
      }
      await db.site.update({
        where: { id: siteId },
        data: { portalEnabled: false },
      })
      revalidatePath('/parametres/organisation')
      revalidatePath('/actifs/sites')
    }

    export async function regeneratePortalToken(siteId: string): Promise<{ portalToken: string }> {
      const organizationId = await getOrganizationId()
      await requireAdminOrManager(organizationId)
      const existing = await db.site.findUnique({
        where: { id: siteId },
        select: { organizationId: true },
      })
      if (!existing || existing.organizationId !== organizationId) {
        throw new Error('Site introuvable')
      }
      const portalToken = crypto.randomUUID()
      await db.site.update({
        where: { id: siteId },
        data: { portalToken },
      })
      revalidatePath('/parametres/organisation')
      revalidatePath('/actifs/sites')
      return { portalToken }
    }
    ```

    Ne pas modifier les actions existantes (`createSite`, `updateSite`, `deleteSite`, `createLocation`, `updateLocation`, `deleteLocation`).
  </action>
  <verify>
    <automated>grep -E "^export async function enablePortal" src/actions/sites.ts && grep -E "^export async function disablePortal" src/actions/sites.ts && grep -E "^export async function regeneratePortalToken" src/actions/sites.ts && grep -E "crypto\\.randomUUID\\(\\)" src/actions/sites.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^export async function enablePortal" src/actions/sites.ts` renvoie 1 ligne
    - `grep "^export async function disablePortal" src/actions/sites.ts` renvoie 1 ligne
    - `grep "^export async function regeneratePortalToken" src/actions/sites.ts` renvoie 1 ligne
    - `grep "crypto.randomUUID()" src/actions/sites.ts` renvoie au moins 2 lignes (enable lazy + regenerate)
    - `grep "requireAdminOrManager" src/actions/sites.ts` renvoie au moins 4 lignes (1 def + 3 appels)
    - `npx tsc --noEmit` n'introduit pas de nouvelle erreur TS dans `src/actions/sites.ts` (les erreurs préexistantes éventuelles dans d'autres fichiers sont tolérées)
  </acceptance_criteria>
  <done>Trois Server Actions exportées, contrôle de rôle admin|manager, isolation par organizationId vérifiée, revalidation des paths admin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Helper createPortalWorkOrder (sans Clerk) + tests Vitest</name>
  <files>src/lib/portal-validation.ts, src/lib/portal-work-order.ts, src/lib/portal-work-order.test.ts</files>
  <read_first>
    - src/app/api/v1/work-orders/route.ts (pattern numérotation + création BT sans auth())
    - src/lib/api-validation.ts (style Zod schemas du projet)
    - prisma/schema.prisma (lignes 276-307 : model WorkOrder, énumérations WorkOrderType/Status/Priority)
  </read_first>
  <behavior>
    - Test 1: portalSubmitSchema.safeParse({}) renvoie success=false (champs requis manquants)
    - Test 2: portalSubmitSchema.safeParse({ requesterName: 'X', requesterEmail: 'a@b.c', description: 'court' }) → success=false (description < 10 chars)
    - Test 3: portalSubmitSchema.safeParse(payload valide) → success=true et data.honeypot === undefined si non fourni
    - Test 4: portalSubmitSchema considère `honeypot` rempli comme valide au niveau Zod (la détection spam est faite par l'appelant) — on vérifie juste que le champ est accepté optionnel
    - Test 5: createPortalWorkOrder appelle db.workOrder.findFirst puis db.workOrder.create avec type='service_request', status='open', priority='medium', organizationId+siteId passés en paramètre (jamais depuis payload)
    - Test 6: createPortalWorkOrder calcule number = (last?.number ?? 0) + 1
    - Test 7: createPortalWorkOrder retourne l'objet créé incluant `number`
  </behavior>
  <action>
    1. Créer `src/lib/portal-validation.ts` :
    ```typescript
    import { z } from 'zod'

    export const portalSubmitSchema = z.object({
      requesterName: z.string().min(1).max(100),
      requesterEmail: z.string().email().max(200),
      description: z.string().min(10).max(1000),
      locationDescription: z.string().max(200).optional(),
      // Honeypot: champ caché dans le formulaire — rempli = bot
      honeypot: z.string().max(0).optional().or(z.literal('')),
    })

    export type PortalSubmitInput = z.infer<typeof portalSubmitSchema>
    ```

    2. Créer `src/lib/portal-work-order.ts` :
    ```typescript
    import { db } from '@/lib/db'
    import type { PortalSubmitInput } from '@/lib/portal-validation'

    export interface CreatePortalWorkOrderParams {
      organizationId: string
      siteId: string
      input: Omit<PortalSubmitInput, 'honeypot'>
    }

    export async function createPortalWorkOrder({ organizationId, siteId, input }: CreatePortalWorkOrderParams) {
      const last = await db.workOrder.findFirst({
        where: { organizationId },
        orderBy: { number: 'desc' },
        select: { number: true },
      })
      const number = (last?.number ?? 0) + 1

      const title = `Demande portail — ${input.requesterName}`.slice(0, 120)
      const description = [
        `Demandeur: ${input.requesterName} <${input.requesterEmail}>`,
        input.locationDescription ? `Localisation: ${input.locationDescription}` : null,
        '',
        input.description,
      ].filter(Boolean).join('\n')

      const wo = await db.workOrder.create({
        data: {
          organizationId,
          siteId,
          number,
          title,
          description,
          type: 'service_request',
          status: 'open',
          priority: 'medium',
        },
        select: {
          id: true,
          number: true,
          title: true,
          description: true,
          status: true,
          priority: true,
          type: true,
          siteId: true,
          organizationId: true,
          createdAt: true,
        },
      })
      return wo
    }
    ```

    3. Créer `src/lib/portal-work-order.test.ts` avec Vitest. Mocker `@/lib/db` exactement comme `src/lib/api-auth.test.ts` (voir ce fichier pour le pattern de mock vi.mock). Couvrir les 7 comportements listés dans `<behavior>`.

    Pour les tests Zod (1-4), pas de mock nécessaire. Pour les tests createPortalWorkOrder (5-7), mocker `db.workOrder.findFirst` (retourne `null` ou `{ number: 7 }`) et `db.workOrder.create` (retourne l'objet attendu).

    Vérifier ASSV V4: l'argument `organizationId` passé à `db.workOrder.create` doit STRICTEMENT être celui du paramètre `params.organizationId`, JAMAIS dérivé de `input` (assertion `expect(mockCreate).toHaveBeenCalledWith(expect.objectContaining({ data: expect.objectContaining({ organizationId: 'org-test-id' }) }))`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/portal-work-order.test.ts src/lib/portal-validation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/portal-work-order.test.ts` passe avec au moins 7 tests verts
    - `grep "export const portalSubmitSchema" src/lib/portal-validation.ts` renvoie 1 ligne
    - `grep "export async function createPortalWorkOrder" src/lib/portal-work-order.ts` renvoie 1 ligne
    - `grep "type: 'service_request'" src/lib/portal-work-order.ts` renvoie 1 ligne
    - `grep "auth(" src/lib/portal-work-order.ts` renvoie 0 ligne (CRITIQUE : aucun appel à Clerk)
    - Le test 5 ou équivalent assert explicitement que `organizationId` vient des params, pas de l'input
  </acceptance_criteria>
  <done>Schéma Zod et helper de création BT découplés de Clerk, testés en isolation. Plan 02 importe ces deux modules dans le Route Handler.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Template email PortalConfirmationEmail + sendPortalConfirmationEmail()</name>
  <files>src/emails/portal-confirmation.tsx, src/lib/email.ts</files>
  <read_first>
    - src/emails/work-order-assigned.tsx (style HTML email à reproduire : table-based, palette Korvia #E8830C, footer standard)
    - src/lib/email.ts (intégral : pattern d'export, FROM, render + resend.emails.send)
    - .planning/phases/05-portail-de-demandes-de-travail/05-RESEARCH.md (section "Pattern 6")
  </read_first>
  <action>
    1. Créer `src/emails/portal-confirmation.tsx` en répliquant la structure visuelle exacte de `src/emails/work-order-assigned.tsx` (header `#E8830C`, table 600px, footer gris). Props :
    ```typescript
    interface Props {
      requesterName: string
      workOrderNumber: number
      siteName: string
      organizationName: string
    }

    export function PortalConfirmationEmail({ requesterName, workOrderNumber, siteName, organizationName }: Props) { ... }
    ```
    Le corps doit contenir EN TEXTE LITTÉRAL (verrifiable par grep) :
    - "Bonjour {requesterName}"
    - "Votre demande a bien été reçue"
    - "Demande #{workOrderNumber}"
    - "Site : {siteName}" (utiliser la variable siteName interpolée)
    - "Organisation : {organizationName}"
    - Footer identique à work-order-assigned.tsx ("Korvia — Gestion de maintenance assistée par ordinateur")

    Pas de lien de tracking, pas de bouton CTA (le demandeur n'a pas de compte).

    2. Modifier `src/lib/email.ts` :
       - Ajouter l'import en haut, à côté des autres imports d'emails :
         ```typescript
         import { PortalConfirmationEmail } from '@/emails/portal-confirmation'
         ```
       - Ajouter à la fin du fichier (après `sendLowStockAlertEmail`) :
         ```typescript
         export async function sendPortalConfirmationEmail(params: {
           to: string
           requesterName: string
           workOrderNumber: number
           siteName: string
           organizationName: string
         }): Promise<void> {
           const html = await render(PortalConfirmationEmail(params))
           await resend.emails.send({
             from: FROM,
             to: [params.to],
             subject: `[Korvia] Votre demande #${params.workOrderNumber} a été reçue`,
             html,
           })
         }
         ```
  </action>
  <verify>
    <automated>grep -E "^export function PortalConfirmationEmail" src/emails/portal-confirmation.tsx && grep "PortalConfirmationEmail" src/lib/email.ts | wc -l | grep -E "^[2-9]" && grep -E "^export async function sendPortalConfirmationEmail" src/lib/email.ts && grep "Votre demande a bien été reçue" src/emails/portal-confirmation.tsx && grep -E "subject:.*Votre demande #" src/lib/email.ts && npx tsc --noEmit -p . 2>&1 | grep -E "portal-confirmation|sendPortalConfirmation" | grep -v "node_modules" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function PortalConfirmationEmail" src/emails/portal-confirmation.tsx` renvoie 1 ligne
    - `grep "Bonjour {requesterName}" src/emails/portal-confirmation.tsx` OU `grep "Bonjour " src/emails/portal-confirmation.tsx | grep requesterName` renvoie 1 ligne
    - `grep "Demande #" src/emails/portal-confirmation.tsx` renvoie au moins 1 ligne
    - `grep "Korvia — Gestion de maintenance" src/emails/portal-confirmation.tsx` renvoie 1 ligne (footer)
    - `grep "export async function sendPortalConfirmationEmail" src/lib/email.ts` renvoie 1 ligne
    - `grep "import { PortalConfirmationEmail }" src/lib/email.ts` renvoie 1 ligne
    - `grep 'subject: `\[Korvia\] Votre demande #' src/lib/email.ts` renvoie 1 ligne (ou regex équivalent — le sujet contient `[Korvia] Votre demande #`)
    - Aucune erreur TypeScript introduite sur ces deux fichiers
  </acceptance_criteria>
  <done>Email de confirmation rendable, sendPortalConfirmationEmail exporté, prêt à être appelé fire-and-forget depuis le Route Handler du Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser admin → Server Action (sites.ts) | Admin Clerk authentifié modifie l'état du portail d'un site |
| Cron/Internal → Prisma | Mutation directe DB pour activer le portail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | enablePortal/disablePortal/regeneratePortalToken | mitigate | Vérifier `site.organizationId === auth().orgId` avant toute mutation (helper getOrganizationId + lookup explicite) — implémenté dans Task 3 |
| T-05-02 | Elevation of Privilege | Server Actions portal | mitigate | requireAdminOrManager() bloque les rôles `technician`/`viewer` — implémenté dans Task 3 |
| T-05-03 | Information Disclosure | portalToken stocké | accept | UUID v4 (2^122) — fuite via copie URL est intentionnelle (c'est le mécanisme de partage) |
| T-05-04 | Spoofing | createPortalWorkOrder paramètre organizationId | mitigate | organizationId/siteId TOUJOURS passés en argument depuis l'appelant (résolus serveur via token DB) ; test 5 vérifie qu'ils ne viennent pas de `input` — Task 4 |
| T-05-05 | Tampering | Champ description / locationDescription | mitigate | Zod `.max(1000)` / `.max(200)`, React échappe par défaut — Task 4 |
| T-05-06 | Information Disclosure | Email confirmation envoie numéro BT à inconnu | accept | Le demandeur fournit lui-même son email, l'info envoyée est sa propre demande |
</threat_model>

<verification>
1. `npx prisma validate` → exit 0
2. `npx prisma db push --accept-data-loss` → "in sync"
3. `npx vitest run src/lib/portal-work-order.test.ts` → 7+ tests verts
4. `npx tsc --noEmit` → pas de nouvelle erreur introduite par les fichiers modifiés
5. `grep -c "portalToken\|portalEnabled" prisma/schema.prisma` → ≥ 3
6. `grep "/portail(.*)" src/proxy.ts` → 1 match
</verification>

<success_criteria>
- Schema Prisma migré et client régénéré (db.site.portalToken/portalEnabled disponibles dans le type généré)
- proxy.ts laisse passer /portail(.*) et /api/portal(.*) sans appel à auth.protect()
- sites.ts expose enablePortal, disablePortal, regeneratePortalToken avec contrôle de rôle admin|manager
- portal-work-order.ts crée des BT sans aucun appel à auth(), avec organizationId/siteId garantis serveur
- email.ts expose sendPortalConfirmationEmail, template français Korvia-branded
- Tests unitaires verts pour portal-validation + portal-work-order (7+ assertions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-portail-de-demandes-de-travail/05-01-SUMMARY.md`
</output>
