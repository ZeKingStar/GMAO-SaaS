---
phase: 06
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(app)/parametres/organisation/page.tsx
  - src/components/settings/closure-requirements-section.tsx
  - src/components/settings/team-table.tsx
autonomous: true
requirements:
  - TERRAIN-01
  - TERRAIN-02

must_haves:
  truths:
    - "Sur /parametres/organisation, un admin/manager voit une section 'Exigences de clôture' avec 3 cases à cocher (Code de panne, Temps passé, Pièces utilisées)"
    - "Cocher/décocher et cliquer Sauvegarder persiste vers Organization.closureRequirements"
    - "Un admin/manager voit une colonne 'Taux horaire' dans la TeamTable avec un input numérique pour chaque membre"
    - "Modifier un taux horaire et valider persiste vers Membership.hourlyRate"
    - "Un technicien (non-admin) ne voit PAS la section 'Exigences de clôture' ni la colonne taux éditable (lecture seule ou masquée)"
  artifacts:
    - path: "src/app/(app)/parametres/organisation/page.tsx"
      provides: "Chargement des champs closureRequirements (org) + hourlyRate (members) + ajout de la nouvelle section"
      contains: "ClosureRequirementsSection"
    - path: "src/components/settings/closure-requirements-section.tsx"
      provides: "Composant client 3-checkbox + bouton Sauvegarder"
      exports: ["ClosureRequirementsSection"]
    - path: "src/components/settings/team-table.tsx"
      provides: "Colonne Taux horaire éditable pour admin/manager, lecture seule sinon"
      contains: "hourlyRate"
  key_links:
    - from: "ClosureRequirementsSection"
      to: "updateClosureRequirements (Server Action)"
      via: "form submit → useTransition → revalidatePath"
      pattern: "updateClosureRequirements\\("
    - from: "TeamTable"
      to: "updateMemberHourlyRate (Server Action)"
      via: "blur ou Enter sur input → Server Action"
      pattern: "updateMemberHourlyRate\\("
---

<objective>
Construire la UI admin pour configurer les exigences de clôture (TERRAIN-01) et les taux horaires par technicien (TERRAIN-02). Branchée sur les Server Actions de Plan 01 (`updateClosureRequirements`, `updateMemberHourlyRate`).

Purpose: Sans cette UI, les Server Actions admin sont inaccessibles depuis l'app. C'est ce qui rend TERRAIN-01 réellement configurable et TERRAIN-02 réellement utilisable.

Output: Section "Exigences de clôture" + colonne "Taux horaire" éditable dans Team Table, gardées par role admin/manager.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md
@.planning/phases/06-int-grit-des-donn-es-terrain/06-01-PLAN.md

@src/app/(app)/parametres/organisation/page.tsx
@src/components/settings/team-table.tsx
@src/components/settings/org-settings-form.tsx
@src/lib/closure-requirements.ts
@src/actions/settings.ts

<interfaces>
<!-- Contrats consommés depuis Plan 01 -->

From src/lib/closure-requirements.ts :
```typescript
export type ClosureRequirements = { faultCode: boolean; timeSpent: boolean; partsUsed: boolean }
export const DEFAULT_CLOSURE_REQUIREMENTS: ClosureRequirements
export const CLOSURE_FIELD_LABELS: { faultCode: string; timeSpent: string; partsUsed: string }
export function parseClosureRequirements(raw: unknown): ClosureRequirements
```

From src/actions/settings.ts :
```typescript
updateClosureRequirements(req: ClosureRequirements): Promise<void>
updateMemberHourlyRate(membershipId: string, hourlyRate: number | null): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Créer ClosureRequirementsSection (composant client avec 3 checkbox + save)</name>
  <files>src/components/settings/closure-requirements-section.tsx</files>
  <read_first>
    - src/components/settings/org-settings-form.tsx (template de composant settings client)
    - src/lib/closure-requirements.ts (CLOSURE_FIELD_LABELS, ClosureRequirements type)
    - src/actions/settings.ts (signature updateClosureRequirements)
  </read_first>
  <action>
    Créer `src/components/settings/closure-requirements-section.tsx` :

    ```typescript
    'use client'

    import { useState, useTransition } from 'react'
    import { Button } from '@/components/ui/button'
    import { toast } from 'sonner'
    import { updateClosureRequirements } from '@/actions/settings'
    import {
      type ClosureRequirements,
      CLOSURE_FIELD_LABELS,
    } from '@/lib/closure-requirements'

    type Props = {
      initial: ClosureRequirements
    }

    export function ClosureRequirementsSection({ initial }: Props) {
      const [pending, startTransition] = useTransition()
      const [req, setReq] = useState<ClosureRequirements>(initial)
      const dirty =
        req.faultCode !== initial.faultCode ||
        req.timeSpent !== initial.timeSpent ||
        req.partsUsed !== initial.partsUsed

      function toggle(key: keyof ClosureRequirements) {
        setReq(r => ({ ...r, [key]: !r[key] }))
      }

      function handleSubmit(e: React.FormEvent) {
        e.preventDefault()
        startTransition(async () => {
          try {
            await updateClosureRequirements(req)
            toast.success('Exigences de clôture enregistrées')
          } catch (e) {
            toast.error(e instanceof Error ? e.message : 'Erreur')
          }
        })
      }

      const fields: { key: keyof ClosureRequirements; label: string }[] = [
        { key: 'faultCode', label: CLOSURE_FIELD_LABELS.faultCode },
        { key: 'timeSpent', label: CLOSURE_FIELD_LABELS.timeSpent },
        { key: 'partsUsed', label: CLOSURE_FIELD_LABELS.partsUsed },
      ]

      return (
        <form onSubmit={handleSubmit} className="space-y-4">
          <p className="text-sm text-muted-foreground">
            Choisissez les champs que le technicien doit obligatoirement remplir avant de pouvoir
            marquer un bon de travail comme « Résolu » ou « Fermé ».
          </p>
          <div className="space-y-2">
            {fields.map(f => (
              <label key={f.key} className="flex items-start gap-3 p-3 rounded-lg border bg-card hover:bg-muted/30 cursor-pointer">
                <input
                  type="checkbox"
                  checked={req[f.key]}
                  onChange={() => toggle(f.key)}
                  className="mt-0.5 h-4 w-4 rounded border-input"
                />
                <span className="text-sm">{f.label}</span>
              </label>
            ))}
          </div>
          <div className="flex justify-end">
            <Button type="submit" disabled={!dirty || pending}>
              {pending ? 'Enregistrement…' : 'Sauvegarder'}
            </Button>
          </div>
        </form>
      )
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "closure-requirements-section\.tsx" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use client'" src/components/settings/closure-requirements-section.tsx` retourne 1 ligne (1ère ligne)
    - `grep "export function ClosureRequirementsSection" src/components/settings/closure-requirements-section.tsx` retourne 1 ligne
    - `grep "updateClosureRequirements" src/components/settings/closure-requirements-section.tsx` retourne au moins 2 lignes (import + appel)
    - `grep "CLOSURE_FIELD_LABELS" src/components/settings/closure-requirements-section.tsx` retourne au moins 2 lignes
    - `grep "type=\"checkbox\"" src/components/settings/closure-requirements-section.tsx` retourne 1 ligne
    - `grep "disabled={!dirty" src/components/settings/closure-requirements-section.tsx` retourne 1 ligne
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>Composant 3-checkbox + Sauvegarder, bouton désactivé tant qu'aucune modification, build TS propre.</done>
</task>

<task type="auto">
  <name>Task 2: Étendre TeamTable avec une colonne Taux horaire éditable (admin/manager) ou lecture seule</name>
  <files>src/components/settings/team-table.tsx</files>
  <read_first>
    - src/components/settings/team-table.tsx (voir pattern actuel : `canManage`, `handleRoleChange`, table HTML)
    - src/actions/settings.ts (signature updateMemberHourlyRate)
  </read_first>
  <action>
    Modifier `src/components/settings/team-table.tsx` :

    1. Ajouter en haut : `import { updateMemberHourlyRate } from '@/actions/settings'`.

    2. Étendre le type `Member` (déjà déclaré dans le fichier) :
       ```typescript
       type Member = {
         id: string
         email: string
         firstName: string | null
         lastName: string | null
         avatarUrl: string | null
         role: MemberRole
         hourlyRate: number | null    // <-- NOUVEAU
         createdAt: Date
       }
       ```

    3. Ajouter dans le composant un handler :
       ```typescript
       function handleRateBlur(membershipId: string, raw: string) {
         const trimmed = raw.trim()
         if (trimmed === '') {
           startTransition(async () => {
             try { await updateMemberHourlyRate(membershipId, null); toast.success('Taux retiré') }
             catch (e) { toast.error(e instanceof Error ? e.message : 'Erreur') }
           })
           return
         }
         const value = parseFloat(trimmed)
         if (!Number.isFinite(value) || value < 0 || value > 10000) {
           toast.error('Taux invalide (0 à 10 000 $/h)')
           return
         }
         startTransition(async () => {
           try { await updateMemberHourlyRate(membershipId, value); toast.success('Taux mis à jour') }
           catch (e) { toast.error(e instanceof Error ? e.message : 'Erreur') }
         })
       }
       ```

    4. Ajouter une colonne dans le `<thead>` (après "Rôle") :
       ```tsx
       <th className="text-left px-4 py-2.5 font-medium text-muted-foreground hidden md:table-cell">Taux horaire</th>
       ```

    5. Ajouter une cellule correspondante dans le `<tbody>` map, juste après la cellule Rôle. La cellule doit :
       - Si `canManage` : afficher un `<input type="number" defaultValue={member.hourlyRate ?? ''} ...>` avec `onBlur={e => handleRateBlur(member.id, e.target.value)}`
       - Sinon : afficher le taux en lecture seule (`{member.hourlyRate != null ? \`${member.hourlyRate.toFixed(2)} $/h\` : '—'}`)

       ```tsx
       <td className="px-4 py-3 hidden md:table-cell">
         {canManage ? (
           <div className="flex items-center gap-1">
             <input
               type="number"
               step="0.5"
               min="0"
               max="10000"
               defaultValue={member.hourlyRate ?? ''}
               placeholder="—"
               onBlur={e => handleRateBlur(member.id, e.target.value)}
               className="h-8 w-24 rounded-md border border-input bg-background px-2 text-xs focus:outline-none focus:ring-2 focus:ring-ring/50"
             />
             <span className="text-xs text-muted-foreground">$/h</span>
           </div>
         ) : (
           <span className="text-xs text-muted-foreground">
             {member.hourlyRate != null ? `${member.hourlyRate.toFixed(2)} $/h` : '—'}
           </span>
         )}
       </td>
       ```

    6. Pas de modification du colonnage `Depuis` (hidden lg:table-cell) — il reste.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "team-table\.tsx" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "updateMemberHourlyRate" src/components/settings/team-table.tsx` retourne au moins 2 lignes (import + au moins 1 appel)
    - `grep "hourlyRate: number | null" src/components/settings/team-table.tsx` retourne 1 ligne
    - `grep "Taux horaire" src/components/settings/team-table.tsx` retourne au moins 1 ligne
    - `grep "handleRateBlur" src/components/settings/team-table.tsx` retourne au moins 2 lignes
    - `grep "onBlur={e => handleRateBlur" src/components/settings/team-table.tsx` retourne 1 ligne
    - `grep "value > 10000" src/components/settings/team-table.tsx` retourne 1 ligne (validation client miroir du serveur)
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>Colonne Taux horaire ajoutée à TeamTable, input éditable pour admin/manager, lecture seule sinon, validation client + serveur.</done>
</task>

<task type="auto">
  <name>Task 3: Câbler ClosureRequirementsSection dans /parametres/organisation/page.tsx + passer hourlyRate aux members</name>
  <files>src/app/(app)/parametres/organisation/page.tsx</files>
  <read_first>
    - src/app/(app)/parametres/organisation/page.tsx (voir les sections existantes : Organisation / Abonnement / Équipe / API / Portails)
    - src/components/settings/closure-requirements-section.tsx (Task 1)
    - src/components/settings/team-table.tsx (Task 2 : Member.hourlyRate)
    - src/lib/closure-requirements.ts (parseClosureRequirements)
  </read_first>
  <action>
    Modifier `src/app/(app)/parametres/organisation/page.tsx` :

    1. Ajouter imports :
       ```typescript
       import { ClosureRequirementsSection } from '@/components/settings/closure-requirements-section'
       import { parseClosureRequirements } from '@/lib/closure-requirements'
       import { ClipboardCheck } from 'lucide-react'
       ```

    2. Étendre le `db.organization.findUnique` pour inclure `closureRequirements: true` :
       ```typescript
       const org = await db.organization.findUnique({
         where: { clerkId: orgId },
         select: { id: true, name: true, industry: true, size: true, closureRequirements: true },
       })
       ```

    3. La requête `db.membership.findMany` retourne déjà tous les scalars par défaut (pas de `select`). Vérifier — si elle utilise `select`, ajouter `hourlyRate: true`. Sinon, aucune modification (les nouveaux champs Prisma sont inclus automatiquement).

       Si la requête est sans `select` (cas actuel), Prisma retourne `hourlyRate` automatiquement après régénération du client (Plan 01 Task 2). Aucun changement requis.

    4. Calculer `closureRequirements` :
       ```typescript
       const closureReq = parseClosureRequirements(org.closureRequirements)
       ```

    5. Insérer une nouvelle section "Exigences de clôture" entre "Équipe" et "Clés API" (admin/manager only) :
       ```tsx
       {currentMembership && ['admin', 'manager'].includes(currentMembership.role) && (
         <section className="space-y-4">
           <div className="flex items-center gap-2">
             <ClipboardCheck className="h-4 w-4 text-muted-foreground" />
             <h2 className="font-semibold">Exigences de clôture des bons de travail</h2>
           </div>
           <div className="border rounded-lg p-5 bg-card">
             <ClosureRequirementsSection initial={closureReq} />
           </div>
         </section>
       )}
       ```

    6. Vérifier que la `TeamTable` reçoit les members avec leur `hourlyRate` (cas automatique si pas de select).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "parametres/organisation/page\.tsx" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ClosureRequirementsSection" src/app/\(app\)/parametres/organisation/page.tsx` retourne au moins 2 lignes (import + JSX)
    - `grep "parseClosureRequirements" src/app/\(app\)/parametres/organisation/page.tsx` retourne au moins 2 lignes (import + appel)
    - `grep "closureRequirements: true" src/app/\(app\)/parametres/organisation/page.tsx` retourne 1 ligne (select étendu)
    - `grep "Exigences de clôture" src/app/\(app\)/parametres/organisation/page.tsx` retourne 1 ligne (heading section)
    - `grep "currentMembership.role\|currentMembership && \['admin'" src/app/\(app\)/parametres/organisation/page.tsx` retourne au moins 1 ligne (role gate)
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>Section "Exigences de clôture" affichée pour admin/manager, TeamTable reçoit hourlyRate, build propre. UI admin complète pour Phase 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → updateClosureRequirements | Inputs : 3 bools — coerce serveur + auth admin/manager |
| Browser → updateMemberHourlyRate | Input : membershipId + number — auth admin/manager + bornes [0, 10000] |
| UI gate (role check) | Section et input éditable masqués pour non-admin, mais c'est cosmétique : la sécurité vraie est serveur |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Elevation of Privilege | Un technicien tente d'appeler updateClosureRequirements en bypassant le client | mitigate | Plan 01 Task 5 : `getAdminOrg()` throw si role !== admin/manager — UI gate est cosmétique |
| T-06-17 | Tampering | Input "Taux horaire" envoie une valeur négative ou massive | mitigate | Validation client (0..10000) + validation serveur (Plan 01 Task 5) — defense-in-depth |
| T-06-18 | Information Disclosure | Lecture seule du taux horaire d'un autre membre par un non-admin | accept | D-16 : taux horaire est interne à l'org, lecture acceptable ; le rendu lecture-seule l'affiche déjà dans TeamTable |
| T-06-19 | Tampering | Bypass UI checkbox via DevTools pour activer faultCode | mitigate | Le serveur sanitize via `!!req.faultCode` (Plan 01 Task 5) — pas de bypass possible |
</threat_model>

<verification>
- `npx tsc --noEmit` : aucune erreur TS
- Inspection visuelle dans la session de checkpoint Plan 02 (le checkpoint humain peut couvrir cette UI aussi en passant) : section "Exigences de clôture" visible pour admin avec 3 checkboxes ; colonne "Taux horaire" éditable pour admin et lecture seule pour autres rôles
- Test fonctionnel : modifier une exigence → revalidatePath → BT detail se met à jour au prochain chargement
</verification>

<success_criteria>
- ClosureRequirementsSection créé et câblé dans /parametres/organisation (admin/manager only)
- TeamTable étendue avec colonne Taux horaire (éditable admin/manager, lecture sinon)
- Server Actions de Plan 01 (updateClosureRequirements, updateMemberHourlyRate) appelées depuis l'UI
- Validation client miroir de la validation serveur (bornes, types)
- Build TypeScript propre
</success_criteria>

<output>
After completion, create `.planning/phases/06-int-grit-des-donn-es-terrain/06-03-SUMMARY.md`
</output>
</content>
</invoke>