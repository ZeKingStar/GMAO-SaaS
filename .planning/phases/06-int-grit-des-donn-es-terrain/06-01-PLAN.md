---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/closure-requirements.ts
  - src/actions/work-orders.ts
  - src/actions/settings.ts
autonomous: true
requirements:
  - TERRAIN-01
  - TERRAIN-02

must_haves:
  truths:
    - "Le schéma Prisma définit WorkOrder.faultCategory, WorkOrder.faultDescription, Organization.closureRequirements (Json), Membership.hourlyRate (Float?)"
    - "Le modèle WorkOrderPart existe avec workOrderId, sparePartId (optionnel), name, quantity, unitCost (optionnel)"
    - "La base de données contient les nouvelles colonnes après `npx prisma db push`"
    - "Une Server Action serveur refuse status=resolved|closed quand un champ requis (faultCode|timeSpent|partsUsed) est manquant"
    - "Une Server Action startTimer crée un WorkOrderTimeLog avec endedAt=null"
    - "Une Server Action stopTimer ferme la session active (endedAt + minutes calculés)"
    - "Une Server Action upsertWorkOrderPart crée/met à jour une pièce sur un BT, décrémentant SparePart.quantityOnHand si sparePartId est lié"
    - "Une Server Action updateClosureRequirements (admin) sauvegarde le JSON sur Organization"
    - "Une Server Action updateMemberHourlyRate (admin) sauvegarde le taux sur Membership"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Schema étendu (WorkOrderPart, faultCategory, faultDescription, closureRequirements, hourlyRate)"
      contains: "model WorkOrderPart"
    - path: "src/lib/closure-requirements.ts"
      provides: "Type ClosureRequirements + default + validateClosure(workOrder, requirements)"
      exports: ["ClosureRequirements", "DEFAULT_CLOSURE_REQUIREMENTS", "validateClosure", "FAULT_CATEGORIES"]
    - path: "src/actions/work-orders.ts"
      provides: "Server Actions étendues : updateWorkOrderStatus (validation), startTimer, stopTimer, closeActiveTimer (manager), upsertWorkOrderPart, deleteWorkOrderPart, setWorkOrderFault"
      contains: "export async function startTimer"
    - path: "src/actions/settings.ts"
      provides: "Server Actions admin : updateClosureRequirements, updateMemberHourlyRate"
      contains: "export async function updateClosureRequirements"
  key_links:
    - from: "updateWorkOrderStatus"
      to: "validateClosure helper"
      via: "appel synchrone lit closureRequirements depuis Organization puis throw si gap"
      pattern: "validateClosure\\("
    - from: "upsertWorkOrderPart"
      to: "SparePart.quantityOnHand"
      via: "transaction Prisma : décrémente stock si sparePartId présent"
      pattern: "quantityOnHand"
    - from: "startTimer / stopTimer"
      to: "WorkOrderTimeLog"
      via: "db.workOrderTimeLog.create / update"
      pattern: "workOrderTimeLog\\.(create|update|findFirst)"
---

<objective>
Fonder Phase 6 : étendre le schéma Prisma (faultCategory, faultDescription, WorkOrderPart, closureRequirements, hourlyRate), pousser à la base, et créer toutes les Server Actions nécessaires (timer, pièces, validation clôture, config admin).

Purpose: Toute la logique métier doit exister côté serveur AVANT que les UI puissent y câbler. Cette fondation découple le schéma et les actions de l'UI, ce qui permet aux plans 02 (UI BT) et 03 (UI admin) de s'exécuter en parallèle en Wave 2.

Output: Schema migré, types/helpers de validation, Server Actions complètes pour timer/pièces/clôture/config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md

@prisma/schema.prisma
@src/actions/work-orders.ts
@src/actions/settings.ts
@src/lib/auth.ts

<interfaces>
<!-- Contracts produced by this plan — consumed by plans 02 et 03 -->

From src/lib/closure-requirements.ts (à créer) :
```typescript
export type ClosureRequirements = {
  faultCode: boolean    // Si true, faultCategory + faultDescription requis
  timeSpent: boolean    // Si true, au moins 1 WorkOrderTimeLog avec minutes > 0
  partsUsed: boolean    // Si true, au moins 1 WorkOrderPart
}

export const DEFAULT_CLOSURE_REQUIREMENTS: ClosureRequirements = {
  faultCode: false,
  timeSpent: false,
  partsUsed: false,
}

export const FAULT_CATEGORIES = ['mecanique', 'electrique', 'hydraulique', 'autre'] as const
export type FaultCategory = typeof FAULT_CATEGORIES[number]
export const FAULT_CATEGORY_LABELS: Record<FaultCategory, string> = {
  mecanique: 'Mécanique',
  electrique: 'Électrique',
  hydraulique: 'Hydraulique',
  autre: 'Autre',
}

export type ClosureCheckInput = {
  faultCategory: string | null
  faultDescription: string | null
  timeLogsMinutesTotal: number
  partsCount: number
}

// Retourne un tableau de codes ('faultCode'|'timeSpent'|'partsUsed') manquants
export function validateClosure(input: ClosureCheckInput, req: ClosureRequirements): string[]
```

From src/actions/work-orders.ts (additions) :
```typescript
export async function startTimer(workOrderId: string): Promise<{ timeLogId: string; startedAt: Date }>
export async function stopTimer(timeLogId: string): Promise<{ minutes: number }>
export async function closeActiveTimer(timeLogId: string): Promise<void>  // manager/admin only
export async function upsertWorkOrderPart(workOrderId: string, data: {
  id?: string
  sparePartId?: string | null
  name: string
  quantity: number
}): Promise<void>
export async function deleteWorkOrderPart(workOrderId: string, partId: string): Promise<void>
export async function setWorkOrderFault(workOrderId: string, data: {
  faultCategory: string | null
  faultDescription: string | null
}): Promise<void>
// updateWorkOrderStatus : étendre la signature existante avec validation closureRequirements
```

From src/actions/settings.ts (additions) :
```typescript
export async function updateClosureRequirements(req: ClosureRequirements): Promise<void>
export async function updateMemberHourlyRate(membershipId: string, hourlyRate: number | null): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre prisma/schema.prisma (WorkOrderPart + champs fault/rate/closureRequirements)</name>
  <files>prisma/schema.prisma</files>
  <read_first>
    - prisma/schema.prisma (voir modèles existants WorkOrder, Organization, Membership, SparePart)
    - .planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md (D-04, D-06, D-12, D-13, D-16)
  </read_first>
  <action>
    Modifier `prisma/schema.prisma` :

    1. Ajouter au modèle `WorkOrder` (après `closureNotes`) :
       ```
       faultCategory   String?
       faultDescription String?
       ```
       Et ajouter la relation : `parts WorkOrderPart[]` dans le bloc des relations (après `timeLogs WorkOrderTimeLog[]`).

    2. Ajouter au modèle `Membership` (après `avatarUrl`) :
       ```
       hourlyRate  Float?
       ```

    3. Ajouter au modèle `Organization` (après `logoUrl`) :
       ```
       closureRequirements Json?
       ```

    4. Ajouter au modèle `SparePart` la relation inverse : `workOrderParts WorkOrderPart[]` (après `organization Organization @relation...`).

    5. Créer un nouveau modèle `WorkOrderPart` après `WorkOrderTimeLog` :
       ```
       model WorkOrderPart {
         id           String   @id @default(cuid())
         workOrderId  String
         sparePartId  String?
         name         String
         quantity     Float
         unitCost     Float?
         createdAt    DateTime @default(now())
         updatedAt    DateTime @updatedAt

         workOrder WorkOrder  @relation(fields: [workOrderId], references: [id], onDelete: Cascade)
         sparePart SparePart? @relation(fields: [sparePartId], references: [id], onDelete: SetNull)

         @@index([workOrderId])
         @@index([sparePartId])
       }
       ```

    6. NE PAS modifier `WorkOrderTimeLog` — le modèle existant est suffisant (D-09).

    Tous les champs ajoutés sont nullable/optionnels — donc additif et non destructif.
  </action>
  <verify>
    <automated>grep -E "(faultCategory|faultDescription|closureRequirements|hourlyRate|model WorkOrderPart|workOrderParts WorkOrderPart)" prisma/schema.prisma | wc -l | grep -E "^[6-9]|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "faultCategory   String?" prisma/schema.prisma` retourne 1 ligne
    - `grep "faultDescription String?" prisma/schema.prisma` retourne 1 ligne
    - `grep "hourlyRate  Float?" prisma/schema.prisma` retourne 1 ligne
    - `grep "closureRequirements Json?" prisma/schema.prisma` retourne 1 ligne
    - `grep "model WorkOrderPart" prisma/schema.prisma` retourne 1 ligne
    - `grep "parts WorkOrderPart\[\]" prisma/schema.prisma` retourne 1 ligne (relation inverse sur WorkOrder)
    - `grep "workOrderParts WorkOrderPart\[\]" prisma/schema.prisma` retourne 1 ligne (relation inverse sur SparePart)
    - `grep "onDelete: SetNull" prisma/schema.prisma` retourne au moins 1 ligne (pour sparePart relation)
  </acceptance_criteria>
  <done>schema.prisma contient les 4 extensions de modèles + le nouveau modèle WorkOrderPart, avec toutes les relations inverses correctement déclarées.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Pousser le schéma à la base + régénérer le client Prisma</name>
  <files>prisma/schema.prisma (lecture), src/generated/prisma/ (généré)</files>
  <read_first>
    - prisma/schema.prisma (vérifier la sortie de Task 1)
    - package.json (vérifier les scripts npm pour Prisma)
  </read_first>
  <action>
    Exécuter dans cet ordre :

    1. `npx prisma format` — valide la syntaxe du schema
    2. `npx prisma db push` — pousse les changements additifs (aucune perte de données attendue, tous les champs sont optionnels). Si l'outil demande une confirmation interactive et que le shell est non-TTY, ajouter `--accept-data-loss` UNIQUEMENT si les changements sont confirmés additifs. Tous les ajouts de cette phase sont nullable donc le flag ne devrait pas être nécessaire.
    3. `npx prisma generate` — régénère le client Prisma TypeScript dans `src/generated/prisma/`

    Si `db push` échoue à cause d'une connexion DB, marquer la task non-autonome et flagger pour intervention manuelle (vérifier `.env`/`DATABASE_URL`).

    [BLOCKING] Cette étape DOIT précéder toute utilisation des nouveaux modèles côté code. Le build TypeScript passerait sans cette étape (les types viennent du client généré), mais les requêtes runtime échoueraient avec "column does not exist".
  </action>
  <verify>
    <automated>npx prisma validate && grep -l "workOrderPart" src/generated/prisma/index.d.ts 2>/dev/null || grep -lr "WorkOrderPart" src/generated/prisma/ | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma validate` exit code 0
    - `grep -rE "WorkOrderPart|workOrderPart" src/generated/prisma/` retourne au moins 5 matches (modèle + delegate + types input)
    - `grep "faultCategory" src/generated/prisma/index.d.ts` ou client équivalent retourne au moins 1 match
    - `grep "hourlyRate" src/generated/prisma/index.d.ts` retourne au moins 1 match
    - `grep "closureRequirements" src/generated/prisma/index.d.ts` retourne au moins 1 match
  </acceptance_criteria>
  <done>Schema poussé, client Prisma régénéré, types disponibles pour les tasks suivantes. Le shell ne reporte aucune erreur de migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Créer src/lib/closure-requirements.ts (types + validateClosure helper)</name>
  <files>src/lib/closure-requirements.ts, src/lib/closure-requirements.test.ts</files>
  <read_first>
    - .planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md (D-03, D-12, D-13, D-15)
    - src/lib/portal-validation.ts (pattern de helper de validation pure)
    - src/lib/auth.test.ts (pattern de test vitest dans ce projet)
  </read_first>
  <behavior>
    - Test 1: `validateClosure({faultCategory:null, faultDescription:null, timeLogsMinutesTotal:0, partsCount:0}, {faultCode:false, timeSpent:false, partsUsed:false})` retourne `[]`
    - Test 2: `validateClosure({faultCategory:null, faultDescription:null, ...}, {faultCode:true, ...})` retourne `['faultCode']`
    - Test 3: `validateClosure({faultCategory:'mecanique', faultDescription:'', ...}, {faultCode:true, ...})` retourne `['faultCode']` (description vide compte comme manquante)
    - Test 4: `validateClosure({faultCategory:'mecanique', faultDescription:'roulement HS', ...}, {faultCode:true, ...})` retourne `[]`
    - Test 5: `validateClosure({timeLogsMinutesTotal:0, ...}, {timeSpent:true, ...})` retourne `['timeSpent']`
    - Test 6: `validateClosure({timeLogsMinutesTotal:15, ...}, {timeSpent:true, ...})` retourne `[]`
    - Test 7: `validateClosure({partsCount:0, ...}, {partsUsed:true, ...})` retourne `['partsUsed']`
    - Test 8: `validateClosure({partsCount:1, ...}, {partsUsed:true, ...})` retourne `[]`
    - Test 9: Multi-manquants : retourne tous les codes dans cet ordre `['faultCode','timeSpent','partsUsed']`
  </behavior>
  <action>
    Créer `src/lib/closure-requirements.ts` avec EXACTEMENT cette signature :

    ```typescript
    export type ClosureRequirements = {
      faultCode: boolean
      timeSpent: boolean
      partsUsed: boolean
    }

    export const DEFAULT_CLOSURE_REQUIREMENTS: ClosureRequirements = {
      faultCode: false,
      timeSpent: false,
      partsUsed: false,
    }

    export const FAULT_CATEGORIES = ['mecanique', 'electrique', 'hydraulique', 'autre'] as const
    export type FaultCategory = typeof FAULT_CATEGORIES[number]

    export const FAULT_CATEGORY_LABELS: Record<FaultCategory, string> = {
      mecanique: 'Mécanique',
      electrique: 'Électrique',
      hydraulique: 'Hydraulique',
      autre: 'Autre',
    }

    export const CLOSURE_FIELD_LABELS = {
      faultCode: 'Code de panne (catégorie + description)',
      timeSpent: 'Temps passé (au moins une session enregistrée)',
      partsUsed: 'Pièces utilisées (au moins une pièce ajoutée)',
    } as const

    export type ClosureCheckInput = {
      faultCategory: string | null
      faultDescription: string | null
      timeLogsMinutesTotal: number
      partsCount: number
    }

    export function validateClosure(input: ClosureCheckInput, req: ClosureRequirements): string[] {
      const missing: string[] = []
      if (req.faultCode) {
        const cat = input.faultCategory?.trim()
        const desc = input.faultDescription?.trim()
        if (!cat || !desc) missing.push('faultCode')
      }
      if (req.timeSpent && input.timeLogsMinutesTotal <= 0) missing.push('timeSpent')
      if (req.partsUsed && input.partsCount <= 0) missing.push('partsUsed')
      return missing
    }

    // Helper pour lire depuis Organization.closureRequirements (Json | null)
    export function parseClosureRequirements(raw: unknown): ClosureRequirements {
      if (!raw || typeof raw !== 'object') return DEFAULT_CLOSURE_REQUIREMENTS
      const r = raw as Record<string, unknown>
      return {
        faultCode: typeof r.faultCode === 'boolean' ? r.faultCode : false,
        timeSpent: typeof r.timeSpent === 'boolean' ? r.timeSpent : false,
        partsUsed: typeof r.partsUsed === 'boolean' ? r.partsUsed : false,
      }
    }
    ```

    Créer `src/lib/closure-requirements.test.ts` avec vitest, couvrant les 9 cas listés dans `<behavior>`. Suivre le style de `src/lib/auth.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/closure-requirements.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/closure-requirements.test.ts` exit code 0
    - `grep -c "test\|it(" src/lib/closure-requirements.test.ts` retourne au moins 9
    - `grep "export function validateClosure" src/lib/closure-requirements.ts` retourne 1 ligne
    - `grep "export function parseClosureRequirements" src/lib/closure-requirements.ts` retourne 1 ligne
    - `grep "FAULT_CATEGORY_LABELS" src/lib/closure-requirements.ts` retourne au moins 2 matches
  </acceptance_criteria>
  <done>Fichier helper + tests, tous les tests vitest passent, types exportés conformes à l'interface du context.</done>
</task>

<task type="auto">
  <name>Task 4: Étendre src/actions/work-orders.ts (timer, pièces, fault, validation clôture)</name>
  <files>src/actions/work-orders.ts</files>
  <read_first>
    - src/actions/work-orders.ts (voir helper getOrgAndMembership, pattern updateWorkOrderStatus existant)
    - src/lib/closure-requirements.ts (Task 3, pour importer validateClosure + parseClosureRequirements)
    - .planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md (D-03, D-07, D-09 à D-11, D-14, D-15)
  </read_first>
  <action>
    Modifier `src/actions/work-orders.ts` :

    1. Ajouter en haut : `import { validateClosure, parseClosureRequirements } from '@/lib/closure-requirements'`.

    2. **Remplacer** la fonction existante `updateWorkOrderStatus` par cette version (qui valide les exigences de clôture) :
       ```typescript
       export async function updateWorkOrderStatus(
         id: string,
         status: WorkOrderStatus,
         closureNotes?: string
       ) {
         const { organizationId } = await getOrgAndMembership()

         // Validation TERRAIN-01 : si on passe à resolved/closed, vérifier closureRequirements
         if (status === 'resolved' || status === 'closed') {
           const [wo, org] = await Promise.all([
             db.workOrder.findFirst({
               where: { id, organizationId },
               select: {
                 faultCategory: true,
                 faultDescription: true,
                 timeLogs: { select: { minutes: true } },
                 parts: { select: { id: true } },
               },
             }),
             db.organization.findUnique({
               where: { id: organizationId },
               select: { closureRequirements: true },
             }),
           ])
           if (!wo) throw new Error('Bon de travail introuvable')

           const req = parseClosureRequirements(org?.closureRequirements)
           const timeLogsMinutesTotal = wo.timeLogs.reduce((sum, l) => sum + (l.minutes ?? 0), 0)
           const missing = validateClosure(
             {
               faultCategory: wo.faultCategory,
               faultDescription: wo.faultDescription,
               timeLogsMinutesTotal,
               partsCount: wo.parts.length,
             },
             req
           )
           if (missing.length > 0) {
             throw new Error(`Champs requis manquants pour clôture: ${missing.join(', ')}`)
           }
         }

         const extra: Record<string, unknown> = {}
         if (status === 'in_progress') extra.startedAt = new Date()
         if (status === 'resolved' || status === 'closed') extra.completedAt = new Date()
         if (closureNotes !== undefined) extra.closureNotes = closureNotes

         await db.workOrder.update({
           where: { id, organizationId },
           data: { status, ...extra },
         })

         revalidatePath('/bons-de-travail')
         revalidatePath(`/bons-de-travail/${id}`)
       }
       ```

    3. Ajouter `setWorkOrderFault` :
       ```typescript
       export async function setWorkOrderFault(
         workOrderId: string,
         data: { faultCategory: string | null; faultDescription: string | null }
       ) {
         const { organizationId } = await getOrgAndMembership()
         await db.workOrder.update({
           where: { id: workOrderId, organizationId },
           data: {
             faultCategory: data.faultCategory?.trim() || null,
             faultDescription: data.faultDescription?.trim() || null,
           },
         })
         revalidatePath(`/bons-de-travail/${workOrderId}`)
       }
       ```

    4. Ajouter `startTimer` :
       ```typescript
       export async function startTimer(workOrderId: string) {
         const { organizationId, membershipId } = await getOrgAndMembership()

         // Vérifier que le BT appartient à l'org
         const wo = await db.workOrder.findFirst({
           where: { id: workOrderId, organizationId },
           select: { id: true },
         })
         if (!wo) throw new Error('Bon de travail introuvable')

         // Refuser si une session ouverte existe déjà pour ce membre sur ce BT
         const active = await db.workOrderTimeLog.findFirst({
           where: { workOrderId, membershipId, endedAt: null },
           select: { id: true },
         })
         if (active) throw new Error('Une session est déjà active sur ce bon')

         const log = await db.workOrderTimeLog.create({
           data: { workOrderId, membershipId, startedAt: new Date() },
           select: { id: true, startedAt: true },
         })
         revalidatePath(`/bons-de-travail/${workOrderId}`)
         return { timeLogId: log.id, startedAt: log.startedAt }
       }
       ```

    5. Ajouter `stopTimer` (le membre propriétaire ferme sa propre session) :
       ```typescript
       export async function stopTimer(timeLogId: string) {
         const { membershipId } = await getOrgAndMembership()
         const log = await db.workOrderTimeLog.findFirst({
           where: { id: timeLogId, membershipId, endedAt: null },
           select: { id: true, workOrderId: true, startedAt: true },
         })
         if (!log) throw new Error('Session active introuvable')

         const endedAt = new Date()
         const minutes = Math.max(0, Math.round((endedAt.getTime() - log.startedAt.getTime()) / 60000))

         await db.workOrderTimeLog.update({
           where: { id: timeLogId },
           data: { endedAt, minutes },
         })
         revalidatePath(`/bons-de-travail/${log.workOrderId}`)
         return { minutes }
       }
       ```

    6. Ajouter `closeActiveTimer` (admin/manager peut fermer la session d'un autre — Discretion CONTEXT.md) :
       ```typescript
       export async function closeActiveTimer(timeLogId: string) {
         const { orgId, userId } = await auth()
         if (!orgId || !userId) throw new Error('Non autorisé')
         const org = await db.organization.findUnique({ where: { clerkId: orgId }, select: { id: true } })
         if (!org) throw new Error('Organisation introuvable')
         const me = await db.membership.findFirst({
           where: { clerkUserId: userId, organizationId: org.id },
           select: { role: true },
         })
         if (!me || (me.role !== 'admin' && me.role !== 'manager')) throw new Error('Accès refusé')

         const log = await db.workOrderTimeLog.findFirst({
           where: { id: timeLogId, endedAt: null, workOrder: { organizationId: org.id } },
           select: { id: true, workOrderId: true, startedAt: true },
         })
         if (!log) throw new Error('Session active introuvable')

         const endedAt = new Date()
         const minutes = Math.max(0, Math.round((endedAt.getTime() - log.startedAt.getTime()) / 60000))
         await db.workOrderTimeLog.update({ where: { id: timeLogId }, data: { endedAt, minutes } })
         revalidatePath(`/bons-de-travail/${log.workOrderId}`)
       }
       ```
       Ajouter `import { auth } from '@clerk/nextjs/server'` si pas déjà présent (il l'est déjà).

    7. Ajouter `upsertWorkOrderPart` (D-05, D-06, D-07, D-08) :
       ```typescript
       export async function upsertWorkOrderPart(
         workOrderId: string,
         data: { id?: string; sparePartId?: string | null; name: string; quantity: number }
       ) {
         const { organizationId } = await getOrgAndMembership()
         const wo = await db.workOrder.findFirst({
           where: { id: workOrderId, organizationId },
           select: { id: true },
         })
         if (!wo) throw new Error('Bon de travail introuvable')
         if (!data.name.trim()) throw new Error('Nom de pièce requis')
         if (!Number.isFinite(data.quantity) || data.quantity <= 0) throw new Error('Quantité invalide')

         // Si lié à inventaire, hydrater name+unitCost+vérifier appartenance org
         let sparePartUnitCost: number | null = null
         let resolvedName = data.name.trim()
         if (data.sparePartId) {
           const sp = await db.sparePart.findFirst({
             where: { id: data.sparePartId, organizationId },
             select: { id: true, name: true, unitCost: true },
           })
           if (!sp) throw new Error('Pièce inventaire introuvable')
           sparePartUnitCost = sp.unitCost ?? null
           resolvedName = sp.name
         }

         if (data.id) {
           // Update : pas de réajustement de stock automatique (compromis de simplicité Phase 6)
           const existing = await db.workOrderPart.findFirst({
             where: { id: data.id, workOrderId },
             select: { id: true },
           })
           if (!existing) throw new Error('Pièce de BT introuvable')
           await db.workOrderPart.update({
             where: { id: data.id },
             data: {
               sparePartId: data.sparePartId ?? null,
               name: resolvedName,
               quantity: data.quantity,
               unitCost: sparePartUnitCost,
             },
           })
         } else {
           // Create + décrément de stock si lié à inventaire (D-07)
           await db.$transaction(async tx => {
             await tx.workOrderPart.create({
               data: {
                 workOrderId,
                 sparePartId: data.sparePartId ?? null,
                 name: resolvedName,
                 quantity: data.quantity,
                 unitCost: sparePartUnitCost,
               },
             })
             if (data.sparePartId) {
               await tx.sparePart.update({
                 where: { id: data.sparePartId },
                 data: { quantityOnHand: { decrement: data.quantity } },
               })
             }
           })
         }
         revalidatePath(`/bons-de-travail/${workOrderId}`)
       }
       ```

    8. Ajouter `deleteWorkOrderPart` :
       ```typescript
       export async function deleteWorkOrderPart(workOrderId: string, partId: string) {
         const { organizationId } = await getOrgAndMembership()
         const part = await db.workOrderPart.findFirst({
           where: { id: partId, workOrderId, workOrder: { organizationId } },
           select: { id: true },
         })
         if (!part) throw new Error('Pièce introuvable')
         await db.workOrderPart.delete({ where: { id: partId } })
         revalidatePath(`/bons-de-travail/${workOrderId}`)
       }
       ```
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "actions/work-orders\.ts" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function startTimer" src/actions/work-orders.ts` retourne 1 ligne
    - `grep "export async function stopTimer" src/actions/work-orders.ts` retourne 1 ligne
    - `grep "export async function closeActiveTimer" src/actions/work-orders.ts` retourne 1 ligne
    - `grep "export async function setWorkOrderFault" src/actions/work-orders.ts` retourne 1 ligne
    - `grep "export async function upsertWorkOrderPart" src/actions/work-orders.ts` retourne 1 ligne
    - `grep "export async function deleteWorkOrderPart" src/actions/work-orders.ts` retourne 1 ligne
    - `grep "validateClosure(" src/actions/work-orders.ts` retourne au moins 1 ligne
    - `grep "decrement: data.quantity" src/actions/work-orders.ts` retourne 1 ligne
    - `grep "endedAt: null" src/actions/work-orders.ts` retourne au moins 2 lignes (vérif active session, requête closeActive)
    - `npx tsc --noEmit -p tsconfig.json` exit code 0 (aucune erreur TS dans le fichier)
  </acceptance_criteria>
  <done>Server Actions étendues, validation clôture câblée à validateClosure, timer + pièces + fault implémentés, build TypeScript propre.</done>
</task>

<task type="auto">
  <name>Task 5: Ajouter Server Actions admin dans src/actions/settings.ts (closure requirements + hourly rate)</name>
  <files>src/actions/settings.ts</files>
  <read_first>
    - src/actions/settings.ts (voir helper getAdminOrg + updateMemberRole comme template)
    - src/lib/closure-requirements.ts (Task 3, pour le type ClosureRequirements)
  </read_first>
  <action>
    Modifier `src/actions/settings.ts` :

    1. Ajouter en haut : `import { type ClosureRequirements } from '@/lib/closure-requirements'`.

    2. Ajouter `updateClosureRequirements` (admin/manager only — utilise `getAdminOrg`) :
       ```typescript
       export async function updateClosureRequirements(req: ClosureRequirements) {
         const organizationId = await getAdminOrg()

         // Coerce strict types avant persistance (defense-in-depth — l'UI envoie ces 3 bools)
         const sanitized: ClosureRequirements = {
           faultCode: !!req.faultCode,
           timeSpent: !!req.timeSpent,
           partsUsed: !!req.partsUsed,
         }

         await db.organization.update({
           where: { id: organizationId },
           data: { closureRequirements: sanitized },
         })
         revalidatePath('/parametres/organisation')
       }
       ```

    3. Ajouter `updateMemberHourlyRate` (admin/manager only) :
       ```typescript
       export async function updateMemberHourlyRate(membershipId: string, hourlyRate: number | null) {
         const organizationId = await getAdminOrg()

         if (hourlyRate !== null) {
           if (!Number.isFinite(hourlyRate) || hourlyRate < 0 || hourlyRate > 10000) {
             throw new Error('Taux horaire invalide')
           }
         }

         await db.membership.update({
           where: { id: membershipId, organizationId },
           data: { hourlyRate },
         })
         revalidatePath('/parametres/organisation')
       }
       ```

    Notes :
    - L'organisation et la membership sont scopées par `organizationId` retourné par `getAdminOrg` — empêche un admin de modifier hors org.
    - `hourlyRate` est borné à [0, 10000] (T-06-04, rate limit / sanity check).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "actions/settings\.ts" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function updateClosureRequirements" src/actions/settings.ts` retourne 1 ligne
    - `grep "export async function updateMemberHourlyRate" src/actions/settings.ts` retourne 1 ligne
    - `grep "getAdminOrg()" src/actions/settings.ts` retourne au moins 3 occurrences (existant updateMemberRole + 2 nouvelles)
    - `grep "from '@/lib/closure-requirements'" src/actions/settings.ts` retourne 1 ligne
    - `grep "hourlyRate < 0 || hourlyRate > 10000" src/actions/settings.ts` retourne 1 ligne
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>2 nouvelles Server Actions admin disponibles pour Plan 03 (UI admin) avec auth guard et validation des bornes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action (Phase 6 actions) | Inputs non fiables : `workOrderId`, `quantity`, `hourlyRate`, `closureRequirements` JSON |
| Server Action → DB (Prisma) | Toutes les requêtes scopées par `organizationId` issu de la session Clerk |
| Server Action timer → ownership | `stopTimer` ne ferme que les sessions appartenant au `membershipId` courant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | upsertWorkOrderPart — `sparePartId` cross-org | mitigate | `db.sparePart.findFirst({where:{id, organizationId}})` avant link/décrément, throw si introuvable |
| T-06-02 | Tampering | updateWorkOrderStatus — bypass validation clôture côté client | mitigate | Validation `validateClosure()` 100% côté serveur, lit `closureRequirements` depuis DB (pas du body) |
| T-06-03 | Elevation of Privilege | updateClosureRequirements / updateMemberHourlyRate par non-admin | mitigate | `getAdminOrg()` vérifie role admin|manager — pattern identique à `updateMemberRole` existant |
| T-06-04 | Tampering | updateMemberHourlyRate — valeur négative ou astronomique | mitigate | Validation `0 <= hourlyRate <= 10000`, throw sinon (cap réaliste pour QC : taux horaire <500$/h en pratique) |
| T-06-05 | Tampering | upsertWorkOrderPart — `quantity` négative / NaN | mitigate | Check `Number.isFinite(quantity) && quantity > 0`, throw sinon |
| T-06-06 | Information Disclosure | stopTimer accessible à un autre membre du même org | mitigate | `findFirst({where:{id, membershipId, endedAt:null}})` — scope membership |
| T-06-07 | Elevation of Privilege | closeActiveTimer par technicien (devrait être admin/manager) | mitigate | Check explicite `role === admin || role === manager` dans la Server Action |
| T-06-08 | Denial of Service | Spam startTimer/stopTimer | accept | Server Actions Next.js ont rate-limit implicite via Vercel ; risque faible (tech interne authentifié) ; à reconsidérer Phase 8 si abus |
| T-06-09 | Repudiation | Suppression silencieuse de WorkOrderPart sans log | accept | Phase 6 n'a pas d'audit log ; à ajouter Phase 7 (analytique) ou Milestone 4 |
| T-06-10 | Tampering | closureRequirements JSON non typé (Json field) | mitigate | `parseClosureRequirements()` valide les 3 clés bools et fallback aux defaults |
</threat_model>

<verification>
- `npx prisma validate` : schema syntactiquement valide
- `npx prisma db push` : changements appliqués à la base sans erreur
- `npx prisma generate` : client TypeScript régénéré
- `npx vitest run src/lib/closure-requirements.test.ts` : 9 tests verts
- `npx tsc --noEmit -p tsconfig.json` : aucune erreur TS dans `src/actions/work-orders.ts` ou `src/actions/settings.ts`
- Inspection manuelle : pas de mutation côté client envoyée pour `closureRequirements` (lecture serveur uniquement)
</verification>

<success_criteria>
- Schéma Prisma étendu et poussé à la base (faultCategory, faultDescription, WorkOrderPart, closureRequirements, hourlyRate)
- Helper `validateClosure` testé (9 cas)
- Server Actions complètes : startTimer, stopTimer, closeActiveTimer, upsertWorkOrderPart, deleteWorkOrderPart, setWorkOrderFault, updateWorkOrderStatus (avec validation), updateClosureRequirements, updateMemberHourlyRate
- Threat model traité (10 menaces STRIDE avec disposition)
- Tous les contrats d'interface exportés pour consommation par plans 02 et 03 en Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/06-int-grit-des-donn-es-terrain/06-01-SUMMARY.md`
</output>
</content>
</invoke>