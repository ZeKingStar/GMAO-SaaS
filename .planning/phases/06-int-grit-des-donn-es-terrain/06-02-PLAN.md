---
phase: 06
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(app)/bons-de-travail/[id]/page.tsx
  - src/components/work-orders/work-order-detail.tsx
  - src/components/work-orders/work-order-timer.tsx
  - src/components/work-orders/work-order-parts.tsx
  - src/components/work-orders/work-order-fault-form.tsx
  - src/components/work-orders/work-order-closure-banner.tsx
autonomous: false
requirements:
  - TERRAIN-01
  - TERRAIN-02

must_haves:
  truths:
    - "Sur la page d'un BT (status in_progress), un technicien voit un bouton Démarrer/Arrêter le minuteur"
    - "Le minuteur affiche un compteur en temps réel (mm:ss) côté client tant qu'une session est active"
    - "Le technicien peut ajouter une pièce inventaire (sélection dropdown SparePart) OU une pièce libre (texte + qté) — même formulaire"
    - "Le coût de main-d'œuvre est affiché dans le sidebar (totalMinutes/60 × hourlyRate), affiche '—' si aucun taux pour le membre principal"
    - "Le formulaire de clôture (catégorie + description de panne) est visible et soumet via setWorkOrderFault"
    - "Quand l'admin a activé une exigence et qu'elle n'est pas remplie, un banner liste les champs manquants et désactive le bouton 'Marquer résolu'"
    - "Un admin/manager voit un bouton X pour fermer une session de timer abandonnée par un autre technicien"
  artifacts:
    - path: "src/app/(app)/bons-de-travail/[id]/page.tsx"
      provides: "Chargement étendu : parts, hourlyRate de l'assigné, closureRequirements de l'org, session timer active"
      contains: "parts: true"
    - path: "src/components/work-orders/work-order-detail.tsx"
      provides: "Intégration des nouveaux sous-composants (timer, pièces, fault, banner de clôture)"
      contains: "WorkOrderTimer"
    - path: "src/components/work-orders/work-order-timer.tsx"
      provides: "Composant client : Démarrer/Arrêter + ticker setInterval pour session active"
      exports: ["WorkOrderTimer"]
    - path: "src/components/work-orders/work-order-parts.tsx"
      provides: "Composant client : liste + form hybride inventaire/libre pour pièces"
      exports: ["WorkOrderParts"]
    - path: "src/components/work-orders/work-order-fault-form.tsx"
      provides: "Composant client : sélecteur catégorie + textarea description, debounce save"
      exports: ["WorkOrderFaultForm"]
    - path: "src/components/work-orders/work-order-closure-banner.tsx"
      provides: "Composant : affiche les champs manquants et désactive l'action Résoudre/Fermer si gaps"
      exports: ["WorkOrderClosureBanner", "computeMissingForClosure"]
  key_links:
    - from: "WorkOrderDetail"
      to: "startTimer / stopTimer"
      via: "WorkOrderTimer appelle Server Actions via useTransition + revalidatePath"
      pattern: "startTimer\\(|stopTimer\\("
    - from: "WorkOrderParts"
      to: "upsertWorkOrderPart / deleteWorkOrderPart"
      via: "form submit → Server Action → revalidatePath /bons-de-travail/[id]"
      pattern: "upsertWorkOrderPart\\(|deleteWorkOrderPart\\("
    - from: "WorkOrderClosureBanner"
      to: "validateClosure helper"
      via: "Calcul côté client des champs manquants pour désactiver les boutons de transition"
      pattern: "validateClosure\\("
    - from: "page.tsx"
      to: "Membership.hourlyRate"
      via: "select hourlyRate sur l'assigné principal pour calculer le coût"
      pattern: "hourlyRate"
---

<objective>
Câbler les nouveaux Server Actions de Phase 6 (Plan 01) dans la page de détail d'un bon de travail. Ajouter : minuteur live, formulaire de pièces hybride (inventaire OR libre), formulaire de fault code, calcul de coût de main-d'œuvre, banner bloquant pour la clôture.

Purpose: Phase 6 livre la valeur côté utilisateur ICI — c'est la page que les techniciens utilisent au quotidien. Sans cette intégration, les Server Actions sont mortes.

Output: WorkOrderDetail enrichi avec 4 nouveaux sous-composants client, page.tsx étendue pour charger les nouvelles données.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md
@.planning/phases/06-int-grit-des-donn-es-terrain/06-01-PLAN.md

@prisma/schema.prisma
@src/app/(app)/bons-de-travail/[id]/page.tsx
@src/components/work-orders/work-order-detail.tsx
@src/lib/closure-requirements.ts
@src/actions/work-orders.ts

<interfaces>
<!-- Contrats consommés depuis Plan 01 -->

From src/lib/closure-requirements.ts :
```typescript
export type ClosureRequirements = { faultCode: boolean; timeSpent: boolean; partsUsed: boolean }
export const DEFAULT_CLOSURE_REQUIREMENTS: ClosureRequirements
export const FAULT_CATEGORIES = ['mecanique','electrique','hydraulique','autre'] as const
export type FaultCategory
export const FAULT_CATEGORY_LABELS: Record<FaultCategory, string>
export const CLOSURE_FIELD_LABELS: { faultCode: string; timeSpent: string; partsUsed: string }
export function validateClosure(input, req): string[]
export function parseClosureRequirements(raw: unknown): ClosureRequirements
```

From src/actions/work-orders.ts :
```typescript
startTimer(workOrderId): Promise<{ timeLogId, startedAt }>
stopTimer(timeLogId): Promise<{ minutes }>
closeActiveTimer(timeLogId): Promise<void>
upsertWorkOrderPart(workOrderId, { id?, sparePartId?, name, quantity }): Promise<void>
deleteWorkOrderPart(workOrderId, partId): Promise<void>
setWorkOrderFault(workOrderId, { faultCategory, faultDescription }): Promise<void>
updateWorkOrderStatus(id, status, closureNotes?): Promise<void>  // throw si missing fields
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Étendre page.tsx + types WorkOrderDetail pour charger parts, hourlyRate, closureRequirements, sparePartsInventory, session active</name>
  <files>src/app/(app)/bons-de-travail/[id]/page.tsx, src/components/work-orders/work-order-detail.tsx</files>
  <read_first>
    - src/app/(app)/bons-de-travail/[id]/page.tsx (voir la requête Prisma actuelle)
    - src/components/work-orders/work-order-detail.tsx (voir le type WorkOrder + props existants)
    - src/lib/closure-requirements.ts (pour parseClosureRequirements + ClosureRequirements)
    - prisma/schema.prisma (vérifier que WorkOrderPart + faultCategory + hourlyRate + closureRequirements sont bien présents — issus du Plan 01)
  </read_first>
  <action>
    **A. Modifier `src/app/(app)/bons-de-travail/[id]/page.tsx`** :

    1. Étendre la requête `db.workOrder.findFirst` `include` :
       - Ajouter `parts: { include: { sparePart: { select: { id: true, name: true, partNumber: true, quantityOnHand: true, unitCost: true } } }, orderBy: { createdAt: 'asc' } }`
       - Modifier `assignees.include.membership.select` pour ajouter `hourlyRate: true`
       - Modifier `timeLogs.include.membership.select` pour ajouter `id: true` (besoin pour matcher currentMembershipId)

    2. Ajouter au `select` de `WorkOrder.findFirst` les champs : `faultCategory: true, faultDescription: true` (lever le `select` si nécessaire — sinon utiliser le mode include par défaut qui retourne tous les scalars).

    3. Ajouter en parallèle dans `Promise.all` :
       - `db.organization.findUnique({ where: { id: org.id }, select: { closureRequirements: true } })` → variable `orgConfig`
       - `db.sparePart.findMany({ where: { organizationId: org.id }, select: { id: true, name: true, partNumber: true, quantityOnHand: true, unitCost: true }, orderBy: { name: 'asc' } })` → variable `spareParts`
       - `db.membership.findFirst({ where: { organizationId: org.id, clerkUserId: userId }, select: { id: true, role: true } })` → étendre le currentMembership pour inclure `role`

    4. Importer `parseClosureRequirements` depuis `@/lib/closure-requirements`.

    5. Passer les nouvelles props au composant :
       ```tsx
       <WorkOrderDetail
         workOrder={workOrder}
         allMembers={allMembers}
         allSites={allSites}
         allAssets={allAssets}
         spareParts={spareParts}
         currentMembershipId={currentMembership?.id ?? ''}
         currentRole={currentMembership?.role ?? 'viewer'}
         closureRequirements={parseClosureRequirements(orgConfig?.closureRequirements)}
       />
       ```

    **B. Modifier `src/components/work-orders/work-order-detail.tsx`** :

    1. Étendre le type `WorkOrder` :
       - Ajouter `faultCategory: string | null` et `faultDescription: string | null`
       - Ajouter `parts: { id: string; sparePartId: string | null; name: string; quantity: number; unitCost: number | null; sparePart: { id: string; name: string; partNumber: string | null; quantityOnHand: number; unitCost: number | null } | null }[]`
       - Modifier `assignees[].membership` pour inclure `hourlyRate: number | null`
       - Modifier `timeLogs[].membership` pour inclure `id: string`

    2. Étendre le type `Props` :
       ```typescript
       type SparePartLite = { id: string; name: string; partNumber: string | null; quantityOnHand: number; unitCost: number | null }
       type Props = {
         workOrder: WorkOrder
         allMembers: Member[]
         allSites: Site[]
         allAssets: Asset[]
         spareParts: SparePartLite[]
         currentMembershipId: string
         currentRole: MemberRole  // import depuis '@/generated/prisma/enums'
         closureRequirements: ClosureRequirements  // import depuis '@/lib/closure-requirements'
       }
       ```

    3. Ne PAS encore intégrer les nouveaux sous-composants — Task 2-4 les créent, Task 5 les câble. Pour Task 1, juste étendre les types et accepter les nouvelles props (sans warning TS).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "(bons-de-travail/\[id\]/page\.tsx|work-orders/work-order-detail\.tsx)" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "parts:" src/app/\(app\)/bons-de-travail/\[id\]/page.tsx` retourne au moins 1 ligne pour l'include Prisma
    - `grep "spareParts" src/app/\(app\)/bons-de-travail/\[id\]/page.tsx` retourne au moins 2 lignes (findMany + prop)
    - `grep "hourlyRate: true" src/app/\(app\)/bons-de-travail/\[id\]/page.tsx` retourne au moins 1 ligne
    - `grep "closureRequirements" src/app/\(app\)/bons-de-travail/\[id\]/page.tsx` retourne au moins 1 ligne
    - `grep "parseClosureRequirements" src/app/\(app\)/bons-de-travail/\[id\]/page.tsx` retourne 1 ligne
    - `grep "faultCategory" src/components/work-orders/work-order-detail.tsx` retourne au moins 1 ligne (dans le type)
    - `grep "spareParts: SparePartLite" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "ClosureRequirements" src/components/work-orders/work-order-detail.tsx` retourne au moins 2 lignes (import + prop)
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>page.tsx charge toutes les nouvelles données, WorkOrderDetail accepte les nouvelles props avec types stricts. Build TS propre. Aucun nouveau composant utilisé pour l'instant.</done>
</task>

<task type="auto">
  <name>Task 2: Créer WorkOrderTimer (composant client avec live ticker)</name>
  <files>src/components/work-orders/work-order-timer.tsx</files>
  <read_first>
    - src/components/work-orders/work-order-detail.tsx (voir le pattern client component existant : useTransition, toast)
    - src/actions/work-orders.ts (signatures startTimer/stopTimer/closeActiveTimer)
    - .planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md (Discretion : Démarrer/Arrêter visible en in_progress, ticker temps réel, manager peut fermer)
  </read_first>
  <action>
    Créer `src/components/work-orders/work-order-timer.tsx` :

    ```typescript
    'use client'

    import { useEffect, useState, useTransition } from 'react'
    import { Play, Square, X } from 'lucide-react'
    import { Button } from '@/components/ui/button'
    import { toast } from 'sonner'
    import { startTimer, stopTimer, closeActiveTimer } from '@/actions/work-orders'
    import type { MemberRole, WorkOrderStatus } from '@/generated/prisma/enums'

    type ActiveSession = {
      id: string
      startedAt: Date
      membership: { id: string; firstName: string | null; lastName: string | null }
    }

    type Props = {
      workOrderId: string
      workOrderStatus: WorkOrderStatus
      activeSession: ActiveSession | null      // session ouverte (endedAt = null), pour ce BT
      currentMembershipId: string
      currentRole: MemberRole
    }

    function formatElapsed(seconds: number): string {
      const h = Math.floor(seconds / 3600)
      const m = Math.floor((seconds % 3600) / 60)
      const s = seconds % 60
      if (h > 0) return `${h}h ${String(m).padStart(2,'0')}m ${String(s).padStart(2,'0')}s`
      return `${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`
    }

    export function WorkOrderTimer({ workOrderId, workOrderStatus, activeSession, currentMembershipId, currentRole }: Props) {
      const [, startTransition] = useTransition()
      const [elapsed, setElapsed] = useState(() =>
        activeSession ? Math.floor((Date.now() - new Date(activeSession.startedAt).getTime()) / 1000) : 0
      )

      // Live tick toutes les secondes tant qu'une session est active
      useEffect(() => {
        if (!activeSession) { setElapsed(0); return }
        const startMs = new Date(activeSession.startedAt).getTime()
        const tick = () => setElapsed(Math.max(0, Math.floor((Date.now() - startMs) / 1000)))
        tick()
        const id = setInterval(tick, 1000)
        return () => clearInterval(id)
      }, [activeSession])

      const canStart = workOrderStatus === 'in_progress' && !activeSession
      const isOwnSession = activeSession?.membership.id === currentMembershipId
      const isManager = currentRole === 'admin' || currentRole === 'manager'

      function handleStart() {
        startTransition(async () => {
          try { await startTimer(workOrderId); toast.success('Minuteur démarré') }
          catch (e) { toast.error(e instanceof Error ? e.message : 'Erreur') }
        })
      }

      function handleStop() {
        if (!activeSession) return
        startTransition(async () => {
          try { await stopTimer(activeSession.id); toast.success('Minuteur arrêté') }
          catch (e) { toast.error(e instanceof Error ? e.message : 'Erreur') }
        })
      }

      function handleCloseOther() {
        if (!activeSession) return
        if (!confirm(`Fermer la session de ${activeSession.membership.firstName ?? 'ce membre'} ?`)) return
        startTransition(async () => {
          try { await closeActiveTimer(activeSession.id); toast.success('Session fermée') }
          catch (e) { toast.error(e instanceof Error ? e.message : 'Erreur') }
        })
      }

      // Si BT pas démarré et aucune session → ne pas afficher de UI (rien à faire)
      if (!activeSession && workOrderStatus !== 'in_progress') return null

      return (
        <div className="rounded-xl border bg-card p-4 flex items-center gap-3">
          {activeSession ? (
            <>
              <div className="flex-1 min-w-0">
                <div className="text-xs text-muted-foreground">
                  Minuteur actif — {activeSession.membership.firstName ?? '?'} {activeSession.membership.lastName ?? ''}
                </div>
                <div className="font-mono text-lg tabular-nums">{formatElapsed(elapsed)}</div>
              </div>
              {isOwnSession ? (
                <Button onClick={handleStop} size="sm" variant="outline">
                  <Square className="h-4 w-4 mr-1.5" />Arrêter
                </Button>
              ) : isManager ? (
                <Button onClick={handleCloseOther} size="sm" variant="ghost" title="Fermer la session d'un autre membre">
                  <X className="h-4 w-4 mr-1.5" />Fermer
                </Button>
              ) : null}
            </>
          ) : canStart ? (
            <>
              <div className="flex-1 text-sm text-muted-foreground">Démarrer le minuteur pour cette intervention</div>
              <Button onClick={handleStart} size="sm">
                <Play className="h-4 w-4 mr-1.5" />Démarrer
              </Button>
            </>
          ) : null}
        </div>
      )
    }
    ```

    Notes :
    - Le composant accepte la session active calculée depuis le serveur — pas de re-fetch
    - L'horloge est purement décorative côté client ; la source de vérité est `WorkOrderTimeLog.startedAt` côté DB
    - Aucune persistance locale : un reload page recalcule l'elapsed depuis `startedAt`
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "work-order-timer\.tsx" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use client'" src/components/work-orders/work-order-timer.tsx` retourne 1 ligne (1ère ligne)
    - `grep "export function WorkOrderTimer" src/components/work-orders/work-order-timer.tsx` retourne 1 ligne
    - `grep "setInterval(tick, 1000)" src/components/work-orders/work-order-timer.tsx` retourne 1 ligne
    - `grep "startTimer\|stopTimer\|closeActiveTimer" src/components/work-orders/work-order-timer.tsx` retourne au moins 3 lignes
    - `grep "formatElapsed" src/components/work-orders/work-order-timer.tsx` retourne au moins 2 lignes
    - `grep "isManager" src/components/work-orders/work-order-timer.tsx` retourne au moins 2 lignes (role check pour fermer session)
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>Composant timer fonctionnel, ticker live, support session perso (Arrêter) + manager (Fermer), build propre.</done>
</task>

<task type="auto">
  <name>Task 3: Créer WorkOrderParts (formulaire hybride inventaire / libre + liste)</name>
  <files>src/components/work-orders/work-order-parts.tsx</files>
  <read_first>
    - src/components/work-orders/work-order-detail.tsx (pattern UI cards/forms/select existant)
    - src/actions/work-orders.ts (signatures upsertWorkOrderPart / deleteWorkOrderPart)
    - .planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md (D-05, D-06, D-07 : hybride, déduction stock auto)
  </read_first>
  <action>
    Créer `src/components/work-orders/work-order-parts.tsx` :

    ```typescript
    'use client'

    import { useState, useTransition } from 'react'
    import { Plus, Trash2, Package, Pencil } from 'lucide-react'
    import { Button } from '@/components/ui/button'
    import { toast } from 'sonner'
    import { upsertWorkOrderPart, deleteWorkOrderPart } from '@/actions/work-orders'

    type SparePartLite = { id: string; name: string; partNumber: string | null; quantityOnHand: number; unitCost: number | null }

    type WOPart = {
      id: string
      sparePartId: string | null
      name: string
      quantity: number
      unitCost: number | null
      sparePart: SparePartLite | null
    }

    type Props = {
      workOrderId: string
      parts: WOPart[]
      spareParts: SparePartLite[]
    }

    const SELECT_CLASS = 'h-9 rounded-lg border border-input bg-background px-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-ring/50'
    const INPUT_CLASS = 'h-9 rounded-lg border border-input bg-background px-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-ring/50'

    export function WorkOrderParts({ workOrderId, parts, spareParts }: Props) {
      const [, startTransition] = useTransition()
      const [showForm, setShowForm] = useState(false)
      const [editId, setEditId] = useState<string | null>(null)
      const [mode, setMode] = useState<'inventory' | 'free'>('inventory')
      const [form, setForm] = useState({ sparePartId: '', name: '', quantity: '1' })

      function resetForm() {
        setForm({ sparePartId: '', name: '', quantity: '1' })
        setMode('inventory')
        setEditId(null)
        setShowForm(false)
      }

      function handleEdit(p: WOPart) {
        setEditId(p.id)
        if (p.sparePartId) {
          setMode('inventory')
          setForm({ sparePartId: p.sparePartId, name: '', quantity: String(p.quantity) })
        } else {
          setMode('free')
          setForm({ sparePartId: '', name: p.name, quantity: String(p.quantity) })
        }
        setShowForm(true)
      }

      function handleSubmit(e: React.FormEvent) {
        e.preventDefault()
        const qty = parseFloat(form.quantity)
        if (!Number.isFinite(qty) || qty <= 0) { toast.error('Quantité invalide'); return }
        const payload = mode === 'inventory'
          ? { id: editId ?? undefined, sparePartId: form.sparePartId || null, name: '', quantity: qty }
          : { id: editId ?? undefined, sparePartId: null, name: form.name.trim(), quantity: qty }
        if (mode === 'inventory' && !form.sparePartId) { toast.error('Pièce inventaire requise'); return }
        if (mode === 'free' && !payload.name) { toast.error('Nom de pièce requis'); return }
        if (mode === 'inventory') {
          // Server résout name depuis l'inventaire — on passe un placeholder
          payload.name = spareParts.find(sp => sp.id === form.sparePartId)?.name ?? 'Pièce'
        }
        startTransition(async () => {
          try {
            await upsertWorkOrderPart(workOrderId, payload)
            toast.success(editId ? 'Pièce mise à jour' : 'Pièce ajoutée')
            resetForm()
          } catch (e) {
            toast.error(e instanceof Error ? e.message : 'Erreur')
          }
        })
      }

      function handleDelete(partId: string) {
        if (!confirm('Supprimer cette pièce du BT ?')) return
        startTransition(async () => {
          try { await deleteWorkOrderPart(workOrderId, partId); toast.success('Pièce supprimée') }
          catch (e) { toast.error(e instanceof Error ? e.message : 'Erreur') }
        })
      }

      return (
        <div className="rounded-xl border bg-card p-4 space-y-4">
          <div className="flex items-center justify-between">
            <h2 className="text-sm font-semibold">Pièces utilisées ({parts.length})</h2>
            <Button variant="outline" size="sm" onClick={() => { setShowForm(v => !v); if (showForm) resetForm() }}>
              <Plus className="h-4 w-4 mr-1.5" />Ajouter
            </Button>
          </div>

          {showForm && (
            <form onSubmit={handleSubmit} className="border rounded-lg p-3 space-y-3 bg-muted/30">
              <div className="flex gap-2 text-xs">
                <button type="button" onClick={() => setMode('inventory')}
                  className={`flex-1 h-8 rounded-md border ${mode === 'inventory' ? 'bg-primary text-primary-foreground border-primary' : 'border-input'}`}>
                  Depuis l&apos;inventaire
                </button>
                <button type="button" onClick={() => setMode('free')}
                  className={`flex-1 h-8 rounded-md border ${mode === 'free' ? 'bg-primary text-primary-foreground border-primary' : 'border-input'}`}>
                  Pièce libre
                </button>
              </div>

              {mode === 'inventory' ? (
                <select
                  value={form.sparePartId}
                  onChange={e => setForm(f => ({ ...f, sparePartId: e.target.value }))}
                  className={`w-full ${SELECT_CLASS}`}
                >
                  <option value="">— Choisir une pièce —</option>
                  {spareParts.map(sp => (
                    <option key={sp.id} value={sp.id}>
                      {sp.name}{sp.partNumber ? ` (${sp.partNumber})` : ''} — stock: {sp.quantityOnHand}
                    </option>
                  ))}
                </select>
              ) : (
                <input
                  placeholder="Nom de la pièce"
                  value={form.name}
                  onChange={e => setForm(f => ({ ...f, name: e.target.value }))}
                  className={`w-full ${INPUT_CLASS}`}
                />
              )}

              <div className="flex gap-2">
                <input
                  type="number"
                  step="0.01"
                  min="0.01"
                  placeholder="Quantité"
                  value={form.quantity}
                  onChange={e => setForm(f => ({ ...f, quantity: e.target.value }))}
                  className={`w-32 ${INPUT_CLASS}`}
                />
                <div className="flex-1" />
                <Button type="button" variant="ghost" size="sm" onClick={resetForm}>Annuler</Button>
                <Button type="submit" size="sm">{editId ? 'Mettre à jour' : 'Ajouter'}</Button>
              </div>
              {mode === 'inventory' && (
                <p className="text-xs text-muted-foreground">
                  Le stock sera décrémenté automatiquement à l&apos;ajout (uniquement à la création, pas à la modification).
                </p>
              )}
            </form>
          )}

          {parts.length === 0 ? (
            <p className="text-sm text-muted-foreground">Aucune pièce enregistrée.</p>
          ) : (
            <div className="space-y-2">
              {parts.map(p => (
                <div key={p.id} className="flex items-center justify-between py-1.5 border-b last:border-0">
                  <div className="flex items-center gap-2 text-sm min-w-0">
                    <Package className="h-3.5 w-3.5 text-muted-foreground shrink-0" />
                    <span className="font-medium truncate">{p.name}</span>
                    {p.sparePart && (
                      <span className="text-xs text-muted-foreground">(inventaire)</span>
                    )}
                  </div>
                  <div className="flex items-center gap-2 text-xs">
                    <span className="font-mono tabular-nums">x {p.quantity}</span>
                    {p.unitCost !== null && (
                      <span className="text-muted-foreground">— {(p.quantity * p.unitCost).toFixed(2)} $</span>
                    )}
                    <button onClick={() => handleEdit(p)} className="text-muted-foreground hover:text-foreground">
                      <Pencil className="h-3.5 w-3.5" />
                    </button>
                    <button onClick={() => handleDelete(p.id)} className="text-muted-foreground hover:text-destructive">
                      <Trash2 className="h-3.5 w-3.5" />
                    </button>
                  </div>
                </div>
              ))}
            </div>
          )}
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "work-order-parts\.tsx" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use client'" src/components/work-orders/work-order-parts.tsx` retourne 1 ligne (1ère ligne)
    - `grep "export function WorkOrderParts" src/components/work-orders/work-order-parts.tsx` retourne 1 ligne
    - `grep "upsertWorkOrderPart\|deleteWorkOrderPart" src/components/work-orders/work-order-parts.tsx` retourne au moins 2 lignes
    - `grep "mode === 'inventory'" src/components/work-orders/work-order-parts.tsx` retourne au moins 3 lignes (UI hybride)
    - `grep "Pièce libre" src/components/work-orders/work-order-parts.tsx` retourne 1 ligne
    - `grep "stock:" src/components/work-orders/work-order-parts.tsx` retourne 1 ligne (affichage du stock en dropdown)
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>Composant pièces avec onglets inventaire/libre dans le même formulaire, édition + suppression, liste avec coût ligne.</done>
</task>

<task type="auto">
  <name>Task 4: Créer WorkOrderFaultForm + WorkOrderClosureBanner</name>
  <files>src/components/work-orders/work-order-fault-form.tsx, src/components/work-orders/work-order-closure-banner.tsx</files>
  <read_first>
    - src/lib/closure-requirements.ts (FAULT_CATEGORIES, FAULT_CATEGORY_LABELS, CLOSURE_FIELD_LABELS, validateClosure)
    - src/actions/work-orders.ts (signature setWorkOrderFault)
    - src/components/work-orders/work-order-detail.tsx (pattern existant)
  </read_first>
  <action>
    **A. Créer `src/components/work-orders/work-order-fault-form.tsx`** :

    ```typescript
    'use client'

    import { useState, useTransition } from 'react'
    import { Button } from '@/components/ui/button'
    import { toast } from 'sonner'
    import { setWorkOrderFault } from '@/actions/work-orders'
    import { FAULT_CATEGORIES, FAULT_CATEGORY_LABELS, type FaultCategory } from '@/lib/closure-requirements'

    type Props = {
      workOrderId: string
      faultCategory: string | null
      faultDescription: string | null
      required: boolean
    }

    const SELECT_CLASS = 'h-9 rounded-lg border border-input bg-background px-2.5 text-sm focus:outline-none focus:ring-2 focus:ring-ring/50'

    export function WorkOrderFaultForm({ workOrderId, faultCategory, faultDescription, required }: Props) {
      const [, startTransition] = useTransition()
      const [form, setForm] = useState({
        category: faultCategory ?? '',
        description: faultDescription ?? '',
      })
      const [dirty, setDirty] = useState(false)

      function handleSubmit(e: React.FormEvent) {
        e.preventDefault()
        startTransition(async () => {
          try {
            await setWorkOrderFault(workOrderId, {
              faultCategory: form.category || null,
              faultDescription: form.description.trim() || null,
            })
            toast.success('Code de panne enregistré')
            setDirty(false)
          } catch (e) {
            toast.error(e instanceof Error ? e.message : 'Erreur')
          }
        })
      }

      function set<K extends 'category' | 'description'>(key: K, value: string) {
        setForm(f => ({ ...f, [key]: value }))
        setDirty(true)
      }

      return (
        <form onSubmit={handleSubmit} className="rounded-xl border bg-card p-4 space-y-3">
          <div className="flex items-center justify-between">
            <h2 className="text-sm font-semibold">
              Code de panne {required && <span className="text-destructive">*</span>}
            </h2>
          </div>
          <div className="space-y-2">
            <label className="text-xs font-medium block">Catégorie</label>
            <select
              value={form.category}
              onChange={e => set('category', e.target.value)}
              className={`w-full ${SELECT_CLASS}`}
            >
              <option value="">— Sélectionner —</option>
              {FAULT_CATEGORIES.map(c => (
                <option key={c} value={c}>{FAULT_CATEGORY_LABELS[c as FaultCategory]}</option>
              ))}
            </select>
          </div>
          <div className="space-y-2">
            <label className="text-xs font-medium block">Description</label>
            <textarea
              value={form.description}
              onChange={e => set('description', e.target.value)}
              rows={3}
              placeholder="Décrire la panne observée…"
              className="w-full rounded-lg border border-input bg-background px-2.5 py-2 text-sm resize-none focus:outline-none focus:ring-2 focus:ring-ring/50"
            />
          </div>
          {dirty && (
            <div className="flex justify-end">
              <Button type="submit" size="sm">Enregistrer</Button>
            </div>
          )}
        </form>
      )
    }
    ```

    **B. Créer `src/components/work-orders/work-order-closure-banner.tsx`** :

    ```typescript
    'use client'

    import { AlertTriangle } from 'lucide-react'
    import {
      validateClosure,
      type ClosureRequirements,
      CLOSURE_FIELD_LABELS,
    } from '@/lib/closure-requirements'

    type Input = {
      faultCategory: string | null
      faultDescription: string | null
      timeLogsMinutesTotal: number
      partsCount: number
    }

    export function computeMissingForClosure(input: Input, req: ClosureRequirements): string[] {
      return validateClosure(input, req)
    }

    type Props = {
      missing: string[]
    }

    export function WorkOrderClosureBanner({ missing }: Props) {
      if (missing.length === 0) return null
      return (
        <div className="rounded-xl border border-amber-300 bg-amber-50 dark:bg-amber-950/30 dark:border-amber-800 p-3 flex gap-3">
          <AlertTriangle className="h-5 w-5 text-amber-700 dark:text-amber-400 shrink-0 mt-0.5" />
          <div className="space-y-1 text-sm">
            <p className="font-medium text-amber-900 dark:text-amber-200">
              Pour clore ce bon, complétez :
            </p>
            <ul className="list-disc list-inside text-amber-800 dark:text-amber-300 space-y-0.5">
              {missing.map(key => (
                <li key={key}>{CLOSURE_FIELD_LABELS[key as keyof typeof CLOSURE_FIELD_LABELS] ?? key}</li>
              ))}
            </ul>
          </div>
        </div>
      )
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "(work-order-fault-form|work-order-closure-banner)\.tsx" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function WorkOrderFaultForm" src/components/work-orders/work-order-fault-form.tsx` retourne 1 ligne
    - `grep "setWorkOrderFault" src/components/work-orders/work-order-fault-form.tsx` retourne au moins 2 lignes (import + appel)
    - `grep "FAULT_CATEGORIES" src/components/work-orders/work-order-fault-form.tsx` retourne au moins 2 lignes
    - `grep "export function WorkOrderClosureBanner" src/components/work-orders/work-order-closure-banner.tsx` retourne 1 ligne
    - `grep "export function computeMissingForClosure" src/components/work-orders/work-order-closure-banner.tsx` retourne 1 ligne
    - `grep "CLOSURE_FIELD_LABELS" src/components/work-orders/work-order-closure-banner.tsx` retourne au moins 2 lignes
    - `grep "validateClosure" src/components/work-orders/work-order-closure-banner.tsx` retourne au moins 2 lignes
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>Form fault + banner closure créés, banner masqué si missing.length === 0, type-safe.</done>
</task>

<task type="auto">
  <name>Task 5: Câbler les 4 nouveaux composants dans WorkOrderDetail (timer, parts, fault, banner, coût main-d'œuvre)</name>
  <files>src/components/work-orders/work-order-detail.tsx</files>
  <read_first>
    - src/components/work-orders/work-order-detail.tsx (le fichier après Task 1, types déjà étendus)
    - src/components/work-orders/work-order-timer.tsx (Task 2)
    - src/components/work-orders/work-order-parts.tsx (Task 3)
    - src/components/work-orders/work-order-fault-form.tsx (Task 4)
    - src/components/work-orders/work-order-closure-banner.tsx (Task 4)
    - src/lib/closure-requirements.ts
  </read_first>
  <action>
    Modifier `src/components/work-orders/work-order-detail.tsx` :

    1. Ajouter imports :
       ```typescript
       import { WorkOrderTimer } from './work-order-timer'
       import { WorkOrderParts } from './work-order-parts'
       import { WorkOrderFaultForm } from './work-order-fault-form'
       import { WorkOrderClosureBanner, computeMissingForClosure } from './work-order-closure-banner'
       ```

    2. À l'intérieur du composant, calculer :
       ```typescript
       const activeSession = workOrder.timeLogs.find(l => l.endedAt === null) ?? null
       const missingForClosure = computeMissingForClosure(
         {
           faultCategory: workOrder.faultCategory,
           faultDescription: workOrder.faultDescription,
           timeLogsMinutesTotal: totalMinutes,
           partsCount: workOrder.parts.length,
         },
         closureRequirements
       )

       // Coût main-d'œuvre : somme sur tous les logs (minutes / 60) * hourlyRate du membre
       // hourlyRate vient des assignees (snapshot actuel). Si un log n'a pas de membre avec rate → ignoré dans le calcul (affiche "—" si total=0)
       const memberRateMap = new Map<string, number>()
       for (const a of workOrder.assignees) {
         if (a.membership.hourlyRate != null) memberRateMap.set(a.membership.id, a.membership.hourlyRate)
       }
       let laborCost = 0
       let hasAnyRate = false
       for (const log of workOrder.timeLogs) {
         const rate = memberRateMap.get(log.membership.id)
         if (rate != null && log.minutes != null) {
           laborCost += (log.minutes / 60) * rate
           hasAnyRate = true
         }
       }
       const laborCostDisplay = hasAnyRate ? `${laborCost.toFixed(2)} $` : '—'
       ```

    3. Modifier les `STATUS_TRANSITIONS` : transformer le bouton "Marquer résolu"/"Fermer" pour qu'il soit désactivé si `missingForClosure.length > 0`. Trouver le bloc :
       ```tsx
       {STATUS_TRANSITIONS[workOrder.status]?.length > 0 && (
         <div className="flex gap-2 flex-wrap">
           {STATUS_TRANSITIONS[workOrder.status].map(t => (
             <Button key={t.value} variant="outline" size="sm" onClick={() => handleStatusChange(t.value)}>
               {t.label}
             </Button>
           ))}
         </div>
       )}
       ```
       Le remplacer par :
       ```tsx
       {STATUS_TRANSITIONS[workOrder.status]?.length > 0 && (
         <div className="flex gap-2 flex-wrap">
           {STATUS_TRANSITIONS[workOrder.status].map(t => {
             const blocks = (t.value === 'resolved' || t.value === 'closed') && missingForClosure.length > 0
             return (
               <Button
                 key={t.value}
                 variant="outline"
                 size="sm"
                 disabled={blocks}
                 onClick={() => handleStatusChange(t.value)}
                 title={blocks ? `Champs requis manquants : ${missingForClosure.join(', ')}` : undefined}
               >
                 {t.label}
               </Button>
             )
           })}
         </div>
       )}
       ```

    4. Insérer le banner JUSTE AU-DESSUS des status actions (mais sous Description) :
       ```tsx
       <WorkOrderClosureBanner missing={missingForClosure} />
       ```

    5. Insérer le timer en haut du `lg:col-span-2 space-y-6` (au-dessus de Description), uniquement si status pertinent :
       ```tsx
       <WorkOrderTimer
         workOrderId={workOrder.id}
         workOrderStatus={workOrder.status}
         activeSession={activeSession ? {
           id: activeSession.id,
           startedAt: activeSession.startedAt,
           membership: {
             id: activeSession.membership.id,
             firstName: activeSession.membership.firstName,
             lastName: activeSession.membership.lastName,
           },
         } : null}
         currentMembershipId={currentMembershipId}
         currentRole={currentRole}
       />
       ```

    6. Insérer le formulaire fault et le composant parts entre la section Comments et Time logs (ou juste après Time logs — au choix UX, garder cohérent). Suggestion : après les Comments, avant Time logs :
       ```tsx
       {(closureRequirements.faultCode || workOrder.faultCategory || workOrder.faultDescription) && (
         <WorkOrderFaultForm
           workOrderId={workOrder.id}
           faultCategory={workOrder.faultCategory}
           faultDescription={workOrder.faultDescription}
           required={closureRequirements.faultCode}
         />
       )}

       <WorkOrderParts
         workOrderId={workOrder.id}
         parts={workOrder.parts}
         spareParts={spareParts}
       />
       ```

    7. Dans la sidebar (section Détails), ajouter sous "Temps estimé" une ligne "Coût main-d'œuvre" :
       ```tsx
       <div className="text-sm">
         <span className="text-muted-foreground text-xs block mb-0.5">Coût main-d&apos;œuvre</span>
         <span className="font-mono tabular-nums">{laborCostDisplay}</span>
       </div>
       ```

    8. Conserver la section "Temps" existante (logs manuels). Le nouveau timer la complète mais ne la remplace pas.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "work-order-detail\.tsx" | wc -l | grep -E "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import { WorkOrderTimer }" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "import { WorkOrderParts }" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "import { WorkOrderFaultForm }" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "import { WorkOrderClosureBanner" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "<WorkOrderTimer" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "<WorkOrderParts" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "<WorkOrderFaultForm" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "<WorkOrderClosureBanner" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `grep "computeMissingForClosure" src/components/work-orders/work-order-detail.tsx` retourne at least 2 lignes (import + use)
    - `grep "laborCostDisplay" src/components/work-orders/work-order-detail.tsx` retourne au moins 2 lignes
    - `grep "disabled={blocks}" src/components/work-orders/work-order-detail.tsx` retourne 1 ligne
    - `npx tsc --noEmit -p tsconfig.json` exit code 0
  </acceptance_criteria>
  <done>Tous les nouveaux composants intégrés, boutons "Résoudre"/"Fermer" désactivés si missing fields, coût main-d'œuvre affiché ou "—".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Checkpoint humain — vérifier UX bout-en-bout sur un BT réel</name>
  <files>src/app/(app)/bons-de-travail/[id]/page.tsx, src/components/work-orders/work-order-detail.tsx (déjà modifiés)</files>
  <read_first>
    - .planning/phases/06-int-grit-des-donn-es-terrain/06-CONTEXT.md (D-03, D-07, D-09 à D-18)
  </read_first>
  <action>Pause humaine pour vérification UX. Aucune modification de code par Claude — l'utilisateur exécute les étapes décrites dans `<how-to-verify>` et confirme.</action>
  <acceptance_criteria>
    - L'utilisateur retourne "approved" après avoir validé les 14 étapes de la procédure
    - Si un check échoue, l'utilisateur décrit le problème et Claude itère
  </acceptance_criteria>
  <verify>
    <automated>echo "Manual checkpoint — see how-to-verify"</automated>
  </verify>
  <done>L'utilisateur confirme "approved" après exécution des 14 checks listés.</done>
  <what-built>
    - Page détail BT enrichie : timer live, formulaire pièces hybride, formulaire fault code, banner closure, coût main-d'œuvre dans sidebar
    - Boutons "Marquer résolu" / "Fermer" désactivés tant qu'un champ requis (selon Organization.closureRequirements) n'est pas rempli
    - Bouton timer Démarrer visible en status=in_progress, Arrêter visible si session perso active, Fermer (X) si session d'un autre + role admin/manager
  </what-built>
  <how-to-verify>
    Pré-requis : Plan 03 (UI admin) peut ne pas être terminé. Pour ce checkpoint, configurer manuellement `Organization.closureRequirements` via psql/Prisma Studio :
    ```sql
    UPDATE "Organization" SET "closureRequirements" = '{"faultCode":true,"timeSpent":true,"partsUsed":true}'::jsonb WHERE "clerkId" = '<your_org_clerk_id>';
    ```
    Aussi : `UPDATE "Membership" SET "hourlyRate" = 65 WHERE id = '<your_membership_id>';`

    Étapes :
    1. `npm run dev` puis ouvrir un BT en status `open`
    2. Cliquer "Démarrer" (transition `in_progress`) — vérifier que le bloc Timer apparaît avec bouton "Démarrer"
    3. Cliquer "Démarrer" sur le timer — un compteur mm:ss doit tiquer en temps réel
    4. Rafraîchir la page — le compteur doit reprendre depuis startedAt (continuité)
    5. Cliquer "Arrêter" — la session se ferme, le compteur disparaît, la section "Temps" en bas montre la ligne avec minutes
    6. Vérifier le banner amber : "Pour clore ce bon, complétez : Code de panne, ..." (au moins faultCode si timer a tourné assez longtemps)
    7. Remplir Catégorie = "Mécanique", Description = "test" — le banner doit retirer "Code de panne" après "Enregistrer"
    8. Ajouter une pièce libre (texte + qté 2) — le banner retire "Pièces utilisées"
    9. Ajouter une pièce inventaire — vérifier que la quantité de stock diminue (Inventaire ou directement en DB)
    10. Vérifier "Coût main-d'œuvre" dans le sidebar : (minutes / 60) × 65$
    11. Le banner doit être vide ; les boutons "Marquer résolu" et "Fermer" doivent être activables
    12. Cliquer "Marquer résolu" — succès, status devient `resolved`
    13. (Test négatif) Sur un autre BT, mettre status=in_progress et essayer "Marquer résolu" sans rien remplir → bouton désactivé avec tooltip listant les champs manquants
    14. (Test admin) Avec un compte technicien, vérifier qu'on NE voit PAS le bouton "Fermer" sur une session démarrée par un autre membre ; avec un admin, le voir.
  </how-to-verify>
  <resume-signal>Type "approved" si tous les checks passent, ou décrire les problèmes observés.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ Server Action | Tous les inputs UI (workOrderId, sparePartId, quantity, fault fields) sont validés serveur (voir threat_model Plan 01) |
| Composants client ↔ closureRequirements | Le client utilise closureRequirements pour désactiver les boutons (UX) — le serveur ré-applique la validation (defense-in-depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Tampering | Client désactive bouton via missingForClosure — un utilisateur peut bypasser en DevTools | mitigate | Plan 01 Task 4 : `updateWorkOrderStatus` ré-exécute `validateClosure` côté serveur et throw — UI client purement UX |
| T-06-12 | Elevation of Privilege | Un technicien tente d'appeler `closeActiveTimer` (réservé manager) | mitigate | Plan 01 Task 4 : role check serveur ; UI cache aussi le bouton si non-manager |
| T-06-13 | Information Disclosure | hourlyRate d'un autre membre exposé via assignees dans le BT detail | accept | Risque interne org seulement (membres d'une même org se font confiance) ; D-16 prévoit que c'est admin qui voit/configure |
| T-06-14 | Tampering | Spam de start/stop timer (DoS léger) | accept | Server Action throw si session déjà active sur le BT pour ce membre — pas de loop infinie possible |
| T-06-15 | Information Disclosure | sparePart.unitCost exposé dans le coût ligne — visible à tout membre | accept | Coûts d'inventaire considérés non-sensibles dans org (déjà visibles dans /inventaire pour les rôles concernés) |
</threat_model>

<verification>
- `npx tsc --noEmit` : aucune erreur TS dans les fichiers modifiés
- Checkpoint humain : workflow timer + pièces + fault + closure validé end-to-end
- Inspection : le banner reste affiché tant que au moins un champ requis manque ; les boutons Résoudre/Fermer désactivés en conséquence
- Inspection : le timer affiche le temps écoulé correct (différence entre startedAt et now)
</verification>

<success_criteria>
- WorkOrderDetail intègre les 4 nouveaux composants
- Timer live fonctionnel avec start/stop perso + fermeture admin/manager
- Pièces : formulaire hybride inventaire/libre, déduction stock automatique (création seulement)
- Fault form : sélecteur catégorie + textarea description avec save sur changement
- Banner closure : liste les champs manquants, désactive les boutons Résoudre/Fermer
- Coût main-d'œuvre affiché dans sidebar (totalMinutes/60 × hourlyRate) ou "—"
- Checkpoint humain approuvé
</success_criteria>

<output>
After completion, create `.planning/phases/06-int-grit-des-donn-es-terrain/06-02-SUMMARY.md`
</output>
</content>
</invoke>